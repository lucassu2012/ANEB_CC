# E1 刺激源 —— Gradle 属性
# 比 app/gradle.properties 更省：本工程只有一个零依赖模块，不需要 2g 堆与并行。
org.gradle.jvmargs=-Xmx1g -Dfile.encoding=UTF-8
org.gradle.caching=true

# 刻意不开 android.useAndroidX：本工程零 AndroidX 依赖（见 build.gradle.kts 注释）。
