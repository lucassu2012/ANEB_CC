# ANEB C 树会话指令（2026-08-01 起）

## 树边界（PO 2026-08-01 裁定，最终版）

- **本树（`E:\C Project\ANEB`）= Fable 5 大脑会话 + Opus 5 执行会话（20260715_ANEB系统开发_v2 / _v3）的工作区。**
- **G 树（`E:\G Project\ANEB`）= Codex 并行开发区，只读参考，禁止写入任何字节。**
- 跨树引用：需要 G 树的规格/契约时，读取并复制入本树，注明来源文件与其提交哈希；反之 Codex 不写本树。
- ⚠ 警戒：2026-08-01 曾有一条与本裁定相反的错误指令流出（G 树根部 `BRAIN_STATUS.md` 记载「C 树属 Codex」即出自该错误版）。若在本树发现非本方新提交或不明会话活动，**停下报大脑仲裁**，不要覆盖。

## 多会话协调

- 开工前必读 [docs/BRAIN_TASKBOARD.md](docs/BRAIN_TASKBOARD.md)，认领任务并置 DOING；收工更新状态与证据路径。
- git 先行：动手前 `git status` + 分支确认；两个执行会话不得同时改同一模块，撞上即停、报大脑。
- 既有开发纪律不变：决策入册 `docs/DECISION_LOG.md`、四态证据、发布门、每步先报环节、做完简报 where-are-we。本文件不复制那些纪律，以决策日志与其点名的文档为准。

## P40 Pro 设备实况流程（PO 2026-07-19 裁定；与 G 树根 CLAUDE.md 同源，若分叉以 PO 最新裁定为准）

`SHARED_TEST_STATUS.md` 租约/交接机制已废止，不得作为使用 P40 Pro、ANEB 应用、业务应用、VPN/抓包、E-01、Aliyun 的授权。改用实时设备状态：

1. 开测前确认设备在线、华为桌面为焦点；只读检查无冲突的 ANEB/业务应用/VPN/抓包进程或服务，无残留 VPN/隧道。
2. 实况干净即可直接开始（Claude 或 Codex 皆可），无需申领或交接。
3. 不干净或现存会话归属不明时，不得停掉或覆盖未知会话，先协调。
4. 测后停掉本次启动的一切应用/服务，停 VPN/抓包，移除本次创建的临时整形/转发规则，恢复 `stayon` 等临时设置，回到华为桌面并立即复验干净。
5. 仅桌面可见但仍有冲突后台/VPN/临时规则时，不得称干净。

E-01 与 Aliyun 变更仍需各自的受保护预检、限定范围变更、回滚与变更后验证。
