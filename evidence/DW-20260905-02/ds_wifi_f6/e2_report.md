# E2 三通道同轨对拍 —— 判读结果

> 出处：`spec/adapters/INSTRUMENTATION_SPEC.md` §3.3 E2、§3.4 G-3。
> 状态词只用 PASS / FAIL / NOT_EXECUTED。空样本一律 NOT_EXECUTED，不折 0。

## 0. 这一份为什么只有两个时刻

spec §3.3 E2 说的是「三个时刻」。通道 B 的时戳是**宿主侧**的，与设备时钟之间
隔着一次**从未标定过**的 adb 往返，故本脚本不拿它做时序比较（沿用 spec §2.2 与
e1 的既有裁断）。B 在这一页只出一条佐证：它有没有看见这些翻转、采样周期多大。

## 1. 前提

| 项 | 值 |
|---|---|
| 目标包 | `com.deepseek.chat` |
| 可用内容事件 / 他包滤除 / 量纲拒收 | 3747 / 0 / 0 |
| 切轮方式 | `operator-marks`（轮数 6） |
| 一帧 | 16.667 ms（实测，非硬编码 33ms —— spec §3.1） |
| 帧记录条数（去重后 / 重复丢弃） | 40902 / 31595 |
| 墙钟↔BOOTTIME 标定 | PASS（n=3747，残差 p50 0.258 ms / max 7.291 ms） |
| 时钟钉桩（BOOT−MONO） | PASS（偏移 137288516790842 ns，前后漂移 0 ns） |

## 2. `|t_A − t_C|`（判据本体）

| 量 | n | dropped | p50 (ms) | p90 (ms) | p99 (ms) | 判定 |
|---|---|---|---|---|---|---|
| \|t_A − t_C\| | 0 | 6 | — | — | — | **NOT_EXECUTED** — 缺分布或缺实测刷新率 |

**该判定**已设**最小 n（e1_analyze.GATE_MIN_N，n 不足即 NOT_EXECUTED），但**不看 dropped**（T14 C-2 余半）；本次 n=0 dropped=6**

有符号分布（**方向本身是信息**：spec §2.1 记「事件不保证像素已上屏，可能早于或晚于呈现，连符号都未知」）：p50 — / p90 — / p99 — / min — / max — ms。

未能出数的轮次（逐条计数，不静默）：

- 通道 A 该轮不足两簇（A2 无判据）：6 轮

## 3. 通道 B 的佐证（**不是时刻**）

采样 n=452，周期 p50 1500.521 ms / p99 1500.921 ms，检出变化 27 次。

> 检出次数不是时序主张，只说明 ROI 与阈值选得对不对。

## 4. 逐轮

| 轮 | A 簇数 | C 簇数 | 该轮帧数 | Δ = t_A − t_C (ms) |
|---|---|---|---|---|
| 0 | 1 | 1 | 4522 | — |
| 1 | 1 | 1 | 4524 | — |
| 2 | 1 | 1 | 4528 | — |
| 3 | 1 | 1 | 4522 | — |
| 4 | 1 | 1 | 4524 | — |
| 5 | 1 | 1 | 4525 | — |

