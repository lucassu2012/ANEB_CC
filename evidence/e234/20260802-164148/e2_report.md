# E2 三通道同轨对拍 —— 判读结果

> 出处：`spec/adapters/INSTRUMENTATION_SPEC.md` §3.3 E2、§3.4 G-3。
> 状态词只用 PASS / FAIL / NOT_EXECUTED。空样本一律 NOT_EXECUTED，不折 0。

## 0. 这一份为什么只有两个时刻

spec §3.3 E2 说的是「三个时刻」。通道 B 的时戳是**宿主侧**的，与设备时钟之间
隔着一次**从未标定过**的 adb 往返，故本脚本不拿它做时序比较（沿用 spec §2.2 与
e1 的既有裁断）。B 在这一页只出一条佐证：它有没有看见这些翻转、采样周期多大。

## 1. 前提

| 项 | 值 |
|---|---|
| 目标包 | `com.aneb.e1stimulus` |
| 可用内容事件 / 他包滤除 / 量纲拒收 | 0 / 0 / 0 |
| 切轮方式 | `whole-run`（轮数 0） |
| 一帧 | 11.111 ms（实测，非硬编码 33ms —— spec §3.1） |
| 帧记录条数（去重后 / 重复丢弃） | 0 / 0 |
| 墙钟↔BOOTTIME 标定 | NOT_EXECUTED（n=0，残差 p50 — ms / max — ms） |
| 时钟钉桩（BOOT−MONO） | PASS（偏移 94513649503166 ns，前后漂移 -1 ns） |

> ⚠ 本次没有操作者标记，整段按**一轮**处理 —— 样本数结构上就是 1。
> 一个样本也算得出 p99，但那个 p99 什么也不是。

## 2. `|t_A − t_C|`（判据本体）

| 量 | n | dropped | p50 (ms) | p90 (ms) | p99 (ms) | 判定 |
|---|---|---|---|---|---|---|
| \|t_A − t_C\| | 0 | 0 | — | — | — | **NOT_EXECUTED** — 缺分布或缺实测刷新率 |

**该判定未看 dropped、未设最小 n（T14 待裁 C-2）；本次 n=0 dropped=0**

有符号分布（**方向本身是信息**：spec §2.1 记「事件不保证像素已上屏，可能早于或晚于呈现，连符号都未知」）：p50 — / p90 — / p99 — / min — / max — ms。

## 3. 通道 B 的佐证（**不是时刻**）

采样 n=16，周期 p50 5608.417 ms / p99 18185.794 ms，检出变化 4 次。

> 检出次数不是时序主张，只说明 ROI 与阈值选得对不对。

## 4. 逐轮

| 轮 | A 簇数 | C 簇数 | 该轮帧数 | Δ = t_A − t_C (ms) |
|---|---|---|---|---|

