# c_doubao_transport_mix_20260912 — 豆包流量传输构成（战役证据包）

> 本 README 是**包说明**（`evidence/README.md`「目录形态」节：每个日期包自带 README，说明批次、口径与复现命令）。
> 它**只指向、不复述**：口径、前提与判据以同目录 `CRITERIA_PREREG.md` 为准，本文件不抄任何判词或界值。

## 状态（四态）

**测量：`NOT_EXECUTED`。** 本包**只有预登记的判据**，截至 2026-09-26 **从未开跑**，没有任何抓包产物。
⇒ 本包**尚无任何关于豆包传输构成（TCP vs QUIC）的结论**，也不得被引作结论。

未执行的原因：该测量需要一个**有人在设备上操作**的设备窗（PCAPdroid 起自己的 VPN 须人确认同意，
且须覆盖判据写死的若干具名功能操作），至今未排这样的窗。
⚠ 判据 §0 的前提 P0–P5 是**运行时**检查项；本 README **不记录它们此刻是否成立**——那是会过期的现态。

## 包内文件是什么

| 文件 | 性质 |
|---|---|
| `CRITERIA_PREREG.md` | 判据，**跑前写死**，先于设备窗写成（其抬头照录了大脑的三条约束：不得插在 ICS 干跑之前或中间、判据另立一笔、取不到就算了）。PREREG 守卫（`scripts/tests/test_prereg_precedes_results.py`）机械保证它先于本 README 入库 |

结果（若将来跑了）按本仓惯例写进**本 README** 的新一节，连同：操作者、实际执行的操作清单、
字节口径与流数口径**两个**占比、样本充分性是否达标——判据 §1 明写只报一个口径会误判。

## 复现命令

**没有自动化的复现命令，这是如实记录，不是遗漏。** 本测量是人工抓包：
1. 按 `CRITERIA_PREREG.md` §0 逐条核前提（任一不成立即 `NOT_EXECUTED`，不出占比结论）；
2. 用 PCAPdroid 只抓豆包包名的流量，**只读**方向、字节、时戳、远端端口、传输协议，**不解密、不看内容**；
3. 按判据 §2 以**字节**口径算 `UDP:443` 占比并同报流数口径，按判据 §3 的边界写结论（须带「在这组操作下」的限定）。

⚠ 装 App、起 VPN、在设备上操作豆包都是**需要人的动作**；本仓不自动化豆包的 UI，也不代用户账号发请求。
