# T30 长窗批判读：15/15 清洁 NR_SA run 全量分析

> 批采终账：2026-08-03 09:09–11:44，15/15 全清洁 + pounce#1（08-02 16:46，修复前）=
> 16 个干净 NR_SA run，单一连续 NR 窗口跨约 2.6 小时。本文件独立重拉设备库
> （`adb exec-out run-as com.aneb.probe cat databases/aneb-probe.db[,-wal,-shm]`，
> 只读，落本会话 scratchpad 不入仓），对批日志（`nr_batch_brain_20260802.log`）
> 与 rat_watch.tsv 的说法逐项验证，不转录。**登记纪律**：CV/信号质量/PCI 差异均为
> 装置性质登记，不做 NR vs LTE 优劣声称——那是扩展轮口径的事。

## 0. 范围说明：15 个批采 run（修复后）与 pounce#1（修复前）分开看

`FLAG_KEEP_SCREEN_ON` 修复（D-428）落地于 2026-08-03，晚于 08-02 16:46 的 pounce#1。
①项（T25 修复验证）只统计**今天的 15 个批采 run**——pounce#1 是修复前的旧数据，
已在 D-426/T24 完整分析过（2/9 覆盖率），不重新计入"修复后还有 stale"。②项（出口
IP）与部分统计则纳入全部 16 个 run，因为出口 IP 读取不受屏幕修复影响，多一个真实
观测点对"制式 vs 出口"判别有直接价值。

## 1. T25 修复验证：15/15 run、135/135 场景，radio 覆盖 100%，零 stale

逐 run 独立查询 `radio_sample` 表（1Hz 原始采样）：

| 指标 | 结果 |
|---|---|
| 15 个 run 的 `radio_sample` 总行数 | 354–384（各 run 时长 353–384 秒，与 1Hz 节拍吻合） |
| 每个 run 的 stale 行数 | **全部为 0**（15/15） |
| 每个 run 的 nonnull 行数 | **等于总行数**（15/15） |

逐**场景**（`report_body` JSON 内 `network_snapshot.radio`）复核，颗粒度更细：

| 指标 | 结果 |
|---|---|
| 15 run × 9 场景 | 135 个场景 |
| `rat` 字段为 `NR` 且 `stale≠true` 且 rsrp/sinr 均非 null | **135/135**（100%） |
| 逐 run 场景覆盖 | **9/9 × 15 个 run，无一例外** |

**结论**：D-427①/D-428 的修复方向确认有效——run3（D-410）遗留的"前 44 秒健康、
之后归零"、pounce#1（D-424/D-426）的"9 场景仅 2 个非 stale"，在今天连续 2.6 小时、
15 个 run 里**完全没有复现**。这是迄今样本量最大的一次验证（135 个场景 vs 此前的
9/44 个数据点），且是**连续时间窗**（不像 pounce#1 那样只有几分钟）——如果 EMUI 的
息屏节流机制仍然存在，2.6 小时里没有一次是不合理的，`FLAG_KEEP_SCREEN_ON` 确实
让 Activity 全程保持前台可见+屏幕常亮的假设成立。

## 2. 出口 IP：第三次独立 NR 观测，出现第三个不同的出口 IP

逐 run 逐场景读 `network_snapshot.server_observed_addr`（剥端口）：

| 观测 | 时间 | 出口 IP | 场景数 |
|---|---|---|---|
| 07-31 试点 NR 窗（既有记录，`nr_timeline_20260802/README.md` §2） | 07-31 下午 | `106.92.23.196` | （未知，历史记录） |
| pounce#1（D-424/D-426，本次批采前） | 08-02 16:46 | `106.80.108.105` | 9（与 LTE 四窗同址） |
| **今天 15 个批采 run（本条新增）** | 08-03 09:09–11:44 | **`106.83.173.55`** | **135**（15 个 run **全部**同一 IP，逐场景验证，零例外） |

**三次独立 NR_SA 观测，三个互不相同的出口 IP，但每次观测内部（跨场景、跨 run）
完全稳定**。这与"制式与出口完全共线"的既有框架（一个 RAT 对应一个固定出口）
**对不上**：如果出口 IP 是 RAT 决定的，三次 NR 观测应该收敛到同一个（或至少重复
出现）出口，而不是三次三个。**更像**出口 IP 是 CGNAT 会话级分配（每次蜂窝数据
连接建立/重建时由运营商网关分配，与 RAT 无必然关系），今天这 2.6 小时窗口里设备
全程保持同一个蜂窝数据会话（135 个场景零变化），出口自然稳定；但重建会话（比如
今天 09:09 这次批采相对昨天 pounce#1 是一次新的会话）会拿到不同的出口。

**如实标注**：这仍是**三个观测点**（n=3 个独立 NR 会话），不构成统计显著性检验，
不排除"NR 与出口有某种更复杂的关联，只是这三次都恰好赶上了会话重建"这类替代
解释。LTE 侧目前只核实过"四个窗同址"（D-393），没有专门核过"LTE 是否也会话级
换出口"——若 LTE 侧同样能观测到跨会话换出口，则"共线"框架更彻底地站不住；若
LTE 侧稳定不变而 NR 侧三次三样，那差异本身也值得追问（但这需要专门设计的对照，
不是本条能回答的）。

## 3. STRADDLED 核对 + 52 分钟停顿：DB 地面真值确认零丢失

批日志（`nr_batch_brain_20260802.log`）自报 `ratStart=NR_SA ratEnd=NR_SA` +
`CLEAN single-RAT` 逐 run 出现，`grep -c STRADDLED` = **0**（15/15 全清洁，
日志自身说法与 grep 结果一致）。

**52 分钟停顿（RUN 8 END 10:04:27 → RUN 9 STARTED 10:57:09）**：按大脑的方法论
指示——不从日志侧猜，查 DB 地面真值：

```sql
SELECT runId, startedAtEpochMs FROM test_run
WHERE startedAtEpochMs BETWEEN <10:04 本地> AND <10:56 本地>;
-- 0 行
```

**该区间内 `test_run` 表零记录**——不是"有一个半截 run"，是**压根没有任何 run 尝试
启动过**。结合两侧唯一的信息来源都指向同一个空区间（DB 无记录、日志无 RUN 9
之前的任何行），最合理的解释是**批脚本本身在这段时间里没有触发新一轮采集**（可能
在轮询/重试逻辑里卡住，或某个判定条件迟迟不满足），而不是"run 启动了但数据丢了"。
**无数据丢失**，但这 52 分钟本身是一次真实的批采效率问题，值得脚本属主关注（本文件
不诊断脚本内部逻辑，只确认数据侧干净）。

## 4. NR 侧首次 CV：与 LTE 侧同量级

逐 run 取 `n1RttP50Ms`（N1 网络侧 KPI）场景内中位数，按 profile 分组，15 个 run
作为 15 个独立样本点算 CV：

| profile | n（run 数） | 均值 (ms) | 标准差 (ms) | **CV%** | LTE 侧对照（试点，既有记录） |
|---|---|---|---|---|---|
| s1_chat | 15 | 57.842 | 3.708 | **6.41%** | 5.5% |
| s3_multimodal | 15 | 59.544 | 3.594 | **6.04%** | 5.9% |

**同量级**：NR 侧两个 profile 的 CV（6.41%/6.04%）与 LTE 侧（5.5%/5.9%）差距在
1 个百分点以内，均落在个位数百分比区间。**登记，不下声称**：这不代表"NR 比 LTE
波动大"（差距在这个尺度下不足以下此判断，且未做显著性检验、未控制忙闲/时段等
混杂因素）；能登记的是——**n≥15 这个判据在 NR 格上暂无理由认为需要显著放大**，
两条制式的离散度在同一数量级，扩展轮把 NR 也按同一采样量惯例设计是有实测支撑的，
不是凭空沿用 LTE 的经验。

## 5. mPci 观察：两条 PCI 读取路径能力不同，实测证实

`rat_watch.tsv` 第 6 列（PCI）在 NR_SA 期间恒为 `2147483647`
（`CellInfo.UNAVAILABLE` 哨兵——`telephony.registry` 的 `mPci` 字段在这台设备
NR_SA 状态下不可得，大脑已在续派消息里订正此前"屏超时残留"的误读）。

**radio_ctx（`RadioCollector` 走 `CellInfoNr` 路径）在同一时间窗口读到的 PCI**：

```
135/135 个场景，PCI 恒为 672（单一整数值，非哨兵）
```

**两条路径能力确认不同**：`telephony.registry`（rat_watch 轮询的路径）在 NR_SA
下读不到 PCI，`CellInfoNr`（radio_ctx 采集走的路径）读得到、且整个 2.6 小时窗口
稳定为同一个值（672，与设备全程未换服务小区吻合）。这不是矛盾，是两条系统 API
在这台设备/这个 ROM 上对 NR 的支持深度不同——`radio_ctx` 的设计（D-284/D-367/D-368）
选择走更完整的 `CellInfoNr` 路径这个决定，在这次真实数据上得到了直接印证：如果
当初采集器也走了 `telephony.registry`，今天的批采会拿不到任何 NR 场景的 PCI。

## 6. NR 信号质量：首份真实语料

`radio_ctx` 里 NR 场景的 rsrp/sinr 分布（135 个场景，全部 `rat=NR`、单一服务小区
`pci=672`）：

| 量 | n | min | median | max |
|---|---|---|---|---|
| `rsrp_dbm` | 135 | −90.0 | **−85.0** | −81.0 |
| `sinr_db` | 135 | 15.0 | **20.0** | 24.0 |

数值量级合理（RSRP −90~−81 dBm 属中上信号强度、SINR 15–24 dB 属良好信噪比，
非哨兵、非占位），且波动范围窄（RSRP 极差 9dB、SINR 极差 9dB）——与"全程同一
服务小区、设备静止未移动"的场景吻合。**这是仓库里第一份非空的真实 NR 信号质量
分布**（此前所有 radio_ctx 真机验证要么是 LTE，要么是 NR 但仅 2/9 场景的孤立点，
D-424）。仅登记数值分布，不据此判断"信号好不好会不会影响 KPI"——那需要专门的
相关性分析，不在本条范围内。

## 7. 本文件明确没有做到的（诚实缺口清单）

1. 出口 IP 三次观测的"会话级分配"解释是**推断**，没有拿到运营商侧或系统侧任何
   直接证据（比如一次蜂窝数据会话的建立/重建事件日志）证实这个机制，只是从
   "组内稳定、组间不同"这个模式反推最简解释。
2. LTE 侧是否同样存在"会话级换出口"现象，本文件没有专门核实（D-393 的四窗
   同址观测本身没有设计成"检验会话是否重建过"）。
3. 52 分钟停顿的**根因**（批脚本内部为什么卡住）本文件没有诊断，只确认了数据侧
   干净——这需要读脚本自身的轮询/重试逻辑，不在本次判读范围。
4. NR CV 的两个百分点（6.41%/6.04%）与 LTE（5.5%/5.9%）差距是否有统计学意义，
   本文件没有做显著性检验（也不具备条件——两组样本特性、混杂因素都未对齐）。
5. mPci 差异背后的具体系统 API 行为原因（是华为 ROM 定制、还是 Android 平台在
   NR_SA 下 `telephony.registry` 本身的已知限制）本文件没有深入，只确认了现象。

## 8. 订正批（D-439，2026-08-03）：两处被证伪，一处重大新事实——独立复核后追加，不改原文

**与 L-3 并列存档原则一致：只追加，不改上文任何一句。**

### 8.1 §0/§2「pounce#1」认错——本文件独立复核确认，与大脑核验一致

上文 §0/§2 把"批日志里的 pounce#1"读成了 `019fc1a6`（昨日 08-02 16:46）——
**读错了**。批日志（`nr_batch_brain_20260802.log`）的"pounce#1"实际指
`019fc520-7d0d-718f-af85-cc1d8f5ef98a`（**今晨** 08-03 08:57:48，本文件独立
查库确认）。命名冲突诱因是批横幅文案陈旧（大脑脚本侧责任，已认领）；混淆的
是我：昨日 T24/D-426 写的报告里 `019fc1a6` 也自称"pounce 1/3 done"，我沿用了
那个印象去对应今天批日志里出现的同名字符串，没有去查批日志这次具体指向哪个
run_id。**订正**：昨日的 `019fc1a6` 是一次独立、已完整分析过的历史观测
（D-424/D-426），与今天这个 NR 窗口（08:57 起）无关；今天窗口的完整构成是
**`019fc520`（08:57，独立单发） + 15 个批采 run（09:09–11:44）**。

### 8.2 §2 出口 IP：不是三次观测，是四次——独立重查 `019fc520` 确认第四个出口

本文件独立查库（`test_run`+`radio_sample`+`report_body`，非转录大脑核验结果）：

```
019fc520 started_local = 2026-08-03T08:57:48.563
egress_ips_seen = {'106.85.27.201'}   # 9 个场景全部同一值，与批采 15 run 内部稳定同一形状
```

**四次独立 NR_SA 观测，四个互不相同的出口 IP**：

| 观测 | 时间 | 出口 IP |
|---|---|---|
| 07-31 试点 NR 窗 | 07-31 下午 | `106.92.23.196` |
| `019fc1a6`（D-424/D-426） | 08-02 16:46 | `106.80.108.105` |
| **`019fc520`（本条新增）** | **08-03 08:57** | **`106.85.27.201`** |
| 15 个批采 run | 08-03 09:09–11:44 | `106.83.173.55` |

**n=4 比原文 n=3 更强支持 §2 的 CGNAT 会话级分配假说**：`019fc520`→批采仅相隔
**12 分钟**（08:57:48→09:09:14）就换了出口，证明"同一天早晨"不能被当作一个
稳定单元外推——原文 §2 "今天这 2.6 小时窗口里设备全程保持同一个蜂窝数据会话"
这句话现在看，那个"全程"的边界必须收紧到**批采自己的 15 个 run**，不能扩大到
包含 `019fc520`。四次观测、四个出口，组内稳定、组间必换，模式比三次时更清晰。

### 8.3 §1 重大新事实：屏灭节流假说被证伪——本文件独立复核确认

**独立验证**（未转录大脑核验，自己重查）：

1. **APK 版本一致**：`019fc520` 与 15 个批采 run 的 `appVersionName`/`appVersionCode`
   **完全相同**（`0.1.0-phase0` / `1`）——不是修复前的旧安装。
2. **屏幕状态独立核实**：`rat_watch.tsv` 08:57:41–09:05:49（覆盖 `019fc520` 整个
   运行时长）逐条读数**全部为 `ON`**，本文件自己 grep 逐行确认，非转录。
3. **`019fc520` 的 radio 覆盖率**：本文件独立查询 `radio_sample`+`report_body`——
   **2/9**（`s1_chat`/`s2_coding_agent` 两个场景非 stale，其余 7 个全 stale），
   与昨日 `019fc1a6`（修复前）**逐字节同形状**。

**同一个 APK、屏幕独立确认全程 ON，仍然复现与"屏灭"时代完全相同的 2/9 归零
模式**——这直接证伪了 §1 原文"`FLAG_KEEP_SCREEN_ON` 修复让屏幕全程常亮从而
解决归零问题"这条因果解释。**§1 的经验事实部分保留不动**：15 个批采 run 确实
135/135、100% 覆盖，这是本文件亲自查库测出来的真实数字，没有错。**撤回的是
因果归因**——"因为屏幕常亮所以覆盖率满"这条推断线，被 `019fc520`（同样屏幕
常亮却仍 2/9）直接反驳。批采 15 run 为什么 9/9、`019fc520` 为什么 2/9，两者
真正的差异变量还不知道（机制重新开放，见大脑派单 T35，v2 在查"发射路径差分"
——`nr_batch_brain.ps1` 与 `nr_pounce.ps1` 用不同脚本拉起 App，这是目前唯一
已知的、`019fc520` 与批采 15 run 之间的系统性差异，但本文件没有验证这是不是
真正的原因，留给 T35）。

## 9. 订正（D-442，2026-08-03）：§4 的"LTE 侧对照"实为另一次 NR 观测

**与 L-3 并列存档原则一致：只追加，不改上文任何一句（§4/§8 均不动）。**

T34（另一会话，`evidence/nr_timeline_20260802/T34_EGRESS_RAT_DECOMPOSITION_20260803.md`）
三条独立证据链核实：§4 表格"LTE 侧对照（试点，既有记录）5.5%/5.9%"这两个数字
来自 `evidence/m2_pilot_20260731/pilot_labelled.jsonl`（11 run 主战役语料）——
该文件的 11 个 run 时间戳（07-31 13:36–14:23）完整落在 `README.md` §1 记录的
`telephony.registry` 直测连续 NR_SA 段（07-30T20:37:05→07-31T21:24:41）内，
出口 `106.86.109.60`。**"LTE 侧对照"实为另一次独立 NR 观测（出口 106.86，
与本文件 §2 六格分解表里今天三格的 106.83/106.85/106.80 均不同）**，不是
真实 LTE 数据。

**§4 表格数字本身不受影响**——6.41%/6.04%/5.5%/5.9% 都是从各自真实文件正确
算出来的，错的只是给"5.5%/5.9%"那一列贴的标签。**§4 原文"同量级"的登记
性质结论需要重新表述**：不是"NR 侧 CV 与 LTE 侧同量级"，而是**"NR@106.83
（本批 15 run）的 CV 与另一次独立 NR 观测（NR@106.86，试点主战役语料）同
量级"**——是**同 RAT 跨出口**的 CV 比较，不是**跨 RAT**的比较。这不改变
"n≥15 判据暂无理由需要显著放大"这条落地建议本身（两次都是 NR 观测，离散度
接近，对"这个采样量级够不够用"这个问题的参考价值依然成立），但改变了它
能支持的论证范围——不能再用它论证"NR 与 LTE 的离散度相近"，只能论证"两次
不同出口的 NR 观测彼此离散度相近"。

**真正的 LTE 侧 CV 对照**（若需要）应改算自 `evidence/m2_idleprobe_20260731/
idle_labelled.jsonl`（D-365，真实 LTE@106.80，4 run/36 场景）——但该文件只有
4 个 run，样本量本身是否够算 CV 存疑，本条不代算，留给需要这条对照线的下一
份判读。

**为什么会错**：与 D-424 备忘（`NR_RUN_REGISTRATION_20260802_019fc1a6.md` §6
已同批订正）是**同一个误标模式独立出现两次**——都是把"07-31 试点"这个日期
标签直接读成"LTE"，没有核对 `README.md` 早就记录过的"07-31 试点批跑在
NR_SA 上"这条既有结论。两份文件相隔一天、作者角色相同（v3），仍然重复了
同一个失误，值得作为方法论教训单独记一笔：**引用"试点"语料前，先查它的
RAT，不要凭日期/项目阶段的印象代入。**

---

## 10. 补充观察（D-454/T39，2026-08-03）：序位偏倚与预热效应——追加，不改原文

**与 L-3 并列存档原则一致：只追加，不改上文任何一句。**

T39（报告链彩排，`evidence/t39_report_chain_rehearsal_20260803/`）跑
`campaign_report.py` 时，摘要段落报出两条此前从未在本批语料上算过的观察：

- **疑似序位偏倚 3/9 处**：`n1_rtt_p50_ms` 在三个 profile（s1_chat/s2_coding_agent/
  s3_multimodal）上均现位置-KPI 相关，极差分别 19.2%/18%/10.8%（反平衡可能失效，
  须复核）。
- **疑似预热效应**：`n1_rtt_p50_ms` 首轮系统性更差 16.4%（首轮读数偏保守；跨格
  比较不受影响，因每格一样冷）。

**范围诚实标注**：这两条观察算的是 T39 的 16-run 池化语料（本文件 §0 已定的
「pounce#1（修复前）」+「15 个批采 run（修复后）」合并成 T39 单一格），**不是**
本文件 §1/§4 单独算过的「15 个批采 run」纯子集——本文件当年因二者 radio 健康状态
不同而刻意分开看（§0），T39 为了走报告链彩排把两者按 (点位,运营商,忙闲) 池化成
一格，序位效应/预热效应算的是这个池化后的 16-run 总体，**未单独对 15-run 批子集
重算过**。若要判断这两个效应是否也在纯批子集里成立，需要一次独立重算（本条不代
做）。

**意义**：与本文件 §4 的落地建议相呼应——D-379 的取证子集「每格 5 轮」设计
（每位次各 5 个样本）正是为了让「序位偏倚」这类效应可被识别而不是被样本量掩盖；
这两条观察是该设计价值的一次实证注脚：**先有 5 轮的判据，才有能力在真实数据上
看见 3/9 处序位相关，而不是稀里糊涂把它平均掉**。 | T39 完整备忘：
`evidence/t39_report_chain_rehearsal_20260803/README_REHEARSAL_MEMO.md` §3；
D-379/D-388（取证子集轮次设计）。

---

*T30 长窗批判读 · v3 · 2026-08-03 · 数据源=独立拉取的设备 Room 库副本；
批日志与 rat_watch.tsv 引用均逐项核实，非转录*
*§9 订正 · 2026-08-03 · 承 T34（另一会话核实）+ D-442（大脑裁定）*
