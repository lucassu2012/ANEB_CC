# `DW-20260831-01` 证据包（豆包 ＋ DeepSeek × {WiFi, 蜂窝}）

> **本包在飞** —— 窗未收，格陆续落地。**收窗前不得引用本包任何数值下结论。**

## 0. 这一窗是什么

- **批次 ID**：`DW-20260831-01`（命题单 `docs/BATCH_PROPOSITION_DW-NEXT.md`，锁定 `a85115f` @ 09:43）
- **窗令**：`docs/DW_NEXT_WINDOW_ORDER_DRAFT_20260831.md`（已激活；D-635／D-636）
- **格阵**（T33 §3，**逐格交替、不按条件分组**）：

| # | 格 | App | 形态 | 功能 | 轮 | 命题 |
|---|---|---|---|---|---|---|
| 1 | `wifi_f6` | 豆包 | WiFi | F6 图像生成 | 6 | P2 |
| 2 | `cell_f6` | 豆包 | 蜂窝 | F6 | 6 | P2 |
| 3 | `ds_wifi_f6` | DeepSeek | WiFi | F6 | 6 | **P1（预期失败＝产出）** |
| 4 | `ds_cell_f6` | DeepSeek | 蜂窝 | F6 | 6 | P1 |
| 5 | `ds_wifi_f1` | DeepSeek | WiFi | F1 短答 | 6 | P3 |
| 6 | `ds_cell_f1` | DeepSeek | 蜂窝 | F1 | 6 | P3 |

## 1. 采集参数（**开窗前定死，逐格照抄**）

```
python tools/e234/e234_collect.py --serial <SN> --pkg <PKG> \
  --roi 400,1800,400,200 --allow-real-device --device-window DW-20260831-01 \
  --session-seconds 700 --screencap-period-ms 1500 --framestats-period-s 1 --no-marks \
  --out evidence/wave1_20260831/<格名>
```

- **`--framestats-period-s 1`**：本批唯一相对 DW-02 改动的采集参数（D-599／D-600）。
  界 ＝ `127 × sf_latency 首行` ＝ `127 × 16666666ns` ＝ **2.117s**（读 DW-02 的值，
  **首格产物落地后回验**）；周期 1s **＜** 界 ✅。
  ⚠ **因此本批 C 侧与 DW-02 的 C 侧不直接可比**——那正是次命题 P4 要量的东西。
- **`--screencap-period-ms 1500` 与 `--session-seconds 700`**：**照抄 DW-02**，
  不是新选的——DW-02 `wifi_f6` 实测样本 467、跨度 700s、间隔 p50 **1500ms**（本窗开窗前实测复核）。
  **改它会破坏 P2 与 DW-02 初值的可比性。**
- **驱动器**：`tools/e234/drive_cell.py`，**每格记提交哈希**（P3／D-621）。

## 2. 逐格记录

（每格落地后补：轮数／答窗与静置的**意图值与实际值**／驱动器哈希／
`layer` 是否非空／`clock_pin.status`／`turn_method`／制式前后两读／`ip route get` 出口。）

### 格 1 · `wifi_f6`（豆包 × WiFi × F6）—— **完成**

| 项 | 值 |
|---|---|
| 轮数 | **6/6** |
| 答窗 意图／实际 | 75.0 ／ **75.01–75.04** |
| 静置 意图／实际 | 20.0 ／ **20.01–20.04** |
| 轮期实际 | **104.01–104.51s**（DW-02 同格 104.0 ⇒ **可比**） |
| 驱动器哈希 | **`b336a5b`** |
| 通道 C | `layer` ＝ `…AliasActivity1#2609` **非空**、**无 `sf_status`** ⇒ 成功（按订正后判据） |
| 通道 B | `screencap_samples` **466**（DW-02 同格 467） |
| 制式 开跑前／跑完后 | `NR_SA,Unknown` ／ `NR_SA,Unknown` |
| 出口 开跑前／跑完后 | `wlan0` ／ `wlan0` |
| `e2_precheck` | **`WORTH_RUNNING`**；**A 侧可用轮 6/6** |

> ⚠ **驱动器哈希与窗令不符，按「记实际不记意图」记实际**：窗令写 `a9abdf4`，
> **工作树实际是 `b336a5b`**（窗令写就时该修复尚未落）。
> **两版差异不触及签名**——`git diff a9abdf4 b336a5b -- tools/e234/drive_cell.py`
> 实录只有 `sh()` 的 `encoding=`／`errors=` 两个关键字与其 docstring，
> **标记与打字的先后一字未动** ⇒ 与 DW-02 的偏移签名同族。
> **这条是可核的，别当断言读——上面那条命令自己跑一次。**

**① 次命题 P4 在本格上成立**（周期 20 → 1）：

| 量 | DW-02 同格（周期 20） | 本格（周期 1） |
|---|---|---|
| dump 次数 | 35 | **583** |
| 相同／重叠／**不相交** | 17／8／**9** | 413／169／**0** |
| C 侧已观测间隔 | 25 | **42** |
| **不可判间隔** | **9** | **0** |

**机制与预期一致**：周期（1s）压到界（**2.117s**）以下 ⇒ 环装得下、相邻 dump 必然重叠
⇒ **「环之间有洞」归零**。⚠ **这是 1/6 格，不是全批结论**；P4 要六格都看。

**② 主命题 P2 复现**（按命题单 §2 P2 登记的口径自算，不引 DW-02 的数当第二源）：

| | 逐轮最大相邻间隔（ms） | 中位 | 事件数／轮 |
|---|---|---|---|
| 本格 | 8739／**2724**／9096／8978／9153／9165 | **9037.0** | 105／**236**／159／104／109／163 |
| DW-02 同格 | 9138／8533／9215／9291／**675**／8822 | **8980.0** | 117／106／114／113／**30**／130 |

**中位差 57ms（0.6%）** ⇒ 与 DW-02 初值一致。
⚠ **两格各有一个结构性离群轮，但形状相反**：本格轮 2 是**事件多、间隔短**（236 事件／2724ms），
DW-02 轮 5 是**事件少、间隔短**（30 事件／675ms）。**只有事件数能把它们分开，数值本身分不开**
⇒ **离群轮按结构剔除，不按数值剔除**（此为本窗第一次实用该判据）。

### DeepSeek 接线预备（格 3–6 用；**截图人工核，图未存档**）

**取法**：`adb exec-out screencap -p` 落 scratchpad（**仓外**）→ PIL 裁成顶条／底条、
**中间对话区随原图一起销毁，我不曾看到** → 读完即删。**只留下面这些数字。**
仓内既有方法本就如此（`tools/e234/drive_cell.py` 文件头：「用 screencap 人工核，
**不要用 `uiautomator dump`**」）。屏 **1200×2640**。

| 元素 | 坐标 | 验证状态 |
|---|---|---|
| ☰（左上） | `(75, 228)` | **仅目视识别，用途未验** |
| 「+」（右上） | `(1123, 232)` | **⚠ 用途未证实**——点它之后草稿 `draftprobe` **仍在** ⇒ **它没有开新对话**，或那一下没点到 |
| 输入框（键盘收起态） | `(290, 2370)` | **已验**：点后 `mInputShown` `false`→`true` |
| 发送键（键盘弹起态，蓝底 ↑） | `(1073, 1615)` | 目视识别；**未用动作验证**（验它就要发一次，占额度） |
| 「深度思考」芯片 | 弹起 `(220, 1615)`／收起 `(220, 2525)` | **已验**（见下） |

> 🔴 **本窗最重要的一个发现：DeepSeek 有两个默认开启的模式开关，而命题单没提过它们。**
> **样式语义是实验判出来的，不是看出来的**：点一下 → 由**浅蓝填充＋蓝字**变**白底＋黑字**
> ⇒ **浅蓝＝开、白＝关**；随即点回复原。
> - **「深度思考」默认 ON** —— 对 P1 是好事，**思考期确实存在**；
> - **「智能搜索」默认 ON** —— **这是混淆项**：DeepSeek 每问都会先联网检索，
>   而豆包的 F1／F6 都不检索。**P3 要比的「DeepSeek F1 vs 豆包 F1」会变成「带检索 vs 不带检索」。**
>
> ⇒ **两个开关必须在格 3 之前定死并记录状态**，否则 P1／P3 测的都不是它们自称测的东西。
> ⚠ 我**改动过设备持久设置并已复原**（两次点击，前后截图确认回到浅蓝＝开）——如实记。

_（格 2–6 待落）_

## 3. 状态


**已收窗**（2026-08-31 深夜；触发条款＝**D-643(c) 第三次机械性滑落无条件收窗**）。

---

## 4. 格 1b · `wifi_f6_b_VOID1`（豆包 × WiFi × F6）—— **作废**

**判词**：`e2_precheck` ＝ **`CANNOT_TELL`**；**A 侧可用轮 2/6**；**C 侧已观测间隔 1**。
**不得用作 P2 的任何一条腿。** 保留入仓，因为**它记录的失败模式本身是产出**。

### 4.1 表面全部正常——这正是它危险的地方

6/6 轮跑完、逐轮 **103.80–104.20s**（晨格 104.01–104.51，同）、`layer` 非空、无 `sf_status`、
`screencap_samples` 462、会话跨度 699s、`E4MARK` **12 条**（与晨格一致）。
**⇒ 按既有收窗清单逐条核，这一格会全绿。**

### 4.2 两条通道同时坏，而四个产物互相矛盾

| 产物 | 今晚／今晨 | 说明 |
|---|---|---|
| `sf_latency`（通道 C） | **0.04×** | 塌了 |
| `framestats`（也是通道 C） | **1.54×** | **反而涨了** |
| `adapter.log`（通道 A） | **0.34×** | |
| `screencap_index`（通道 B） | **0.98×** | **一模一样** |
| `E4MARK` | **12 ／ 12** | 驱动器完全正常 |

### 4.3 诊断（做到哪一步写到哪一步，没查出来的说没查出来）

**① `dump=45` 不是「只跑了 45 次」，是「只有 45 次可用」。**
`sf_latency` 的**周期头计数**：今晚 **569**、今晨 **583** ⇒ **采集循环跑得好好的**。
差的是每次拿回多少帧：今晚 **6284 行 ÷ 569 次 ≈ 11**，今晨 **74624 ÷ 583 ≈ 128**。
⚠ **我第一次把 `e2_precheck` 的 `dump=` 读成了尝试次数、并据此报了大脑；此处订正。**

**② 断点是干净的。** 逐段帧数 ＝ 前 45 段**整齐 127**，第 45 段起**全 0**
⇒ 会话开始后约 **55 秒**图层失效，此后 524 次全空，**且轮 2／4／5 有事件时也没恢复**。
⇒ **采集器开跑时只挑一次图层**（本格 `#2756`），图层一旦被重建就永久取不到帧；
而 `framestats` 走的是**包名**，所以照常——**§4.2 那张自相矛盾的表由此解释**。

**③ 两个主要假设都被实验否定，如实记：**

- **「是我盲点 `(600,1200)` 造成的」——否**：复现测试中该点前后**图层号不变**（`#2857`），
  两屏差异只有 `(866,67)–(914,88)` 那块状态栏，**未发生任何界面跳转**。
- **「是新对话动作重建了 surface」——否**：模拟 ☰→✎ 后**图层号不变**，
  **新旧图层名都取得到 128 行**。

**⇒ 图层为何在 55 秒处失效，本窗没查出来。不编。**

**④ 轮 1／轮 3 豆包完全没回答。** 逐轮内容事件数 **[0, 83, 0, 87, 222, 10]**
（晨格 [105, 236, 159, 104, 109, 163]）。轮 1 的 `turn_start` 到 `answer_complete` 之间
**一个事件都没有**。全部 634 条事件的 `pkg` **都是** `com.larus.nova`
⇒ **驱动器没有点到别的应用**。

**⑤ 「是不是额度触顶」——否，用一轮试水判死。** 收窗前单跑一轮，**254 条内容事件**，
豆包答得好好的 ⇒ **账号没触顶**；轮 1／轮 3 的零回答是**瞬时故障**，不是系统性封顶。

---

## 5. 收窗（`DW-20260831-01`）

### 5.1 本窗产出

| 格 | 判词 | 归属 |
|---|---|---|
| `wifi_f6`（10:17–10:29） | **`WORTH_RUNNING`**、A 侧 6/6 | **时段对照格**（D-640 ①）；**不是 P2 的腿** |
| `wifi_f6_b_VOID1`（22:22–22:34） | **`CANNOT_TELL`** | **作废**，保留作失败模式证据 |

**⇒ 本窗没有产出可用的 P2 对照腿。** F6 两条件对照仍以 DW-02 的 n=6+6 初值为准。
**格 1 的 P4 结果仍然成立**（见 §2），它不依赖 P2 的配对。

### 5.2 收窗为什么发生在这里

**D-643(c)：窗内第三次机械性滑落 ＝ 无条件收窗。** 三次如实列：

1. 取 DeepSeek 截图时**按「键盘收起态」的坐标带去裁，而键盘是弹起的**
   （浪费一次取图，**无数据损害**）；
2. 给速查卡加行时**锚只截到 1a 行的前两个字段**，把该行从中间劈开
   （**被我自己的字段数检查逮到、未进仓**，已复原并改为**按行插入＋两条断言**）；
3. 🔴 **我明知 logcat 缓冲只覆盖约 8 秒，却用 `logcat -d` 去量一个 89 秒的轮次**——
   **而这条正是我当天亲手写进速查卡步 1b 的**。幸而同一条命令里数了「缓冲总行 3597」，
   当场识破并改用**流式捕获**重测（重测得 254 条，**结论因此翻面：不是触顶**）。
   **⇒ 记住一条规矩，与在正确时刻想起它，是两件事。**

### 5.3 红线收窗自证（当场做）

前台＝华为桌面；豆包／DeepSeek 已 `force-stop`；**自家探针未 `force-stop`**（D-611），
`Bound services` 非空、`Crashed services:{}`；出口 `dev wlan0`、`wifi_on=1`、
制式 `NR_SA,Unknown`；`state UP` 隧道 **0**；PC 侧 `e234_collect` 进程 **0**；
仓根 `*.raw` **0**；仓内新增像素文件 **0**；温区 **39.8–40.1 °C**。

> 🔴 **自证查出一个两个窗都漏掉的缺口。**
> **scratchpad（仓外）里躺着 15 个整屏截图与 2 个 `roi_*.raw`，日期 08-29／08-30，合计 29.9 MB**
> ——其中 `roi_busy.raw`／`roi_idle.raw` **正是「收窗必删」点名的那一类**。
> **原因**：自证问的是「**仓根**有没有 `*.raw`」，而整屏像素**落在仓外**。
> **「查的地方」与「东西所在的地方」不是同一处，而清单读起来像已经查过了。**
> 本窗已全部删除（删后残留 **0**）。
> ⇒ **建议把自证项从「仓根」改为「仓根 ＋ 本会话 scratchpad」。**

### 5.4 移交给下一窗的三件

1. **图层中途失效，无解释、且当场看不出来。**
   ⇒ **`sf_latency` 只在开跑后查一次不够**：本窗步 1a 在 **30 秒**时通过，而失效发生在 **55 秒**。
   **下一窗需要一次约 2 分钟处的复查**——看它是否仍在**满帧**增长（每段应为 127），
   不满即**当场作废重跑**，别烧满 11 分钟与 6 轮额度。
2. **DeepSeek 的两个模式开关**（深度思考／智能搜索，**默认都开**）与四个坐标见
   §「DeepSeek 接线预备」；开关裁定＝**D-641**（3/4 格思考 ON ＋ 搜索 OFF；5/6 格双 OFF）。
3. **`drive_cell_ds.py` 尚未创建**：右上「+」的用途**未证实**（点它之后草稿仍在），
   发送键 `(1073,1615)` **只经目视、未经动作验证**。**不靠猜坐标盲点。**
