# ICS 转发可整形性干跑：**转发流量未被整形**；根因**未定**（2026-09-12）

> 判据**先于本份单独入库**（`0199092` 的 `CRITERIA_PREREG.md`，落在 PO 开热点之前）。
> 本份所有数字对着那份读。接线单＝`docs/ICS_FORWARD_DRYRUN_SETUP_20260912.md`。

## 0. 一句话结论

**PC 自发流量被整形（11 → 213 ms），同一时刻经 PC 转发的 P40 流量完全不受影响（27.1 → 27.6 ms）。**
⇒ 落判据 §3 判定表的**第 2 行或第 3 行**——**而区分这两行的那个证据（对照 B）没拿到**，
见 §3。⇒ **根因（层不对 vs 匹配没中）本份不判。** 两行的处置方向相反，故不得择一。

## 1. 前提五项（§0，我自己只读核的，非采信转述）

| # | 实测 | 判定 |
|---|---|---|
| P1 | `adb devices` ＝ `8MY0221126002537 device`，仅一台 | ✅ |
| P2 | `223.5.5.5 via 192.168.137.1 dev wlan0 table 1040 src 192.168.137.129` | ✅ **出口是 wlan0 且 src 在热点子网** |
| P3 | PC `arp -a`：`192.168.137.129  02-fb-c7-3e-fd-74` | ✅ 对端在网 |
| P4 | 焦点＝`com.huawei.android.launcher…UniHomeLauncher`；无 VPN/抓包进程；无 tun/ppp | ✅ |
| P5 | `sc query WinDivert` ＝ 1060 | ✅ 干净起点 |

🔴 **P2 这一轮不是形式**：PO 最初开热点时共享源被 Windows 自动选成了 WLAN，那块网卡有 IP、
有默认路由、Up 780 Mbps 而上游是死的（`ping -S` 对照：WLAN 3/3 全丢、以太网 0 丢）。
**那个状态下手机连得上、路由全对、就是不通**；若那时开跑会得到「RTT 没抬」，**而真因与
WinDivert 的层毫无关系**。P2 写成前提而不是脚注，这一轮兑现了。

## 2. 三阶段实测（P40 与 PC **并行**，同一时间窗）

| 阶段 | P40 → 223.5.5.5 | PC → 223.5.5.5 |
|---|---|---|
| 一 基线 | n=20/20 **中位 27.1**（14.1–180.0） | n=20/20 **中位 11.0**（10–12） |
| 二 整形中 `delay_lat200` | n=20/20 **中位 27.6**（26.1–29.4） | n=20/20 **中位 213.0**（211–222） |
| 三 收尾后 | n=20/20 **中位 26.4**（24.9–32.2） | n=20/20 **中位 11.0**（11–18） |

- **判据①（仪器判据）✅**：PC 抬 **+202.0**，落 `+200±30`。⇒ **这次整形确实起了**，
  不是「压根没起」那一行（判定表第 4 行排除）。
- **判据②（主判据）❌**：P40 中位 27.6，判据带 `[197.1, 257.1]`，**差一个数量级**。
  三阶段 27.1 → 27.6 → 26.4，**全程在噪声内，看不出任何注入痕迹**。
- P40 三阶段**零丢包**（20/20/20），⇒ 不是「量法没读数」那种阴性。
- 起停留痕：`14:31:49 START delay_lat200 => --filter out --dst-ip 223.5.5.5 --latency 200 …`；
  `14:36:56 STOP`。驱动 1060 → `TYPE 1 KERNEL_DRIVER / STATE 4 RUNNING` → 1060。
  工具自述 `matcher dst_ip: 223.5.5.5`，`WinDivert filter: outbound and (ip or ipv6) and
  (tcp or udp or icmp or icmpv6)`——**运行时日志里同样没有任何层/转发相关字样**。

## 3. 🔴 对照 B **不可判**——而且失效的是**我预登记的判据本身**，不是执行

判据 §2-③ 写：W0（双侧静默 30 s）量背景包率，W1（仅 P40 打 ping）量信号，
**W1 增量 ≥ 3×W0 且 ≥ 50** 记「涨」。实测：

```
W0  pkts 23433 -> 28522   delta = 5089     (背景, 30 s, 双侧静默)
W1  pkts 28522 -> 33343   delta = 4821     (仅 P40, 150 ping 全收, 29.9 s)
3*W0 = 15267 ;  W1 = 4821  =>  倍数门 FAIL, 绝对门 PASS
```

**按判据的字面，结论是「不涨」。本份拒绝这么记。** 理由：

- **信号只有约 150 个包，背景是 5089 个包 ⇒ 信噪比 2.9%。** 一个分辨不出 3% 变化的量法，
  它给出的「没涨」**不是「没有」的证据，是「看不见」的证据**。
- 我那两个门是为「背景小」的世界写的：**倍数门防的是背景高时假阴，而它在背景高到
  淹没信号时会直接把真信号也判成阴**——**这正是它本该防的那个失效，只是我把它写成了
  「背景高」而不是「背景高到信噪比 3%」。判据的适用范围我自己也写宽了一格。**
- ⇒ 正确的记法：**对照 B NOT_EXECUTED（量法分辨率不足）**，判定表第 2 行与第 3 行
  **均未被排除**。两行处置方向相反（第 2 行「别改菜单或 filter」，第 3 行「可修」），
  **故不得择一**。

⚠ **背景为何这么高未查**：双侧静默时 PC 出站 ≈170 包/秒。它可能是 PC 自身后台，
也可能**正是被转发的设备流量**（若真如此，反而说明转发包被看见了）。**这两种成因在
本份的数据上不可分**，而分开它们需要一个能压过背景的流量源——见 §6。

## 4. 🔴 我的跑前预测：**未被证实**，尽管结果与它同向

判据里我写下的预测是第 2 行「**驱动根本没把转发包交上来（层不对）**」。
主判据的结果**与该预测同向**（P40 确实没被整形）。**但区分第 2 行与第 3 行的唯一证据
就是对照 B，而它不可判** ⇒ **预测未被证实，也未被推翻。**

**这一条单独写出来，是因为它是最容易被顺手记成「预测被证实」的一格**：
结果与预测方向一致时，人会把**未验证的那半**一起算进已验证。
本份的实际状态是：「**转发流量没被整形**」已证；「**因为层不对**」**没证**。

## 5. 未拿到的两项（如实记，不补叙）

- **LAN 段分离**（判据 §1）：`ping 192.168.137.1` 在设备上**无任何回应**
  ⇒ Windows 对入站 ICMP 默认丢弃。**记为量法不可用，不是 LAN 段不通**
  （证据：同一时刻 P40 到 223.5.5.5 20/20 全通，链路显然在）。
  ⇒ 「P40 那 27 ms 抬在哪一段」本份答不了——**但本轮 P40 根本没抬，该项不影响主判据。**
- **对照 B**：见 §3。

## 6. 偏离预登记之处（两处，逐处写明）

1. **W1 的 ping 参数**：判据写 `-c 100 -i 0.2`（＝20 s），而同一条判据要求 W0/W1
   **等长 30 s** —— **判据自身两句不一致**。执行时取「等长」为准，用 `-c 150 -i 0.2`
   填满 30 s 窗口；`≥50` 的绝对门不变（实发 150 全收）。
2. **对照 B 的门限失效**：见 §3，属判据缺陷，已按「不可判」处置而非按字面判「不涨」。

## 7. 收尾与现场（判据⑤）

```
schtasks /Run /TN ANEB-Shaper-Stop  ->  进程无残留 ; sc query WinDivert = 1060 已卸载
start.log: 2026-09-12T14:36:56 STOP
收尾后 RTT: PC 11.0(回基线) ; P40 26.4(回基线)
设备侧: 焦点仍为华为桌面 UniHomeLauncher ; 无 VPN/抓包/tun 残留
       本次我在设备上只跑了只读的 ping 与 ip route/dumpsys/ps, 未启动任何 App 或服务
热点与 USB 未动(按大脑交代, 留给后续可能的传输构成采集)
```

## 8. 流量账

P40 侧 ping 20＋20＋20＋150＋10(网关,无回应) ≈ 220 次；PC 侧 60 次。**全部 ICMP，量级 < 100 KB。**
**不涉 E-01、不涉那道 200 MB 闸。**

## 9. 本份能支撑与不能支撑的结论

**能**：
- 经 PC 转发的设备流量，**在本工具当前配置下不被整形**（同一时刻 PC 自发流量被整形 +202 ms）。
- 因此 **B2 搭建单第 8 步那一类验证（从设备侧观察到注入）在 ICS 路径上不成立**。
- 延迟档与丢包档此前的「通过」**只对 PC 自发流量成立**，**不构成战役可用**。

**不能**：
- **不能**判根因是层（`NETWORK_FORWARD`）还是匹配（NAT 前后地址）——对照 B 不可判。
- **不能**据此判 gnirehtet 路径可行与否（另一条路径、另一份验证）。
- **不能**据此重开三选一——那需要根因，且属大脑裁定范围。

## 10. 要往下走需要什么（不由本份决定）

分开第 2 行与第 3 行，需要**一个能压过背景（≈170 包/秒）的设备侧流量源**，
使信噪比至少到数倍而非 3%。本份**未试**，因为大脑交代「任一步不过就停，不试参数」。
可选方向（均需先裁）：①设备侧持续大流量（需确认设备上有可用的生成手段）；
②改用**字节**口径而非包数（仍受同一背景问题制约，须先量背景字节率）；
③在 PC 侧用独立抓包工具按 IP 过滤（`pktmon` 系统自带，但要管理员窗）。
