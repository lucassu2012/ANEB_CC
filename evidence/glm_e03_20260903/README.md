# `GLM-E03-20260903` 证据包（GLM 真端点 × token 流量法对照）

> 🔑 **目录名与批次 ID 的映射（放最上面，承上一批的教训）**：
> **批次 ID ＝ `GLM-E03-20260903`，目录 ＝ `evidence/glm_e03_20260903/`** ——
> 同源但**大小写与分隔符不同**，字符串比对不会相等。上一批（`DW-20260831-01` ↔
> `evidence/wave1_20260831/`）因「钥匙没刻在门上」害人找错过一次派件路径，故本行前置。

- **命题单**：`docs/BATCH_PROPOSITION_GLM_E03_20260903.md`（双签钉 `1b0a44e`）
- **开跑令**：D-669⑥（唯一 GO，大脑一处发出）
- **判读产物**：`app/probe/src/test/java/.../GlmRealWireE03Test.kt`（**生产解析器**吃真 wire）

---

## 0. 一句话结论

**P1 不成立**，而**发现不是「谁数错了」，是一个名字**：`arrivals`／「token 到达」
**实为「内容帧到达」**；两者在 GLM 上差 **~1.5%**，且**差额随长度增长**。

---

## 1. 前置（当场取值，不引用旧值）

| 项 | 值 |
|---|---|
| 时刻 | **2026-09-03 11:37:03** |
| C 盘可用 | **352.9 MB**（E 104.2 GB） |
| 工具链 | `compileDebugUnitTestKotlin --rerun-tasks` ⇒ **21 actionable: 21 executed** ＋ BUILD SUCCESSFUL |

⚠ **判据用汇总行不用任务行**：`preBuild`／`preDebugBuild` 是**无动作的生命周期锚任务，
Gradle 永远标它们 UP-TO-DATE**。我原先写的「任务行零 UP-TO-DATE」**照字面永远不可能通过**，
会以错误的理由把前置判红（D-672⑤ 已订正）。

## 2. 数据（12 笔，用满 ≤12 上限）

**冒烟三笔**（闸门，`max_tokens=800`）：解析器 243／265／241 vs usage 248／269／246，
`finish_reason` 全 `stop`。⇒ 闸门结论：**GLM 默认即发 usage，无需 `stream_options.include_usage`**。

**正式 3×3**（三档 × 三笔，生产同构体：无 `stream_options`、无 `temperature`）：

| 档 | 帧数（＝解析器数出） | usage | `finish_reason` | 差 | 差/usage |
|---|---|---|---|---|---|
| t60 | 59／59／59 | 60 | `length` | **1** | 1.7% |
| t150 | 148／147／148 | 150 | `length` | **2／3／2** | 1.3–2.0% |
| t800 | 246／253／242 | 250／257／246 | `stop` | **4／4／4** | ~1.6% |

「解析器数出 == 非空 content 帧数」在**九格全部成立**（JVM 测试断言，非事后核对）。

## 3. 判读（两解并列，均**未排除**）

按 §1b-2 **开跑前写死**的分支：**差额随长度增长 ⇒ 撤销「帧合并已排除」**。

| 候选 | 状态 | 依据 |
|---|---|---|
| **帧合并**（一帧多 token）⊂ §2「量法缺陷」 | **未排除，领先候选** | 差额近似按 usage 的 ~1.5% 成比例；与已确立的「`arrivals` 实为内容帧到达」**同源互证** |
| **分词口径差**（服务端计入不下发的 token）⊂ §2「口径差」 | **未排除** | 无 GLM 分词器；「每 N token 合一帧」与「某类 token 不单独成帧」**在本批数据上同形** |
| ~~固定开销（EOS 型）~~ | **已否证** | 截断轮（无自然结束 token）差额**非零且随长度走**：1 → 2–3 |

⚠ **不写「已证实」**：比例一致**支持**帧合并，但证不了机制。**两解并列是结论，不是待办。**

### 3.1 ⚠ 我先前的结论错在哪里（本批最值钱的一段）

冒烟阶段我写过「差额恒定 4–5、与长度无关 ⇒ **固定开销**，帧合并解释不了」。**它被本批否证。**
错因**不是算错**：冒烟三笔的 usage 全落在 **241–269 一个窄带**里，
而**在单一长度带上，「恒定」与「成比例」不可分辨**——我据此**排除**了一个候选，
那是**把窄带上的观察外推到全域**。

⇒ **3×3 正是为拆开这一点设计的，它兑现了。**
⇒ 也是「**判读分支写死在开跑前**」第一次真正起作用：**若不是写死的，
拿到 1／2／4 时我很容易读成「还是很小，仍算恒定」。**

## 4. 池纪律（九取三）

`finish_reason` 非 `stop` ⇒ **不进 P3 形状池**（形状被人为截断，进池污染形状结论），
**但仍进 P1 差额分析**（usage 反映实际吐出的量，对账依然有效）。
⇒ t60／t150 六笔只进 P1；仅 t800 三笔进 P3。**两个用途不混池。**

## 5. ⚠ 归因订正：「夹具转换环节被钉住」的**真钉子**是哪一条（D-671③）

命题单正文把该结论挂在「v4 两侧计数一致」上——**那个推理撑不起它**（计数只覆盖帧数一维）。
**真钉子**是 `tools/e03/tests/test_glm_capture.py::test_the_kotlin_fixtures_are_a_verified_derivation_not_a_second_copy`，
它断言 **`actual == expect`——全文相等**（仅归一化行尾）：仓内 `.sse` 必须能由证据 jsonl **逐字重生**。

⇒ **结论为真、归因写错。** v4 七轮曾据此判「读高」并建议收窄，**核实后自撤**（`d1bce6f`）。
⚠ **教训**：**把一个真结论削弱，比留一个错理由更难被发现——因为削弱后的文本读起来更谨慎、更像对的。**
与「理由错了 ≠ 守卫没用」**互为镜像**（D-671②）。

⚠ 另：该守卫此前**不在 `verify_all.ps1` 清单里**（本仓「门真绿、只是不在清单上」第三例），
即这个结论一度**挂在一条不进门禁的守卫上**。**本批已接入**（`obs-tools-e03-unit`，gate_count 25→26）。

## 6. 目录

| 格 | 用途 |
|---|---|
| `smoke_a`／`smoke_c` | 闸门：生产同构体（无 `stream_options`） |
| `smoke_b` | 闸门：＋`stream_options.include_usage` |
| `t60_{1,2,3}`／`t150_{1,2,3}`／`t800_{1,2,3}` | 正式 3×3 |

每格含 `raw_sse.jsonl`（逐行原始 SSE ＋ 宿主时戳）与 `capture_meta.json`
（**结构上不含请求头**：`build_meta()` 不接受 header/key 形参）。

⚠ `smoke_*` 的 `note_label_fix` 字段是**事后就地补正的标签**（未重跑调用）。
**它的文本里引用了字段名 `params.stream_options`**，曾让一次「把整份 JSON 序列化再搜」
的检查三笔全命中而误判——**注记引用字段名会污染按名搜索的量法**，
对策是**搜那个字段本身，不搜整个对象**。
