# c_2x_decompose_20260912 — 2× 分解（战役证据包）

> 本 README 是**包说明**（`evidence/README.md`「目录形态」节：每个日期包自带 README，说明批次、口径与复现命令）。
> 它**只指向、不复述**：口径以同目录 `CRITERIA_PREREG.md` 为准，本文件不抄任何判词、区间或计数。

## 状态（四态）

**测量：`NOT_EXECUTED`。** 本目录里**没有任何一份 stdout 含抓到的包**
（截至 2026-09-26 逐份核过：全部止于 `err=5` 未提权、提权门拒跑、或第一跳门拒跑）。
⇒ 本包**尚无任何关于「2× 由什么构成」的结论**，也不得被引作结论。

未执行的原因（截至 2026-09-26）：正式轮需要**管理员窗**，且需**设备到目标的第一跳往返**成立；
另有终审待修项未清——**以最新一份终审裁定为准**（当前为 `docs/C_2X_DECOMPOSE_REVIEW_V4_20260918.md`），
本 README 不枚举其条目（枚举会过期，且子集会被读成全集）。

## 包内文件是什么

| 文件 | 性质 |
|---|---|
| `CRITERIA_PREREG.md` | 判据，**跑前写死**。PREREG 守卫（`scripts/tests/test_prereg_precedes_results.py`）机械保证它先于本 README 入库 |
| `MECHANISM_SWEEP.md` | 机制扫描：考虑过哪些机制、排除哪些及理由、**哪一片没扫过** |
| `stdout_<YYYYMMDD-HHMMSS>.txt` | 探针每次运行**自写**的逐字副本（UTF-8，不经控制台代码页）。**全部是仪器自证／烟测的 `NOT_EXECUTED` 记录，不是测量结果** |

⚠ **stdout 副本不可追改**：其中印出的任何数（例如早期几份里的门条数）只在**印出那一刻**成立，按史实读；
订正写在判据或裁定里，不改这些文件。
⚠ 要知道某一轮跑的是哪些字节，**读那一轮 stdout 的前几行**（脚本自印 `WORKTREE_CLEAN/DIRTY`、SHA256、`git rev-parse HEAD`），
**不读本 README**。运行时间线用 `git log -- evidence/c_2x_decompose_20260912/` 查。

## 复现命令

```
python scripts/diag/decompose_2x_probe.py
```

- **须在提权壳里跑**。脚本在加载驱动之前依次自查，任一不过即**拒跑**（退出码非 0，并把原因印进逐字副本）：
  自我标识 → 提权门 → 量法自证（本解释器能否读出 `ping.exe` 的发包数）→ 第一跳往返（收到回复，不是 `rc==0`）→ 其余前提。
- 逐字副本自动写进本目录，无需重定向。
- 收尾只清**本轮自己装的**那份驱动（跑格前服务已在则不停不删），终局标签由收尾之后的读数算出。
- ⚠ Windows 原生工具的退出码经 python `returncode` 或 PowerShell `$LASTEXITCODE` 读；**不用 bash `$?`**（8 位截断，1060 会读成 36）。
