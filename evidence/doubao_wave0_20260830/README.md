# 豆包先行批 · wave0 · 2026-08-30

**批次**：豆包先行批（T78/T80）｜**设备窗** `DW-20260830-01`｜**条件** WiFi / 蜂窝（自然对照，无整形）
**时间窗**：2026-08-30 约 10:20–11:17（各格时间戳见下表）｜**执行方式**：**自主操作模式**（会话经 adb 驱动，授权载体 `D-587`／`D-588`）

> **🔴 本批未完成**：计划 4 功能 × 2 条件 + 锚格，**实际完成 5 格**（`wifi_f1` / `wifi_f2` / `cell_f1` / `cell_f2` / `cell_f1b`）。**锚格仍未跑。**
> 其余格未采。**本文只记已采到的，不替未采的下任何结论。**

## 1. 格阵实况与每格时间戳

| 格 | 条件 | 功能 | 轮数 | 收工时刻 | 标记 t/a | ADAPTER_EVT | 通道B | `e2` |
|---|---|---|---|---|---|---|---|---|
| `wifi_f1` | WiFi | F1 文本 | 6 | 10:38:59 | 6/6 | 727 | PASS，检出 13 | `NOT_EXECUTED`（n=4 dropped=2） |
| `wifi_f2` | WiFi | F2 深度思考 | 6 | 11:00:02 | 6/6 | 2451 | PASS，检出 21 | `NOT_EXECUTED`（n=2 dropped=4） |
| `cell_f1` | 蜂窝 | F1 文本 | 8 | 11:16:50 | 8/8 | 1076 | PASS，检出 25 | `NOT_EXECUTED`（n=3 dropped=5，**A 侧**） |
| `cell_f2` | 蜂窝 | F2 深度思考 | 6 | 12:00:37 | 6/6 | — | PASS，检出 29 | **`FAIL`（n=6 dropped=0）** |
| `cell_f1b` | 蜂窝 | F1 文本（**答窗 45s**） | 6 | 12:12 | 6/6 | — | PASS，检出 21 | `NOT_EXECUTED`（n=4 dropped=2，**A 侧**） |

## 2. 网络切换时间戳（G-4）

**全部切换见同目录 `switch_log.txt`（本节只放指针，不复述其中任何一行）。**
⚠ **2026-08-30 夜订正**：本节原文**引了四条切换里的第一条**，而本包收窗时 `switch_log.txt` 已有 **4 行**
（两窗各两次）⇒ **只读 README 的人会以为整批只切过一次**。**引一行就是第二真相源**，
且它**不会报错**——被引的那行仍然为真，假的是「这就是全部」这个印象。
⇒ 现改为纯指针。**起始状态：块 1 之前为 WiFi，未做切换。**
**下游读者**（协议 G-4 原文点名）＝**漂移自检与锚格判读**。

## 3b. 制式（G-5）

`gsm.network.type` **原样值 = `NR_SA,Unknown`**（逗号分隔＝按 SIM 卡槽，承载在卡 1）。
本批**蜂窝条件是 5G 独立组网，不是 LTE**；四格前后读数一致，**未观察到制式漂移**。

## 4. 每功能的提示词（逐字，5–8 轮完全相同）

- **F1**：`What is 5G in one sentence`
- **F2**：`Think step by step and explain in detail: why is the square root of 2 irrational?`
- F5／F6：**本批未采**。⚠ **2026-08-30 订正**：本行原写「提示词已由裁定给出，见协议」，**是假的**——全 `docs/` 搜 F5／F6 的提示词裁定**零命中**（同一量法搜 `What is 5G` 命中 5 处，证明量法在这些文件上工作）。§②-A **G-2 只规定了 F1 的提示词**，F5／F6 **从未被裁**。⇒ **下一窗必须先定这两句并抄进 README**，否则 G-2「每功能一句固定提示词、逐轮逐字相同」无从遵守。
- **F3／F4：`NOT_EXECUTED`** —— 原因＝自主模式下文件／图片选择的 UI 导航未验证（**非能力否定**），大脑 2026-08-30 裁定本窗砍。

## 5. F3/F4 素材

**N/A**（该两功能本窗未采，见上）。

## 6. UI 栈判定读数

**`cadence_p50_ms = 99.4`**（切前台后**首条** `ADAPTER_OBS`）⇒ 落 99–112ms 带 ⇒ **仍 View 系**，照 §2 开跑。
⚠ **必须读首条**：同一会话 5 秒后即被流式稀释到 **23.8**（该量是会话累计中位数）。

## 7. ROI 与两帧自检

**ROI = `400,1800,400,200`**（1200×2640 屏）。两帧自检：静止帧 **232.90** / 流式帧 **223.90**，**差 9.00 ≥ `flip_threshold=8.0`**。
⚠ **全屏均值差仅 0.72** —— 大 ROI 会把变化平均掉，必须小块且落在新文本渲染处。

## 8. 锚格 vs 块 1

**本批未跑锚格**（未完成）。**故本批无漂移锚，不得据本批下「无漂移」的结论。**

## 9. `deviceidle` 白名单

`user,com.aneb.probe,10306` —— **既有残留，非本批添加，收窗未撤**。`stay_on_while_plugged_in=7` 同为既有值，本批未改。

## 10. 全部偏离（四态诚实）

1. **本批未完成**：3/9 格。
2. **`e2_analyze` 前三格未出判词，第四格出了判词而判词是 `FAIL`** —— 前三格：两通道失败模式相反（短负载 C 侧切不出簇、长负载 A 侧切不出簇），**共用 400ms 门限**所致；**且落在哪一侧随条件翻面**（`wifi_f1` 丢在 C，`cell_f1` 丢在 A）。
   **`cell_f2` 是本批唯一让 e2 跑起来的一格**（n=6 dropped=0），而它给出 **`FAIL`**：`|t_A − t_C|` p99 = **19805.8ms** vs 判据 **1 帧 = 16.667ms**，逐轮 −17913/−19806/−16841/−16205/−14330/**+3306** ms（**符号会翻**）。**时钟对齐极好**（`wall_to_boot` 残差 p50 0.26ms、`clock_pin` 漂移 1563ns）⇒ **这 20 秒不是时基问题，是两条通道各自指认了不同的事件。**
   ⇒ **本批仍无可用的跨通道印证**，但理由从「跑不起来」变成了「**跑起来了、并且不成立**」。
3. **`s` 标记未发** —— 自主模式下其精度（±3s，见 `D-588`）对 2–3 秒量级的 TTFT 无意义，**故不发一个已知无意义的数**；`t`／`a` 轮界完整。
4. **`mark_rtt.jsonl` 不存在** —— 采用 `--no-marks` ＋ 会话外发 `E4MARK`（判读器读 `adapter.log`，见协议）。**故无每条标记的 adb 往返上界记录。**
5. **三个作废格**（按 §④-A′ 改名保留、未删）：`_VOID1` 冒烟标签错（`E4MARK` vs `AnebE4MARK`）致标记全失；`_VOID2` 采集器 `PermissionError` 崩溃；`_VOID3` 5 轮版 n=4 < `GATE_MIN_N`。
6. **实际轮数**：wifi_f1 6、wifi_f2 6、cell_f1 8、cell_f2 6、cell_f1b 6，**外加前置与作废共约 31 轮**（含 `cell_f2_VOID1` 的 6 轮）⇒ 本窗豆包总消耗约 **63 轮**。
7. **🔴 双峰未复现，且我的判决性检验设计有缺陷**：`cell_f1b`（同功能同条件，仅答窗 17.4s→45s）**零个高值**。但我**一次改了两个量**（答窗 **与** 时段），故**不能归因**。「答窗边沿触发」这个具体机制**已被排除**（值没有跟着窗长挪到 ≈43s，而是回到正常 TTFT）。正确设计是**同一格内交替两种答窗**；本窗额度不足以补。详见 `FINDINGS_RAW.md` §1a。
7. **一次操作事故**：误入实时语音通话界面（单独用了「新对话」第二击坐标），**即时退出、无残留**，未产生任何 F7 数据。

## 11. 已装探针 / 被测 App

- 探针 `com.aneb.probe`：**`versionName=0.1.0-phase0`，`lastUpdateTime=2026-08-20 11:17:40`**（**9 天前的构建**；`adapter/` 自安装日起零提交，源码↔二进制对应见协议 §1.D）。
- 豆包 `com.larus.nova`：**`versionName=14.7.0`**。

## 11b. 绝对值的时间窗限定

**本文所有绝对值仅对 2026-08-30 10:20–11:17 这一窗成立**，不得写成「豆包的 TTFT 是 X」。
**且本批无可用的跨通道印证**（见偏离 2——`cell_f2` 跑出了判词，但判词是 `FAIL`），绝对值只有通道 A 的自报值。
🔴 **本段已整段撤回（2026-08-30 傍晚，对抗核查后；详见 `FINDINGS_RAW.md` §0）。**
原文写「那四轮回答在 ≈15.7s 才开口、被截断在开口约 1.7s 后」——**实测那四轮的内容事件在 +5.3~5.9s 就跑完了，回答根本没有被截断**。原文那三条否证也已作废（其中最强的一条是分母错误，归一化后论证翻面）。
**现在的正确说法**：那四个 ≈15.7s 的值**不是 TTFT**，它们是「渲染流里第一次停顿超过 400ms 的时刻」的兜底值；而**低值轮的语义仍未判定**。⇒ **`cell_f1` 的绝对值一个都不要单独引用。**

## 12. 复现命令

```
python tools/e234/e234_collect.py --serial <SN> --pkg com.larus.nova \
  --roi 400,1800,400,200 --allow-real-device --device-window DW-20260830-01 \
  --session-seconds 600 --screencap-period-ms 1500 --no-marks \
  --out evidence/doubao_wave0_20260830/<格名>
```
**最终采用 `--screencap-period-ms = 1500`**（实测周期 p50 1500.4ms）。
轮界由会话外发：`adb shell log -p i -t AnebE4MARK "E4MARK kind=turn_start n=<N>"`（**标签是 `AnebE4MARK`，`E4MARK` 只是载荷前缀**）。

## 13. 原始观察

见同目录 **`FINDINGS_RAW.md`** —— 含 `ttft_cluster_ms` 的去重测量值与**其语义边界**（每会话只测一次，OBS 条数 ≠ 测量次数）。
**判词归判读侧（v3），本文不下结论。**

---

## 14. 收窗复验与红线自证（2026-08-30 12:33，逐条实测，不用「应该」）

### 14.1 设备复验（`adb` 实跑，非推断）

| # | 项 | 实测 |
|---|---|---|
| 1 | 前台 | `com.huawei.android.launcher/...UniHomeLauncher` —— 已回华为桌面 |
| 2 | 默认出口 | `1.1.1.1 via 10.10.0.1 dev wlan0` —— **已从 `rmnet0` 恢复为 WiFi** |
| 3 | `wifi_on` | `1` |
| 4 | 被测 App | `am force-stop com.larus.nova` 后 `ps -A` 中 grep `larus` **无输出** |
| 5 | 活动隧道 | `ip link` 里 `tun`/`ppp`/`ipsec` 各项**均无 `state UP`** |
| 6 | PC 侧采集进程 | `e234_collect` 匹配数 **0** |
| 7 | 仓根 `*.raw` | **0 个** |
| 8 | 温度 | 各区 **36.8–38.1 °C**（对照：`--screencap-period-ms 400` 时曾见 cluster1 57 °C） |

⚠ **一项如实记、未动**：`stay_on_while_plugged_in = 7`（已启用）。**本窗未设置过它**，
也无法核实是谁、何时设的 ⇒ **按「不覆盖未知会话的状态」不予改动，只报**。

### 14.2 红线自证

| 红线 | 自证 |
|---|---|
| **只计长不读内容** | **全窗未用 `uiautomator dump`**（它会把整屏文本落盘）。定位控件用的是 `screencap` PNG，**全部落在 scratchpad、未入仓**；仓内无任何截屏。 |
| **凭据绝不代输** | 全窗未输入任何密码。恢复 WiFi 用 `svc wifi enable`，**网络为设备已保存项，无凭据交互**。 |
| **SSID/BSSID 不入库** | 本包及本窗全部提交中**无 SSID/BSSID**；出口只记 IP 与网卡名。 |
| **不碰他会话未跟踪文件** | 全部提交均以 `git commit <pathspec>` 指向 `evidence/doubao_wave0_20260830/`。 |
| **G 树只读** | 本窗**未对 G 树发起任何写操作**。 |
| **ROI 原始截屏收窗必删** | 仓根 `*.raw` = 0；`.gitignore` 的根锚定 `/*.raw` 仍在。 |

### 14.3 两起操作事故（如实，均已即时处置、无残留）

1. **误入实时语音通话界面** —— 单独点了「新对话」那一对里的第二击，而抽屉未开，
   坐标落在电话图标上。**即时退出，未产生 F7 数据，未留残留。**
2. **`KEYCODE_BACK` 退出了整个 App** —— 本意是关抽屉。随后三次盲点全落在华为桌面与手机管家上，
   `probe` 一词打入未知位置。**已核：未触发「一键优化」、未改任何设置**；`am start` 拉回后为原会话、输入框干净。
   ⇒ **驱动器已改为每段盲点序列之前复核 `mCurrentFocus`，不是豆包即抛错停住。**
   **教训**：一段盲点序列里只要有一步的效果与假设不同，**其后每一步都会精确地点在别的应用上，且全程零报错**。

### 14.4 交接给判读侧

- **数与限定**：本 README + `FINDINGS_RAW.md`（**两者必须一起读**；绝对值全部带时间窗与截断限定）。
- **本批未完成**：5/9 格，**锚格未跑** ⇒ **不得据本批下「无漂移」结论**。
- **最需要下一批做的一件事**：`FINDINGS_RAW.md` §1a 的单变量实验
  —— **同一格内交替两种答窗**，以及**发送后 60s 不做任何操作**、看第二簇是否仍形成。

---

## 15. 第二个设备窗 `DW-20260830-02`（2026-08-30 下午–傍晚）

> **本包现共 10 格**（DW-01 五格 + DW-02 五格）。**T78 的 F1／F2／F5／F6 四功能 × 两条件已齐**，
> F3／F4 仍 `NOT_EXECUTED`（自主模式下文件／图片选择的 UI 导航未验证，非能力否定）。

### 15.1 格阵

| 格 | 条件 | 功能 | 轮 | 答窗＋静置 | A 侧 | 前置断言 | e2 |
|---|---|---|---|---|---|---|---|
| `wifi_f1_anchor` | WiFi | F1 | 6 | 18.2s ＋ 无 | 3/6 | `CANNOT_TELL` | `NOT_EXECUTED` |
| `wifi_f5` | WiFi | F5 联网搜索 | 6 | 45s ＋ 20s | **6/6** | **`WORTH_RUNNING`** | `FAIL` p99 10431.6 |
| `cell_f5` | 蜂窝 | F5 | 6 | 45s ＋ 20s | **6/6** | **`WORTH_RUNNING`** | `FAIL` p99 11455.5 |
| `cell_f6` | 蜂窝 | F6 图像生成 | 6 | 75s ＋ 20s | **6/6** | **`WORTH_RUNNING`** | `FAIL` p99 12289.1 |
| `wifi_f6` | WiFi | F6 | 6 | 75s ＋ 20s | **6/6** | **`WORTH_RUNNING`** | `FAIL` p99 17230.2 |

**静置期**＝答完标记之后、切下一轮之前什么都不做的一段（大脑 08-30 批，两条件：时长定死进本文 ＋
**逐轮把实际值落盘**）。实际值见各格的 `*_driver_timing.jsonl`：
答窗意图 45.0／75.0 → 实际 45.015–45.05／75.02–75.19；静置意图 20.0 → 实际 20.01–20.05。
⚠ **写下这条纪律的直接原因**：DW-02 首格把常量写成 17.4 而等待循环按 1.0s 轮询，
**实际跑出 18.2s**——一个精确的常量被一个粗的循环悄悄改掉，而锚格正靠它可比。

### 15.2 两条可用的结果

**① F5（联网搜索）：两条件间未测出可辨差异。**
WiFi 中位 **4163.2ms**、蜂窝 **3822.2ms**（蜂窝低 8%，方向与「蜂窝更慢」的朴素预期相反）。
**两格全部 12 个值都验过不是兜底**（逐轮最长渲染静默 625.9–2721.4ms，**0/6 轮 <400ms**）。n=6+6，LOW。

**② F6（图像生成）：主要耗时对这两条件不敏感。**
答窗内最长渲染静默：蜂窝中位 **9127.9ms**（极差 765.2）、WiFi 中位 **8980.0ms**
（剔除轮 5 那个 674.9ms 的真离群点后极差 758.6）⇒ **中位差 1.6%、离散度差 1%**。
⚠ **「它是服务端生成时间」是机制假设不是实测**；同样相容的是「两条件带宽都够、传输可忽略」。
**两者操作上同义、机制上不同，本批分不开 ⇒ 判读只用操作版。**
⚠ **F6 的 `ttft_cluster_ms` 语义存疑**（首字多半是文字确认不是图），**本批不用它下任何结论**。

### 15.3 锚格失败（如实）

`wifi_f1_anchor` **作为漂移锚不可用**：与 `wifi_f1` 相比答窗 17.3→18.2s、轮期 23.0→29.2s，
差因是**我为防误点他应用而加的前台复核**与**重写驱动器时放长的 sleep**。
⇒ **本批仍然没有可用的漂移锚**，§8 那句「不得据本批下无漂移结论」继续有效。
⚠ 而这一格意外触发了本批最大的一次自纠——详见 `FINDINGS_RAW.md` §0 总撤回。

### 15.4 收窗复验（`adb` 实跑）

前台已回华为桌面；默认出口 `wlan0`（与开窗前一致）；`wifi_on=1`；豆包已 `force-stop`；
无 `state UP` 隧道；PC 侧 `e234_collect` 匹配数 **0**；仓根 `*.raw` **0**；温区 **37.9–39.0 °C**。
⚠ 同 §14.1 一项**只报不动**：`stay_on_while_plugged_in = 7`，**本窗未设置过它**。

### 15.4b 🔴 各格的驱动器版本（D-619 ② 补记）——**而本批只能记签名，记不了哈希**

**为什么要记**：`turn_start` 与打字斜坡的先后**由驱动器版本决定**，
**会在两个窗之间静默改变，且光看数据认不出来**（判读侧若假设「标记在前」会得出假偏移）。

**实测签名**（`EditText` 的 `txt_len` 递增段末尾 相对 `turn_start` 的中位偏移，负＝打字在标记之前）：

**取点定义（先说死，否则数没法比）**：
**参照点 ＝ `AnebE4MARK kind=turn_start` 那条 logcat 行的时刻**；
**被测点 ＝ 该轮打字斜坡的「最后一个」`ADAPTER_EVT type=content cls=…EditText txt_len>0` 事件**。
斜坡段的切法：`txt_len` 单调递增且相邻间隔 < 1s，段长 ≥ 3 个事件。

**全十一格用统一取点重量一遍（2026-08-30 深夜补全；此前只重量过六格）**：

| 版本 | 格 | 中位偏移（取斜坡末尾） | 符号 |
|---|---|---|---|
| **一版** | `wifi_f1` | **+1.84s** | ＋ |
| **一版** | `cell_f1` | **+1.92s** | ＋ |
| **一版** | `wifi_f2` | **+2.62s** | ＋ |
| **二版**（ROI 判答完） | **`cell_f2_VOID1`** | **−2.08s** | － |
| 三版 | `cell_f2` | −2.00s | － |
| 三版 | `cell_f1b` | −2.00s | － |
| 三版 | `wifi_f1_anchor` | −2.04s | － |
| 四版 | `wifi_f5` | −2.00s | － |
| 四版 | `cell_f5` | −1.99s | － |
| 四版 | `cell_f6` | −2.09s | － |
| 四版 | `wifi_f6` | −2.00s | － |

**⇒ 翻转点在「一版 → 二版」之间；二／三／四版同侧（−1.99 ～ −2.09s）。**
**⇒ 四版谱系不动摇「符号由驱动器版本决定」这个结论**——归属格不变，「全格全轮无例外」仍立。
**🔴 而钉住翻转点的那一格是 `cell_f2_VOID1`——一个作废格。**
它是唯一跑过二版的格；**没有它，翻转点只能定位到「一版与三版之间」这个更宽的区间。**
⇒ **「作废格是数据」这条在这里兑现了一次具体的价值，不只是口号。**

**⚠ 一处如实记的异常**：`wifi_f2` **轮 1 ＝ +53.48s**（同格另五轮 2.54–2.65）。
这是**段匹配的假象**——该轮标记附近没有斜坡，最近邻取到了远处一段。
**中位数不受它影响，但这个值本身不可用**；写出来是因为**逐轮表里留一个没解释的 53 秒，下一个人会拿它当发现**。

---

**（下表为最初那六格的详表，含斜坡段长，保留）**

| 格 | 中位偏移（**取斜坡末尾**） | 斜坡段长 | 版本 | 顺序 |
|---|---|---|---|---|
| `wifi_f1` | **+1.84s** | 0.50s | **首版** | **先标记，后打字** |
| `cell_f1` | **+1.92s** | 0.53s | **首版** | 同上 |
| `wifi_f2` | **+2.62s** | 1.26s | **首版** | 同上 |
| `cell_f2` | **−2.00s** | 1.19s | 改版（固定答窗） | **先打字，后标记** |
| `wifi_f5` | **−2.00s** | 1.29s | 改版＋静置期 | 同上 |
| `cell_f6` | **−2.09s** | 0.91s | 改版＋静置期 | 同上 |

**🔴 本表订正了我 2026-08-30 深夜首次报出的那组数，两处**：
**① 取点不一致**——我当时对**负偏移取斜坡末尾、对正偏移取斜坡起点**（代码里「最近一个 ≤ 标记的事件；
若无则取之后最早的」两个分支落在斜坡的两端）。**而斜坡长 0.5–1.3s，对一个 2 秒量级的效应是重量级的**。
`wifi_f2` 因此从 **+1.36s（起点）** 订正为 **+2.62s（末尾）**。
**② 「多数轮该轮内无斜坡」是假象**——那是我那句粗糙的「取最近一个 ≤ 标记的」回溯了整整一轮造成的。
**按段匹配后，每一轮都有斜坡**，`wifi_f1`／`cell_f1` 是 **+1.84／+1.92s**，稳定而非缺失。
⇒ **结论（符号由驱动器版本决定）不但成立，而且更干净**：首版 **+1.8～+2.6s**、改版 **−2.0～−2.1s**，
**全格全轮无例外**。⚠ **斜坡段长随提示词长度走**（F1 短句 ≈0.5s，F2／F5 长句 ≈1.2–1.3s）
⇒ **跨功能比这个偏移时，必须说清取的是斜坡哪一端。**

**✅ 2026-08-30 深夜更新(D-621 驱动器入仓令)**：驱动器已入仓 → **`tools/e234/drive_cell.py`，提交 `a9abdf4`**。
⚠ **但这个哈希只对上表下半段有效，且不是逐字节相同**：
**① 覆盖范围——🔴 我最初写的这一条是错的，现订正。**
我原写「`cell_f2`／`cell_f1b`／DW-02 五格用的是它」。**逐格核过产物后：`a9abdf4` 只覆盖四格。**
**判据**：`*_driver_timing.jsonl` 是该版才有的产物，**只有 `wifi_f5`／`cell_f5`／`cell_f6`／`wifi_f6` 有**；
`cell_f2`／`cell_f1b`／**`wifi_f1_anchor`** 都没有 ⇒ **它们跑的是更早的一版**。

**版本谱系（全量搜完，更早的版本一个都不剩）**：

| 版 | 跑了哪几格 | 存续 |
|---|---|---|
| 一版 | `wifi_f1`／`wifi_f2`／`cell_f1` | **已丢** |
| 二版（ROI 判答完） | `cell_f2_VOID1` | **已丢** |
| 三版（固定答窗，无静置、无逐轮落盘、轮询 1.0s） | `cell_f2`／`cell_f1b`／**`wifi_f1_anchor`** | **已丢** |
| **四版 ＝ `a9abdf4`**（＋静置期＋逐轮落盘＋轮询 0.05s） | `wifi_f5`／`cell_f5`／`cell_f6`／`wifi_f6` | **在仓** |

**⚠ 而丢掉的三版仍可从数据认出来，判据两条**：
**(a) 有无 `*_driver_timing.jsonl`**（四版才有）；
**(b) 答窗实际值与意图值的偏差**——三版轮询 1.0s ⇒ **常量 17.4 跑出 18.2（溢出约 800ms）**；
四版轮询 0.05s ⇒ **意图 45.0 跑出 45.015–45.044（偏差 <45ms）**。**轮询粒度本身就是版本指纹。**
⇒ **搜索找不到源码，不等于认不出版本**——但**认出「是哪一版」与知道「那一版长什么样」是两回事**，后者已永久丢失。
**② 非逐字节相同**——入仓时我加了一段 docstring（入仓理由、坐标的设备前提、红线提醒），
**可执行代码一字未改**，但**文件本身与当时跑的那份不是同一串字节**。这一点要写出来，
否则日后有人拿 `a9abdf4` 去做逐字节比对会得出「对不上」的结论。

**⚠ 原先那段「不可执行」的记录保留如下，因为它是这条规矩最强的论据**：
**那个驱动器从未入仓**——它整批都活在会话的 scratchpad 里，`git log --all -- '*drive_cell*'` **零命中**。
⇒ **决定这个偏移的那样东西，本身不在版本控制之下。**
⇒ 本节只能记**可从数据复原的签名**（上表，任何人拿 `adapter.log` 都能重算），**记不了版本身份**。
⇒ **给下一批的硬要求**：**驱动器必须入仓**——否则 X7 那条规矩**写得再对也执行不了**，
而「执行不了的规矩」与「没有规矩」在结果上一样，**只是前者读起来像已经防住了**。

### 15.5 交接

- **台账**：`corpus-ledger-fresh` 在窗内一直红着（新增格未提交），**收窗后由 v3 按 T81 §3.1 重算**，我不中途刷。
- `evidence/phase0/badges.txt` 与 `sha256-manifest.txt` 为他人跑 `verify_all` 重写、非本窗产物，我不代提交。
- **豆包轮数**：本窗 30 轮，累计约 **93 轮**。⚠ **免费档上限仍未核**，只知 **> 93**。
