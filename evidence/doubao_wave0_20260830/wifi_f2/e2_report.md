# E2 三通道同轨对拍 —— 判读结果

> 出处：`spec/adapters/INSTRUMENTATION_SPEC.md` §3.3 E2、§3.4 G-3。
> 状态词只用 PASS / FAIL / NOT_EXECUTED。空样本一律 NOT_EXECUTED，不折 0。

## 0. 这一份为什么只有两个时刻

spec §3.3 E2 说的是「三个时刻」。通道 B 的时戳是**宿主侧**的，与设备时钟之间
隔着一次**从未标定过**的 adb 往返，故本脚本不拿它做时序比较（沿用 spec §2.2 与
e1 的既有裁断）。B 在这一页只出一条佐证：它有没有看见这些翻转、采样周期多大。

## 1. 前提

| 项 | 值 |
|---|---|
| 目标包 | `com.larus.nova` |
| 可用内容事件 / 他包滤除 / 量纲拒收 | 2441 / 10 / 0 |
| 切轮方式 | `operator-marks`（轮数 6） |
| 一帧 | 16.667 ms（实测，非硬编码 33ms —— spec §3.1） |
| 帧记录条数（去重后 / 重复丢弃） | 1730 / 1987 |
| 墙钟↔BOOTTIME 标定 | PASS（n=2451，残差 p50 0.272 ms / max 4.869 ms） |
| 时钟钉桩（BOOT−MONO） | PASS（偏移 84664573136564 ns，前后漂移 520 ns） |

## 2. `|t_A − t_C|`（判据本体）

| 量 | n | dropped | p50 (ms) | p90 (ms) | p99 (ms) | 判定 |
|---|---|---|---|---|---|---|
| \|t_A − t_C\| | 2 | 4 | 14920.010 | 16101.563 | 16101.563 | **NOT_EXECUTED** — 样本量不足：n=2，另有 4 条被丢弃 < 最小 5（本判据 PROVISIONAL 门限）——n<100 时 p99 恒等于最大值，n 太小则「p99 ≤ 1 帧」退化成「这一次没超」，不足以当结论 |

**该判定**已设**最小 n（e1_analyze.GATE_MIN_N，n 不足即 NOT_EXECUTED），但**不看 dropped**（T14 C-2 余半）；本次 n=2 dropped=4**

有符号分布（**方向本身是信息**：spec §2.1 记「事件不保证像素已上屏，可能早于或晚于呈现，连符号都未知」）：p50 -16101.563 / p90 -14920.010 / p99 -14920.010 / min -16101.563 / max -14920.010 ms。

未能出数的轮次（逐条计数，不静默）：

- 通道 A 该轮不足两簇（A2 无判据）：4 轮

## 3. 通道 B 的佐证（**不是时刻**）

采样 n=397，周期 p50 1500.381 ms / p99 1900.203 ms，检出变化 21 次。

> 检出次数不是时序主张，只说明 ROI 与阈值选得对不对。

## 4. 逐轮

| 轮 | A 簇数 | C 簇数 | 该轮帧数 | Δ = t_A − t_C (ms) |
|---|---|---|---|---|
| 0 | 1 | 2 | 253 | — |
| 1 | 3 | 3 | 259 | -16101.563 |
| 2 | 1 | 3 | 253 | — |
| 3 | 1 | 3 | 253 | — |
| 4 | 2 | 4 | 379 | -14920.010 |
| 5 | 1 | 3 | 253 | — |

