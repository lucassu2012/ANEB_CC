# 豆包先行批 · 原始观察(采集侧记录,不作判读结论)

> **本文只记「量到了什么」与「量的语义边界」,判词归判读侧(v3)。**
> 采集:DW-20260830-01,自主操作模式(D-587/D-588),`--screencap-period-ms 1500`,ROI `400,1800,400,200`。

## 0. 🔴 总撤回(2026-08-30 傍晚,对抗核查后)——先读这一节

**本文 §1a / §1a-bis / §1b / §2 里关于「高模/双峰」的因果结论,全部作废。**
撤回由我自己发起的对抗性核查(三份独立重算 ＋ 三条不同透镜的反驳 ＋ 一份综合重放)促成,
三条要害我已逐条自查复核成立:

**① `null` 是一个合并 token,而我把它全读成了「没形成次簇」。**
`ObsStats.kt:120` **`TTFT_CEILING_MS = 30_000.0`**,`sane()`(:125–126、:313)**把 >30s 的簇对静默转 `null`**。
⇒ `null` 同时可能是:**没形成 / 形成了但 >30s 被删 / 形成了但没播报**——**日志里三者长得一模一样**。
**⚠ 出处分层(v3 复核后补,2026-08-30 傍晚)**:
**(i) 机制层——我独立验过,成立**:`cell_f1b` 逐轮最大内容事件间隔 = `356.2 / 403.8 / 615.2 / 672.5 / 381.3 / 710.2` ms ⇒ **只有轮 1 与轮 5 全程 <400ms**,
而**恰恰就是这两轮为 `null`**。⇒ 「这两轮的簇窗没闭合、值兜底、再被天花板删」这条**机制成立**。
**(ii) 幅值层——我没验,是转述**:`49470.3 / 49459.0`(以及 `wifi_f2` 的 `46655.3 / 46638.5`、
`cell_f2` 轮6 的 `21365.9`)来自对抗核查的设备侧状态机重放,**我未独立复算**。
v3 用宿主侧 `v3_anchors` 重算得 `2172.6 / 1903.5`——**那不是反证,是另一个量**
(设备侧按输入活动重置、宿主侧按标记切轮,同名不同义)。
⇒ **在有人独立复算出幅值之前,别引这几个数;而撤回不依赖它们**——`sane()` 存在即足以
让「零个高值」不成立。**这一条是我把子代理的数当成验过的传了出去,v3 咬住的。**

**② 真机制:400ms 簇分割门限切在一个单峰连续量的正中,方向与我的结论相反。**
只改门限、驱动器一字节不动的扫描:**200–300ms 处高模全灭,400→1200ms 单调迁移**
——**这是阈值伪影的签名,不是双峰现象的签名**。触发量＝**答窗内最大内容事件间隔**,
排序后 `…383.0 / 389.2 / 389.7 ‖ 400.6 / 403.8 / 411.4…`,**400ms 处没有任何断层**。
⇒ **高模标记的是渲染最顺、一次卡顿都没有的那些轮**(簇窗一直没闭合,兜底成了驱动器下一次点屏)。
⇒ **我自称最薄弱的那一问(`wifi_f1` 与 `cell_f1` 同轮期为何一有一无)有答案了**:
`wifi_f1` 六轮最小间隔 **483.9ms 全在 400 之上**,`cell_f1` 八轮**有五轮在其下**。

**③ 我最强的那条论证是分母错误。** 我拿**4 轮**高值极差 196ms 比**8 轮**答窗极差 604ms 说「稳三倍」;
**⚠ 而「同样那 4 轮」是哪四轮,本身没定死,我原来那个 0.98 是过度精确了**:
按机制(最大内容间隔 <400ms)筛出的候选是**五轮 {2,3,6,7,8}**,而高值只有四个 ⇒ 五种组合:
`{2,3,6,7}` 极差 201ms(比值 **0.98**)、其余四种极差 539–604ms(比值 **0.32–0.36**)。
⇒ **正确写法是「比值 0.32–0.98,五种可能全部 <1」**——**结论(论证不成立)在任一组合下都成立,
但那个 0.98 只是五分之一,不该被当成「那个数」。**
(v3 用的是 `{3,4,7,8}` 得 413ms,**其中轮 4 的最大间隔 646.8ms > 400 ⇒ 它的簇窗闭合了、
值是真 TTFT,不该算进高值轮**。)
锚格更糟:高值三轮的答窗极差仅 **21ms** 而高值极差 76ms ⇒ **比自己的尺子松 3.6 倍,论证翻面**。

**⇒ 现在成立的三段判词**:
「这些 15/22 秒**是采集侧伪影**」＝**已判定**(比我原来论证得硬,但**换了机制**);
「**由驱动脚本轮次节拍造成**」＝**不成立**;「**不是豆包或网络的性质**」＝**不成立**
(触发量就是豆包渲染流的性质;**是否被网络调制:未判定**,本批分不开)。

**④ 🔴 最该记的一条,关于我自己:证据早上就在我手里,我没看见。**
「高模在 WiFi 上出现」——**`wifi_f1_VOID1` 里就有 `ttft_cluster_ms=17054.2`**
(WiFi、同一早上、**原驱动器**),**而它在我几小时前自己的终端输出里印过**
(`[1880.1, 6073.4, 17054.2, 2910.7]`),我扫过去没停。
⇒ **我为了拿这个证据开了第二个设备窗,并在那一格里把漂移锚毁掉了。一条 `grep` 本来就够。**
⚠ 并记:**六位独立工作者里 0 人碰过作废格**——**作废格被所有人当成「没有数据」,而它们是数据。**

---

## 1. `ttft_cluster_ms` 的不同测量值(去重后的变化点,已按标记逐轮归位)

| 格 | 轮数 | 逐轮取值(ms) |
|---|---|---|
| `wifi_f1` | 6 | 1977.7 / 3291.6 / 3936.7 / 431.5 / 2944.6 /(轮6 未变) |
| `cell_f1` | 8 | 2642.4 /(轮2 未变)/ **15636.6** / **15719.1**+3013.6 / 2806.5 /(轮6 未变)/ **15558.4** / **15754.8** |
| `wifi_f2` | 6 | 仅轮2 3251.3、轮5 3267.3,其余四轮无第二簇 |
| `cell_f2` | 6 | 2919.6 / 3829.6 / 3917.7 / 3431.4 / 3832.7 /(轮6 无) |

| **`cell_f1b`** | 6 | **4161.1 / 3626.8 / 3713.1 / 3856.7**(答窗 45s)——**零个高值** |
| **`wifi_f1_anchor`** | 6 | 435.0 / **22538.9 / 22566.6 / 22490.8** / 4066.2 / 3594.2(**第二窗 DW-02**,WiFi,答窗 18.2s)——**三个高值,跨度仅 76ms** |
| **`wifi_f5`** | 6 | 4145.5 / 4181.0 / 3702.0 / **7883.2** / 3863.8 / 4228.8(DW-02,WiFi,答窗 45s ＋ **静置 20s**)——**零高值,且 6/6 轮全出值** |

**形状一:蜂窝 F1 双峰**——3 次 2.6–3.0s、**4 次 15.6–15.8s**,高峰簇跨度仅 ~196ms。
**形状二:蜂窝 F2 无高模**——五个值全在 2.9–3.9s。

### 🔴 1a-bis. 第二窗的锚格把 1a 推翻了:高模在 WiFi 上出现,而且换了个数

`wifi_f1_anchor`(2026-08-30 13:44,**WiFi**,与 `wifi_f1` **同一句提示词**):

```
wifi_f1        WiFi 轮期 23.0s 答窗 17.3s -> 零个高值(最大 3936.7)
cell_f1        蜂窝 轮期 23.0s 答窗 17.4s -> 15558/15637/15719/15755  跨度 196ms
wifi_f1_anchor WiFi 轮期 29.2s 答窗 18.2s -> 22491/22539/22567        跨度  76ms
```

**三件事同时成立**:①**高模在 WiFi 上出现了** ⇒ 它不是蜂窝/5G 的性质;
②**数值跟着驱动节拍从 15.7s 移到 22.5s** ⇒ 它不是一个固定时长的 App 或网络机制;
③**离散度反而更紧**(196→76ms) ⇒ 对网络现象不合理。

⚠ **但本文不下判定**,因为有一处答不上来:**`wifi_f1` 与 `cell_f1` 轮期同为 23.0s,
为什么前者没有高模而后者有?** 这是「伪影」这一解释最薄弱的地方。
已发起对抗性核查(独立重算 ＋ 三条不同透镜的怀疑者),**结论强度待其回来后再写**。

### 🟢 1a-quater. 静置期把三件事一起修好了,并回答了那个判决性预测

`wifi_f5`(DW-02,WiFi,F5 联网搜索,答窗 **45s** ＋ 答完后**静置 20s** 才切下一轮):

| | 结果 |
|---|---|
| 高模 | **零个**。预测过「若由驱动器造成,应出现在 ≈45+20=65s 或轮期 74.5s 附近」——**都没有** |
| `ttft_cluster_ms` | **6/6 轮全出值**,且全部在轮内 **+5.93～6.13s** 播报(一个节流周期),无「延到下一轮才播报」 |
| `e2_analyze` | **n=6 dropped=0**,A 簇 5/6/3/2/4/6、C 簇 2/4/3/2/3/4,**两侧全够** |

🔴 **本段的因果已撤(见 §0)。** `wifi_f5` 干净的真实原因是**它六轮的最大内容事件间隔 646–2721ms
全部远超 400ms**(核查扫到 600ms 门限时它仍是 0 高值)——**与静置 20s 无关**。
下面这三行观测仍然为真,但**不能作为「驱动器一停手就干净」的证据**:
**把切轮动作推远 20 秒,高模整个消失,而且每一轮的真簇都形成并被正常报出。**
⚠ **但仍不是判定**:F5 与 F1 是不同功能、答窗也从 17～18s 变成 45s,**我一次动了三个量**
(功能、答窗、静置)。**能说的是方向一致,不能说是哪一个起的作用。**

**⚠ 而 e2 的判词仍是 `FAIL`**:p99 = **10431.6ms** vs 判据 1 帧 = 16.667ms。

🔴 **本节此前写「连同 `cell_f2`,两格两次都 FAIL,不再是孤例」——现予收回一半。**
v3 交付的前置断言 `tools/e234/e2_precheck.py` 对 `cell_f2` 判**「记录太碎:不可判间隔 12 > 可核静默 9
⇒ C 侧的次簇有一半以上可能是 dump 排期的洞,不是思考静默」**。**我据此写的『拿真事件去减一个仪器空洞的边界』这个说法不准,而且我把它推广得太宽——见下。**

🔴 **二次订正(v3 复核 ＋ 我自查):**
**(a) 我那条『凡 e2 在 C 侧丢的轮都可能是 dump 空洞』是错的,且适用范围只有 2 轮。**
被丢的轮逐格拆开:`wifi_f1` 丢 2 轮(**全在 C 侧**)、`cell_f1` 丢 5 轮(**全在 A 侧**)、
`wifi_f2` 4 轮、`cell_f1b` 2 轮、`wifi_f1_anchor` 3 轮——**除 `wifi_f1` 那 2 轮外,全部是 A 侧丢**。
而那 2 轮的 `frames` 是 **127 / 126**,**不是零** ⇒ 它们拿到了一个完整的环,被丢是因为
**那一个环内部没有 >400ms 间隔**,不是没数据。⇒ **空洞盖住整轮才会丢轮;空洞落在轮内造的是假静默(多一个簇)。
症状相同(该轮出不了数),病因相反,修法也相反。**

**(b) `cell_f2` 的 19805.8ms 应当整个撤出证据链,不留作『存疑支撑』。**
真正的理由比我写的狠:逐轮 `frames` 恒为 127/253 ⇒ **一轮只拿到一到两个环**,
而单环覆盖实测中位 **4.3s**、该格轮期 **86.6s** ⇒ **C 侧每轮只看得见约 5–10% 的时长**。
⇒ **C 的 A2 基本上就是环缓冲碰巧停下的位置**,那 19.8 秒**更像在量『dump 排期与答窗的相位差』**,
不是在量任何通道间偏差。**『存疑但保留』在下游会被读成『两格都 FAIL 的第二个数』**——
这正是今天反复吃到的亏,故整条撤出。

**(c) 由此露出一条我自己的处置造成的副作用**:每轮拿到的环数几乎与轮期无关(恒一到两个),
**所以轮期越长,C 侧的可见比例越低**——实测 `cell_f1`/`anchor`(轮期 23–29s)为 **15–19%**,
而 `cell_f2`/`wifi_f5`(轮期 74–87s)只有 **5–11%**。
⇒ **我为压住 A 侧伪影而加的静置期,把 C 侧变得更瞎了。** 又一次:一个消费方的修法是另一个的退化。

而 `wifi_f5` 前置断言判 **PASS**(可核静默 14 次、A 侧 6/6 轮可用)⇒ **只有它的 FAIL 值得采信**,
且它也只是「可核多于不可判」(14 vs 10),不是干净。
⇒ **正确说法:e2 目前只有一个可采信的 FAIL(`wifi_f5`);`cell_f2` 那个已整条撤出。**
「在带内跑起来命题依然不成立」这条**仍是单格支撑**,不是两格。

### 🔵 1a-quinquies. 本批第一个功能级信号(带一个无法消除的混淆)

同为 WiFi:**F1 文本对话中位 ≈2945ms,F5 联网搜索中位 ≈4163ms,高约 41%**。
方向与「多一次检索往返」相符。
⚠ **混淆无法消除**:F5 的提示词本身长得多(`Search the web and tell me: what is the latest
stable Android version released this year?` vs `What is 5G in one sentence`),
**更长的提示词本就意味着更多 prefill、更高 TTFT**。而**每个功能各有自己的提示词是设计,不是疏忽**
⇒ **「功能差」与「提示词长度差」在本设计下永远共线**,除非另做等长提示词对照。
**n=5/6,LOW,登记级。**

### 🔴 1a-ter. 而锚格自己废了——它作为锚不可用,原因是我自己引入的两个变量

锚格的唯一职责是与 `wifi_f1` **逐字同条件**。实际:

| | `wifi_f1` | `wifi_f1_anchor` | 差因 |
|---|---|---|---|
| 答窗 | 17.23–17.48s | **17.43–18.26s** | `ANSWER_WAIT_S=17.4`,而等待循环按 **1 秒**轮询,溢到 18.2 |
| 轮期 | 22.76–23.05s | **28.06–29.25s** | 为防误点他应用而加的前台复核 ＋ 重写驱动器时放长的 sleep |

⇒ **本批仍然没有可用的漂移锚**,§8 那句「不得据本批下无漂移结论」继续有效。
⇒ 形状:**我为安全加的检查、和我为精确写的常量,一起毁了那个「什么都不许变」的格**。
「改一个参数前先列出还有谁在读它」——**读它的正是锚格**。
⚠ 一处**错误归因已自纠**:我最初把轮期变长归给 `dumpsys window`,实测单次仅 **0.12–0.19s**、
三次共 0.4s ⇒ **那个解释是错的**,差额来自 sleep。**猜的机制与量出来的机制不是同一个。**

### 🔴 1a. 判决性检验的结果是**阴性**——形状一没有复现

`cell_f1b`:**同一功能、同一条件、同一提示词,只把答窗从 17.4s 改成 45s**,6 轮。
**四个值全在 3.6–4.2s,零个高值。** 按 `cell_f1` 的 4/8 高值率,6 轮全不中的概率约 **1.6%**。

**但我不能据此下判**,因为**我一次改了两个东西**:答窗(17.4→45s)**与时间**(相隔约 1 小时,射频条件未受控)。
⇒ **两个候选仍然并列**:①高值与我的 17.4s 答窗有关;②高值是真实的、随时间变化的网络/服务端现象,这一格没赶上。

**⚠ 而「答窗边沿触发」这个具体机制被排除了**:若第二簇是被答窗关闭那一刻触发的,
45s 窗下的值应当挪到 ≈43s。**它没有,它回到了正常 TTFT(3.6–4.2s)。**

**⇒ 这是我的设计错误,写下来给下一批**:判决性检验**只能变一个量**。
正确设计是**在同一格内交替 17.4s 与 45s 两种答窗**——那样时间被摊平在两组之间,
**而我把两组放到了不同的时段**。本窗额度已不足以补(剩约 7 轮,交替设计需 ≥8 轮)。

⚠ **本节此前把形状一称作「本批第一个真信号」,现予收窄**:它是**一个未复现、成因未判定的形状**。

### 1b. 我对「高值是我自己的驱动器造成的」这个假设的排查(结论:否)

`cell_f1` 用固定等待,**答窗恒 ≈17.4s**,而高值 15.56–15.75 **恰比它低 1.6–1.9s**;
四个高值又**全部播报于该轮 `turn_start` 之后 +0.04/+0.09/+0.11/+0.20 秒**(正常值播报于 +5.0～5.4s)。
两条都指向「测的是我自己的超时」。**但三条独立证据否掉了它**:

1. **`wifi_f1` 用同一驱动器、同一 17.4s 答窗**(17.47/17.30/17.25/17.23/17.25/17.48),**零个高值**。
2. **离散度反证**:我的答窗抖动 ≈600ms、轮期抖动 ≈570ms,而**四个高值跨度仅 196ms**——
   **比造它的那把尺还稳三倍,就不可能是那把尺造的**。
3. **播报晚 ≠ 事件晚**:`ADAPTER_OBS` 由活动驱动 + 5s 节流。第二簇在 ≈16.0s 形成后该轮再无内容事件
   ⇒ 不再发 OBS,直到我 ≈21.5s 打下一轮的字才恢复。**同一形状在没有高值的 `wifi_f1` 轮3 也出现**
   (播报于 +17.42s、值 3936.7)⇒ **它是播报机制,不是高值成因**。

⇒ **代价如实**:高值那四轮回答在 ≈15.7s 才开口,而我 17.4s 就切走 ⇒ **那四轮的回答被截断在开口约 1.7s 后**。
**`cell_f1` 的 TTFT 可用**(事件发生在截断前),**但那四轮的流式/节奏/通道 C 不可用于任何「回答多长/多稳」的判断**。

## 1d. 🟢 F6 条件对照——**本批最像「网络诉求」的一个结果,而它的答案是「不敏感」**

F6 独有的量:**答窗内最长的一段渲染静默**(最可能对应「等图出来」)。逐轮实测:

| 条件 | 逐轮最长静默(ms) | 中位 | 极差 |
|---|---|---|---|
| 蜂窝 `cell_f6` | 9022.5 / 8926.1 / 9111.7 / 9691.3 / 9144.1 / 9157.2 | **9127.9** | 765.2 |
| WiFi `wifi_f6` | 9138.3 / 8532.0 / 9214.5 / 9290.6 / **674.9** / 8821.8 | **8980.0** | 8615.7 |

**⚠ `wifi_f6` 轮 5 的 674.9ms 是个真离群点**,与同格另五轮(8532–9291)差一个数量级。
**我不知道那一轮发生了什么**——可能没生成图、可能只回了文字、可能被拒。**不猜,只登记。**
**剔除该轮后**:WiFi 中位 ≈9000、极差 **758.6**——**与蜂窝的 765.2 几乎完全一致**。

⇒ **F6 那段约 9 秒的等待,在 WiFi 与蜂窝之间中位差 1.6%、离散度差 1%。**
⇒ **可操作的结论:该负载的主要耗时对这两条件不敏感**——
**它更像服务端生成时间,而不是网络传输时间。**
⚠ **但「服务端生成」是我给的机制,不是我测到的**:同样与数据相容的解释是
「**两条件的带宽都足够,传输那一段本来就可以忽略**」。**两者在操作上同义**(这个负载在这两条件下不受网络限制),
**在机制上不同**,而本批**分不开**。⇒ 判读只用操作版。

**两格 A 侧全 6/6、前置断言均 `WORTH_RUNNING`,且 C 侧是全批最干净的两格**
(可核静默 **23 / 25**,不可判 10 / 9;对比 F5 两格的 10 / 14)。
**机制**:9 秒的静默期间环缓冲不推进 ⇒ 相邻 dump 内容完全相同 ⇒ 该静默落在同一连续段内 ⇒ 可核。
**下界很好写:只要静默 > 单环覆盖(≈4.3s),它就跨得过 dump 空洞。** F6 的 8.9–9.7s 是约 2 倍余量。

⚠ **F6 的 `ttft_cluster_ms` 语义存疑,本文不用它下任何结论**:F6 的「首字」多半是文字确认
(「好的,正在为你生成…」),**不是图**。⇒ **F6 只用「最长静默」这一列。**
**并且不与 F5 的 `ttft` 并排比较**——`wifi_f5` 中位 4163.2 与 `cell_f6` 中位 4171.9 几乎相等,
**而两个几乎相等的数最容易被并排放,恰恰在它们不是同一件事的时候最危险。**

## 1c. 🟢 F5 条件对照——**本批第一对「每个值都经过验证不是兜底」的格**

先解 `null`,再报数。按 §0② 的机制,判据是**逐轮「答窗内最大内容事件间隔」与 400ms 的关系**:
`>400ms` ⇒ 簇窗闭合 ⇒ 该轮出真 TTFT;**全程 `<400ms` ⇒ 簇窗不闭合 ⇒ 值兜底成驱动器下一次点屏**
(再可能被 30s 天花板删成 `null`)。逐轮实测:

| 格 | 逐轮最大内容事件间隔(ms) | 全程 <400ms 的轮 |
|---|---|---|
| `wifi_f5` | 888.8 / 713.5 / 1499.7 / 978.9 / 646.0 / 2721.4 | **0 / 6** |
| `cell_f5` | 880.9 / 806.5 / 1346.0 / 625.9 / 740.1 / 814.0 | **0 / 6** |
| (对照)`wifi_f1` | 1250.3 / 558.1 / 655.4 / 483.9 / 746.5 / 1077.8 | 0 / 6 |
| (对照)`cell_f1` | 442.0 / 345.2 / 344.3 / 646.8 / 636.0 / 383.0 / 321.7 / 323.4 | **5 / 8** |

⇒ **`wifi_f5` 与 `cell_f5` 的全部 12 个值都是真 TTFT,没有一个是兜底。**
(这四行是我**独立于对抗核查**重算的,与其报数逐值相同——两条不同代码路径。)

**结果**:

| 条件 | 逐轮 `ttft_cluster_ms`(ms) | 中位 | e2 | 前置断言 |
|---|---|---|---|---|
| WiFi | 4145.5 / 4181.0 / 3702.0 / **7883.2** / 3863.8 / 4228.8 | **4163.2** | FAIL p99 10431.6 | `WORTH_RUNNING` |
| 蜂窝 | 3527.1 / 3843.3 / 3837.0 / 3792.4 / 3807.5 / 4114.1 | **3822.2** | FAIL p99 11455.5 | `WORTH_RUNNING` |

⇒ **F5 在两条件间未测出可辨差异**(蜂窝中位低 8%,方向与「蜂窝更慢」的朴素预期相反;
去掉 WiFi 那个 7883.2 的离群点后两侧离散度相当,587 vs 526ms)。
**n=6/6,LOW,登记级。**

⚠ **仍在的限定**:①两格相隔约 30 分钟,**时段未受控**(比 F1/F2 那一小时好,但不是零);
②`e2` 两格皆 `FAIL`,而按 §0 与 1a-ter,**C 侧每轮只看得见 5–11% 的时长**
⇒ **这两个 FAIL 都不宜作跨通道结论**,只登记。
  🔴 **理由订正(v3 自纠,我采纳)**:此前我把理由写成「C 侧每轮只看得见 5–11% 的时长」——**那个判据不成立**。
  反例就在本批:`cell_f6` 轮期 95s、**可见比例更低**,却是全批最干净的一格,**因为它的静默够长**。
  ⇒ **正确判据是「你用来定 A2 的那次静默可核不可核」,不是可见比例。**
  按此,F5 两格的前置断言其实都是 `WORTH_RUNNING`(可核 14/10 vs 不可判 10/10),
  **它们的 C 侧并未被可见比例否掉**;真正被否掉的是 `cell_f2`(**不可判 12 > 可核 9**)。
  本行保留「只登记不下结论」的处置(n=6、单批、时段未控),但**理由换成上面这条**;
③本对照回答的是「F5 这个功能在两条件下差多少」,**不回答「F5 比 F1 慢多少」**——后者与提示词长度共线(见 1a-quinquies)。

## 2. 语义边界(必须连着数一起读)

- **`ttft_cluster_ms` 是逐次发送的,不是每个 App 会话一次。** `ObsStats.kt` v3.1/v3.2:
  **簇窗在每次 `textLen>0` 的真实打字上重置**(len=0 的 TEXT_CHANGED 不重置)。
  ⚠ **本文此前写成「每个 App 会话只测一次」,错**——那会让人以为该字段不能逐轮用。
  它逐轮可用,**只是与宿主侧 `e2_analyze` 的轮界对不上**(宿主按 `turn_method` 切,同名不同义)。
- **每条 OBS 重复同一个值** ⇒ **OBS 条数 ≠ 测量次数**,上表已取变化点并按标记归位。
- **🔴 `ADAPTER_OBS` 里混着「陈旧重发行」,logcat 时戳不可用于归轮。**
  判据:`session_span_ms = (lastEventNanos − observeStartNanos)/1e6`(`ObsStats.kt:296`)是一条**事件时钟**;
  把它与墙钟对齐,`cell_f1` 的 24 条 OBS 偏差**双峰**——多数恒为常数,**七条迟到约 +9.7s**,
  **且这七条的 `ttft_cluster_ms` 全是前一条的逐字重复**。`cell_f1b` 18 条里同样有 6 条。
  ⇒ **凡按 logcat 时戳把 OBS 归轮都可能放错一轮,且不报错。**
  **剔除判据**:`session_span_ms` 与墙钟对不上的那一条,不用它归轮。
  ⚠ **我的「取变化点」恰好把这些行滤掉了(它们是重复值),但那是运气不是设计**——
  当初取变化点是为了「OBS 条数 ≠ 测量次数」,不是为了防这个。
- **归轮必须归「事件时刻」,不能归「播报时刻」**;剔除陈旧行后,
  高值仍落在某个 `turn_start` 之后 +0.04～+0.20s ⇒ **它们描述的是上一轮**(v3 订正,已采纳)。
- **`cadence_p50_ms` 是会话累计中位数**,流式一来就被稀释(同会话 99.4 → 23.8)⇒ **不可跨格比中位数**。
- **原始帧数是常数,不含信息**:`dumpsys SurfaceFlinger --latency` 每次固定返回 **127 行帧**,
  按 `--framestats-period-s`(默认 20s)周期 dump ⇒ **原始帧数 ＝ 127 × dump 次数**。
  逐格验过:21×127=2667 / 30×127=3810 / 30×127=3810。**凡引帧数一律用去重后的数。**
- **⚠ 「次簇形成率」跨格不可比**:`wifi_f2` 2/6 与 `cell_f2` 5/6 看似是条件差异,
  **但我把答窗从 49s 改成了 75s**——更长的窗本身就给第二簇更多机会。**这个对比作废。**
  ~~**唯一不受窗长影响的是「高模的缺席」**:更长的窗只会让长 TTFT **更**容易被看见~~
  🔴 **符号是反的,已撤(见 §0)**:答窗越长 ⇒ 兜底值越大 ⇒ 越容易越过 30s 天花板 ⇒ **高模越不可见**。
  且 `cell_f2` **轮 6 实际形成了 21365.9ms**,只是没播报。**「`cell_f2` 无高模」不成立。**

## 3. 四格的通道健康度(均已定型后测)

| 格 | 标记 t/a | 答窗 | 通道B | e2 |
|---|---|---|---|---|
| `wifi_f1` | 6/6 | 17.4s | PASS,检出 13 | n=4 dropped=2(**C 侧**)→ NOT_EXECUTED |
| `wifi_f2` | 6/6 | 49s | PASS,检出 21 | n=2 dropped=4(**A 侧**)→ NOT_EXECUTED |
| `cell_f1` | 8/8 | 17.4s | PASS,检出 25 | n=3 dropped=5(**A 侧**)→ NOT_EXECUTED |
| **`cell_f2`** | 6/6 | 75.2s | PASS,检出 29 | **n=6 dropped=0 → FAIL** |
| `cell_f1b` | 6/6 | 45.2s | PASS,检出 21 | n=4 dropped=2(**A 侧**)→ NOT_EXECUTED |

**⇒ `cell_f2` 是本批第一格让 e2 真正跑起来的**,而它给出的判词是 **FAIL**:
`|t_A − t_C|` p99 = **19805.8ms**,判据是 **1 帧 = 16.667ms**——**差三个数量级**。
逐轮 `t_A − t_C`:**−17913 / −19806 / −16841 / −16205 / −14330 / +3306 ms**(五负一正,**符号会翻**)。
**而时钟对齐极好**(`wall_to_boot` 残差 p50 **0.26ms**、`clock_pin` 漂移 **1563ns**)
⇒ **这 20 秒不可能是时基问题**。它是「**两条通道各自指认了不同的事件**」被量了出来:
A 的第二簇落在思考静默之后,C 的第二簇要等到长流式结束才出现。

⇒ **对判读侧的意义**:e2 的问题**不止是「适用带窄、常跑不起来」**——
**在带内跑起来的那一格,命题依然不成立,且不成立的量级是判据的三个数量级。**

## 4. 作废格(按 §④-A′ 保留不删)

- `wifi_f1_VOID1`：冒烟标签错(`E4MARK` vs `AnebE4MARK`)，五轮标记全被过滤。
- `wifi_f1_VOID2`：采集器 `PermissionError` 崩溃。
- `wifi_f1_VOID3`：5 轮版，n=4 < GATE_MIN_N=5。
- **`cell_f2_VOID1`:答完判据提前触发。** 用「ROI 跳变后连续 3 个静止样本 = 答完」,
  六轮**全部在 6–11s 宣布完成**(F2 实际约 50s/轮)⇒ **每轮都被下一轮的「新对话」拦腰截断,且零报错**。
  两层根因:①ROI `(400,1800,400,200)` 是按 **F1 短答**选的,落在输入框/键盘带上——
  **它跳的是我自己的打字与键盘收起**;②判据**没有下限**,任何一段静止都能触发。
  **⇒ 思考静默既是 e2 用来定 A2 的东西,也是让「它答完了吗」提前触发的东西——同一个特征,两个消费方,后果相反。**
  改为**固定等待**(**宁可标晚,绝不标早**:标早截断真数据且不报错,标晚只多算一段空闲)。
  **ROI 保持不变**以保跨格可比,只是不再当判据。
