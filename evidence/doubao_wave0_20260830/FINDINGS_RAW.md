# 豆包先行批 · 原始观察(采集侧记录,不作判读结论)

> **本文只记「量到了什么」与「量的语义边界」,判词归判读侧(v3)。**
> 采集:DW-20260830-01,自主操作模式(D-587/D-588),`--screencap-period-ms 1500`,ROI `400,1800,400,200`。

## 1. `ttft_cluster_ms` 的不同测量值(去重后的变化点,已按标记逐轮归位)

| 格 | 轮数 | 逐轮取值(ms) |
|---|---|---|
| `wifi_f1` | 6 | 1977.7 / 3291.6 / 3936.7 / 431.5 / 2944.6 /(轮6 未变) |
| `cell_f1` | 8 | 2642.4 /(轮2 未变)/ **15636.6** / **15719.1**+3013.6 / 2806.5 /(轮6 未变)/ **15558.4** / **15754.8** |
| `wifi_f2` | 6 | 仅轮2 3251.3、轮5 3267.3,其余四轮无第二簇 |
| `cell_f2` | 6 | 2919.6 / 3829.6 / 3917.7 / 3431.4 / 3832.7 /(轮6 无) |

| **`cell_f1b`** | 6 | **4161.1 / 3626.8 / 3713.1 / 3856.7**(答窗 45s)——**零个高值** |
| **`wifi_f1_anchor`** | 6 | 435.0 / **22538.9 / 22566.6 / 22490.8** / 4066.2 / 3594.2(**第二窗 DW-02**,WiFi,答窗 18.2s)——**三个高值,跨度仅 76ms** |

**形状一:蜂窝 F1 双峰**——3 次 2.6–3.0s、**4 次 15.6–15.8s**,高峰簇跨度仅 ~196ms。
**形状二:蜂窝 F2 无高模**——五个值全在 2.9–3.9s。

### 🔴 1a-bis. 第二窗的锚格把 1a 推翻了:高模在 WiFi 上出现,而且换了个数

`wifi_f1_anchor`(2026-08-30 13:44,**WiFi**,与 `wifi_f1` **同一句提示词**):

```
wifi_f1        WiFi 轮期 23.0s 答窗 17.3s -> 零个高值(最大 3936.7)
cell_f1        蜂窝 轮期 23.0s 答窗 17.4s -> 15558/15637/15719/15755  跨度 196ms
wifi_f1_anchor WiFi 轮期 29.2s 答窗 18.2s -> 22491/22539/22567        跨度  76ms
```

**三件事同时成立**:①**高模在 WiFi 上出现了** ⇒ 它不是蜂窝/5G 的性质;
②**数值跟着驱动节拍从 15.7s 移到 22.5s** ⇒ 它不是一个固定时长的 App 或网络机制;
③**离散度反而更紧**(196→76ms) ⇒ 对网络现象不合理。

⚠ **但本文不下判定**,因为有一处答不上来:**`wifi_f1` 与 `cell_f1` 轮期同为 23.0s,
为什么前者没有高模而后者有?** 这是「伪影」这一解释最薄弱的地方。
已发起对抗性核查(独立重算 ＋ 三条不同透镜的怀疑者),**结论强度待其回来后再写**。

### 🔴 1a-ter. 而锚格自己废了——它作为锚不可用,原因是我自己引入的两个变量

锚格的唯一职责是与 `wifi_f1` **逐字同条件**。实际:

| | `wifi_f1` | `wifi_f1_anchor` | 差因 |
|---|---|---|---|
| 答窗 | 17.23–17.48s | **17.43–18.26s** | `ANSWER_WAIT_S=17.4`,而等待循环按 **1 秒**轮询,溢到 18.2 |
| 轮期 | 22.76–23.05s | **28.06–29.25s** | 为防误点他应用而加的前台复核 ＋ 重写驱动器时放长的 sleep |

⇒ **本批仍然没有可用的漂移锚**,§8 那句「不得据本批下无漂移结论」继续有效。
⇒ 形状:**我为安全加的检查、和我为精确写的常量,一起毁了那个「什么都不许变」的格**。
「改一个参数前先列出还有谁在读它」——**读它的正是锚格**。
⚠ 一处**错误归因已自纠**:我最初把轮期变长归给 `dumpsys window`,实测单次仅 **0.12–0.19s**、
三次共 0.4s ⇒ **那个解释是错的**,差额来自 sleep。**猜的机制与量出来的机制不是同一个。**

### 🔴 1a. 判决性检验的结果是**阴性**——形状一没有复现

`cell_f1b`:**同一功能、同一条件、同一提示词,只把答窗从 17.4s 改成 45s**,6 轮。
**四个值全在 3.6–4.2s,零个高值。** 按 `cell_f1` 的 4/8 高值率,6 轮全不中的概率约 **1.6%**。

**但我不能据此下判**,因为**我一次改了两个东西**:答窗(17.4→45s)**与时间**(相隔约 1 小时,射频条件未受控)。
⇒ **两个候选仍然并列**:①高值与我的 17.4s 答窗有关;②高值是真实的、随时间变化的网络/服务端现象,这一格没赶上。

**⚠ 而「答窗边沿触发」这个具体机制被排除了**:若第二簇是被答窗关闭那一刻触发的,
45s 窗下的值应当挪到 ≈43s。**它没有,它回到了正常 TTFT(3.6–4.2s)。**

**⇒ 这是我的设计错误,写下来给下一批**:判决性检验**只能变一个量**。
正确设计是**在同一格内交替 17.4s 与 45s 两种答窗**——那样时间被摊平在两组之间,
**而我把两组放到了不同的时段**。本窗额度已不足以补(剩约 7 轮,交替设计需 ≥8 轮)。

⚠ **本节此前把形状一称作「本批第一个真信号」,现予收窄**:它是**一个未复现、成因未判定的形状**。

### 1b. 我对「高值是我自己的驱动器造成的」这个假设的排查(结论:否)

`cell_f1` 用固定等待,**答窗恒 ≈17.4s**,而高值 15.56–15.75 **恰比它低 1.6–1.9s**;
四个高值又**全部播报于该轮 `turn_start` 之后 +0.04/+0.09/+0.11/+0.20 秒**(正常值播报于 +5.0～5.4s)。
两条都指向「测的是我自己的超时」。**但三条独立证据否掉了它**:

1. **`wifi_f1` 用同一驱动器、同一 17.4s 答窗**(17.47/17.30/17.25/17.23/17.25/17.48),**零个高值**。
2. **离散度反证**:我的答窗抖动 ≈600ms、轮期抖动 ≈570ms,而**四个高值跨度仅 196ms**——
   **比造它的那把尺还稳三倍,就不可能是那把尺造的**。
3. **播报晚 ≠ 事件晚**:`ADAPTER_OBS` 由活动驱动 + 5s 节流。第二簇在 ≈16.0s 形成后该轮再无内容事件
   ⇒ 不再发 OBS,直到我 ≈21.5s 打下一轮的字才恢复。**同一形状在没有高值的 `wifi_f1` 轮3 也出现**
   (播报于 +17.42s、值 3936.7)⇒ **它是播报机制,不是高值成因**。

⇒ **代价如实**:高值那四轮回答在 ≈15.7s 才开口,而我 17.4s 就切走 ⇒ **那四轮的回答被截断在开口约 1.7s 后**。
**`cell_f1` 的 TTFT 可用**(事件发生在截断前),**但那四轮的流式/节奏/通道 C 不可用于任何「回答多长/多稳」的判断**。

## 2. 语义边界(必须连着数一起读)

- **`ttft_cluster_ms` 是逐次发送的,不是每个 App 会话一次。** `ObsStats.kt` v3.1/v3.2:
  **簇窗在每次 `textLen>0` 的真实打字上重置**(len=0 的 TEXT_CHANGED 不重置)。
  ⚠ **本文此前写成「每个 App 会话只测一次」,错**——那会让人以为该字段不能逐轮用。
  它逐轮可用,**只是与宿主侧 `e2_analyze` 的轮界对不上**(宿主按 `turn_method` 切,同名不同义)。
- **每条 OBS 重复同一个值** ⇒ **OBS 条数 ≠ 测量次数**,上表已取变化点并按标记归位。
- **🔴 `ADAPTER_OBS` 里混着「陈旧重发行」,logcat 时戳不可用于归轮。**
  判据:`session_span_ms = (lastEventNanos − observeStartNanos)/1e6`(`ObsStats.kt:296`)是一条**事件时钟**;
  把它与墙钟对齐,`cell_f1` 的 24 条 OBS 偏差**双峰**——多数恒为常数,**七条迟到约 +9.7s**,
  **且这七条的 `ttft_cluster_ms` 全是前一条的逐字重复**。`cell_f1b` 18 条里同样有 6 条。
  ⇒ **凡按 logcat 时戳把 OBS 归轮都可能放错一轮,且不报错。**
  **剔除判据**:`session_span_ms` 与墙钟对不上的那一条,不用它归轮。
  ⚠ **我的「取变化点」恰好把这些行滤掉了(它们是重复值),但那是运气不是设计**——
  当初取变化点是为了「OBS 条数 ≠ 测量次数」,不是为了防这个。
- **归轮必须归「事件时刻」,不能归「播报时刻」**;剔除陈旧行后,
  高值仍落在某个 `turn_start` 之后 +0.04～+0.20s ⇒ **它们描述的是上一轮**(v3 订正,已采纳)。
- **`cadence_p50_ms` 是会话累计中位数**,流式一来就被稀释(同会话 99.4 → 23.8)⇒ **不可跨格比中位数**。
- **原始帧数是常数,不含信息**:`dumpsys SurfaceFlinger --latency` 每次固定返回 **127 行帧**,
  按 `--framestats-period-s`(默认 20s)周期 dump ⇒ **原始帧数 ＝ 127 × dump 次数**。
  逐格验过:21×127=2667 / 30×127=3810 / 30×127=3810。**凡引帧数一律用去重后的数。**
- **⚠ 「次簇形成率」跨格不可比**:`wifi_f2` 2/6 与 `cell_f2` 5/6 看似是条件差异,
  **但我把答窗从 49s 改成了 75s**——更长的窗本身就给第二簇更多机会。**这个对比作废。**
  **唯一不受窗长影响的是「高模的缺席」**:更长的窗只会让长 TTFT **更**容易被看见,
  ⇒ **`cell_f2` 没有 15s 高模,这一条成立。**

## 3. 四格的通道健康度(均已定型后测)

| 格 | 标记 t/a | 答窗 | 通道B | e2 |
|---|---|---|---|---|
| `wifi_f1` | 6/6 | 17.4s | PASS,检出 13 | n=4 dropped=2(**C 侧**)→ NOT_EXECUTED |
| `wifi_f2` | 6/6 | 49s | PASS,检出 21 | n=2 dropped=4(**A 侧**)→ NOT_EXECUTED |
| `cell_f1` | 8/8 | 17.4s | PASS,检出 25 | n=3 dropped=5(**A 侧**)→ NOT_EXECUTED |
| **`cell_f2`** | 6/6 | 75.2s | PASS,检出 29 | **n=6 dropped=0 → FAIL** |
| `cell_f1b` | 6/6 | 45.2s | PASS,检出 21 | n=4 dropped=2(**A 侧**)→ NOT_EXECUTED |

**⇒ `cell_f2` 是本批第一格让 e2 真正跑起来的**,而它给出的判词是 **FAIL**:
`|t_A − t_C|` p99 = **19805.8ms**,判据是 **1 帧 = 16.667ms**——**差三个数量级**。
逐轮 `t_A − t_C`:**−17913 / −19806 / −16841 / −16205 / −14330 / +3306 ms**(五负一正,**符号会翻**)。
**而时钟对齐极好**(`wall_to_boot` 残差 p50 **0.26ms**、`clock_pin` 漂移 **1563ns**)
⇒ **这 20 秒不可能是时基问题**。它是「**两条通道各自指认了不同的事件**」被量了出来:
A 的第二簇落在思考静默之后,C 的第二簇要等到长流式结束才出现。

⇒ **对判读侧的意义**:e2 的问题**不止是「适用带窄、常跑不起来」**——
**在带内跑起来的那一格,命题依然不成立,且不成立的量级是判据的三个数量级。**

## 4. 作废格(按 §④-A′ 保留不删)

- `wifi_f1_VOID1`：冒烟标签错(`E4MARK` vs `AnebE4MARK`)，五轮标记全被过滤。
- `wifi_f1_VOID2`：采集器 `PermissionError` 崩溃。
- `wifi_f1_VOID3`：5 轮版，n=4 < GATE_MIN_N=5。
- **`cell_f2_VOID1`:答完判据提前触发。** 用「ROI 跳变后连续 3 个静止样本 = 答完」,
  六轮**全部在 6–11s 宣布完成**(F2 实际约 50s/轮)⇒ **每轮都被下一轮的「新对话」拦腰截断,且零报错**。
  两层根因:①ROI `(400,1800,400,200)` 是按 **F1 短答**选的,落在输入框/键盘带上——
  **它跳的是我自己的打字与键盘收起**;②判据**没有下限**,任何一段静止都能触发。
  **⇒ 思考静默既是 e2 用来定 A2 的东西,也是让「它答完了吗」提前触发的东西——同一个特征,两个消费方,后果相反。**
  改为**固定等待**(**宁可标晚,绝不标早**:标早截断真数据且不报错,标晚只多算一段空闲)。
  **ROI 保持不变**以保跨格可比,只是不再当判据。
