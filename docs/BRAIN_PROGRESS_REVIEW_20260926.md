# ANEB 开发进展全面审视与下一步计划（大脑，2026-09-26）

> **性质**：大脑会话应 PO 令「完整审视和分析开发进展与下一步计划，并与各会话协同、加速开发」所作。**基准**：提交 `8f356eb8`（D-916 已入册）。**证据等级**：凡写「板面所记」「D-xxx」即引自 `docs/BRAIN_TASKBOARD.md` / `docs/DECISION_LOG.md`，本文件没有重新跑；凡写「按提交说明推算」即由 `git log` 主题行推出、以属主回执为准；凡写「大脑实测」即本会话今日亲跑。
> **一个数只写一处**：本文件不复述任何计数的来源文件，只给指针。

## 0. 一句话

项目在 **M3（真实业务与语音）后半段**。四条会话零设备可做的活已并行铺开，但**三条主线全部卡在会话之外**：① E-01 迁址有硬期限（北京 **2026-10-11 08:37** 旧证书到期，D-912），三个决定里 PO 今日定了一个（证书方案 A，D-916）、两个未定；② 整形/画像采集那条线的前置＝2× 分解判据 **7 条拦窗未清**（按提交说明推算，v4 回执为准），清完才开提权窗；③ 设备侧三层网络当前不通（热点已关联、到 223.5.5.5 0/3），**这是所有设备格的第一道门**。加速的空间不在写更多代码，在把这三处外部依赖各配一条「不等它也能推进」的并行动作，并把等它的那一刻要做的事提前备好。

## 1. 里程碑现况（对照 `docs/SYSTEM_DEV_PLAN_v1.0.md` §6）

| 里程碑 | 状态 | 依据 | 备注 |
|---|---|---|---|
| M0 契约冻结 | 已过 | 板面 T1–T12 全 DONE | — |
| M1 核心闭环 | 已过 | 同上 | 债：`radio_ctx` 未进契约（大脑记忆，未在本文件重核） |
| M2 外场 MVP | 已过 | 07-31 首份真实数据战役（D-366 系列） | 外场线 D-587 终止 ⇒ 「城市热力卡」**目标撤销**（不是延期），呈现面换成 App × 网络形态矩阵（D-595⑦，板面 T81） |
| **M3 真实业务与语音** | **进行中** | 板面 T78/T50/T81 | Profile 3 适配器＝豆包战役报告 v0.1.8 可交 PO（板面 T78）；Profile 4 语音回环激活准备 DONE（D-482）；**业务画像采集（抓包→拟合→回填）依赖整形窗＋通道 D，未启动** |
| M4 产品化 | 部分提前 | 板面 T45/T48/T62/T88 | 3-tab UI 与 MainActivity 拆分 DONE；T88 历史页缺陷 5 件落主线、余 1 件需屏上复现 |

M3 的验收标准（适配器打点误差 ≤1 帧、画像参数替换 [GUESS] 初值）**两条都还没到能测的状态**——前者要整形窗出三档数据，后者要通道 D 上行字节证据；两者的共同前置是 §2-B。

## 2. 四条战线：到哪了、卡在哪、谁在做

### A. E-01 迁址（服务器切换）——日历硬期限

- **已定**：证书方案 A（Let's Encrypt IP 证书），前提 MIW 先签测试证书验链（PO 2026-09-26；D-916）。大脑实测：**决定手机接不接受的是密钥类型**——P40 只有 ISRG Root X1（D-913），RSA 链判 0、ECDSA 链判 20，旧 sslip 证书恰是 acme.sh 默认 ECDSA。请求文件 `C:\Users\lucas\Desktop\ANEB方案A_测试证书验证请求_给MIW运维_20260926.md`，**待 PO 转发**；判据已跑前写死，MIW 回端口号即可验，不占设备。
- **未定（PO）**：节点命名（沿用 E-01 / 新 ID）、停旧时间。
- **已备**：V1 的旧址逐处清单 `docs/E01_OLD_IP_INVENTORY_20260926.md`（只分类不修改；抬头自带量法与计数；V1 已提交 `748decb9`，大脑核收 D-917）；其 §6 四条「待判」中两条大脑已裁（均史实不改）、两条归 v3。V1 顺带核出：真正现态与史实混在同一文件的只有 `BRAIN_TASKBOARD.md`；工作区里还有一个**未跟踪的二进制证据库**含旧址，D-914 禁令已扩到它。
- **进行中**：V3 今日起做「不依赖命名」那一片——设备侧语料无条件取每个 run 的 `serverBase`、sslip 与裸 IP 归一为物理端点、可比性层拦跨服务器与「未知」三态（大脑已批，条件三条已发 V3）。
- **等命名**：v4 整形器档位 `cap_up_1mbit_e01` 的目标 IP；V3 `server_tier_endpoint` 取值。
- **风险**：距到期 15 天，链条是 MIW 签证书 → 验链 → MIW 正式部署 → App 端点切换 → 设备实测 → 停旧。**只要 MIW 一环晚三天，方案 B（自签 CA、重新打包、装机、D-581 重验）就要在剩下的时间里走完，且至少占一次设备窗。** 处置见 §5。

### B. 2× 分解 / 整形「可整」检验（v4）——所有整形与画像工作的前置

- **现况**：判据 v4 终审 no_go（D-908）：11 条拦窗 + 16 条 conditions + 21 条未投票（`docs/C_2X_DECOMPOSE_REVIEW_V4_20260918.md`）。v4 三批已落（`8c098a13` / `0f7d771a` / `c7f57f67`）；**v4 对盘上现态逐条重数（回执 09-26）**：拦窗已落 4（§2-5 降级为「代码已落、判据 §4.6 缺一句」）、**余 7**；conditions 已落 2、**余 14**，其中 §3-14／§3-15 只在 §5 执行时才有意义，大脑裁随 (a) 那笔做、挪出本轮但不划掉。v4 预计再分 6 批（批四在飞），每批先实调复现、每条配门，大脑当日核收。
- **链条**：拦窗清零 → 一次 PO 提权窗跑探针（12 格）→ 分解 2× 的来源 → 写 NETWORK_FORWARD 最小「可整」检验 → 整形器 → 豆包 v0.2 三档数据 → 画像采集。**每一环都在上一环之后，没有可并行的设备步骤。**
- **卡点**：不在 PO，在 v4 的修法速度与大脑核收速度。近三轮终审都 no_go，主因是每轮都有「修法引入回归」（D-909/D-910）。

### C. 豆包网络诉求战役 T78（v2，编制外）

- **现况**：报告 v0.1.8（`fb9c0ba`）**自 09-06 起可交 PO**（板面 T78），头部版本号滞后一条已派；v0.2 三档数据等 §2-B。
- **判断**：这份报告是 M3「适配器」一格目前**唯一成稿的对外产出**，PO 应先读它，再决定 v0.2 是否值得一次提权窗。

### D. 分析报告线 SPEC-3（v3）

- 3.1 台账 / 3.2 分层减负 / 3.5 周报 / 3.3 wave0 判读 DONE；3.4 目标撤销后重挂；T91 A-4/A-5 已交，A-3/A-2 状态本文件未核（回执待 V3）。今日起并行接 §2-A 的 serverBase 片。

### E. App 侧（v4）

- T47 批④边界样本：依赖 E-01 蜂窝路径（T49 BLOCKED，D-481）或真 WiFi 路径（T54 DOING，首份 s4 样本已落）。**迁址完成即同时解 T49**——这是 §2-A 的第二个收益。
- T88 余 1 件轻件需屏上复现，排在下一次设备窗顺带。

### 设备前提（横切）

P40 已连 PC 热点（PO 09-26 报），但大脑实测三层不通（0/3 到 223.5.5.5）。上次同症状（D-871）真因是 Windows 把共享源选成了 WLAN——**PC 侧只读即可判**，不必等窗。任何设备格开跑前须过「收到回复的第一跳 ping」这道门（D-906）。

## 3. 关键路径

```
E-01 迁址        MIW 签测试证书 ──验链(大脑,不占设备)──▶ 正式部署 ──▶ 端点切换 ──▶ 设备实测 ──▶ 停旧   ⏰ 10-11 08:37
                 └─ 并行: V1 清单入库 / V3 serverBase 归一 / 大脑裁待判 / (等命名) v4 档位 IP、V3 槽取值
整形·画像        v4 清 7 条拦窗 ──大脑核收──▶ 提权窗(PO) ──▶ 12 格探针 ──▶ 可整检验 ──▶ 整形器 ──▶ v0.2 三档 ──▶ 画像采集
                 └─ 并行: V1 设备 L3 只读诊断(PC 侧), 免得开窗那天才发现路不通
```

两条链在「设备窗」处汇合：迁址后的设备实测与探针提权窗**可以是同一个窗**，前提是那时拦窗已清零。这是本计划里唯一能省下一次 PO 时间的地方。

## 4. 下一步（按会话；每条自带输入与前提）

| 会话 | 现在做 | 输入 | 交付判据 |
|---|---|---|---|
| **V1**（SPEC-1/2） | ① 清单 `docs/E01_OLD_IP_INVENTORY_20260926.md` 已提交 `748decb9`，剩推送（fetch → ff-only → push）；② **设备 L3 前提门只读诊断**：PC 侧共享源网卡、其 IP/默认路由/上游可达（指定源地址 ping）、热点虚拟卡 ifIndex 现值、P40 `ip route get 223.5.5.5` 与**收到回复的第一跳 ping**；不改任何系统设置，需要改的写成给 PO 的一行动作 | D-871 判决性对照（指定源 ping）、D-906 第一道门、D-907 ifIndex 会漂 | 一张读数表，每格标时刻；结论只许「通／不通／未执行」三态 |
| **V2**（编制外） | T78 报告头部版本号滞后修完后，把 v0.1.8 的**一页 PO 摘要**（结论、不可测项、数据来源哈希）另起文件；然后待命 | 板面 T78 行；D-832 摘要面规则 | 摘要每个数指向报告节号，不复述 |
| **V3**（SPEC-3） | serverBase 归一片（进行中，条件三条已发）；顺带裁 V1 清单 §6 第 3、4 条（`demo_results.jsonl` 与其生成器）；回执 A-3/A-2 现态 | V1 清单 §6；D-914 | 回执带 commit、run_all RC、serverBase 取值分布四个数 |
| **V4**（SPEC-4） | 2× 余下拦窗与 conditions 继续按批交，**每批先实调复现再修、每条配会红的门**；档位 IP 等命名不动 | `C_2X_DECOMPOSE_REVIEW_V4_20260918.md` §2/§3 全集；D-909/910 两条回归的形状 | 每批回执：本批覆盖哪些条（按 §2-n/§3-n 编号）、剩几条、run_all RC；大脑当日核收 |
| **大脑** | 追 PO §5 三件；MIW 回端口即按 D-916 判据验链；裁 V1 清单 §6 第 1、2 条（T78 行内 clumsy 指令＝史实，D-809 已换整形器，不改；spine4 蓝图＝带作废横幅的史实，不改）；核收各批；迁址三决定齐后开一份切换 runbook | — | — |

## 5. 需要 PO 的（最小集合，按时间序）

1. **今天**：把桌面那份请求转给 MIW 运维（**务必 RSA 密钥**，请求文件里已写清为什么）。
2. **本周**：定节点命名、定停旧时间。停旧时间大脑建议的形状：验链通过 + 新址设备实测通过后 ≥48 小时，且不晚于 10-10。
3. **读** `docs/DOUBAO_NETPERF_REPORT_20260906.md`（v0.1.8）——它决定 v0.2 是否值一次提权窗。
4. **保险丝（PO 定是否拉）**：若 **09-30** 前 MIW 无测试端口，启动方案 B 预备（自签 CA 打包流程只写 runbook 不执行）——代价是 v4 从 2× 上分出半天。不拉则接受「10-08 前若 A 未通即无退路」的风险。
5. 2× 拦窗清零后：一次提权窗（与迁址设备实测合并，见 §3）。

## 6. 本文件没有做的

- 没有重跑任何计数；§2-B 的「余 7 条」是按提交说明推算。
- 没有核 T91 A-3/A-2、T88 轻件、T54 后续的现态，等各属主回执。
- 没有评估方案 B 的具体工时，只给了触发日期。
