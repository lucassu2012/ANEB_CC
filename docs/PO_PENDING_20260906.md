# PO 待办三件 —— **已全部办结（2026-09-07 00:0x）**

> 🟢 **三件都有结果了，本文件从「待办单」转为「结果单」，正文保留原样不改写**（它当时是对的）。

| # | 结果 |
|---|---|
| 一 | **PO 已跑 §2，两个任务建成、ACL 正确。** 🔴 并且那条「非提权能否触发最高权限任务」的**无实证假设已证成**——大脑在非提权窗（`IsInRole=False`）触发 `HighestAvailable` 任务，rc=0 且**任务真的执行**（日志 0→1 行，Last Result=0），用零副作用的 Stop 任务证、**未加载驱动**（D-774）。⚠ 但 **§4 自检是我写坏的**：英文字段名在中文 Windows 上恒空，PO 实跑返回空**而任务其实建成了**，已改用 `/XML`（D-775）。 |
| 二 | **问询稿已备**：`docs/E01_PROFILE_INQUIRY_20260907.md`，待 PO 转发给 `20260827_MIW云端运维接入`（该窗为云端会话，收得到但无法回话给大脑，故由 PO 转发并读取回复）。 |
| 三 | **PO 已批准** `okhttp3:mockwebserver:4.12.0`，v4 已开工，范围＝调用点时戳捕获 ＋ 请求侧行为两项。 |

---

## 以下为 2026-09-06 收盘时的原文（保留，不改写）


> 三件都只能由 PO 做。编队其余部分**没有别的东西在等**——四个执行窗均待命或在做已放行的活。
> 每件都写了「不做的后果」，因为三件的紧迫程度不同。

---

## 一、在管理员 PowerShell 里跑一次提权单子的 §2（约 2 分钟）

**文件**：`docs/E2_SCHTASKS_SETUP_20260906.md`，**只跑 §2**。

🔴 **§3 不要在管理员窗里跑**——那一步同时是整套设计的判决性检验：本方案压在「**非提权会话能否触发 `/RL HIGHEST` 任务**」这条**至今无实证**的假设上。在管理员窗里跑它一定会过，而它证明不了任何要证的事（管理员窗本来就能起整形器，根本不需要计划任务）。§3 已改派非提权窗跑。

**不做的后果**：整形窗开不了 ⇒ 段 B → 段 C → B-2 三档 → T78 报告 v0.2 整条链停在原地。**这是目前唯一的关键路径。**

**做完之后不需要您再管**：链条与每步的主、判据写在 `docs/E2_WINDOW_FIRST_HOUR_20260906.md`。

---

## 二、E-01 上服务端 `-profiles` 指向的目录还在不在

**背景（本日实测）**：101 个真实 run 里 **89 个**的溯源字段记着 `s1_chat@0.2.1`，而**这个版本号在本仓从未存在过**（全历史零命中，对照 `0.2.0` 命中 5 个文件）。已查明成因：那批 profile **确曾在运行时被加载**（版本串取自实际加载文件的字段，非拼接），**但从未入库**。受影响的 run 跨 2026-07-31 至 08-20，含 08-20 那批验收数据。

**要您答的只是一个事实**：那个目录还在，还是已经没了。

- **还在** ⇒ 那批 profile 定义可以捞回来，D-707 的回放腿能覆盖 88% 的语料；
- **没了** ⇒ 如实登记「已不可考」，回放的验收分母改成「可解析版本的 run」，现测 **12 比 101**。

**不做的后果**：不阻塞任何人，但拖越久那批文件越可能真的消失。

---

## 三、批准把 `okhttp3:mockwebserver:4.12.0` 加为测试依赖

**为什么要问您**：加新依赖会触发构件下载，按仓内成规下载类动作归 PO。

**范围今天缩了四次，现在定了**。原判断是四条路径都需要它，实际**已闭合三条、全部没用它**（改成把逻辑抽成纯函数直接测）：

| L1-F8 四条路径 | 状态 |
|---|---|
| TTFT 公式 | 已闭合 `a4e0961` |
| echo 公式 | 已闭合 `47d84c1` |
| 流式 gap／重复／尾截断／畸形 | 已闭合 `0950e5e` ＋ `778cfc4` |
| **窗口上行** | **仅剩两项需要它** |

**仅剩的两项，共用同一句理由**：**没有一个会读的对端，就观测不到「我们在到点那一刻停了笔」，也观测不到「响应头是什么时候到的」。**

1. **调用点供给层零覆盖**（⚠ **2026-09-07 订正**，原写「唯一一枚已知存活的突变」是错的）：该调用点六个实参里有四个存在可替换的错值，**四个各造一枚突变，四枚全部存活**——时戳退回最后一次写入返回／写出量供成 0／`underrun` 供成常假／起点戳供错。**截至 `b541222`。**
2. **请求侧行为**——窗口到点停止写入、`windowUnderrun`、chunked 编码。

**不做的后果**：A-8 带着上面**四枚**已知存活的突变收口，且窗口上行的请求侧行为零覆盖。**不是「少几个测试」**：**装配判据已经有守卫了**（该信服务端计数还是客户端写出量、终点戳取响应头到达——这些今天已钉住），**没有守卫的是喂给它的那四个值**；而**上行速率的分子（字节）与分母（时长）都经由这一层**。（⚠ 原句「那一行…没有任何守卫」把范围一边说小、一边说大，已改。）

⚠ **这个范围不会再自己缩，理由值得您知道**：它**不是「暂时没再发现更多」，是撞到了一条需要有人裁的线**——再缩一刀就得给生产代码开注入缝（把 HTTP 客户端或拦截器传进去），那是**用改生产代码结构去换测试覆盖**。**编队不单方面做这种取舍，它该由您拍板。** 若您认为那条缝值得开，那是另一个决定，我们照办。

---

## 不需要您做的（写在这里是为了让您知道它们不在等您）

- T78 报告已到 v0.1.7，**可交**；三档数据待整形窗。
- 门禁绿（`run_all.py` / 主树 / cwd `scripts\tests` = 839/839），app 侧 970 tests / 0 failures。
- 今日入册 D-714..D-772。
