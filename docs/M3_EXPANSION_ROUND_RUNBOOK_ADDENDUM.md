# M2 Runbook · M3 扩展轮增补（草案）

> 作者：v3 执行会话 · 2026-08-01 · 状态：**草案，待 PO 批复后并入 runbook**
> 承接：[`M3_EXPANSION_ROUND_PROPOSAL.md`](M3_EXPANSION_ROUND_PROPOSAL.md)（T3，大脑验收 PASS）
> 彩排证据：[`../evidence/m3_expansion_rehearsal_20260801/`](../evidence/m3_expansion_rehearsal_20260801/README.md)（T6 ②）

## 0. 这份文件是什么、以及它现在**不是**什么

**载体选择（本文的决定）**：写成**独立草案**，不直接改
[`M2_CAMPAIGN_RUNBOOK.md`](M2_CAMPAIGN_RUNBOOK.md)。

理由是 D-311 那一课的**反面用法**：runbook 是**在现场被逐条执行**的那份文件，
把一套 PO 尚未批复的协议写进去，操作者就会照着执行。故：

> ⚠ **在 PO 批复之前，现场作业一律照 runbook §0.6 现行流程执行；本文不生效、不覆盖、不并行。**

同时，为了不重蹈 D-311 的**正面陷阱**（提案改了、runbook 没改，于是现场照着过期的那份跑），
§7 预先写死了**并入时要改 runbook 哪几处**——批复当天照 §7 机械执行即可，不必重读本文推导。

**本文对 D-366（预热轮）与 D-354（轮转正确性）只给指针，不复述其内容。**
理由：复述会漂。§3 里凡涉及这两条的地方，写的是「照 D-366 §0.6.1③ 现行口径执行」
「轮转正确性已由 D-354 实测确认，不在此重新论证」，**判据本身留在决策日志与 runbook 原文里，
只有一份**（§2.14 一处规则的同一条纪律）。本文因此**刻意不含**：预热轮的记账步骤细则、
「射频已热 App 冷」口径的定义、`order_index` / `repeat_index` 的取值证据。

---

## 1. 采样量：每格 **n ≥ 15**（网络侧计入 run 数）

- 判据与推导**见提案 §1**（依 s1/s3 的网络侧 CV，排除 s2；承 D-372）。**此处不重推。**
- 现场核算仍照 runbook §3：首点位测完当天用 `stability.py --plan` 核 `需 n≥(80%)` 列。
  **现场核算需跑两条 `--plan`（默认 KPI `t1_ttft_ms` + 本节判据自己依据的网络侧
  `n1_rtt_p50_ms`），命令见 §5 第 4 项**——只跑默认那条等于把判据立论的依据（网络侧
  CV）一次都没核过（走查③）。
- ⚠ **`--plan` 只喂 quick 主体分面**（理由见 §5 与彩排 F-4）：
  取证 run 一条带 9 个场景，混进来会让「每格几个网络样本」这个数不再可读。
- 彩排实证：32 格 × n=15 的形状下，发布门「样本充分性」✅ PASS「全部 32 个格样本充足」，
  **没有任何守卫因 n=15 报错或误拒**。

## 2. s2 单列口径 · 标记名**定案** = `SCENARIO_INTRINSIC_JITTER`

提案 §2 把这个名字留给了「分析层属主」。**属主即本会话，现予定案。**

### 2.1 名字为什么长这样（先看仓里现有的怎么起名，再定）

清点了 `scripts/` 现役标记词汇：`MIXED_CAMPAIGN`、`MIXED_MODE`、`MIXED_PROFILE_VERSION`、
`MIXED_HIST_EDGES`、`TIER_ENDPOINT_CONFLICT`、`TIER_ENDPOINT_UNVERIFIED`、`TIER_MISSING`、
`CELL_CHANGED`、`CELL_CONFOUNDED`、`UNREPLICATED_POSITIONS`、`UNREPLICATED_ROUNDS`、
`SINGLE_ROUND`、`MEDIAN_NEAR_ZERO`、`IMPLAUSIBLE_VALUE`、`LOW_VALID_RATE`、
`UNKNOWN_VALIDITY`、`RULED_OUT`、`GRADE_TIE`、`ORDER_UNTRUSTWORTHY`、`NEED_2_POSITIONS`。

归纳出的三条成文规则，`SCENARIO_INTRINSIC_JITTER` 逐条对齐：

| 规则 | 现有实例 | 本标记 |
|---|---|---|
| 裸 `UPPER_SNAKE`，无命名空间前缀、无模块名 | 全部 20 个 | ✅ |
| 「**名词短语**说出**是什么情况**」，不是「该怎么办」 | `CELL_CHANGED`（不是 `RECHECK_CELL`） | ✅ 说的是「抖动是场景内生的」 |
| 首词是**被限定的对象**，其后是限定语 | `TIER_ENDPOINT_*`、`CELL_*`、`MIXED_*` | ✅ 首词 `SCENARIO_` = 抖动的**归属对象** |

**唯一一处需要如实说明的偏差**：长度 25 字符，比上表 20 个**渲染标记**中最长的
`TIER_ENDPOINT_UNVERIFIED`（24）多一个字符（比对时只数渲染标记；`DEFAULT_TARGET_EFFECT_PCT`
一类 Python 常量名不在此列，它们不出现在任何面上）。渲染面均为表格备注列，
25 字符不影响换行，**接受**。

（备选 `INTRINSIC_JITTER` 更短，**不取**：首词不是对象，读者无法从名字看出抖动归属于「场景」
而非「格」或「链路」，而**归属正是 D-372 全部的结论**。）

### 2.2 语义（判据结构，不预写门限数字）

标注对象 = **复测稳定性段的一行**，即 `(campaign_id, point_id, carrier, time_band, tier, profile_id) × KPI`。

触发结构：**同一格同一 profile 下，场景侧 KPI 的 CV 超门，而其网络侧 KPI 的 CV 未超门。**

- 场景侧 = `t1_ttft_ms` / `t2_itl_p95_ms` / `u2_tool_loop_p95_ms`；网络侧 = `n1_rtt_p50_ms` / `n2_jitter_ms`。
- 这正是 D-372 用来裁定 H3 的那对判别量（同批 RTT 平稳而 TTFT 独抖）。
- **门限值不在本文新设**：沿用 `stability.DEFAULT_CV_GATE`（M1 验收口径）。
- ⚠ **待定**：是否额外要求「网络侧 CV 低于场景侧若干倍」这一强化条件，**本文不擅定**
  （任何倍数都是新常量，需要自己的来源）。登记于 §8。

彩排实证（合成语料，数字虚构）：整形后的 quick 主体上，`t1_ttft_ms` 超 CV 门的格
**s2 占 27/32，s1 与 s3 各 4/32**，而同批 `n1_rtt_p50_ms` 三个 profile 的格中位 CV
分别为 2.66 / 2.59 / 2.49% ——**该形状可被这条判据干净地分出来**。

### 2.3 单列口径落地时的三面渲染契约（照 `RULED_OUT` 的先例，D-366）

D-337 的纪律：**标记在每个面用该面自己的词汇编码同一事实，不要求字面一致。**

| 面 | 形态 |
|---|---|
| **markdown** | 复测稳定性表**备注**列增 `SCENARIO_INTRINSIC_JITTER` 前缀标记；段首横幅一句：这些格的 `需 n≥` **不作为加测网络样本的理由**。 |
| **HTML** | 同段同行的中文短语（如 `场景内生抖动`），与 md 语义等价、措辞按 HTML 自己的惯例。 |
| **CSV** | `<prefix>_stability.csv` 增**独立列**（布尔），与 `unstable` / `low_confidence` 并列——CSV 没有横幅可看（D-141/D-303 的同一条理由）。 |

**并且**：s2 的 `需 n≥` 与 CV 门**单列呈现**，不与 s1/s3 的网络侧采样量核算并栏。

> **本节已由 D-382/D-383/D-388 实现**；契约与今日实现的逐面对账见 **§9.6**（三处偏离全部
> 是「实现比契约多」，本节正文**不回填**，状态只在 §9.6 一处记——D-380 定的规矩）。

> 注：CV>门的格「无论 n 多大都应先查测量装置」是既有口径（D-170），单列口径与之相容——
> 单列口径只是把「查什么」从「测量装置」细化到「场景/服务端侧」。

## 3. 轮次协议

### 3.1 预热轮

**照 D-366 现行口径执行，即 runbook §0.6.1 第③步，逐字不变。**

本增补只加**扩展轮特有的两件事**（原文没有、也不该塞进原文的）：

1. **每格记账 = n_计入 + 1 预热丢弃**；取 n≥15 则每格 **16 次采集动作、15 条计入**（提案 §4.1）。
2. **取证格同样先跑一轮丢弃的预热**，口径同上——不是「丢弃取证第 1 轮」
   （那是 D-358 未被选中的选项②，改用它需另立决策）。

> ⚠ **彩排实证的硬提醒（F-3，诚实的否定）**：分析层**没有任何一个面**能指认哪条 run 是预热轮。
> 含预热轮与不含预热轮的两份语料，发布门**逐条同判**（FAIL 1 / WARN 8 / N/A 2），
> 差别**只出现在数字上**（网络侧 CV 由 ~5.7% 降到 ~2.6%），不出现在任何标记或判词上。
> 这与 D-366 的原话一致（台账是唯一能认出它的地方）。
> **推论：「预热轮排干净了没有」只能靠人对台账核，发布门永远不会替你发现漏排。**
> 收工清单因此有专门一项（§5）。

### 3.2 3×3 拉丁方轮转

- **缺陷来源**：D-353（quick 单一顺序，位次差与场景差不可分离）。
- **修法**：扩展轮以**取证模式**承载轮转；**轮转正确性已由 D-354 实测确认，不在此重新论证**。
- **方阵**见提案 §4.2（3 个场景位次的循环方；≥2 轮为**能不能算**的最低线，仍成立）。
  **每格轮数 = 5**（2026-08-01 裁定，见 §8 #3 落定段）——提案 §4.2 的「建议 4 轮」已作废。
- 语料形态：一条取证 run = 9 场景，`scenario_order` 为三个轮次以 `|` 连接
  （契约字段说明里给的就是这个写法）。
- 彩排实证：取证子集单独过门时，**序位效应 ✅ PASS**、**序位效应·单元混杂 ✅ PASS**、
  **预热效应 ✅ PASS**——D-353 的「拉丁方未轮转」在这一面上**已消失**。

> ⚠ **但必须单独过门**，理由见 §5 第 2 条。这是本次彩排最重要的一条发现。

**触发命令（走查③补，D-415③——真缺口，`M2_CAMPAIGN_RUNBOOK.md`/本文此前均未交代；
唯一书面出处此前是 `MainActivity.kt:78` 代码注释，全仓无一条归档过的完整命令行；
本节内容并入 §0.7 时随之迁入基座 runbook）**：

```
adb shell am start -n com.aneb.probe/.ui.MainActivity \
  --es server <url> --ez autorun true --es mode forensic \
  [--es transport auto|wifi|cellular] [--es inject truncate:50]
```

- `--es mode forensic` 是本表触发取证模式（9 场景/拉丁方轮转）的唯一开关——
  完整取值集合是 `quick|forensic|continuity|ab`（`MainActivity.kt:78`），本文/runbook
  只用得到前两个。
- `--es server <url>` 与 `--ez autorun true` 是既有必填项，与 quick 模式一致，
  不因 mode 而变。
- `--es transport`/`--es inject` 为可选项，语义同 runbook 既有的 quick 模式用法，
  不因取证模式而变化（M-A:78 注释原文即两种模式共用同一组可选参数）。
- 一次 `am start` 产出一条完整的 9 场景取证 run（3×3 拉丁方 × 每格 5 轮，
  见本节 §3.2 上文）；`scenario_order` 由 App 自动生成写入语料，操作者不需要
  手动拼接。

## 4. 硬前置：radio 八字段 + CGNAT 出口 IP **随采**

跨时段扩点的**前置条件**，不满足则「格子差 vs 信号差 vs 制式差」不可分离
（D-365 闲时探针、D-370 早高峰批两次实证）。

| 项 | 落在哪 | 谁在核 |
|---|---|---|
| `radio` 八字段（`rat` / `rsrp_dbm` / `sinr_db` / `pci` / `tac` / `arfcn` / `sampled_n` / `stale`） | **语料内**：`scenarios[].network_snapshot.radio`（规格已全量落地，D-367 施工 / D-368 真机验收） | `radio_rollup` + 发布门「无线上下文」「忙闲同小区」两条 |
| **CGNAT 出口 IP** | **`scenarios[].network_snapshot.server_observed_addr`（语料内，逐场景）** | `radio_rollup`（自 D-376/T9，2026-08-01）+ 发布门「出口 IP」（D-429 追补，2026-08-03）+ `campaign_report` CSV 三面齐 |

> ⚠ **本行此前的措辞方向说反了，2026-08-02 订正（订正本身，不是"新发现问题"）**：
>
> **二次订正（D-429，2026-08-03）**：上一版订正把"谁在核"这一格整体写成"尚未读它——
> 本条订正前无人读"——**这句话只对一半**。`radio_rollup.egress_ip()`
> 自 **D-376/T9（2026-08-01）**起就是读者，`analyze()` 聚合 `egress_ips`、
> `render_markdown()` 渲 `MIXED_EGRESS:N`、`campaign_report.py` 也早已读进 CSV，
> 三面齐、604/604 测试；**只有发布门（`publish_check.py`）此前真的零读者**，
> 这半才是真缺口，已由 T28 补上（新增"出口 IP"检查项，WARN/PASS 两态，
> `scripts/tests/test_publish_check.py` 新增 2 条反例）。**教训对称登记**：
> 「A 没有 X」这类断言，写下与转录各是一次独立的对账义务——上一手（本文件自己）
> 说过，不代表下一手（大脑转录时、以及 v3 自己写这句时）不需要重新对代码验证；
> 这次错的正是撰写者自己前一天刚做的接线工作，被自己隔一天写反了方向。
> 旧文写"`server_observed_addr` 是客户端观测到的服务端地址，不是客户端的出口 IP，
> 结果契约没有承载出口字段"——**这句话与 schema 和服务端代码都对不上**。
> `spec/schemas/result-run.schema.json:176` 的字段描述逐字是"**服务端观察到的客户端源
> IP:port（路径对账）**"；`server/handlers_echo.go:13` 的实现注释逐字"observed 回显
> **客户端源 IP:port**，供路径对账（R-01/R-31）"——方向是**服务端看客户端**，即服务端
> 收到这次连接时看到的源地址。对一台经 CGNAT 出网的移动客户端而言，服务端看到的源
> IP **就是**运营商分配的公网（出口）地址本身，不是什么"服务端的地址"。**契约其实
> 承载着这个字段，且逐场景都有，不是只能靠台账人工记的东西**。
>
> **未验证的部分，如实标注**：`server_observed_addr` 与"外部独立查询页看到的出口 IP"
> 是否逐字节一致，本条订正**没有做过一次外部交叉核验**（比如同一时刻访问
> `ifconfig.me` 并核对是否是同一个值）——理论上两者应该相同（都是运营商 NAT 网关分给
> 这次会话的同一个公网地址），但 ANEB 服务端与外部查询页之间若存在任何差异路径
> （不同数据中心、不同协议栈观测点），值可能不同。**建议下次外场窗顺手核一次**，
> 核过之后可以把 §5 收工清单里"人工记出口 IP"那一项降级为可选交叉验证，而不是必需项。
>
> **交叉验证方法（原走查④建议，降级为验证手段而非唯一来源）**：设备联网状态下访问
> 一个显示公网出口 IP 的查询页（例如 `ifconfig.me`/`ip.sb` 之类）并截图存证，与该批
> 语料里 `server_observed_addr` 的值核对是否一致——不再是"唯一记录手段"，是"验证
> 自动记录是否可信"的一次性动作。

## 5. 出报告与收工清单（扩展轮专用；方向 B 形状，**PO 已批复采纳**）

> 大脑裁定方向 **B =「quick 主体 + 取证子集」**（依据即提案 §4.3）；
> **PO 已批复采纳**（2026-08-02 14:3x「按照你推荐继续推进」；`docs/BRAIN_TASKBOARD.md`
> 「PO 批复（2026-08-02 14:3x…）」节逐字：「采集口径方向 B（quick 主体+取证子集）就此
> 落定（PO 授权大脑推荐生效）」）。**点位真名（D1）与 D2/D4 仍待 PO**（§6/§8 #1），
> 与本节无关——下述各项均未依赖任何具体点位名（核对见 §9.7）。
> 下面这份清单**由彩排逐条跑出来**，不是推想的：完整命令与产出见证据包 README §1。

> ⚠ **前置缺口（2/3/4 三项都依赖它）——已解决，2026-08-02，`scripts/split_by_run_mode.py`
> （D-415②/T20①）**：下段是缺口发现当时（本节整理时）的原始记录，**保留不改**（与本文件
> §9 一贯的"漂移记录+已修标注"做法一致）；缺口本身已不再存在，2/3/4 项照下面的命令
> 直接可跑，不需要"先定下来怎么分面"这一步了。原始记录：2/3/4 三项的
> 操作对象是「按 `run.mode` 拆好的两份分面语料」（`quick`/`forensic`，契约必填字段，
> `spec/schemas/result-run.schema.json:29`）。**`scripts/` 目前没有任何生产工具能从真实
> `labeled/*.jsonl` 产出这两份**——彩排用的两份分面文件由一次性合成整形器
> `shape_expansion_corpus.py` 在造语料时内部切出，不适用于真实数据；`coverage_matrix.py`
> 与 `stability.py` 均**无** `--mode` 一类过滤参数（已查源码 `add_argument` 全量确认）。
> `M3_EXPANSION_ROUND_GUARD_DIFF.md` A-4 / C-1 把这一步称为「运行时分面……操作纪律，
> 不是守卫」，但两处都没给出分面本身怎么做。**这不是文档整理该顺手补的空白**——分面
> 工具/命令是一次独立的工具或口径决定，超出本次「整理收工清单」的范围，**不擅自发明**。
> **执行扩展轮之前需先把这一步定下来**（新写一个小工具，或明确人工做法）——**已定：
> `split_by_run_mode.py`**，按 `run.mode` 把 `<counted>.jsonl` 切成
> `<quick-subset>.jsonl`/`<forensic-subset>.jsonl`（用法见 `scripts/README.md`）。
> 下面 2/3/4 项的命令里这两个占位符，现在就是这个工具的两份真实输出（占位写法同本节
> 其余处的 `<counted>.jsonl`，不是点位名占位，见 §9.7）。

> **三份语料各用在哪（走查②补，避免操作者靠猜文件名对不对）**：

| 语料 | 用于 | 产出方式 |
|---|---|---|
| `<counted>.jsonl`（全量，quick+forensic 合并） | 第 1、6 项 | 契约门校验对象；第 6 项"原流程"复用它 |
| `<quick-subset>.jsonl` | 第 3、4 项 | `split_by_run_mode.py <counted>.jsonl` 的 `--quick-out` |
| `<forensic-subset>.jsonl` | 第 2 项 | 同上，`--forensic-out` |

> 喂错文件不会报错（三份结构相同），报告照常跑出来但数字是错的——比如把取证子集
> 喂进第 6 项的主报告，会让序位效应因单元混杂而误判（同第 2 项已列举的坏结果）。
>
> **1–5 项按每个采集日收工时执行一次；第 6 项（出正式报告）留到全部点位测完后统一做**
> （走查⑤补）——`扩展轮跨多天，覆盖检查等本该每天发现"今天有没有漏测的格"的动作，
> 不能推迟到最后才做，否则回头补测成本远高于当天发现当天补。**第 4 项尤其不可
> 推迟到出发之后**：`--plan` 采样量核算是首点位测完当天的时敏动作（runbook §3:277-285
> "这时改采集计划还来得及"），过了当天再想加复测，代价已经不一样——这正是分面工具
> （`split_by_run_mode.py`）被 D-415 裁定升级为**扩展轮出发前必做项**的理由，不是
> "报告可以晚点出，工具也能晚点补"。

1. **契约门先行**：`validate_results.py <counted>.jsonl` —— exit 非 0 就停，别带病出报告。
2. ⛔ **取证子集必须单独出一次报告 / 过一次门**，**不能只看主链路那一份**。
   彩排实测：两块池化在一份报告里时，
   - 「序位效应」判**不可单独归因(单元混杂)**——报告点名 `CELL_CONFOUNDED … 等 24 个 未出现在每个位次`；
   - 「预热效应」印出 **21% 的首轮劣势**，而各轮样本数是 **1443 / 88 / 91**——
     那 21% 全是**格构成差**，一点预热都不是。
   
   根因是构造性的：quick 主体只供位次 #0–#2 且 32 格全供样，取证子集供位次 #0–#8 但只有 8 格供样。
   **守卫是对的，错的是把两块喂给同一次分析。** 单独跑取证子集，三条全部 PASS。
   命令（语法同 runbook §4 报告 / §5 发布门，对取证分面文件**单独再跑一次**）：
   `campaign_report.py <forensic-subset>.jsonl --md report_forensic.md --html report_forensic.html --csv tables_forensic --provenance provenance_forensic.json`；
   `publish_check.py <forensic-subset>.jsonl`。
3. **覆盖检查喂 quick 分面**：全量语料下子集格印「样本 **20**」（15 quick + **5** 取证，D-379），
   只喂 quick 分面才印 15。「样本」列数的是 run 数、不分模式
   （`repeat_index` 那一列已由 D-344 撤销，理由仍成立），故**只有分面才核得动 n≥15 这条判据**。
   命令（语法同 runbook §3:262）：
   `coverage_matrix.py <quick-subset>.jsonl --config ../docs/campaign_grid_shenzhen.json`。
4. **采样量核算（`--plan`）喂 quick 分面**，同上。命令（语法同 runbook §3:278；承其默认
   KPI `t1_ttft_ms` 之外，另加 §1 判据本身依据的网络侧 KPI `n1_rtt_p50_ms`——彩排两个都
   跑过，证据包 README §1 命令 6/7）：
   `stability.py <quick-subset>.jsonl --kpi t1_ttft_ms --plan`；
   `stability.py <quick-subset>.jsonl --kpi n1_rtt_p50_ms --plan`。
5. **人工核台账**（自动化核不了，见 §3.1）：
   - 每格「完成 N / 尝试 M」，M>N 时在采集概况里如实写原因；
   - 每格恰好 **1 条**标「预热丢弃」的 `run_id`，且**确已排除出计入语料**；
   - **CGNAT 出口 IP 已随语料自动落盘**（`server_observed_addr`，见 §4 订正）——
     不再是人工必记项；首次外场窗顺手做一次 §4 说的外部交叉核验即可。
6. 其余照 runbook §4 / §5 原流程。

## 6. 点位：全部 `PENDING-PO`

- 扩展轮网格 = **6–8 点位 × 忙闲 × 双运营商 × 单层级**（D-48 单实例 E-01）。
- **本文不预写任何真实点位名**。所有点位一律记为 `PENDING-PO-01` … `PENDING-PO-08`。
- 点位数（D1）、是否双运营商（D2）、是否忙闲两趟（D4）**均待 PO**（提案 §3、§5）。
- 真名下来后同改：`campaign_grid_shenzhen.json` + 补注命令 + 本文 §6 + runbook §0.6 的补注示例。
- ⚠ 彩排语料里的点位名是 `SYNTH-P01..P08`，**那是合成标记 #2 的一部分，不是占位命名规范**，
  不要照抄到外场配置里。

## 7. 批复当天的并入清单（照做即可，不必重读本文）

| # | 改哪 | 改成什么 |
|---|---|---|
| 1 | `M2_CAMPAIGN_RUNBOOK.md` 新增 **§0.7 扩展轮** | 本文 §1–§6 正文迁入；本文降级为「设计说明」并加「已并入」横幅 |
| 2 | runbook §0.6 开头 | 加一句「§0.6 = 试点单点位；扩展轮见 §0.7」，避免两节被当成并行可选 |
| 3 | runbook §3（覆盖检查） | 补「扩展轮喂 quick 分面」一句 + **§3 里那两条命令各改一次**：`coverage_matrix.py`(:262) 与 `stability.py --plan`(:278)，分别对应本文 §5 第 3、4 项（原写「那条命令」是单数，漏了 `--plan` 那条——见 §9.6.2） |
| 4 | runbook §5（发布前复核） | 补「取证子集单独过门」一项与两条对着核的实测数字 |
| 5 | `campaign_grid_shenzhen.json` | 点位真名（PO 给出后） |
| 6 | `docs/BRAIN_TASKBOARD.md` | 登记并入完成 |

## 8. 待定清单（**不得预写**）

| # | 待定项 | 状态 | 说明 |
|---|---|---|---|
| 1 | 点位真名 / 点位数（D1）、双运营商（D2）、忙闲两趟（D4） | **待 PO** | 提案 §3/§5 |
| 2 | 采集模式口径（方向 A 取证全量 / **B** quick 主体+取证子集） | ✅ **已裁定 = 方向 B**（2026-08-02 14:3x） | 大脑已裁定方向 B；**PO 已批复采纳**（`docs/BRAIN_TASKBOARD.md`「PO 批复（2026-08-02 14:3x…）」节：「采集口径方向 B（quick 主体+取证子集）就此落定（PO 授权大脑推荐生效）」）。**扩展轮仅余 D1 点位真名 / D2 / D4 待 PO**（同节原话），见 #1 |
| 3 | 取证子集每格轮数 | ✅ **已裁定 = 5 轮**（2026-08-01，D-379） | 见下方「#3 落定」段。提案 §4.2 的「建议 4 轮」**就此作废**，§3.2 已同步。 |
| 4 | `SCENARIO_INTRINSIC_JITTER` 是否附加「网络侧 CV 低于场景侧 N 倍」的强化条件 | **待定** | 任何倍数都是新常量，需自己的来源（§2.2） |
| 5 | 新协议下 s2 的 CV 实值 | **待测** | 提案 §5 第 4 项 |
| 6 | 每格取证 run 数（联合满足 n≥15 网络 + 每位次 n≥**5** 序位） | 取证侧已由 #3 定死 = **5**；**网络侧 n≥15 仍待现场核算** | 首点位当天用 `stability.py --plan` 算（喂 quick 分面，§1）。**「每位次 n≥4」的旧写法随 #3 一并作废** |
| 7 | 扩展轮总工时 / 外场日 | ✅ **已重算 = 3.414 外场日**（2026-08-03，T33/D-444） | 依赖 #1–#6；提案 §4.3 只给了 quick-only 下界（2.4 天，不含取证子集）。**重算时取证侧基数用 5 轮**（#3），并计入下方那 **260min/4.3h**（订正后数字，见下方 #3 段——原"+52 min"是 D-379 定 5 轮前的残留，已改对）；完整推导见 `docs/T33_GRID_PREP_20260803.md` §3 |

### #3 落定：取证子集**每格 5 轮**（2026-08-01 裁定，D-379）

**判据**：一个数同时满足两条**互不相同**的判据，故不必在两者间取舍——

| 判据 | 数的是什么 | 5 轮为何满足 |
|---|---|---|
| 序位判据（提案 §4.2） | **每个执行位次**的样本数 | 一条取证 run 在每个 `order_index` 上各出一个场景，故每格 5 条 run ⇒ **每位次 n=5** |
| 样本充分性（`stability.DEFAULT_MIN_SAMPLES` = 5） | **每格的 run 数** | 每格 5 条 run ⇒ **n=5，不再 `low_conf`** |

**代价（2026-08-03 订正——原"每格多一条 run"的框架与本节 #3 自己裁定的
"每格 5 条 run"不一致，是 D-379 定 5 轮前的残留数字，未跟着改；D-444 核算
时发现，大脑裁定本文件属主为 v3，直接改对不留待）**：单条取证 run ≈
**6.5 min**（彩排 F-5 口径）；每格 **5 条**（#3 落定，D-379）⇒ 每格约
**32.5 min**；取证子集 8 格 ⇒ **8 × 32.5 = 260 min ≈ 4.3 小时**（与 D-388
全链路复跑"取证由 32 run 变 40 run"的实测算术 8 格×5=40 run 吻合）。
**这不再是"不占额外外场日"的量级**——4.3 小时纯测量、+40% 现场开销后约
6 小时，接近一个完整外场日的预算，必须计入总工时（见 :303 与
`docs/T33_GRID_PREP_20260803.md` §3——该文件的 3.414 外场日已经用的是这个
260min/4.333h 基数，不需要跟着改）。

**明确不做的两件事**（写下来，免得下一个人以为漏了）：

1. **不动全局常量** `DEFAULT_MIN_SAMPLES`。它是热力卡/AQS 全局的样本门，为取证子集调低它
   会把**所有**格的置信门一起放松——用一个局部问题去改一个全局判据，正是 §2.2 那条红线。
2. **不加豁免逻辑**（例如「取证格跳过样本门」）。豁免要有人维护、要有守卫、还要有人记得
   它为什么在；把轮数从 4 提到 5 是**零代码改动**，两条判据当场同时成立。

**被这条作废的旧写法**（D-301 的纪律：判据换了，转述它的每一处都要跟着换）：

- 提案 §4.2「≥2 轮为最低、**建议 4 轮**」——建议值改 **5**；「≥2 轮为最低」仍成立（那是
  另一件事：能不能算，而不是算得准不准）。
- 本文 §3.2 对提案 §4.2 的转述（已改）。
- 本文 §8 #6 的「每位次 n≥4」（已改）。
- 彩排证据包 `README.md` 的 F-5 **不改**：证据包记的是**当时**的事实与当时的两条路，
  事后回填会让证据看起来早就知道答案。

---

*承 D-48 / D-141 / D-170 / D-303 / D-311 / D-335 / D-337 / D-344 / D-353 / D-354 / D-358 /
D-365 / D-366 / D-367 / D-368 / D-370 / D-372；提案 §1–§5；runbook §0.6/§3/§4/§5。
守卫与渲染面的改动清单**不在本文**，见 [`M3_EXPANSION_ROUND_GUARD_DIFF.md`](M3_EXPANSION_ROUND_GUARD_DIFF.md)。*

---

## 9. §7 并入清单的核对记录（2026-08-02 夜班 ③ · **只核对，未并入**）

并入待扩展轮开跑令；本节**没有动 runbook 一个字节**。逐行对着**当前**的
`M2_CAMPAIGN_RUNBOOK.md` 与现行裁定核了一遍。

> ⚠ **本节及以下各条目里出现的 `:N` 本文件行号引用，一律是记录当时（各自条目
> 标注的日期）的行号，不是活指针**——本文件当天/次日多次被前置内容插入
> （最近一次：T20/D-419 在 §1/§3.2/§4/§5 开头新增文字），下方行号已随之进一步
> 漂移，不逐条回填校正（那样每次插入都要重新核一遍全部历史引用，成本和收益
> 不成比例，且历史记录本就该钉在"当时"，见 §9.6.2 与本文件一贯的"如实记录
> 不改正文"原则）。要跳转到当前正文对应位置时，请按上下文关键词搜索，不要
> 直接信任行号。

### 9.1 六行清单逐行

| # | 判定 | 依据（当前实况） |
|---|---|---|
| 1 | ✅ 仍成立 | runbook 现有 `## 0.5`(:31) / `## 0.6`(:115) / `## 1`(:200)，**无 §0.7**，插入位干净；本文 §1–§6 齐备 |
| 2 | ✅ 仍成立 | §0.6 标题即「真实数据试点（= 本轮正式战役：当前位置单点位）」；:122 虽已有「后续扩展轮」一句，但**不指向 §0.7**，故该改动不重复且仍有必要 |
| 3 | ✅ 仍成立 | §3「覆盖检查（外场期间每日收工跑）」在 :259 |
| 4 | ✅ 仍成立 | §5「发布前复核」在 :339 |
| 5 | ✅ 仍成立，**但路径要补** | 仓内实际是 **`docs/campaign_grid_shenzhen.json`**；清单写的是裸文件名。runbook 里的命令用 `../docs/campaign_grid_shenzhen.json`（从 `scripts/` 解析），并入时照抄裸名会指向不存在的路径 |
| 6 | ✅ 仍成立 | `docs/BRAIN_TASKBOARD.md` 在 |

### 9.2 漂移 **1 条（P1）——已修（2026-08-02，D-398）**

> **§5 第 3 条的「样本 19（15 quick + 4 取证）」在 D-379 之后已经是 20（15 + 5）。**
> **:157 已改为「样本 20（15 quick + 5 取证，D-379）」。**

§8 #3 自称「提案 §4.2 的『建议 4 轮』就此作废，**§3.2 已同步**」——它**同步了 §3.2（:120 已写「每格轮数 = 5」），
漏了 §5**。而 §5 是**收工清单**，是操作者扩展轮当天**真正照着核**的那一面：
届时覆盖检查会印「样本 20」而清单说 19，读者要么以为多跑了一轮、要么以为漏排了预热轮。
**D-301 原样形状**（判据换了，转述它的那处没跟着改），且**漏的恰是被现场执行的那份**（D-311）。
旁证：D-388 的全链路复跑已实测「取证由 32 run 变 40 run」（8 格 × 4 → 8 格 × 5），算术对得上。

**订正时不满足于算术，去产物里数了一遍**（D-389 那次 5 轮复跑的覆盖检查已经落盘，
不必推算）：`evidence/m3_expansion_gen_20260801/08_coverage_matrix.md`（全量）里
`SYNTH-P01/P02` 那 **8 个取证格逐行印 `20`**、其余 24 格印 `15`；
同批 `09_coverage_matrix_quick_only.md`（quick 分面）**32 格全印 `15`**。
两个面都对得上，故 20 是**测出来的**，不是我按 15+5 算出来的。

**为何只改这一处**：`M3_EXPANSION_ROUND_GUARD_DIFF.md:103`（A-2 的 F-4 实证）与
`evidence/m3_expansion_rehearsal_20260801/README.md:192`（F-4）也写着 19，
但**两处都不该改**——前者由该文「落地状态」节统一记状态、各条正文不回填（D-380 定的规矩），
后者是彩排当时的事实，D-379 已明令「证据包 README 的 F-5 刻意不改，
事后回填会让证据看起来早就知道答案」。**这两处的 19 是历史，§5 的 19 是指令**，
差别不在数字而在这句话是给谁看的。

### 9.3 连带发现 1 条（在 runbook 侧，不属并入清单本身）——**未修**

`M2_CAMPAIGN_RUNBOOK.md` §0.6.1 标题写「采集前必做的**两件**事」，正文实有 **①②③ 三件**
（:130 / :144 / :156）。**第三件正是 D-366 的「丢弃的预热轮」**——也就是本文 §3.1
逐字引用的那一条。**被引用的那一条，恰好就是标题漏数的那一条**（D-333「文字描述/实现/渲染
三处各数一遍」的形状；③ 是 D-366 后补进去的，标题没跟着改）。

**本轮不修**，两个理由都写下来：(a) 本项奉命**只核对**；(b) 该文件此刻另有夜班会话在改
（最近三次提交含 `runbook 补语料拉取步(夜班 5/6)`），避免撞车。**交由 runbook 属主一词修正**。

### 9.4 诚实的否定 3 条（查过，结论是它对的）

1. §5 第 3 条「`repeat_index` 那一列已由 D-344 撤销，理由仍成立」——**对的**。
   `scripts/coverage_matrix.py` 的 `analyze()` docstring(:52-59) 逐字确认：D-340..343 加过
   「不同重复」列，但契约只在 scenario 内定义 `repeat_index`、run 级无生产者、
   **4822 条真实记录 0 条**在 run 级带它，列于是恒印「0 个不同重复(+N 无编号)」
   ——惰性且**倒过来暗示操作者忘了编号**，故 Removed（D-344）。当前代码确实不再产出该列。
2. §3.1「照 runbook §0.6.1 第③步执行，逐字不变」——**该步在且对得上**
   （runbook:156「每格正式采集前，先跑一轮『丢弃的预热轮』（PO 批复 D-366，试点报告 §2.4 选项①）」）。
3. **全部 5 处 `campaign_grid` 引用都指向存在的文件**（runbook :14/:17/:125/:195/:262），
   且带路径的两处按其自己的工作目录（`scripts/`）解析成立——
   **D-320 那类「文档指向一个全仓不存在的名字」的陷阱，在当前 runbook 上不成立**。

### 9.5 本次核对**没做到**的（诚实缺口）

- ~~§2.3 的三面渲染契约（`SCENARIO_INTRINSIC_JITTER`，D-382 已实现）**未逐面核**~~
  → **已补做，见 §9.6.1**（2026-08-02）。
- ~~D-388 新增的 `exp_plan.csv` 是否该进并入清单第 4 行（§5 发布前复核），**未查**~~
  → **已查，结论=不该进第 4 行（诚实的否定），见 §9.6.2**（2026-08-02）。
- 只核了清单**指向的位置**与其中的**数字**，未逐句核 §1–§6 正文迁入后是否有时态错位
  （§2 多处写的是「定案 / 待落地」，而 D-382/D-388 已把其中一部分实现了——**这正是 D-311 的形状，
  下一轮应专查**）。**仍未做**：§9.6.1 只补了 §2.3 一节的时态，§1–§6 其余各节没逐句过。

---

## 9.6 §9.5 两条缺口的补做（2026-08-02）

### 9.6.1 §2.3 三面渲染契约 · 逐面对账

**量法**：拿 D-389 那份已归档的语料 `evidence/m3_expansion_gen_20260801/expansion_counted.jsonl`
（520 条），用**今天的 HEAD** 重跑 `campaign_report.py --md --html --csv`（产物落临时目录，
**不入库**），然后**三个面各自数各自的**——不按模块数，因为按模块数看不见「两张表只导一张」
（D-303）。

| 面 | 契约（§2.3 原文） | 今日实测 | 判定 |
|---|---|---|---|
| **markdown** | 备注列前缀标记 + 段首横幅一句 | 段首横幅 **1** 处；复测稳定性表带标记的行 **22** 行；「不可判」行 **0** 行（本语料无 `no_network_cv`）；**外加契约没写的两处**：摘要 bullet **1**（「其中 22 个属场景内生抖动」）、采样量核算段 **2**（结论句排除 22 个 + ⚠ 单列横幅）。`SCENARIO_INTRINSIC_JITTER` 裸标记全文 **26** 次 = 22 行 + 1 横幅 + 1 摘要 + 2 采样量 | ✅ 契约两项都在；**实现多出 2 个渲染位** |
| **HTML** | 「同段同行的中文短语，措辞按 HTML 自己的惯例」 | 与 md **逐项相同**：横幅 1、带标记 `<tr>` **22**、裸标记 **26**、中文短语 **28** | ⚠ **契约的前提是错的**（见下） |
| **CSV** | `<prefix>_stability.csv` 增**一个**独立布尔列 | **两个文件**各带**两列**：`_stability.csv`（18 列，288 行）与 `_plan.csv`（24 列，288 行，D-388），两者都有 `scenario_intrinsic_jitter` + `scenario_jitter_reason`；两份分布**逐字相同**：True **22** / False **266**；reason `not_applicable` **254** / 空(=True) **22** / `network_side_unstable` **12** / `no_network_cv` **0** | ⚠ **列数与文件数都比契约多** |

**三处偏离，全部是「实现比契约多」，无一是缺陷**——逐条给出处：

1. **HTML 不是自己的措辞，是从 markdown 派生的**（`stability.py:272-277` 的注释逐字写明：
   HTML 报告由 markdown 转换而来，D-107，故中文注文**跟着标记一起走**，另写一套 HTML 措辞
   就是**第二个可以自己漂的真相**）。§2.3 写「措辞按 HTML 自己的惯例」时**没数这个面的渲染者是谁**，
   而 D-337 那条纪律（各面用各面自己的词汇）只适用于**各有独立渲染器**的面——在本仓即 CSV 那一面。
   **契约该改的是它自己，不是代码。**
2. **CSV 是两列**：`scenario_jitter_reason` 是 D-378 加的判别量，与隔壁 `cv_not_computable_reason`
   同一形状——裸 `False` 有**三个**成因要三种不同动作，其中 `no_network_cv` 是「判不了」，
   绝不能被读成「查过了，是网络问题」。契约当初只想到布尔。
3. **CSV 是两个文件**：`_plan.csv`（D-388）也带这对列，理由写在
   `campaign_report.py:2196-2198`——被标记的格上的 `required_n` 是算术、不是外场计划。

**一条如实登记的观察（未修，也不建议本轮修）**：判据覆盖三个场景侧 KPI
（`t1_ttft_ms`/`t2_itl_p95_ms`/`u2_tool_loop_p95_ms`，§2.2），
而复测稳定性与采样量核算两段只渲染 `stability.DEFAULT_STABILITY_KPIS`
=（`t1_ttft_ms`/`n1_rtt_p50_ms`/`u1_goodput_mbps`）——**场景侧只有 `t1` 在里面**。
故 `t2`/`u2` 上的场景内生抖动**今天在任何一个面上都看不见**（横幅之所以只有 1 处而非 3 处，
就是这个原因）。**改 `DEFAULT_STABILITY_KPIS` 是设计决定不是笔误**（它决定整份报告的 KPI 版面），
**登记待裁，不擅改**。

### 9.6.2 `exp_plan.csv` 是否进并入清单第 4 行 → **诚实的否定：不该进**

第 4 行 = 「runbook §5（发布前复核）：补『取证子集单独过门』一项与两条对着核的实测数字」。
`exp_plan.csv` 不属于它，四条判据**各自独立**：

1. 第 4 行是**判读项**（取证子集要不要单独过门），不是**产物清单**。
2. `publish_check.py` 对 `plan` / `required_n` / `采样量` **零命中**——发布前自检**根本不读**
   采样量核算这一段，第 4 行没有可以挂它的钩子。
3. **17→18 这个数在 runbook 里没有承载者**：§4 写「成套 CSV（**运行时逐个打印文件名**，照单归档）」，
   §5 归档项写 `tables_*.csv` **通配**——全文没有一处数张数或列文件名，
   故 D-388 加一张 CSV **不产生任何过期点**。（这正是 §4 那句「运行时逐个打印」当初想避免的事。）
4. `--plan` 的归属地是 **runbook §3**（那条命令已在 :275-279），对应并入清单**第 3 行**。

**连带发现 1 条（真缺口，已就地修）**：第 3 行原文「补『扩展轮喂 quick 分面』一句 + **那条命令**」
是**单数**，而 runbook §3 里有**两条**命令——`coverage_matrix.py`(:262) 与
`stability.py --plan`(:278)——本文 §5 的第 3、4 两项**各要求其中一条**改。
照原字面执行会**只改覆盖检查那条，漏掉 `--plan`**，而 §1 与 §5 第 4 项都明写
`--plan` 只喂 quick 分面（混进取证 run 会让「每格几个网络样本」不可读，彩排 F-4）。
形状＝「一条规则点名 N 个东西，执行者实际只会改一个」。**第 3 行已改为点名两条。**

> ⚠ 本节（§9.6）**没有守卫**，与 §9.2 同——文档核对结论不进任何测试。如实写下。

---

## 9.7 §5 收工清单的终稿整理（2026-08-02，PO 14:3x 批复方向 B 之后）

**任务边界**：把 §5 整理为「真名一到就能拼」的形状；**不并入 runbook**（该动作仍待扩展轮
开跑令）；**不裁定**任何仍待裁项（W-2/W-4、FRLOSS 生产端残留见 D-406，均原样不动）。

### 9.7.1 已核对为「原本就对」的项（未改）

- **5 轮取证 / D-379**：§5 第 3 条「样本 20（15 quick + 5 取证）」已由 D-398（T15）订正到位，
  本轮复核**仍成立**，未再改动。
- **n≥15**：§1 与 §5 第 3/4 条口径一致，未发现漂移。
- **`SCENARIO_INTRINSIC_JITTER`**：§5（收工清单）本就不需要点它的名——该标记是分析层
  渲染产物，不是操作者手填的量；§2.3/§9.6.1 已核过它的三面落地，此处不重复。

### 9.7.2 已修的漂移（1 处，非新裁定，是同步已落地的 PO 批复）

**§5 header 的「待 PO 确认」已过期**：PO 已于 **2026-08-02 14:3x**「按照你推荐继续推进」
批复方向 B（`docs/BRAIN_TASKBOARD.md`「PO 批复（2026-08-02 14:3x…）」节逐字：「采集口径
方向 B（quick 主体+取证子集）就此落定（PO 授权大脑推荐生效）」）。**同步两处**（D-301
纪律：判据换了，转述它的每一处都要跟着换）——§5 header blockquote、§8 #2 状态列
（原「大脑已裁定方向 B，PO 未批」→ 已裁定+批复出处）。**`docs/DECISION_REQUEST_2026-08-02.md`
§1.2 同样还写着「PO 未批」，本轮未改**——该文件不在本任务范围内（T18 属主另行），
在此如实记一笔指针，不代为处置。

### 9.7.3 §6/§7 的占位符约定 · 核对结果（诚实的否定）

任务前提问 §5 里有没有 `PENDING-PO` / `SZ-PILOT-01` 一类点位占位需要换成拼接槽——
**逐行核过，§5 全文零命中**（`grep` 复核）。原因：§5 的六个条目全部是「怎么跑工具/
怎么核台账」的操作步骤，唯一涉及点位配置的第 3 条只引用 `campaign_grid_shenzhen.json`
这个**路径**，不引用其中的点位**值**——真名落地是那份 JSON 内部的事（§6/§7 第 5 行已
点名），§5 的文字本身不需要跟着改。**故本节没有可替换的占位符，也没有新造占位约定**
（本仓既有两套占位写法：点位名用 `PENDING-PO-0N`/真名占位见 §6；文件名用尖括号
`<counted>.jsonl` 见本文多处——本轮新增的 `<quick-subset>.jsonl` / `<forensic-subset>.jsonl`
沿用**后一套**，因为它们占位的是文件不是点位，混用会制造一个假的「点位占位符」）。

### 9.7.4 新登记的缺口（1 条，未擅自处置）

**「quick 分面 / 取证分面」怎么从真实数据切出来，全仓没有答案**：`GUARD_DIFF` A-4/C-1
两处都把它称为「运行时分面……操作纪律，不是守卫」，但从未给出机制；`scripts/` 里
`coverage_matrix.py` 与 `stability.py` 的 `add_argument` 全量核过，**均无 `--mode` 一类
过滤参数**；彩排的两份分面文件由 `shape_expansion_corpus.py`（一次性合成整形器）内部
切出，**不适用于真实 `labeled/*.jsonl`**。§5 现已在 2/3/4 三项前挂一条 ⚠ 前置说明并用
`<quick-subset>.jsonl` / `<forensic-subset>.jsonl` 占位，**但没有替读者把分面工具写出来**
——那是一次独立的工具/口径决定，超出「整理收工清单」的授权范围，**留给执行扩展轮之前
的下一步裁定**。

> ⚠ 本节（§9.7）同 §9.2/§9.6：文档核对结论不进任何测试，如实写下。
