# 业务进展视图 · 2026-08-28（首期）

> 数字全部引 `docs/CORPUS_LEDGER.md`（`python scripts/corpus_ledger.py` 当日重算），未手抄。
> 覆盖期：2026-08-22 → 08-28。模板见 `BIZ_PROGRESS_VIEW_TEMPLATE.md`。

---

## 一、本周新增数据

| 项 | 上周 | 本周 | 增量 |
|---|---|---|---|
| 真实 run | 110 | 110 | **+0** |
| 场景 | 624 | 624 | +0 |
| 覆盖点位 | 3 | 3 | +0（SZ-PILOT-01 57／PENDING-PO-01 16／home_indoor 10，另 27 run 无点位标签） |
| 覆盖运营商 | 1 | 1 | +0（全部 ctcc 83，另 27 run 无标签） |

**一句话结论：本周真实数据 +0 —— 数据资产原地，本周产出全在工具与治理侧。**
外场与设备侧任务在等待期（详见第三节），非本 lane 可解。
（口径说明：110 是本周首次建立台账时的**全量盘点**基线，不是本周新采；此前无客观计数，
「73 run」是 t46 单份语料的数字，全仓真实资产从未被数全过——这正是台账要解决的问题。）

> **⚠ 事后订正（2026-08-29 补，不改上表历史值）**：本页的 **110 是口径受限的计数**。
> 台账初版只扫 `evidence/`，次日把扫描根扩到 `server/data/results/` 后为 **111**
> （提交 `83c4c7c`）——**多出的那 1 条是同一批语料被数得更全，不是新采到的数据**。
> **下周填表时不要把 110→111 写成「+1 run」**：那是口径差异，不是时间变化，
> 把它读成增量会凭空造出一次「本周有新数据」。增量口径以台账当次重算为准。

## 二、回答了什么业务问题

- **「我们到底有多少数据？」→ 本周起可以一句话回答**：真实 110 run / 624 场景 /
  3 个点位 / 1 家运营商，合成语料 560 条单列不混入（`CORPUS_LEDGER.md`，D-565）。
  此前这个问题没有单一事实源，各文档手抄的数字互不相同。
- **「已有数据可信到什么程度？」→ 可以回答，但结论不乐观**：109 个带分 run 里
  **99 个（91%）标 low_confidence**——样本量结构性不足（D-493③ 早有定性，本周首次
  有了全量分母）。**含义：现有语料支持相对比较，不支持对外报绝对档位**。
- **「深圳多点位晚高峰谁更差 / RAT 与忙闲的影响」→ 仍不能回答**，见第三节。

## 三、离《城市热力卡与归因报告》还差几步

| # | 缺什么 | 卡在谁 | 解除条件 |
|---|---|---|---|
| 1 | 多点位真实语料（现仅 3 点位、1 运营商） | 外场执行（PO 指令暂停中） | 外场线恢复 + 设备可用 |
| 2 | RAT/忙闲效应分离 | 同上 | 制式与出口共线需多点位实采打破（D-471） |
| 3 | 吞吐真实路径样本 | Codex（E-01 `s4_throughput` 未部署） | 服务端部署到位 |
| 4 | 豆包战役首批语料 | SPEC-2 lane（设备窗） | 到货后本 lane T+1 出报告 |

**当前判断：工具链已就绪且空转验证过——热力卡/归因的渲染与发布门在合成全网格语料上
跑通多轮，缺的纯粹是外场真实语料。数据一到，报告是 T+1 的事，不是从头开发。**

## 附：本周本 lane 产出（工具与治理侧）

- 语料台账上线（D-565）：一条命令全量重算，产物 md+CSV，配 5 条守卫；
  首算即发现并修复一处标签拆格缺陷（运营商全称「中国电信」与 `ctcc` 此前被劈成两桶）。
- `verify_all` 分层触发改造（进行中，SPEC-3 §3.2）：日常自检从 10 分钟降到秒级，
  19 道门的语义与门数一律未动。

---

_数字与结论口径可回溯至 `CORPUS_LEDGER.md` 与发布门产物；本页由 v3 出。_
