# T78 豆包先行批 · 采集操作协议（**E-1 解锁 ＋ 开窗地点可连 WiFi** 即用，见 §1.B）

> 属主：v2 会话（61bd2401）· 2026-08-29 · 板号 **T78** · 仿 [`T50_VOICE_FIRST_COLLECTION_PROTOCOL_20260804.md`](T50_VOICE_FIRST_COLLECTION_PROTOCOL_20260804.md) 四段形状
> **适用范围**：仅**先行批**（基线 + WiFi/蜂窝自然对照，只卡 E-1）。L3 整形批（批 D/G/A）另需 E-2/E-3，不走本协议。
> **规模与工时**：**65 轮** = 6 功能 × 2 条件 × 每格 N≥5（60）**＋ 同工作负载锚格 5 轮**（§2），≈2 小时 ≈ 0.5 设备窗——推导见 [`DOUBAO_NETPERF_CAMPAIGN_PLAN_20260824.md`](DOUBAO_NETPERF_CAMPAIGN_PLAN_20260824.md) §10。
> **红线**：观察 only（绝不 `performAction`、绝不代启动 App，D-385/D-49）；不解密不 MITM（D-24/D-61）；额度与频次最小化（D-386）。

---

## ⓪ 一页速查（现场就看这一屏；**每条都指回下面的详节，冲突时以详节为准**）

> **📌 小节记法（现场找东西前先看这一行）**：**章标题用圈号 `⓪①②③④⑤`，而正文引用多写作 `§0`–`§5`——两套是同一件事**（`§1` ＝ `①`，`§1.B` ＝ ① 里的小节 B，`①B` 也是它）。
> **所以 Ctrl+F 找目标时搜圈号或小节标题文字，别搜 `§1.B`**——那样只会命中一堆引用、跳不到那一节（全文 `§1.X` 46 处、`①X` 7 处，此前无一处说明两者等价）。
>
> **为什么有这一屏**：本文已 640+ 行、单 §1 就 300+ 行。**操作者是一手拿手机、一手照着做的**，
> 现场读不动全文。本屏**只放动作与顺序**，判据与理由留在详节——**它是索引，不是第二份真相**。

**开窗前（不解锁也能做）**

1. **30 秒冒烟**：跑那条**故意不给 `--allow-real-device`** 的命令，看到 `model=ELS-AN00` 与 `REFUSE`／退出码 3 即证明链路通（§1.E-bis）。**别加真实 DW 号，那会真开始采集。**
2. **板面登记当天新 DW 号**（§1.E）。门只查「这串字符在板面出现过」，**拦不住你用错号**。

**远程陪跑（scrcpy）——把「人到设备旁」变成「人坐到自己电脑前」**

> **前提已实测**：P40 就 **USB 挂在跑 adb 的这台机上**（`USB powered: true`）。所以「到场」不是出门，是**坐到这台电脑前**。
> **红线判定（大脑裁，v2 无异议）**：**人经镜像实时操作豆包，仍是人在驱动**——D-385 禁的是「适配器/自动化**代**驱动」，
> 镜像只是把人的手延长了一段线；**没有任何自动化决定点什么、何时点**。凭据由**人**在镜像里输，会话不碰。
> **EMUI 兼容的一半已实证**：本机 2026-08-29 深夜就是用 `input keyevent` ＋ `input swipe`（scrcpy 注入输入走的同一条
> `InputManager` 路径）把设备解开的——**EMUI 接受注入输入,已证**。

| # | 做什么 | 为什么 |
|---|---|---|
| **R1** | **装 scrcpy**（便携 zip 免装 / `winget install Genymobile.scrcpy`，**装哪种由 PO 定**，会话不代装） | 本机实测 PATH 与常见路径**均无** |
| **R2** | **🔴 绝对不要用 `--turn-screen-off`（`-S`）** | **这是本方案最危险的一个开关**：它让**设备屏黑、而镜像照常显示**。通道 B 的 `screencap` 取的是**设备**帧 ⇒ **全程 `roi_mean=0.0`**，正是 §4 里「屏灭签名」那个失败；**而你在镜像里看画面一切正常，根本不会去查屏**。⇒ **镜像会把故障原因藏起来** |
| **R3** | **开镜像后，当场重测一次单帧 screencap 耗时**（计时跑 `adb exec-out screencap`，三次取中位） | 镜像**持续编码屏幕**，与通道 B 的 `screencap` **是同一条显示捕获路径**。**基线已实测中位 1544ms**（见 §3 通道 B 那条）——**若开镜像后明显更慢，通道 B 的分辨率进一步劣化**，判读时按新值标注 |
| **R4** | **🔴 一票否决项：确认 a11y 事件流未受扰** | **机制上不该受扰**（scrcpy 不注册无障碍服务、走 `SurfaceControl` 抓屏＋`InputManager` 注入），**但本会话无法实证**——需 scrcpy 在跑且豆包在出字。⇒ **做成开窗头 2 分钟的验收动作**：开镜像 → 在镜像里问豆包一句 → 看 `ADAPTER_OBS` 的 `events` 与 `cadence_p50_ms` 是否落在 §1.D 那条带内。**落不进就关掉镜像重测一次**：若关掉就正常，即镜像受扰、**本方案作废**，回「人直接看设备屏」 |
| **R5** | **WiFi 由到场者在镜像里连**（凭据人输） | E-1b 的解法；**会话全程不碰凭据** |
| **R6** | **镜像延迟单列，不并进 `adb_rtt_ms`** | scrcpy USB 典型 <100ms，而**人的反应时间（约 200–300ms）本就更大**。两者都作用在**「看到首字→按 `s`」这一侧**，与 `mark_rtt.jsonl` 记的 **adb 送达那一跳**是**不同的腿**——并进去会让一个**实测量**背上一个**未测量**。⇒ **在 README 里记一次「本窗经 scrcpy 镜像陪跑，镜像延迟未单独标定」即可**；它**单向为正**、且**在条件对照做差时抵消**（两条件同样经镜像），**唯一不抵消的情形是镜像延迟随条件变**——USB 恒定，不太可能 |

> **⚠ 一条证据链上的诚实标注**：镜像注入的输入，在日志里**与脚本注入的无法区分**。
> 故 §5 第 7 条那句「本窗全程由人手动驱动豆包？」**仍然只能由操作者陈述背书**——只是现在现场多了一个**本可以**自动化的工具。**如实写进 README，别让它变成事后说不清的事。**

**自主操作模式（会话代驱动）——⚠ 生效前提：PO 本人直接授权，同伴会话转述不算**

> **授权载体 ＝ `D-587`**（PO 裁定包入册，逐条引 PO 原话「你可以自主操作 P40，我授权」；D-385 的授权面由 PO 本人解除）。
> **开跑触发 ＝ PO 对 WiFi 那问的直接答复**——**这不是我另设的闸，D-587 自己就要求**「WiFi 凭据仍须 PO 亲输…或 PO 告知无可用 WiFi」。
> ⇒ **两道 PO 痕迹**：入册的授权 ＋ 开跑前的亲答。**在 PO 答复到达本会话之前，本段是备好的方案，不执行。**
> （**为什么仍要第二道**：`D-587` 是**大脑会话对 PO 原话的转录**——把断言写进文件不改变它的来源。而 PO 的 WiFi 答复是**直达本会话的一手确认**，两者叠起来才既严谨又不死锁。）
> **本模式与「人经 scrcpy 陪跑」并存**：人在场时优先用陪跑（标记精度高一个量级），本模式是**无人可陪时**的降级。

| 项 | 自主模式下怎么办 | 与陪跑模式的差别 |
|---|---|---|
| **`t`（轮界）** | **精度不降**——发送动作由本会话发出，`t` 就打在发出的那一刻 | 无差别 |
| **`s`／`a`** | **只能从通道 B 的 `screencap_index.jsonl` 实时取**（该文件逐行 `flush`，可 tail）：检测发送后 `roi_mean` 的首次显著跳变 ⇒ `s`；跳变停止 ⇒ `a` | **精度 ＝ 采样周期**：现设 `--screencap-period-ms 3000` ⇒ **±3s**；若回退 1500 ⇒ **±1.5s**。人眼陪跑约 ±0.25s，**差一个量级** |
| **🔴 绝不可从通道 A 取 `s`／`a`** | 用 A 的事件去打 A 的锚点，会让 `e2`／`e4` 变成**拿 A 比 A**——**跨通道印证的意义当场归零**，而且它**不会报错**，只会给出好看的数 | 陪跑模式下人眼本就是独立于 A 的信号源 |
| **核心量** | **不受影响**：TTFT 主承载是通道 A 的 v3 簇分割（D-52 逐字「纯时戳结构法，**不依赖任何锚点事件**」） | 无差别 |
| **红线自证** | §5 第 7 条那行从「**操作者陈述**」改为「**自动化模式声明**」：写明本窗由会话经 `adb`／`scrcpy` 注入驱动、逐格记录 | ⚠ **注入输入在日志里与人手不可分**，故**声明是唯一的记录**——必须显式写，别让它事后说不清 |

> **⚠ 一处必须让 PO 知道的张力**：`--screencap-period-ms=3000`（v3 裁）是为**减负载、护通道 A 主量**；
> 而自主模式的 `s`／`a` 精度**恰恰要短周期**。**两者拉反方向。**
> **✅ 已裁（大脑 2026-08-30）：保 3000、精度如实 ±3s。`D-587` 里的「±1.5s」作废。**
> **冲突缘由已查明并入档**：`D-587` 落笔时**沿用了单帧耗时那个旧数（1544ms→±1.5s）**，**没把同日更早三小时已裁的 `--screencap-period-ms 3000` 的后果算进去**——**即那条 D 号里含一个过期数**。**执行面以本协议为准**；正式订正**已落 `D-588`**（2026-08-30，append-only，`D-587` 旧行保留不改；v2 读册核出冲突、v3 自纠阈值均已入册）。⚠ **`D-587` 那行仍躺在册里可被引用——引它的 ±1.5s 一律以 `D-588` 为准。**
> **裁定理由**：`s`／`a` 在本批本就是**旁证**，主量走 A；
> **若判读侧后来要用 `s`／`a` 做定量结论，须先回来改这个参数并重跑，不能事后补**。

**解锁后，按这个顺序（前三件任一失败就不开跑）**

| 序 | 做什么 | 判据 | 详节 |
|---|---|---|---|
| **0** | **确认 WiFi 连得上** | `ip route get 1.1.1.1` 要走 `wlan*`，不是 `rmnet*`。**2026-08-30 实跑验证该判据正确**（返回 `dev wlan0 table 1040 uid 2000`）——**有人据「per-UID 路由表」推断它会假阴，实测未发生，判据不改**。⚠ 但那层顾虑有理:该命令报的是 **shell（uid 2000）** 的路由;**若要对 App 的走向下断言，交叉核 `dumpsys connectivity` 的 `Active default network` 与 `Transports:`** | §1.B |
| **1** | **a11y 重绑** | 花 10 秒买确定性（`imePkg` 只在绑定时取一次） | §1.B |
| **2** | **UI 栈判定**（**本批最大风险**） | 开豆包问一句，读 `ADAPTER_OBS` 的 `cadence_p50_ms`：落 **99–112ms** 带＝仍 View 系照跑；**小于 5ms ＝已迁 Compose，停批不硬修** | §1.D |
| **3** | **量 ROI ＋ 两帧自检** | 静止帧与流式帧的灰度均值**必须明显不同**；**两个都是 `0.0` 先查屏亮着没有、USB 断没断**，不是 ROI 错 | §1.G |
| **4** | 额度试水 3 轮 | 无限流提示再进正式批 | §1.F |

**每一格的循环（12 格 ＋ 末尾锚格；一格一次会话，跑完该格 5 轮）**

1. 记该格开跑前的 `gsm.network.type`（§②-A G-5）；
2. **🔴 先把豆包切到前台并让它出一屏画面，再起采集命令**——**顺序反了整格通道 C 就死**：采集器启动时用 `dumpsys SurfaceFlinger --list` 找该包图层，**而图层只对「正在出画面的前台 App」存在**；找不到即记 `sf_status=NOT_EXECUTED`、`sf_latency.txt` 根本不生成。（2026-08-29 实测:豆包在后台时 `pick_layer` 返回 **`None`**、桌面在前台时返回其图层名；**归档的 4 次真机跑全部 `layer=None` ＋ `sf_latency` 缺失，与此完全一致**。⚠ 用 `grep` 数 `larus` 会看到一条 `ActivityRecord{…}`——**那正是代码 `LAYER_NOISE` 要滤掉的**，别拿它当「图层找到了」。）
   **2a. 起采集后数秒内判通道 C 死活——⚠ 判据是「剩几帧」，不是「文件在不在」**（本条 2026-08-30 按 v3 实证订正，我原写的「看文件出现没有」挡不住最坏的一种）：
   　**① 文件没出现 ⇒ 必死**（层没找到）；**② 文件出现了也不算活**——历史上 `20260802-172614` 那份**文件在、非空、5 行全是 `11111111`（刷新周期头）、一帧数据都没有**。
   　**⇒ 用消费方自己的解析器判**（2026-08-30 在 PowerShell 与 Git Bash **两个壳各按原样跑过**，阴性样本回 `frames=0`、阳性对照回 `frames=93`，**两个方向都有判别力**）：
```
python -c "import sys,io;sys.path.insert(0,'tools/e1');import e1_analyze as ea;p,f=ea.parse_sf_latency(io.open(sys.argv[1],encoding='utf-8',errors='replace').read());print('frames=%d period=%s'%(len(f),p))" <该格目录>/sf_latency.txt
```
   　**`frames=0` ＝ 通道 C 已死，立刻停掉重来**（确认豆包在前台且已出画面再起），**别让它带着死掉的 C 跑满 5 轮**。
   　**🔴 并且:顺序修对了也可能零帧**——`172614` 那次**图层是找到了的**。所以**第 2 步是必要条件、不是充分条件**，2a 必须真判帧数。
   　**⚠ 绝对不要用行数判**：这些文件由 adb 在 Windows 上落盘，含**双重换行 `

`**（`173031` 有 **387** 个、`e1band` 有 **1935** 个），**`wc -l` 与 Python 行数能差近一倍，而两者都不等于帧数**——实测 `wc -l=10 / py=16 / 帧=0`、`390 / 778 / 93`、`1950 / 3886 / 1178`，**没有任何一种行数近似帧数**。（解析器 `splitlines()+strip()` 会滤掉伪空行，**分析路径没 bug**，坏的只是「拿行数说事」这个判据。）
   　**历史基线（9 次真机跑，2026-08-30 全量实测）**：**7 次通道 C 零可用帧**（4 次连文件都没有 ＋ 3 次有文件但 0 帧），**只有 2 次有帧**（93 / 1178）。**这不是小概率分支，是常态——所以这条检查必须做。**📌 **为什么必须当场看**：`collect_notes.json` 的 `sf_status` 是**收工才写**的，**等你看到它时这一格已经跑完了**；而 `sf_latency.txt` 一起采集就该开始追加。
   **2b. 为什么这一格不能凑合**：通道 B 的分辨率是 **≈1.5s**（见 §3），**答不了 2 秒量级的 TTFT**；**丢了 C 就只剩 A 的自报值、没有第二个通道印证它**。**⚠ 这里要分两层，别混**（大脑 08-30 指出，v2 核后采纳）：**① TTFT 本身不会消失**——它的主承载是**通道 A 的 v3 簇分割**（D-52 逐字「纯时戳结构法，不依赖任何锚点事件」），丢了 C **丢的是印证，不是测量**，结论仍可出、但必须带「**无跨通道印证**」的限定；**② 但 `e2_analyze` 的命题会消失**——它的定义本身就是 **A↔C 同轨对拍**，少一边就不是精度下降而是没有东西可算。（措辞由 v3 判读链持有，此处只定采集侧口径。）
3. **每轮**：按 `t` → 提问（**该功能的固定提示词，逐字相同**）→ 看到首字按 `s` → 回答完按 `a`；**每轮开新对话**（§②-A G-2／G-3）；
4. 5 轮跑完按 `q`；**立刻数两个文件**（§4）——`mark_rtt.jsonl` 里 `t` 的条数、**以及** `adapter.log` 里 `E4MARK kind=turn_start` 的行数（**判读器只读后者**）；**两个都要是 5 且相等**，不等＝logcat 吞了行；**若要重跑，先把该格目录改名 `<格名>_VOID<k>/` 再跑**（§④-A′——**就地重跑这条判据永不收敛**）；
5. 记该格跑完后的 `gsm.network.type`（前后两读用来分「整格／格内漂移」）。

**跨格的两件（都在格与格之间发生，所以最容易漏）**

- **格的顺序照 §2 的「四块交替」走，别按条件分组。** 📌 **先跑完所有 WiFi 再跑蜂窝是最省事的做法，也正是这批设计要避开的那个坑**——那样「条件差」与「时段差」完全共线，**采到的数一个结论都得不出**（D-393 形状）。四块交替只切 **2 次**。
- **每切一次网络，当场把时间戳写下来**（§②-A G-4）。📌 **没有任何东西会自动记它**——`adapter.log` 记的是格内的事，切换发生在格与格之间；**当时不写，事后补不回来**，而 §2 的漂移自检与锚格判读都要拿它对齐。

**三条当场作废的红线**

- **`t` 没按**：轮次边界**事后不可恢复**，该格作废重跑。
- **提示词不逐字相同／中途沿用同一对话**：该格作废；**锚格若犯，整批漂移判据作废**。
- **通道 D 抓到非目标包流量或产物含载荷**：立即停该格 D、**只删该次抓包产物**（不得进 `evidence/`），重配再跑（§1.H）。**⚠ 这条不作废整格——同格 A/B 已采的部分保留**（别顺手删目录，那是不可逆的真数据）。

**收窗**（**照 §5 逐条走完，别照这行省略**——这行是索引，§5 才是清单）：停本次起的一切（含 PCAPdroid／a11y 观察）→ 只恢复**本次改过**的（`deviceidle`／`stayon` **本批都不动**——📌 **前提是开窗时它们已经是对的**：`stayon` 应为 `7`、探针应在 `deviceidle` 白名单，**§1 会让你各花 5 秒复读一次**。**若 `stayon` 不是 `7` 就照这句「不动」，屏幕会在 10 分钟休眠，而一格会话 15 分钟**——那正是 §4 里「`roi_mean` 全程恒 `0.0`」那个失败）→ 回华为桌面复验干净 → 产物落 `evidence/doubao_wave0_<YYYYMMDD>/` 并按 **README 必填清单**（§5 那张表，**逐行照写**）写 README → 板面更新＋窗后待办 → **红线收窗自证**（📌 **当场做，事后补不回**）→ **交接给判读侧**（📌 **点名谁读，不是广播**）。

---

## ① 前提核查清单（任一不满足则不开窗——避免开窗即空跑，D-481 先例）

### A. E-1 设备可达且已解锁（本批唯一硬阻塞）
```
adb devices
adb shell dumpsys window | findstr mDreamingLockscreen
```
> **（这两条 2026-08-29 已在本机按原样实跑：`findstr` 形态在 PowerShell 下可用，`EXIT=0` 并返回 `mDreamingLockscreen=…` 那一行。⚠ 若你在 Git Bash 里跑，`findstr` 不存在，换 `grep`。）**

- `adb devices` 为空 = 设备离线（USB 未连或关机），**比屏锁更前一步**；
- `mDreamingLockscreen=true` = 真锁（PIN/图案），**swipe 不解**，须在场者物理解锁（D-481 同款）。

> **🔴 实况（2026-08-29 本会话只读亲验，未唤醒未改状态）**：
> `adb devices` → `8MY0221126002537 device`（**已在线**）、`ro.product.model` → `ELS-AN00`（确认 P40）；
> `mAwake=false / mScreenOnFully=false / mDreamingLockscreen=true`（**屏灭且锁着**）；
> `mCurrentFocus=NotificationShade` + `mFocusedApp=华为桌面`。
> **⇒ E-1 已从两阶阻塞（离线＋锁屏）降为一阶（仅锁屏）**——一页清 #4 记的「`adb devices` 为空、
> 完全离线」**已过期**，**PO 的到场动作从「连 USB ＋ 解锁」减为「解锁」一件**。
> **`NotificationShade` 不是通知栏被拉下**：按 **D-393** 教训它是**屏灭锁屏的表征**，
> 而 `mFocusedApp=华为桌面` 证明**除锁屏外设备是干净的**（无残留应用），符合开测前提。

### B. 观察通道三件（A/B/D）各自可用
| 通道 | 核查动作 | 不满足的后果 |
|---|---|---|
| **A** 无障碍事件流 | `AnebAccessibilityService` 已启用且**已重绑**；**事件流单会话不碎段**；**豆包 14.7.0 仍是 View 系**（判据见 §1.D） | 主通道（TTFT/cadence/RCT）无数据 |
| **B** `screencap` ROI 帧差 | 现场量出豆包响应区 ROI（`x,y,w,h`）——`e234_collect.py --roi` **必填无默认值**；**量法与两帧自检见 §1.G**（框在静态区不报错，是无声失败） | 交叉验证缺失；⚠ B 采样 ~2–3.5s/帧（E1 实测），**只作粗验、不能判 1 帧级** |
| **D** PCAPdroid 免解密抓包 | 已授 VPN、只读方向字节 | F3/F4 上行诉求**失去证据源**（这是本批最强的一块） |

> **🔵 锁屏下已预验的三条（2026-08-29 只读，开窗时不必重做，直接省窗内时间）**：
> | 前置 | 实测 | 结论 |
> |---|---|---|
> | a11y 服务已启用 | `enabled_accessibility_services` = `com.aneb.probe/…AnebAccessibilityService`；`accessibility_enabled` = `1` | ✅ **已启用**（⚠「**已重绑**」仍须解锁后确认——重绑是解锁态动作） |
> | 豆包版本 | `dumpsys package com.larus.nova` → `versionName=14.7.0` | ✅ **14.7.0 在位**（与方案记载一致；⚠ 版本漂的**真实风险不在正则**——本文 v1.1 订正，见 §1.D） |
> | deviceidle 白名单 | `dumpsys deviceidle whitelist` → `user,com.aneb.probe,10306` | ✅ **探针在白名单**（豆包是被观察方，不需入白名单） |
>
> **🔵 同批只读验掉的另四项（开窗资源前提，2026-08-29）**：
> | 项 | 实测 | 结论 |
> |---|---|---|
> | **通道 D 的 PCAPdroid** | `pm list packages` → `com.emanuelef.remote_capture` | ✅ **已装**——F3/F4 上行诉求的**唯一证据源**在位 |
> | 电量 / 温度 | `level=81`、`temperature=250`（25.0℃）、`status=4` | ✅ 跑 ≈2 小时够用 |
> | 存储 | `/data` 可用 **208 GB**（14% 使用） | ✅ 65 轮产物 + 抓包绰绰有余 |
> | 当前 IME | `default_input_method` = `com.baidu.input_huawei/.ImeService` | ✅ **豁免逻辑认得它**（见下） |
> | **屏幕超时** | `settings get system screen_off_timeout` → **600000**（10 分钟） | ⚠ **短于 `--session-seconds 900`（15 分钟）**，见下方风险 |
> | **充电常亮** | `settings get global stay_on_while_plugged_in` → **7**（AC\|USB\|无线 全开） | ✅ **USB 连着即充电，屏不会息**——但这是**既有状态**，不是本批设的 |
> | **SIM** | `getprop gsm.sim.state` → **`LOADED,ABSENT`** | ✅ **卡槽 1 有卡且已加载**（卡槽 2 空，与待人清单 #3 记载一致）⇒ **蜂窝两块不会是死条件** |
> | **运营商** | `gsm.operator.alpha`／`.numeric` → **中国电信 / 46011** | ✅ 单运营商 **ctcc**，与既有语料标注同源；**双运营商维度本批不做**（清单 #3 未解，如实标注） |
> | **当前制式** | `getprop gsm.network.type` → **`NR_SA,Unknown`**（08-29 按原样实跑的完整回值；**逗号分隔＝按 SIM 卡槽**，卡 1 为 **NR_SA**＝5G 独立组网、卡 2 无服务。**记录时逐字照抄整串、别只写 `NR_SA`**——截断会丢掉「是哪个卡槽在承载」，而那正是换卡/双卡切换时唯一能分辨的线索） | ⚠ **蜂窝条件今天是 NR 不是 LTE**——见 §②-A G-5「制式漂移」 |
> | **起始网络原值** | `wifi_on` → **1**；`mobile_data` → **1**；**但默认路由 `dev rmnet0`、活动网络 `MOBILE[NR] CONNECTED`**；WiFi **射频开着却未关联**——最后一次关联于 **08-27 00:11 被 AP 断开**（`locallyGenerated: false`），**已断两天** | 🔴 **见下「块 1 跑不跑得起来」——本条 2026-08-29 订正了我自己写错的一句** |
> | **通道 D 的 PCAPdroid 版本** | `versionName=1.9.1`（`versionCode=91`，2026-07-18 装、**从未更新**） | ✅ 版本已知（**界面细节仍不预设**，见 §1.H） |
> | **残留 VPN／隧道** | `dumpsys connectivity` → `VpnNetworkProvider:0`、活动网络标 `NOT_VPN`；`ip link` 的 `rmnet_tun*` 全 `DOWN`（基带隧道非 VPN） | ✅ **无残留 VPN/隧道**——根 `CLAUDE.md` 开测前提的这一条**已只读满足** |
> | **a11y 实际绑定态** | `dumpsys accessibility` → `Bound services:{Service[label=ANEB Profile 3 观察打点（只读）…]}`，四类事件（`VIEW_CLICKED`／`VIEW_TEXT_CHANGED`／`WINDOW_STATE_CHANGED`／`WINDOW_CONTENT_CHANGED`）与设计一致 | ✅ **不只是「已启用」，是此刻真的绑着**（此前只验过 settings 开关） |
> | **探针进程** | `pidof com.aneb.probe` → `22981`；`ETIME` → **`3-18:10:30`**（连续运行 3 天 18 小时，起于约 08-25 11:45） | ✅ **服务稳定、无崩溃重绑循环**——通道 A 健康的直接证据 |
> | **已装构建** | `versionName=0.1.0-phase0`、`versionCode=1`、`lastUpdateTime=2026-08-20 11:17` | ⚠ **9 天前的构建**——见 §1.D「源码↔二进制」 |
>
> **🔴 块 1 就是 WiFi，而设备此刻不在 WiFi 上（2026-08-29 实测；本条推翻我自己前一版写的「两者都开＝WiFi 条件的起始态」）**
> `wifi_on=1` **只说明射频开着，不等于已连上 AP**。实测默认路由走 `rmnet0`（蜂窝），
> WiFi 自 **08-27 00:11** 起未关联。设备上**存有多条已保存网络**（开放与 WPA2/WPA3 皆有，
> **具体 SSID 不写进本仓——那是用户的网络名与位置信息**），但**能否关联取决于开窗时人在哪、附近有没有其中之一**。
> **开窗第 0 件事：先确认 WiFi 能连上**（连上后 `ip route get 1.1.1.1` 应走 `wlan*` 而非 `rmnet*`）。
>
> > **⚠ 这条命令要连 `get 1.1.1.1` 一起敲，别简化成裸 `ip route`。**
> > **裸 `ip route` 在 Android 上恒返回 `RC=0` 且输出为空**——路由活在 **per-UID 表**里（正确那条的输出里就写着 `table 1008 … uid 2000`），主表本来就是空的。
> > 于是 **`ip route | grep wlan` 无论设备在不在 WiFi 上都是零命中**，而 §4 的中止判据写着「不走 `wlan*` 就不开跑」——**量法失明长得和「WiFi 没连上」一模一样，会把整个窗白白中止掉**。
> > （2026-08-29 v2 实测：`ip route get 1.1.1.1` 正常返回 `dev rmnet0`；同一时刻裸 `ip route` 空。**我自己就是拿裸的那条差点误报「设备掉网」的。**）
> **连不上则 §2 的块 1／块 4 与锚格全部无法执行**——那不是「少一个条件」，是**整批设计塌掉一半**
> （对照没有了、锚格也没有了）。此时**不要临时改设计**，按 §4 记为前置未满足、不产出数据。
> **⑤ 的「切回原值」也随之订正**：真原值是「**WiFi 射频开、未关联、流量走蜂窝**」——
> 收窗时应**断开本次加入的 AP 并保持射频开**，**不是**「把 WiFi 开回来」（我前一版就是这么写的，错）。
>
> **⚠ 由这两项量出一个此前没人写的静默失败（2026-08-29 实测得出）**：
> 屏幕超时 **10 分钟 < 会话上限 15 分钟**。今天安全**只因为 `stay_on_while_plugged_in=7` 且 USB 连着**——
> **USB 一断、或改用无线 adb、或某格确实跑满 15 分钟而中途拔线，第 10 分钟屏幕就息**，
> 而**息屏后 `screencap` 照抓、照落盘、`roi_mean` 恒 0.0**（本会话锁屏下实测：全屏与中段均值都是 `0.0`，
> **是个合法数字不是 `None`**）——**它长得和「ROI 框在静态区」一模一样**，
> 于是操作者会按 §1.G 的自检判「ROI 作废重量」，**方向完全错**。
> **⇒ 开窗前把这两项当前置核一遍**（本批不必改它们，现值已够；只需确认 **USB 全程连着**）。
>
> **⚠ 由 IME 这项牵出「a11y 必须重绑」的真正理由（不是形式要求）**：
> `AnebAccessibilityService` 的豁免取 `default_input_method` 再 `substringBefore('/')`
> → `com.baidu.input_huawei`，与实测一致，**豁免会生效**。**但 `imePkg` 只在
> `onServiceConnected()` 读一次**：若 IME 在服务绑定之后被换过，豁免就指向旧值、
> **D-51 的会话碎段复现**（D-51 实证：百度输入法的 STATE 与候选栏 CONTENT/TEXT 事件把
> 观察切成 8/1/8 三段；修复＝IME 包全事件豁免，实证 DeepSeek 单会话 `events=108`）。
> **故 ①B 的「已重绑」不是走过场——它是让 IME 豁免重新取值的唯一途径。**
>
> **⚠ 本文 v1.1 订正（我引错了症状）**：v1.0 此处写「碎段导致 `ttft_send_ms` 恒 null」。
> 查 D-51/D-52 后更正——`ttft_send_ms` 在豆包上**与 IME 无关地恒 null**（v1 input-clear
> 因发送后容器重建不发 TEXT_CHANGED 而失效；v2 点击锚点因**豆包自定义 View 零 CLICKED 事件**
> 被 D-52 真机证伪）。**碎段今天真正污染的是 `ttft_cluster_ms` 与 `cadence`**——即本批的核心量，
> 因为 v3 簇分割靠「>400ms 静默」切簇，**碎段会把一次问答切成多个会话、簇结构失真**。
> 结论方向不变（重绑仍是硬前提），但**代价比 v1.0 说的更大，不是更小**。

> **仍须解锁后才能做的四条**：①a11y **重绑**（**已降级**：实测此刻**已绑定**且连续运行 3d18h，
> 故这不再是「未知态待处理」；但**绑定时刻与 IME 最后一次变更的先后关系只读查不出来**，
> 而 `imePkg` 只在 `onServiceConnected()` 取一次——**重绑是花 10 秒买一个确定性**，仍建议做）；②**UI 栈判定**（豆包 14.7.0 是否仍 View 系——
> **本批最大风险项**，判据与做法见 §1.D）；③IME/systemui 豁免核实；④**ROI 现场量**
> （`--roi` 必填无默认值；**量法、设备几何与两帧自检已备齐，见 §1.G**——窗内约 2 分钟，不必现场摸索）。
> **开窗第一件做 ②**：它若失败，后续 65 轮的核心量全部失真，且修复时间挪不出窗外。

### C. OEM 系统侧五条前置（`INSTRUMENTATION_SPEC` §5.3；任一不满足则数据不得入库）
deviceidle 白名单已加 · IME/systemui 已豁免 · a11y 服务已重绑 · 诊断日志走 `Log.i` · **UI 栈判定为 View 系且事件流不碎段**（§1.D）。
> 这五条的共同形状是「**失败时静默出错值或静默无数据而不报错**」（D-386）——所以必须**开窗前逐条核**，不能事后看数据像不像。

### D. 版本漂风险订正（v1.1，2026-08-29；**推翻本文 v1.0 的风险排序**）

**v1.0 写的**：「节点正则是对 14.4.0 写的、已漂 3 版，重验是最大风险项」。**查代码与决策日志后，这个定性是错的。**

**实据三条（可逐条复核）**：

| # | 事实 | 出处 |
|---|---|---|
| 1 | **运行时只编译 `class_name` 正则**，`view_id_regex` 存而不评估（取 `getSource` 跨进程开销，R-16／D-49 偏离 2） | `AnebAccessibilityService.kt` 里搜 `view_id_regex` 那条注释，以及三处 `toRegexSafe(` 全是 `classNameRegex`）；`AdapterSpec.kt` 里搜 `viewIdRegex` 的声明处 |
| 2 | 被评估的两条都是**框架类名**，不含 App 版本特征：input＝`android\.widget\.EditText`，response＝`android\.widget\.TextView\|androidx\.recyclerview\.widget\.RecyclerView`。**唯二含 `com.larus.nova:id/…` 的是 view_id，恰好不评估** | `spec/adapters/doubao.json` |
| 3 | **本批核心量不依赖任何正则**：`ttft_ui_ms` 来自 **v3 簇分割**，D-52 逐字「**纯时戳结构法，不依赖任何锚点事件**」；response 侧 `ruleMatch()` 只做**标注计数、非闸门**（漂了掉 `rule_matched` 计数，不丢数据） | D-52；`AnebAccessibilityService.kt` 里搜 `ruleMatch`（文件头注释＋其实现段） |

> **⚠ 先答一个此前没人问的问题：上面三条实据是我读源码得出的，而设备上跑的是 `2026-08-20 11:17` 装的二进制——它们是同一份吗？**（2026-08-29 核过，答案是**是**）
>
> | 查什么 | 结果 |
> |---|---|
> | `adapter/` 自 08-20 11:17 起的提交数 | **0** |
> | `AnebAccessibilityService.kt` 最后改动 | 2026-08-03（`224326c`） |
> | `ObsStats.kt` 最后改动 | 2026-07-19（`9122ee2`） |
> | `AdapterSpec.kt` 最后改动 | 2026-08-02（`c4170b8`） |
>
> **同一问题往下还有一层：设备加载的不是 `spec/adapters/`，而是打进 APK 的 `assets/spec_adapters/`。**
> 查：两处自 08-20 11:17 起**均 0 提交**；`doubao.json` 的 spec 与 assets 最后改动**同为 2026-08-02 的同一个提交**（`3519b36`，与字节一致守卫相符）
> ⇒ **§1.D 实据 2 引的那份规格，就是设备 APK 里烘着的那份**。
>
> **三者全部早于安装日 ⇒ 上面的实据链对设备上那个二进制成立**（代码／规格／assets 镜像三层全通；诚实的否定：风险查过、已排除）。
>
> **🔴 但由此得出一条反直觉的硬要求：开窗前不要重装探针。**
> `app/probe/` 整体自 08-20 起**已有 37 次提交**（语音摘要、THERMAL 接线、Room 迁移口径等）。
> 重装＝把一个**未经本协议验证**的构建放上设备，**§1.D 的全部核查当场作废**，
> 而它正是本批「开窗第一件事」的判据来源。**要装也得等本批采完。**

**故正则漂移的真实后果**＝`rule_matched` 计数下降，**不是数据缺失**。

> **⚠ 2026-08-29 再订正（由 v3 的追问揪出）——本文原写「判读时按 D-50 基线 `events=28 rule_matched=26`（≈93%）对照即可」，那是错的。**
> **那个基线不能当尺子**，三条理由逐条有据：
> ①**版本未知**：spec 写死基线测于 **2026-07-18**，而 `evidence/adapter_versions_20260802/README.md` 逐列记 doubao `firstInstallTime=2026-07-17 23:49:18`、`lastUpdateTime=2026-07-30 09:42:59`、「相对 07-18：更新晚 12 天」⇒ **07-18 测的是 07-17 装上的构建，14.4.0 是 07-30 之后才有的**。即它测于一个**从未被记录的 pre-14.4.0 构建**（D-405 拒绝回填 `validated_against_version` 正为此）。
> ②**n=1**：那是一次单轮问答的一条 `ADAPTER_OBS`。
> ③**语义是会话内累计**（`ObsStats.kt` 里搜 `eventCount` 与 `ruleMatchedCount`——**两者只在声明处与 `++` 处出现，全文没有任何置零，即全程不重置**），与本批「每格一次会话跑 5 轮」的合计口径不同层。
>
> **改用批内基线**：**块 1 第一格（F1/WiFi）就是 14.7.0 上的 `rule_matched` 实测**，直接作本批基线；末尾锚格同格重跑，还能给出批内稳定性。
> **D-50 的 26/28 降格为历史参考**，引用时必须带「未知 pre-14.4.0 构建 · n=1 · 会话内累计」三项限定，**不作任何判据**。

**真正的版本漂风险是另一件事——UI 栈迁移**：豆包若从 View 系迁到 Compose，按 **D-51 实证**（DeepSeek＝Compose）事件会**同帧突发合流**，`cadence_p50` 从豆包 View 系的 ~100ms **塌到 0.2–0.4ms**，而 v3 簇分割的 >400ms 静默判据建立在逐帧节奏之上——**簇结构随之失真，核心量作废**。D-51 同时记着「两栈打点口径**不可互比**」，故这不是精度问题而是**口径问题**。

**开窗第一件事的判据（一轮问答即可判，无需跑满）**：手动开豆包问一句，读 `ADAPTER_OBS` 的 `cadence_p50_ms`。

| 读到 | 判定 | 动作 |
|---|---|---|
| 落 **99–112ms** 带（D-51/D-52 累计 8 轮 99.4–111.6） | ✅ 仍 View 系 | 照 §2 开跑。**🔴 2026-08-30 实跑要点:必须读「豆包切前台后的第一条 `ADAPTER_OBS`」**——`cadence_p50_ms` 是**会话累计中位数**（`ObsStats` 的 `eventCount`／`ruleMatchedCount` 全程不重置，T83 已验），**流式一来就把它稀释**:当轮实测同一会话内 **99.4 →（+39 个流式事件后）23.8**，**5 秒之隔、同一台设备、同一个 UI 栈**。⇒ **晚读一条就会掉进「介于两者之间⚠不明」，而那是判据本身被稀释、不是 UI 栈变了。** （本窗实测首条 ＝ **99.4**，与 D-51/D-52 下界逐字吻合 ⇒ **判定仍 View 系**。） |
| **< 5ms** | ❌ 已迁 Compose | **停本批**。核心量口径变更，须先重定簇判据——**这是窗内不可压缩项，不要硬跑** |
| 介于两者之间 | ⚠ 不明 | 记录原值、按 §4 中止判据处理，不自行解释 |
| **读不到任何 `ADAPTER_OBS`**（本行 2026-08-29 补——上面三行全以「读到一个数」为键，**这一种此前没有归宿**） | ⚠ **成因至少三个，动作完全不同，别默认往「已迁 Compose」上归** | **按这个顺序排除**：①**屏幕上豆包到底出没出回答**？没出＝根本不是 UI 栈问题，**先看限流/网络**（⚠ **额度试水排在第 4 步，此刻你还没做**，所以「触顶」在这一步是完全可能的且没人告诉你）；②出了回答却无 `ADAPTER_OBS`＝**a11y 没绑上**，回第 1 步重绑，并跑一次 §1.E-bis 的 `E4MARK` 往返证明 logcat 链路活着（**否则你分不清「没事件」与「没在听」**）；③回答正常、a11y 已绑、`E4MARK` 通、仍无 `OBS`——**这才是需要停批研究的那一种** |

**⚠ 别踩的坑之二：`doubao.json` 的 `package_note` 有一句与文件自身矛盾**。它（D-405，2026-08-02 过渡标注）写着「**三条节点规则本就全 PENDING-VALIDATION（正则全 null、宿主走 generic 兜底、规则不作打点闸门）**」。**四份适配器并排数过，这句对豆包是假的**：

| 适配器 | `input_node` / `response_node` / `send_button` 的 status | 「正则全 null」 |
|---|---|---|
| kimi、tongyi | 三段全 `PENDING-VALIDATION` | ✅ **真** |
| **doubao** | `VALIDATED` / `VALIDATED-PARTIAL` / `PENDING-VALIDATION` | ❌ **假**——前两段各带 `view_id`+`class_name` 两条 |
| deepseek | `VALIDATED-PARTIAL` / `PENDING` / `PENDING` | ❌ 部分假（两段各带 `class_name`） |

那句对 kimi/tongyi 成立，却被写进了**唯二有已验证正则**的两份。**结论「只标注不停用」仍对，但理由是 §1.D 实据 1–3（只评估框架类名 + 核心量不经正则），不是「正则全 null」**——按 D-405 自己的话，「**一条经不起查的理由比没有理由更糟**」。开窗时**别照那句去推断适配器跑在 generic 兜底**：D-50 基线 `ADAPTER_OBS mode=doubao events=28 rule_matched=26` 证明它跑在规格模式且规则在匹配。

**⚠ 别踩的坑之一（会白烧窗内时间）**：`spec/adapters/doubao.json` 的 `send_button.note` 写着「发送按钮特征**待**真机 `ADAPTER_EVT` 诊断日志反推回填」。**这条诊断 D-52 已经跑过并证伪**——豆包自定义 View **零 `TYPE_VIEW_CLICKED` 事件**，点击锚点路线对豆包不可用（四正则留 null 是**有意保留能力**给标准控件 App，不是待办）。**不要照那句注记再跑一遍。**（spec 注记本身是双侧严格解析文件，按 D-387 不在本批顺手改，已报大脑。）

### E. e234 装置的设备红线（会挡住你，别现场才发现）
`e234_collect.py` 对 P40（`ELS-*` denylist）**必须**给 `--device-window <ID>`，且该 ID 须能在 `docs/BRAIN_TASKBOARD.md` 查到。
→ **开窗前先在板面登记 DW 号**（`DW-YYYYMMDD-NN`），否则脚本拒跑。
> **这道门实际验的是什么（2026-08-29 实证六案，非文档转述）**：`device_gate()` 用 **纯子串匹配**（`device_window not in taskboard_text`，读整个板面文件）——**验的是「这串字符在板面出现过」，不是「这个窗是给这次采集的、且现在有效」**。实测：已撤销的窗号（板面写着"该窗已撤销"）、别人任务的窗号、以及被另一个号包含的前缀（板上 `DW-…-011` 而你给 `DW-…-01`）**三种都放行**。
> **大脑 08-29 裁定（三种分开处置）**：**「前缀包含」修**——纯技术漏洞，改词边界匹配一行修、零措辞依赖、不引新假拒面，施工并入 v3 在飞的那笔；**「撤销窗 / 别人窗」不修**——甄别只能靠板面措辞，那是新的假拒来源，而**授权强度本来自「人要先去板上登记」这个动作与事后可查，不来自字符串校验的严密度**。故下面这条对操作者的意义**长期有效**。
> **对操作者的意义**：门拦得住「凭记忆瞎写一个号」，但**拦不住你用错号**——所以登记时用**当天当次的新号**，别复用旧号，也别指望门帮你发现拿错了窗（大脑 08-29 裁 7-5=案 A 时已知悉此实况）。

### E-bis. 开窗前 30 秒冒烟：一条**必拒**命令验通整条链（2026-08-29 实跑得出，非推理）

```
python tools/e234/e234_collect.py --serial <SN> --pkg com.larus.nova --roi 100,800,1000,900
```

**故意不给 `--allow-real-device`**，所以它**必然被门拒**（P40 在 denylist），**永远不会开始采集**。
但它跑到被拒那一步时，已经顺带证明了五件事：

| 它证明了 | 怎么看出来 |
|---|---|
| ① python 能跑、脚本能 import | 没有 traceback |
| ② `--roi` 解析通过 | 没有「`--roi` 有误」那一行 |
| ③ **adb 通、设备可达** | 走到了读型号那一步 |
| ④ **型号读得到且识别正确** | 输出行里 `model=ELS-AN00` |
| ⑤ **设备门在工作** | 输出 `-> REFUSE (…)`，**退出码 3** |

> **为什么值得单列**：这五件里任何一件坏掉，都会在**你已经解锁设备、开好豆包、准备开跑**的那一刻才暴露，
> 而那时窗内时间已经在烧。**这条命令零采集、零额度、锁屏下也能跑**（只做一次只读 `getprop`）。
> **⚠ 唯一要小心的**：不要往这条命令里加 `--allow-real-device` 加一个**板面上真实存在**的 DW 号——
> 那样门会放行，**它就真的开始采集了**。冒烟就是要它被拒。

**① 采集工具自身的单元测试（2026-08-29 已跑，开窗时不必重做）**

`tools/e234/tests/run_tests.py` → **88/88 passed（RC=0）**；`tools/e1/tests/run_tests.py` → **82/82 passed（RC=0）**。

**而「全绿」本身证明不了什么——所以同批跑了突变审计**：`tools/e234/tests/mutation_audit.py` → **12/12 CAUGHT（RC=0，基线 170 条＝88＋82，还原后复跑 0 失败）**。被咬住的包括**写盘前的隔离断言**（M2，4 条承重）、**dry-run 语料不得产出标定值**（M9）、**未出数的轮次不得静默改分母**（M10，单点）。**这才是「窗内工具可信」的那句话的依据。**

> 这只审计是**运行时包装（`setattr`/`getattr`）、一个字节都不落盘**，所以 D-321 那条「突变会污染工作区」的告诫**对它不适用**——那条讲的是文件级突变。跑完已用 `git status` 独立核过：`tools/` 与 `app/` 零改动。

> **⚠ 这条绿与「探针那条」不可互相顶替——两半的验证方式不同**：
> **PC 侧工具（`tools/e234`、`tools/e1`）跑的就是 HEAD**，所以**自测绿＝窗内跑的那份是绿的**；
> 而**设备侧探针是 2026-08-20 的构建**，HEAD 上的绿**说明不了它**——那半只能靠 §1.D 的**源码↔二进制核查**（`adapter/` 自安装日起零提交）。
> **「工具都测过了」这句话，对这两半的含义完全不同。**

**🔴 ①-bis 读 logcat 必须用「先开流、再动作」，不能「动作完再 dump」（2026-08-30 实跑教训）**：本机 `logcat -g` 实测 **main 环形缓冲仅 256 KiB、已用 250 KiB**——**几乎满**。当轮我按「打日志 → 等 40 秒 → `adb logcat -d`」取数，**连自己刚写的 `E4MARK` 都读不回来**，一度据此以为通道 A 全死；改成**先起 `adb logcat -v time -s AnebProbe:I E4MARK:I` 流、再动作**，同一分钟内拿到 **`ADAPTER_EVT` 65 行、`ADAPTER_OBS` 2 行**。⇒ **40 秒就足以让一行被挤掉。采集器本身用的正是流式 `popen`，不受影响；受影响的是「事后 dump」这种排查手法。****排查通道 A 时别用 `-d`。**

**② 标记链路往返（2026-08-29 已实测通过，开窗时不必重做）**

②的 🔴 段把「按 `t`/`s`/`a`/`q`」立成了硬要求——但**那些按键是靠 `adb shell log -p i -t E4MARK` 写进 logcat 的**，
而 **D-52 逐字记着「EMUI 默认丢弃 `Log.d`」**，`ADAPTER_EVT` 当年因此恒不可见。
**所以先得问：`log -p i` 在这台 EMUI 上活得下来吗？** 已实测：

```
adb shell log -p i -t E4MARK "E4MARK kind=turn_start n=999 SELFTEST_20260829"
adb logcat -d -v time -s "E4MARK:I"
```

读回 `08-29 06:01:39.458 I/E4MARK ( 8008): E4MARK kind=turn_start n=999 SELFTEST_20260829`
⇒ **标签与优先级都对，标记路径不受 D-52 那个失败类影响**。
> 该测试行**不会污染语料**：`e234_collect.py` 在开采前先 `logcat -c`（`:277`）。
> **本会话没有清缓冲**——清了会毁掉别的会话的诊断历史。

**⚠ 同批验不了的一件（边界如实写）**：`AnebProbe:I` 缓冲当前为空。
这是**预期**而非故障——屏灭无前台事件，服务就不发 `ADAPTER_OBS`（它还忽略自身包）。
故 **`ADAPTER_OBS` 的行格式锁屏下无法验**；**但它恰好由「开窗第一件事」顺带验掉**——
§1.D 的 UI 栈判定就是去读那一行的 `cadence_p50_ms`，**读到了即证明该路径通**。

### F. 额度上限预检（本协议新增，方案 §10.4 点名）
- 豆包免费档**轮次上限未核**；**约 69–70 轮**有触顶风险。**⚠ 不是 65**——**开窗前置本身就吃额度**：UI 栈判定要开豆包问一句（≈1 轮）、量 ROI 需要一个**流式**帧即一个进行中的回答（≈1 轮）、加上下面这 3 轮试水。**按 65 排却实跑 70，触顶会提前发生在你以为还有余量的时候**（方案 §10.4 同批订正）。
- **开窗第一件事**：先跑 3 轮试水，确认无限流提示再进正式批；**触顶即停并如实记录已完成格数**，不换账号（账号是用户资源，D-49）。


### G. ROI 现场量的最小动作（窗内约 2 分钟；`--roi` 必填无默认值）

**设备几何（2026-08-29 锁屏下只读实测，开窗时不必重测）**

| 项 | 值 | 来源 |
|---|---|---|
| 物理分辨率 | **1200 × 2640** | `adb shell wm size` |
| 密度 | 530 dpi | `adb shell wm density` |
| 旋转 | `ROTATION_0`（竖屏） | `dumpsys window` |
| 状态栏高 | **154 px**（`mAppBounds=Rect(0, 154 - 1200, 2640)`） | 同上 |

**格式与校验**（`tools/e234/e234_collect.py` 的 `parse_roi`，逐条对着源码）

- 写法 `x,y,w,h`，**四个非负整数**；`w`/`h` 必须为正，否则报**人话错误不是栈回溯**。
- 边界自检：`x+w` 不超过 1200、`y+h` 不超过 2640。完全越界时 `roi_mean_from_raw` 返回 `None`（**这一种不会静默**）。

**⚠ 真正的危险不是打错，是格式打对却框在静态区**。`parse_roi` 的 docstring 逐字：
「**猜一个坐标出来会让通道 B 安静地测一块空白**（选错层不报错那件事的同款形状）」。
框在留白／标题栏／输入框上时，`roi_mean` 每帧都是一个**像样但永不变**的数，
`--screencap-period-ms` 照常采样、脚本照常成功、产物照常落盘——**没有任何一处会报错**。

**故量完必须做这一步自检（两帧对照，能当场证伪）**：

1. 对话页静止（无回复流动）时取一帧：`adb exec-out screencap > roi_idle.raw`
2. 让豆包出一段回复，**流式呈现进行中**再取一帧：`adb exec-out screencap > roi_busy.raw`
3. 用同一组 `x,y,w,h` 各算一次灰度均值，**两个数必须明显不同**。
   相等或只差末位小数则 **ROI 框在静态区，作废重量**，不要开跑。

> **⚠ 先排除一个假阳性：均值 `0.0` ＝ 屏灭签名，不是 ROI 错**（2026-08-29 本会话锁屏下实测：
> 屏灭时 `screencap` 照样抓到完整帧（16 字节头，`w=1200 h=2640`，`roi_mean_from_raw` 解析正常），
> 但全屏与中段的均值**都是 `0.0`**——纯黑，**是个合法数字不是 `None`**）。
> **两个 `0.0` 时先看屏亮着没有、USB 断没断**（§1.B 的屏幕超时那两行），**别急着改 ROI**。
> **越界 ROI 才返回 `None`**（同批实测确认），那一种与本情形可区分。

```python
# 存成 roi_check.py，**在仓根**跑（`sys.path` 那行按仓根相对路径写）
# 注意：别用 python -c —— PS 内联多行/含引号会吞字
# 本片段 2026-08-29 已按原样实跑验证：import 通、无栈回溯；两个 .raw 与 screencap 同源
import sys
sys.path.insert(0, 'tools/e1')
import e1_collect as e1
x, y, w, h = 0, 0, 0, 0                      # <- 填你量到的 ROI
assert w > 0 and h > 0, '先把 x,y,w,h 换成你量到的 ROI（宽高必须为正）——否则下面只会打印 None'
for name in ('roi_idle.raw', 'roi_busy.raw'):
    v = e1.roi_mean_from_raw(open(name, 'rb').read(), x, y, w, h)
    print(name, v if v is not None else 'None ← ROI 与画面无交集（越界），不是「框在静态区」')
```

> **本节不给候选矩形**：响应区位置是「这台设备这个 App 这一版」的一次实测，
> 我在窗外量不到；按 `parse_roi` 自己的理由，**给一个没量过的坐标比不给更危险**——
> 它会让人跳过上面这步自检。**R-10 同精神：没测到就是没测到，不猜。**

### H. 通道 D（PCAPdroid）的配置要求（2026-08-29 补——此前全协议只有「已授 VPN、只读方向字节」一句）

**为什么必须单列**：通道 D 是 **F3/F4 上行诉求的唯一证据源**（本文自己称它「本批最强的一块」），
同时它是**唯一直接挨着红线**的通道（D-24 不解密／D-57 免解密只读）。
而此前协议对它只有一句「已授 VPN、只读方向字节」的**检查**，与一句「手动起停」——
**怎么配置从没写过**。配错不会报错，只会在收窗后发现「拿到的不是想要的东西」或「拿到了不该留的东西」。

**三条硬要求（本批的口径，不可协商）**

| # | 要求 | 违反的后果 |
|---|---|---|
| 1 | **只抓 `com.larus.nova` 一个包** | 抓全设备＝混入无关流量（分析噪声）＋采到与本批无关的个人流量（不该留的东西） |
| 2 | **产物只保留「方向 + 字节数 + 时戳」级统计，不留载荷** | 违本树红线口径（`pcapdroid/` 那行产物说明逐字：「只留方向字节统计，不留载荷」） |
| 3 | **每格独立起停**，产物落该格目录的 `pcapdroid/` | 跨格混在一个抓包里则无法按格切分，D 单元的每格上行量拿不到 |

**⚠ 窗内必须先定的一件事（我在窗外定不了，也不编）**：
若 PCAPdroid 在本机的可用导出形式**只有完整 pcap**（而非可直接导出的连接级统计），
则**原始 pcap 不得进 `evidence/`**——**先提取「每连接方向字节 + 时戳」，再删原始 pcap**，
并在 README 写明「原始抓包已删、仅留统计」。
**这一步由开窗者当场确认并记录实际做法**；本协议不预设它的界面细节（我没在这台设备上开过它）。

**降级处置（照实，不硬凑）**：若三条要求中任一条在窗内做不到，
**不停批**——通道 A/B 不受影响照跑；但**F3/F4 的上行结论必须标 `NOT_EXECUTED` 并写明原因**，
**不得用 UI 侧的 `upload` 观感代替网络侧字节**（那是跨口径，违 §3 判读口径与铁律 3）。

### I. 硬要求 × 它靠什么成立 × 验没验过（2026-08-29；本节是「扫法」的产物，不是新要求）

本协议立了不少**硬要求**（不按标记键就作废、会话上限 900s、目录怎么命名……）。
**每一条都隐含一个依赖**——而依赖不成立时，要求本身会**静默失效**。
下表逐条把依赖挑明并标注验证状态。**这张表本身可复核**：✅ 那几行都给了实测出处。

| 硬要求 | 它靠什么成立 | 状态 |
|---|---|---|
| 按 `t`/`s`/`a`/`q` 分轮 | `adb shell log -p i -t E4MARK` 在这台 EMUI 上不被丢弃 | ✅ **实测通过**（§1.E-bis ②；**项目里恰有它失败过的先例**——D-52 的 `Log.d` 被 EMUI 丢弃） |
| `--session-seconds 900` | 屏幕全程亮着 | ✅ 实测 `stay_on_while_plugged_in=7` ＋ USB 连着；⚠ **`screen_off_timeout=600000` 小于 900s，USB 一断即失效**（§1.B） |
| `--out evidence/doubao_wave0_…` | 过得了 `assert_isolation_before_write(KIND_DEVICE)`（它在**设备门之后**跑，不过就是「过了门才死」） | ✅ **正反例各一实测**：本批路径与锚格路径均 PASS；把 `dryrun` 塞进路径**确被拒** ⇒ 这道检查**能失败**、不是摆设（`DRY_RUN_DIR_TOKEN='dryrun'`） |
| ROI 两帧自检 | `roi_mean_from_raw` 解析得了本机 `screencap` | ✅ 实测（16 字节头、`w=1200 h=2640`、越界返 `None`）；⚠ **屏灭时均值 `0.0`**（§1.G） |
| DW 号必须登记 | `device_gate` 真的会拦 | ✅ 实测三条路径全 `RC=3`（缺 `--allow-real-device`／缺 `--device-window`／板面查不到的号） |
| 判读走 `e2_analyze` | 它吃的是 `--run-dir` 指的**格目录** | ✅ 读 CLI 确认收 `--run-dir`+`--pkg`；**实效未验**（从未在豆包语料上跑过，§3 已标注） |
| UI 栈判定读 `cadence_p50_ms` | `ADAPTER_OBS` 那行确实会打出来 | ⏳ **锁屏下验不了**（无前台事件就不发）——**由「开窗第一件事」自身承载**：读到了即证明该路径通 |
| 每轮开新对话 | 豆包允许自由新建会话、不因此触发限流 | ⏳ **窗内**，与 §1.F 的 3 轮试水一并观察 |
| 通道 D 三条要求 | PCAPdroid 能按包过滤、且能导出不含载荷的统计 | ⏳ **窗内**，§1.H 已写明由开窗者当场确认并记录实际做法 |

> **⏳ 那三条不是漏项，是「只能在窗内验」**，且**每条都点名了承载者**（第一件事／试水轮／§1.H）——
> **没有一条是「到时候再说」**。
---

## ② 执行顺序（本协议的核心设计点：抗时序混淆 vs 省切换时间）

**问题**：12 格 = 6 功能 × 2 条件。若按条件分组（先 WiFi 全 6 功能 30 轮、再切蜂窝 30 轮），
**只需 1 次网络切换**、最省时；但那样 **WiFi 批全在前、蜂窝批全在后**——期间若服务端负载、
网络状态或账号限流发生漂移，**「条件差」与「时段差」不可分离**。

> 这正是 **D-393** 记下的形状：当年拿 07-31 下午（NR/异出口）比 07-31 闲时（LTE/本出口），
> 把制式与出口的差异读成了时段差异；后来四个同出口窗横跨早晚高峰实测 RTT 极差仅 1.18ms，
> 时段解释因此被否定。**先行批只有 2 个条件、每格又只有 N=5，最经不起这种混淆。**

**定案：分块交替（4 块）**，每块 3 个功能：

| 块 | 条件 | 功能 | 轮数 | 累计切换 |
|---|---|---|---|---|
| 1 | WiFi | F1 文本 / F2 深度思考 / F3 识图 | 15 | 0 |
| 2 | 蜂窝 | F1 / F2 / F3 | 15 | 1 |
| 3 | 蜂窝 | F4 文件 / F5 联网搜索 / F6 图像生成 | 15 | 1（同条件续跑） |
| 4 | WiFi | F4 / F5 / F6 | 15 | 2 |

- **⚠ 上表只到块 4（60 轮／12 格）——本批还有 ＋末尾锚格 5 轮，合计 65 轮**，见下方「⚠ 2026-08-29 订正」块及其单独一行的表。**锚格不是可选项**：整批的漂移判据全靠它，而它**不增加切换**（块 4 已是 WiFi）。
- **切换次数 = 2**（不是 §10 估算的 6，工时略优于估算，**不改结论**）；**块 2→3 同条件续跑是这个数成立的原因**——「4 块交替」听起来该切 3 次，实际只切 2 次；
- **每个功能的两个条件相隔不超过一块**（≈30 分钟），把漂移窗口压到最小；
- **每格必记时间戳**（见 ③）。

> **⚠ 2026-08-29 订正：本节原来的自检钩子是坏的，而且坏在它要防的那个形状上。**
>
> 原写：「事后可用它检验**块 1 的 WiFi 与块 4 的 WiFi**是否一致；两块同条件的读数若显著不同，说明期间有漂移」。
> **不成立**——块 1 是 F1/F2/F3、块 4 是 F4/F5/F6，两块**条件同、工作负载不同**。
> 读数本来就该不同（那正是本批要测的东西），于是「漂移」与「功能差异」**完全共线**，
> 这个钩子什么都区分不了。**它自己就犯了 D-393**（把两个同时变化的因素读成其中一个）。
>
> **改法：末尾补一格同工作负载锚**（**+5 轮，60 → 65**）。

| 块 | 条件 | 功能 | 轮数 | 累计切换 |
|---|---|---|---|---|
| **锚** | WiFi | **F1 文本（与块 1 的 F1/WiFi 同格重跑）** | 5 | 2（块 4 已是 WiFi，**不增加切换**） |

- **为什么是 F1**：单轮最短（≈57s）、形状最稳、是基线功能——**锚要便宜且可比，不要挑最抖的那个**。
- **为什么放最后**：与块 1 的同格读数**跨越整批时间窗**，漂移检测跨度最大。
- **判读**：锚格与块 1 的 F1/WiFi **同条件同功能**，两者显著不同 ⇒ 期间确有漂移，
  **所有跨块对照按该幅度打折**；两者一致 ⇒ 漂移解释被否定（**这是个诚实的否定，同样要写进 README**）。
- **代价**：+5 轮 ≈ **+6 分钟**（含 +40% 现场开销），约占全批 5%——
  **方案 §10.3 的「≈2 小时」结论不变**，仍在 0.5 设备窗内。

### ②-A 受控变量：每轮必须一样的那三样（2026-08-29 补——「被执行面」清单数出的三条缺口）

> **为什么补**：把协议按「操作者在窗内实际会做／敲／建的东西」导出一遍清单，数出**三处从未规定**。
> 三条都**不会报错**，都在事后才发现，而且 **G-2/G-3 直接让 §2 锚格的「同工作负载」前提不成立**。

| # | 此前状态 | 现在的规定 | 不规定的后果 |
|---|---|---|---|
| **G-1 目录名** | `<日期>`／`<条件>`／`<功能>` 三个占位符全文出现 6 次，**一处取值都没规定** | `<日期>`＝`YYYYMMDD`（如 `20260829`）；`<条件>`＝**`wifi`／`cell`** 二选一（全小写）；`<功能>`＝**`f1`…`f6`**，锚格＝**`f1_anchor`** | 操作者自造名（`0829` vs `20260829`、`wifi` vs `WiFi` vs `WLAN`），**分析侧无法 glob**；而判读页正靠目录名区分条件与功能 |
| **G-2 每轮的提示词** | **全文零规定** | **每个功能一句固定提示词，5 轮逐字相同**，并抄进 README；F1 用 **「What is 5G」**（沿用 D-50 的历史负载，便于将来对照——**但这不改变 §1.D「26/28 不可比」的结论**，那是版本与 n=1 的问题，不是提示词的问题） | 格内 5 轮**工作负载不受控**，回答长度一变 cadence/RCT 全变；**更要命的是锚格**——它的全部意义是「与块 1 同工作负载」，提示词自由发挥则锚格什么都锚不住 |
| **G-3 会话新旧** | **全文零规定** | **每轮开新对话**（豆包内新建会话），轮与轮之间不复用上下文 | 同一会话连问 5 轮，上下文逐轮增长，prefill 增大，**TTFT 系统性上升**，会被误读成漂移或条件效应；**末尾的锚格受害最深**（上下文最长），恰好污染漂移判据本身 |

> **F3/F4 的素材同理固定**：识图用**同一张图**、文件上传用**同一个 PDF**，文件名与字节大小抄进 README——
> 上行 payload 大小是 D 单元的自变量，**素材一换，`upload_ms` 就不可比**。
> **本协议不指定具体素材**（我在窗外拿不到、也不该编），但**「全批同一份」是硬要求**，
> 且素材的**字节大小必须记进 README**——没记等于该功能的上行结论不可复算。

**G-4 网络条件切换的具体动作（同批补：§2 只说「切换 2 次」，没说怎么切）**

| 做什么 | 不做什么 | 为什么 |
|---|---|---|
| **只关/开 WiFi**（蜂窝数据全程保持开启），切完**等 10s 并复验连通**（能打开一个页面即可） | **不要用飞行模式** | 飞行模式会**重置 radio 并触发重新驻网**，把「注册时延」混进该块第一轮，而那不是我们要测的量 |
| **记下每次切换的时间戳**（写进 README） | 不要在块中途切 | §2 的自检钩子与锚格判读都要用它对齐；块中途切＝该块条件不纯 |
| 切换由**操作者手动做**（零 PC 介入） | 探针不得代切 | 红线：观察 only（D-385/D-49）——网络条件是实验自变量，由人设定，不由被测装置设定 |

**G-5 制式漂移：锚格控得住时段，控不住制式（2026-08-29 窗外只读核出，本节此前完全没有）**

窗外实测 `gsm.network.type = NR_SA,Unknown`（**完整回值；逗号分隔＝按 SIM 卡槽，承载在卡 1**）——**「蜂窝」这个条件今天是 5G 独立组网，不是 LTE**。
但 **NR↔LTE 在一次批内是会漂的**（信号、负载、网络侧策略都能触发回落）。
若块 2/3 中途发生回落，**「蜂窝」就不是一个条件而是两个**，而两者的 RTT／带宽量级差别
足以盖过我们要测的功能差异。

> **这正是本项目最贵的那个复发形状**：**D-393** 当年把「制式 + 出口」同时不同读成了时段差异，
> 后来靠四个同出口窗才把时段解释否定掉。**§2 的锚格设计控的是时段漂移，对制式漂移完全无能**——
> 因为锚格在 WiFi 上，而制式只影响蜂窝那两块。

**要求（成本极低，不记就永远看不见）**：

| 何时 | 记什么 | 记在哪 |
|---|---|---|
| **每格开跑前** | `adb shell getprop gsm.network.type` 的原样字符串 | 该格 README 行 |
| **每格跑完后**（同一条命令再读一次） | 同上 | 同一行，前后并列 |

> **⚠ 为什么必须前后各读一次（2026-08-29 接 v3 提点订正——本条原写「跑完记是否全程同制式」，
> 却没说拿什么判）**：**只读开跑前那一次，格内漂移会伪装成「整格 NR」**——
> 你手上只有一个 `NR_SA`，而它既可能是「全程 NR」也可能是「开头 NR、后来掉了」，
> 两者判读后果相反。**两条 `getprop` 秒级成本，换的是把三种情形分开的能力**；
> 而观察通道的产物**不含 wire 侧的 radio 块，事后无从复原**——不当场读就永远没有了。

| 前后两次读数 | 判定 | 该格怎么用 |
|---|---|---|
| 都是 `NR_SA` | 整格 NR | 条件成立，照常判读 |
| 都是 LTE 系 | 整格 LTE | 条件成立，但**与 NR 格不可并读**，按制式分池 |
| **两者不同** | **格内确有漂移** | **该格的任何条件结论都用不了**——单列、不并入蜂窝池；数据保留并如实标注 |

> **两点采样仍排除不掉的那一种（如实写在这里，不假装它不存在）**：
> **`NR` → LTE → `NR` 的往返漂移**，前后两读都会显示 `NR_SA`。
> 故本批的正确措辞是「**前后两点实测同制式，格内瞬时漂移不可排除**」，
> **不是**「全程 NR」。**降一档说，比说满了再被推翻强。**

- **上表判定为「整格」** ⇒ 蜂窝条件成立，照常判读（带上面那句限定）；
- **判定为「格内漂移」** ⇒ **不作废**，但该格**单列不并入**，条件对照**必须按「混合制式」标注并降级**，
  **不得**把差异归因于「WiFi vs 蜂窝」——那是 D-393 的原样重犯。
- **本批只有单运营商（ctcc）**，故**不宣称跨运营商结论**（清单 #3 未解，如实标注）。


**采集命令**（**每格一次会话，一次会话跑完该格 5 轮**；参数照 `tools/e234/e234_collect.py`）：

```
python tools/e234/e234_collect.py --serial <SN> --pkg com.larus.nova --roi <x,y,w,h> --allow-real-device --device-window DW-YYYYMMDD-NN --session-seconds 900 --screencap-period-ms 3000 --out evidence/doubao_wave0_<日期>/<条件>_<功能>
```

> **⚠ 2026-08-29 订正（本命令原先有两处会毁窗的错，开窗前查出）**

| 原写法 | 错在哪 | 现写法 |
|---|---|---|
| `--out …/<条件>_<功能>_r<轮次>` | 与本节自己写的「每格一次会话」矛盾；**脚本靠 `t` 标记分轮**，本就设计成一次会话跨多轮。按轮建目录＝60 次会话，而每次会话默认 600s 上限，**排下来 10 小时**，与方案 §10.3 的「≈2 小时」差五倍 | `--out …/<条件>_<功能>`（**每格一个目录**，轮次由 `mark_rtt.jsonl` 里的 `t` 标记分） |
| **不给 `--screencap-period-ms`**（即沿用默认 **400**） | **400 是个幻觉参数**：实际周期 ＝ `max(period, 单帧耗时)`，而单帧实测中位 **1544ms** ⇒ **400 永远达不到，只是把占空比顶满**。持续抓屏实测吃掉约 **1.3 个核**、cluster1 **57°C**——**这份负载全程压在通道 A 这个主量的测量期间**。⚠ 且「**5 个样本**」这个下限（`D-588` ③）**是 v3 的稳定性判断、不是工具门槛**：`e1_analyze.screencap_sampling_stats` 的**硬下限是 `<2` 即 `NOT_EXECUTED`**（「样本 <2，无法构成周期」）；**帧差确是相邻两样本相减**（该函数逐字，已核）——**2 个样本只给 1 个差，分不出一次性抖动与真实变化**；取 **5** 是 v3 随 ±3s 重算的结果（3000ms × 5 ≈ **需 15s 流式**，而 §10.2 **六个功能全部 ≥30s**——F1/F3/F4/F5/F6 ≈30s、F2 ≈60s ⇒ **每一个都有约 2× 余量，含 F1 这个单轮最短（≈57s）且是锚格的功能**。⚠ **但那一列全是 📐 估算**（由「≈300 token × ⚓100ms/token」推得），**无一 ⚓ 实测**；F6 还明写「生成时长未实测，可能显著更长」（**长是安全方向**）。**危险方向是「实际流式远短于估算」，而它没有实测下界。** **好消息：这条不会静默伤人**——阈值的用法本身就是**在首格数真实样本数**，估算若错，操作者当场看到 `<5` 并按阶梯回退，**降级成一次回退，不会变成一个静默的错结论**。⇒ **本依据为 📐 估算，首格实数即校正它。**） | **显式给 `--screencap-period-ms 3000`**（v3 2026-08-30 裁）：**取一帧歇一帧、负载减半，而减掉的恰是热渐变项**——**拿粗量具的余量去护主量**。**回退阶梯（首格跑完当场判，别等窗后——窗后没设备）**：数该轮 `s`→`a` 之间 `screencap_index.jsonl` 的行数，**<5 → 回退 2000；仍 <5 → 回退 1500**（**阈值 2026-08-30 由 `D-588` ③ 定为 ≥5**——v3 随 ±3s 重算自纠，此前经历 **3→4→5**，**本文原写的 3 已过期**）（≈现状，因 `max(1500,1544)=1544`）。按 §10.2 多数功能流式 ≈30s ⇒ 3000ms 下约 **10 个样本**，余量充足。**选定值必须写进该批 README**（判读输入，且随机器/USB 链路变） |
| 不给 `--session-seconds` | 默认 **600s**。F2 深度思考单轮 ≈90s × 5 轮 ≈450s，加操作间隔很接近上限，**到点即止会在第 5 轮没跑完时截断**，且不报错 | 显式 `--session-seconds 900`（上限而非固定时长，**按 `q` 提前收**） |

**🔴 操作者必须按的标记键（此前本协议一个字都没写——不按＝轮次边界不可恢复，65 轮白跑）**

脚本**默认交互**（`interactive = not args.no_marks`），启动后阻塞等键盘输入并打印：
`标记键：a=回答完成  s=回答开始  t=本轮开始  q=结束采集`。

| 键 | 何时按 | 不按的后果 |
|---|---|---|
| `t` | **每轮问答开始前** | 该格 5 轮无法切分，per-round 量全部拿不到 |
| `s` | 看到**响应首字上屏**时 | 失去 UI 口径 TTFT 的人工对照锚（v3 簇分割仍可自动出，但没有旁证） |
| `a` | 该轮**回答完成**时 | 轮次不闭合，流式时长不可判 |
| `q` | 该格 5 轮跑完 | 会话空转到 900s 上限才停，白耗窗内时间 |

> 每次按键都会**往设备 logcat 打一条 `E4MARK`** 并把**往返耗时落盘**（`mark_rtt.jsonl`）——
> 那个 RTT 就是这条标记的**不确定度上界**，判读时按它给标记打折，不要当作精确时刻。

> 通道 D（PCAPdroid）与上述**并行**手动起停，其产物单独归档（脚本不管抓包）。

---

## ③ 产物与判读入口

**落点**：`evidence/doubao_wave0_<日期>/`
> **目录名以 SPEC-2 任务书 §2.1 的验收判据为准**（原写 `doubao_pilot_`，2026-08-29 自查发现与任务书不一致——**验收时目录名对不上就是没达标**，已统一为 `doubao_wave0_`）。
```
wifi_f1/ cell_f1/ …       每格一目录（脚本 --out；命名取值见 §②-A G-1：
wifi_f1_anchor/           条件=wifi|cell，功能=f1..f6，锚格=f1_anchor）
                          轮次靠 mark_rtt.jsonl 的 t 标记分
  adapter.log            通道 A 事件流（TTFT/cadence/RCT 的原料）
  screencap_index.jsonl  通道 B 帧差标量
  mark_rtt.jsonl         人工标记流 + 每条标记的往返耗时（= 该标记的不确定度上界）
  framestats.txt         通道 C gfxinfo framestats（恒出）
  sf_latency.txt         通道 C SurfaceFlinger 时延（恒出）
  collect_notes.json     采集元数据（含时间戳——(2) 的自检钩子靠它）
  pcapdroid/             通道 D 抓包（手动归档，只留方向字节统计，不留载荷）
README.md                  战役说明：格阵、执行顺序实况、每格时间戳、偏离记录
```

> **⚠ 通道 B 的时间分辨率＝400ms，判读前必须知道（2026-08-29 补——`--screencap-period-ms` 全文只出现过一次，还是在一句「不会报错」里，从没交代过它的后果）**
> **🔴 2026-08-29 深夜实测订正:真实分辨率约 ±1.5 秒，不是 ±400ms（本条此前写错近 4 倍）**。`--screencap-period-ms` **默认 400** 只是**目标周期，不是实际周期**：采样循环是「先取帧、再补睡 `period − 已耗时`」，而**单帧 `adb exec-out screencap` 实测中位 1544ms**（用采集器自己的 `Adb` 类量的 5 次：1362/1454/1544/1600/1719ms；帧 12,672,016 字节＝1200×2640×4＋头），**补睡量恒为负、永远不睡** ⇒ **实际周期 ＝ `max(400, 单帧耗时)` ≈ 1.5s**。⇒ **`screencap_index.jsonl` 的首帧变化时刻带 ≈±1.5s 量化底噪**——而**锚定 TTFT ≈2.0s，这是它要交叉印证的那个量的约 77%**。
> **主量不受影响**——TTFT/cadence/RCT 的原料是**通道 A**（`adapter.log`，事件级 `t_boot_ns`）。
> **⇒ 交叉印证基本不成立，别指望它**：拿通道 A 的 TTFT 去比通道 B 的首变时刻，**1.5 秒以内的差全是量化，不是发现**——而 TTFT 本身才 2 秒。**通道 B 在本批只够回答「这一格到底有没有画面在动」，不够回答「首帧早了/晚了多少」。**（这个数与机器/USB 链路有关，**开窗时值得当场重测一次**：跑 `adb exec-out screencap > NUL` 计时三次即可，比这里记的更慢就说明分辨率更差。）
> **⚠ 通道 B 的代价也一并量了（2026-08-29 深夜，同批实测）**：单帧 **12,672,016 字节**、周期 ≈1.5s ⇒ **每格约 600 帧、≈7.6 GB 持续传输**，且**整格 900 秒全程在跑、就跑在测量期间**。设备侧实测:静息 `sys 38% / idle 731%`（8 核共 800%）→ 连续抓屏 `sys 159% / idle 600%`、单进程峰值 **58.6%**，**即持续吃掉约 1.3 个核**；抓屏后 cluster1 **57°C**。
> **按不确定度三问处置**：**单向为正**（只加负载）、**在条件对照做差时抵消**（每格都有）；**唯一不抵消的情形是热累积随批次推进而渐变**——**而那正是末尾锚格（F1/WiFi 对块 1 同格、跨越整批时间窗）要检测的东西**，**设计里已经有这个探测器，不必另加**。⇒ **若锚格与块 1 同格出现显著劣化，先怀疑热累积/持续抓屏，别直接归因于网络或时段。**
> **可选减负（不改设计、不影响结论）**：把 `--screencap-period-ms` 从 400 调到 **3000**。**理由是这个数已经不再买到分辨率**——实际周期由单帧耗时（≈1.5s）决定，400 只是个达不到的目标；调到 3000 相当于**取一帧、歇一帧**，**占空比减半、负载减半**，而通道 B 在本批的用途本就只是「**这一格有没有画面在动**」，30 秒的流式回答仍能采到约 10 帧。**要不要减负是判读侧的取舍，本文只给数与代价，不替它决定。**
> **⚠ 一条诚实的否定，免得下一个人重查**：这 1.5s 的阻塞抓屏**不会挡住标记键**——`_MarkPump.loop` 跑在**自己的守护线程**里，`_sample_roi` 在主线程，两者互不阻塞（`subprocess` 等待期间释放 GIL）。**标记时刻不受此影响。**
> 本文已给 `mark_rtt.jsonl` 记「**每条标记的不确定度上界**」——**通道 B 的这一项是同类量，此前哪儿都没记**，故补在这里。
>
> **⚠ 同一问下去，主量（通道 A）也有一项没记（2026-08-29 补）**：`adapter.log` 的 `t_boot_ns` 取的是 **`onAccessibilityEvent` 入口**的 `SystemClock.elapsedRealtimeNanos()`（`AnebAccessibilityService.kt` 里搜 `SystemClock.elapsedRealtimeNanos()` 与紧随其后那句「传 `now`——即本次 onAccessibilityEvent **入口**算的那个」注释；**不写行号，见 `REQUIREMENTS_BASELINE_v2.0.md` §5 第 6 条**），**不是 `event.eventTime`**。
> **这不是缺陷**——`eventTime` 走 `uptimeMillis`，与本批 KPI 同轴的单调钟不同源，换算反而引入新误差（选型理由在该文件头部注释，搜「单调钟（与 KPI 事件同轴）」）。
> **但后果要写给读者**：`t_boot_ns` 与「界面真正变化的那一刻」之间隔着**无障碍回调的派发延迟**，**本批未测其量级，不给数字**。它**单向为正**（只会让 TTFT 偏大，不会偏小）。
> ⇒ **判读时的实际含义**：**本批比的是条件差（WiFi vs 蜂窝），恒定的系统性偏置在做差时抵消**——**所以结论应落在「差」上，而不是绝对 TTFT**。
> **真正不抵消的情形只有一种**：**派发延迟本身在两个条件下不同**（例如某条件下设备负载更高）。**若某格的绝对 TTFT 异常大，先怀疑这个，别直接归因于网络。**

**判读入口（2026-08-29 补——本节标题一直写着「判读入口」，却一只工具都没点名）**

`tools/e234/` 的三只 analyze **结构上同构**：三者都收 `--run-dir <每格目录> --pkg com.larus.nova`，
即 `e234_collect.py --out` 产出的那个目录。13 格（12 + 锚格）＝每只跑 13 次。

> **🔴 但「结构同构」不等于「都该跑」（2026-08-29 按 v3 实测收窄）**：**本批默认只用 `e2_analyze`**。
> `e3_analyze` / `e4_analyze` **各带语义前提旗标**（口径选择，不是默认适用），
> **须先拿一格实语料验过再定**是否纳入判读。
> **别照「三只」全跑**——那会把口径未定的产出当判读数用。

**🟡 已核实的口径（2026-08-29 v3 复查，判据可复跑）：核心量已结构化投影，但无工具对它出判词**

> **§2.18「口径入口指向判据、不指向结论」**：下表每条给一条能当场复跑的 grep/测试，而非写死「零读者」——那个结论会随代码演进过期。**本条就过期过一次**：普查写下时为真，v3 于 06:46 `4441ea6` 扩投影后已变，而此后三笔编辑（07:41/07:47/07:52）未扫到本段。

| 判据（复跑命令） | HEAD 实况（2026-08-29） |
|---|---|
| `grep -rn ttft_cluster_ms tools/ scripts/ --include=*.py`（去测试行） | **有读者**：命中 `tools/e1/e1_analyze.py` 的 `parse_adapter_obs()` 投影行——`4441ea6`（08-29 06:46）起 **13 键全投影**，不再丢弃 |
| `grep ttft_cluster_ms tools/e1/tests/test_e1_analyze.py` | **有守卫**：值断言（`==910.0`）＋ 13 键全覆盖测试 |
| 有没有工具对它求中位/分位/出判词（`grep ttft_cluster` 后再筛 median/percentile/p50/p95/verdict，排 cadence） | **零命中**——仍无 |

**精确的缺口（投影已修、聚合未做）**：`adapter.log` 的 `ttft_cluster_ms=…` 现已由 `e1_analyze.parse_adapter_obs()`
**结构化投影**到 `summary["adapter_obs_lines"]`（13 键全投影，不再手工 grep）；**但下游无一处对它聚合出数**——
`parse_adapter_obs` 的消费方只有 `_cadence_check`（只读 `cadence_p50_ms`）与整包透传 `summary["adapter_obs_lines"]`。故：

- **不阻塞采集**（数据在盘、规整 kv、且**已被工具结构化读出**）；
- **仍阻塞「判读页自动出数」**——判读页要给数得**自己聚合并标明聚合口径**，不是工具现成产出；
- **分析层是 v3 的 lane**：投影侧已修（`4441ea6`），其判读链按「投影有、聚合无」写着，本协议不替它下结论；
- **操作者须知（v3 2026-08-29 订正）**：窗后该量**已是 `summary["adapter_obs_lines"]` 里的结构化字段、不再是手工 grep**——
  README 要**同时写清两半**：「已由 `parse_adapter_obs()` 投影（13 键）／但无工具出判词，判读页自聚合并标口径」，
  **不要把工具已投影的字段反向误标成手工读数**（照旧写法会让判读侧多做一遍白工）。

| 工具 | 它算什么 | 本批供得上料吗 | 定位 |
|---|---|---|---|
| `e3_analyze.py` | **`A0 → A0′` 间隔**（通道 C 的输入事件取 A0，通道 A 的 v3 首簇首事件取 A0′） | ✅ 两个通道本批都采 | **最该先跑**——它正是 **D-396** 点名的「那个从未被测过的量」，也是 §10.2 里 ⚓ `ttft_ui_ms=1984` 那条口径警告（「不是按下发送到看见首字」）所缺的那一段 |
| `e2_analyze.py` | 三通道同轨对拍（同一锚点 A2，各通道时刻两两求差） | ✅ A/B/C 齐 | 跨通道自洽性检查，**发现打点错位比发现结论更有用** |
| `e4_analyze.py` | `T_quiet` 标定（A4 判据 C-1 的**前置**） | ✅ | **附带收益**，不是本批承诺产出——RCT/stall 仍按下方「未解阻」如实写 |

> **🔴 本节内部有一处未裁决的冲突（2026-08-29 实查标出，不擅自裁决）**：上方收窄段写「**本批默认只用 `e2_analyze`**」（按 v3 实测，理由＝e3/e4 各带语义前提旗标、口径未定），而**本表把 `e3_analyze` 标为「最该先跑」**（理由＝它测的是 D-396 点名那个从未被测过的量）。**两句都有各自的依据，但对「先跑哪只」给的答案相反。** 在 v3 或大脑裁定前，**按两边都支持的那条做**：**先跑 `e2_analyze` 拿自洽性结论**（默认、无口径选择），**同时拿一格试跑 `e3_analyze` 只为把它的口径旗标定下来**、结论暂不入判读。
>
> **⚠ 另补一个具体的失败模式（读 `e2_analyze` 源码得出，比「实效未验」可操作）**：它**逐轮**要求**通道 A 与通道 C 各自分出 ≥2 簇**，否则该轮直接丢弃并记 `drop_reasons`（「通道 A 该轮不足两簇（A2 无判据）」／「通道 C 该轮不足两簇（帧序列未分出思考静默）」）。⇒ **一格可以「5 个 `t` 标记全齐、验收通过」，而 `e2_analyze` 一轮都用不上**——**操作者的验收判据（标记数）与判读器的可用判据（≥2 簇）不是同一个**。**所以窗内跑完第一格就跑一次 `e2_analyze --run-dir <该格目录> --pkg com.larus.nova`，先看 `drop_reasons` 是不是全丢**；若全丢，**趁人还在设备旁**，别等收窗。
>
> **⚠ 如实标注边界**：以上是**结构适用性**（读 CLI 与 docstring 得出），**实效未验**——
> 这三只从未在豆包语料上跑过。**窗后先拿一格试跑，看输出是否有意义，再决定要不要全跑**；
> 跑不出东西**就如实记「工具在本形状语料上无输出」，不要反推去改采集**（那是让结论决定数据）。

**⚠ 战役标识：本批不用 `campaign_id`，而用目录名 + README（与 SPEC-2 §2.1 字面的差异，已核清）**

SPEC-2 §2.1 的验收判据写「首批语料落库（**独立 `campaign_id`**）」——**该字段对本批不适用**，理由是机制层面的：
- `campaign_id` 是 **ANEB probe 的 wire 语料字段**（`run.campaign.campaign_id`，见 `CAMPAIGN_LABELS_CONVENTION.md`），
  缺失即归 `unlabeled` 桶；
- 但**豆包先行批走观察通道**（A 无障碍 / B 帧差 / D 抓包），产物是 `adapter.log` / `screencap_index.jsonl` / pcap，
  **不是 wire 语料**——`tools/e234/e234_collect.py` 全文无 `campaign`/`label` 参数，其 README 亦实测
  「把 dry-run 的 `screencap_index.jsonl` 喂 `scripts/validate_results.py` → **exit 1，contract VIOLATIONS**」，
  即这批产物**结构上就进不了 wire 语料池**，自然也没有那个字段可填。
- **本批的战役标识因此落在两处**：**目录名** `evidence/doubao_wave0_<日期>/` 与 **README 首行**（写明批次、条件、格阵、时间窗）。
  验收时以这两处认定「独立批次」，**不以 wire 的 `campaign_id` 认定**。
- ⚠ **反过来的红线仍在**：本批产物**不得**被塞进 wire 语料池或与 ANEB 自有 run 混池——两者口径完全不同
  （ui-proxy vs 网络层），混池即违 §③ 判读口径与铁律 3。

**⚠ 先行批实际能产出什么（本批的产出承诺边界，2026-08-29 审 §3 后补）**

方案 §9 把先行批描述为测「UI-TTFT / cadence / **RCT** 与多模态上行字节」，但按 §3 指标表逐条核，**其中两项带未解阻的前提**：

| 指标 | 本批能否产出 | 依据 |
|---|---|---|
| **TTFT_ui**（A0′→A2） | ✅ **能** | 通道 A 主判据现成；⚠ 起点是 **A0′（气泡上屏）非 A0（点击发送）**，两者差一个**从未测量过**的量（待 E3），转述时不得说成「按下发送到看见首字」 |
| **cadence_ui**（A3 间隔） | ✅ **能** | 同上；口径＝**节奏代理，非网络 ITL** |
| **上行字节 / upload_ms** | ✅ **能**（通道 D） | 免解密只读方向字节；**网络传输层口径，绝不与 UI 口径相减**（跨口径红线） |
| **RCT**（A4−A0′） | ⚠ **不能，或只能给未标定的临时值** | **A4 判据三级（C-1/C-2/C-3）全 `NOT_EXECUTED`**，`T_quiet` 待 **E4** 标定（`INSTRUMENTATION_SPEC` §1.5）。**E4 恰是本协议 #7 那批、尚未跑** |
| **stall 代理** | ⚠ **阈值未标定** | §3 原文：「阈待标定；原文用 ≥1s，**本战役阈需在豆包 cadence 分布上重定**」。**本批可先采分布、不下 stall 判定** |

**执行时照此办**：①**RCT 与 stall 不作为本批的承诺产出**——采到的原始事件流照常归档，判读页里**如实写「A4/阈值未解阻，本批不给 RCT 与 stall 结论」**；②若开窗时 E4 已跑出 `T_quiet`，再回头用本批录轨补算（事件流已存，不必重采）；③**绝不用一个临时拍的阈值凑出 RCT/stall 数字**——那正是「有数字比没数字更危险」的形状。

**判读口径（R-10 与红线）**：
- 所有 UI 侧指标恒标 **ui-proxy 口径，≠ 网络 ITL/TTFT**，`confidence ≤ LOW`（`INSTRUMENTATION_SPEC` §4.4：G-5 未 PASS 前恒 LOW）；
- **N=5 是登记级下限**——产出**不给置信区间**，恒标 LOW/登记级（方案 §6.2）；
- 缺测记 `null` 不记 0（R-10）；某格没跑成就**如实少一格**，不用别的格补。
- **⚠ 时段边界：本批是「单窗两小时」，所以两类结论的可外推程度完全不同（2026-08-29 补——本文此前 8 处提「时段」全在讲批内漂移，从没讲过结论边界）**：
  > | 结论类型 | 时段上成不成立 | 措辞要求 |
  > |---|---|---|
  > | **对照类**（WiFi vs 蜂窝、功能之间的相对差） | ✅ **成立**——两个条件在**同一个窗内交替采**，时段对两边**同等作用**，正是 §2 分块交替＋锚格要买的东西 | 可直接陈述相对关系 |
  > | **绝对值类**（`ttft_ui_ms`、上传耗时、上行字节速率） | ❌ **只对该时段成立** | **每一处绝对值都必须带时间窗**，例如「**（2026-08-29 起止时刻 单窗实测）**」；**不得**写成「豆包 F3 需要 X Mbps 上行」这种无时段的断言 |
  >
  > **为什么必须写死这条**：本项目在 **D-393** 上付过学费——当年把「制式＋出口」的差异**读成了时段差异**，
  > 后来靠四个同出口窗横跨早晚高峰才把时段解释否定掉。**那次是把时段当成了原因；这次的风险是反过来——
  > 把只在一个时段测到的绝对值，当成与时段无关的常量。** 两个方向的错都出在「**没把时段写进结论**」。
- **不为此挑时段**：E-1 何时解锁取决于在场者，**本协议不规定必须在闲时／忙时开窗**——加一条做不到的要求只会挡住开窗。
  **要求的是记录与措辞**：时间窗按 §5 README 必填第 1 项照记，绝对值按上表带窗表述。**能不能覆盖多时段，是下一批的事。**

---

## ④ 中止判据表（别在设备旁边猜）

> **先认退出码（2026-08-29 五条路径实跑验过，非读代码推理）**：`e234_collect.py` 的失败分两类——
> **RC=2 ＝ 参数/前置错**（`--roi` 格式错、宽高非正、型号读不到、隔离检查不过）；
> **RC=3 ＝ 设备门拒**（缺 `--allow-real-device`／缺 `--device-window`／DW 号板面查不到）。
> **RC=2 的两种 ROI 错在任何设备接触之前就退出**，RC=3 的三种只做一次只读 `getprop`。
> **看码即知该改命令还是该去板面登记**，不必读消息——本协议此前全表一处退出码都没提。

> **本表经与脚本实际拒绝路径逐条对照（2026-08-29 自查）**：`e234_collect.py` 的 `device_gate` 与 `parse_roi` 共 6 条拒绝/异常路径，**初版表漏了其中三类**（型号未知 fail-closed / 读不到任务板 / ROI 格式错），已补齐——**「我自己就是执行者」不能代替核对**。

| 观察到的形状 | 含义 | 处置 |
|---|---|---|
| `adb devices` 空 / `mDreamingLockscreen=true` | E-1 未解 | **不开窗**。如实记 BLOCKED_EXTERNAL，等在场者（D-481 先例：开窗即空跑是浪费） |
| 脚本报「型号命中 denylist 且无 `--device-window`」 | 板面未登记 DW 号 | 先登记再跑；**不要试图用 `--allow-real-device` 硬闯**（它绕不过 denylist，设计如此） |
| 脚本报「**型号未知 / 非模拟器无旗标**」 | **fail-closed**：`getprop` 读不到机型时一律拒（不是 denylist 那条，`--device-window` 救不了） | 先确认 `adb shell getprop ro.product.model` 有输出；读不到通常是设备未授权 USB 调试或掉线，回 ①A 重核 |
| 脚本报「**读不到任务板**」 | 与「查无此项」**不是同一回事**：这是板面文件本身读不到（路径/权限/编码） | 核 `docs/BRAIN_TASKBOARD.md` 是否存在且可读；**别急着改 DW 号**——号可能没错，是板读不到 |
| 脚本抛 `ROI 要写成 x,y,w,h 四个非负整数` / `ROI 的宽高必须为正` | ①B 现场量的 ROI 写错格式或量成了零宽高 | 重量一次：`adb shell wm size` 看分辨率，截图确认响应区像素范围；**四个数用英文逗号、无空格也可**（脚本容忍空格但不容忍缺项） |
| 豆包出现限流/额度提示 | 免费档触顶（①F 预检没挡住） | **立即停**，如实记录已完成格数与触顶轮次；**不换账号**（D-49） |
| 通道 A 无事件 | a11y 未重绑 / 进程被 iAware 冻结 / 豁免失效。**⚠ 但「A 真的没事件」与「A 压根没在听」在输出上一模一样**——判之前先回 §1.E-bis 的 `E4MARK` 往返冒烟确认链路活着，否则你停的可能是一条好批 | 停本批，记为前置未满足，不产出数据（**不要靠事后看数据像不像**，D-386） |
| `cadence_p50_ms` < 5ms | **豆包已迁 Compose**，v3 簇判据失效（§1.D） | 停本批。**本批最大风险项**；记原值与版本号，核心量口径须先重定，**不在窗内硬修** |
| 某格的 5 轮提示词**不逐字相同**，或中途**沿用了同一个对话** | 违 §②-A G-2／G-3——工作负载或上下文长度成了未受控自变量 | **该格作废重跑**。⚠ **锚格若犯，整批漂移判据作废**（它的全部意义是与块 1 同负载） |
| 通道 D 抓到了**非 `com.larus.nova` 的流量**，或产物里**含载荷** | 未按 §1.H 三条要求配置 | **立即停该格的 D**，删除该次抓包产物（不得进 `evidence/`），重配后再跑；A/B 已采部分可保留 |
| 开窗时 **WiFi 关联不上**（`ip route get 1.1.1.1` 仍走 `rmnet*`；**必须带 `get 1.1.1.1`——裸 `ip route` 恒空，见 §1.B 的护栏，用它判会假中止**） | 人不在已保存网络覆盖内 | **不开跑**。块 1／块 4 与锚格全依赖 WiFi，**塌掉的是整批设计的一半**（对照与锚格同时没了）。**不要临时改设计**，按前置未满足记录、不产出数据 |
| 通道 B 的 `roi_mean` **全程恒 0.0** | **多半是屏灭**（超时 10 分钟 < 会话 15 分钟；USB 断开即失去常亮），**不是 ROI 框错** | **先查屏与 USB**，别改 ROI。该格的通道 B 作废重跑；通道 A/D 不受影响可保留（如实标注 B 缺失） |
| F3/F4 的素材**换过**，或字节大小**没记进 README** | 上行 payload 是 D 单元的自变量 | 该功能的**上行结论不可复算**，如实标注；素材大小事后补不回来 |
| 收窗时该格 `mark_rtt.jsonl` **为空**，或 `t` 标记数 **≠ 5** | 操作者漏按标记键（②的 🔴 段）。**⚠ 但「为空」这一半同属上一族**：漏按与「标记链路没通」输出相同——**为空时先回 §1.E-bis 冒烟，别直接判自己漏按**（`t` 数 ≠5 那一半没有这个歧义，有数就说明链路是通的） | **该格作废重跑，但必须先按 §④-A′ 把作废目录改名 `<格名>_VOID<k>/`**——**就地重跑会让本判据永不收敛**（`mark_rtt.jsonl` 追加、`adapter.log` 覆盖：3＋5＝8 个 `t`，仍 ≠5，每补一次离 5 更远）。轮次边界事后不可恢复，`adapter.log` 再全也切不出 per-round 量。**每格收工立刻数一次，且必须数两个文件**（📌 **它们是两次独立写入，会分叉**——采集器先 `adb shell log` 打进设备 logcat，**成功之后**才写 `mark_rtt.jsonl`）：①`mark_rtt.jsonl` 里 `t` 的条数＝**你按了几次**；②`adapter.log` 里 `E4MARK kind=turn_start` 的行数＝**判读器真正读到的那个**（`e4_analyze` 走 `E4MARK` 行，**不读 `mark_rtt.jsonl`**）。**两个都要是 5，且必须相等**——若 ①=5 而 ②<5，是 **logcat 环形缓冲吞了行**（900s 会话真会发生），**此时你的验收通过了、分析侧却静默少了轮次**。计数命令（2026-08-29 三种壳按原样各跑过一次，同一样本均回 5）：PowerShell `(Select-String -Path adapter.log -Pattern "E4MARK kind=turn_start").Count`；Git Bash `grep -c "E4MARK kind=turn_start" adapter.log`。**③ 若两个数不等，`collect_notes.json` 里的 `marks` 能告诉你丢在哪一级**（它是**第三个计数**，且**没有任何判读器读它**——只有人会看，所以这里点名）：三个数分别产生在流水线的三个位置——`marks`＝按键计数（`self.n += 1` 在 **adb 调用之前**）→ `mark_rtt.jsonl` 行数＝**adb 送达之后**才写 → `adapter.log` 的 `E4MARK` 行数＝**设备 logcat 留住**的。**`marks` > `mark_rtt` ⇒ adb 那一跳丢了（设备卡顿/超时）；`mark_rtt` > `adapter.log` ⇒ logcat 环形缓冲吞了行。** **别等收窗** |
| 某格会话**跑满 900s 才停**而非按 `q` 结束 | 漏按 `q`，或该格 5 轮确实超时（F2/F4 最可能） | 不作废，但**记下来**：该格末轮可能被上限截断，判读时单独标注；后续同类格把 `--session-seconds` 调高 |
| `rule_matched / events` 显著低于**批内基线**（块 1 首格，见 §1.D） | 节点正则对 14.7.0 漂了 | **不停批**——`ruleMatch` 是标注计数非闸门（§1.D 实据 3）。记录比值，产物照出，回填 spec 留下轮。**⚠ 别拿 D-50 的 26/28 当尺子**（未知 pre-14.4.0 构建 · n=1 · 会话内累计，§1.D 再订正） |
| 通道 D 抓不到豆包流量 | VPN 未授或 App 走了未捕获通道。**⚠ 同上一族**：「App 没发流量」与「我没在抓」输出相同——**先确认 D 在别的格抓到过东西**，再下这个结论 | F3/F4 的上行结论**不得给出**（失去唯一证据源）；F1/F2 的 UI 侧仍可继续 |
| 同一格 5 轮里 UI 读数跨度极大（如 TTFT 相差 >3×） | 可能撞上网络波动或服务端排队 | **如实记录全部 5 轮，不剔除异常值**（登记级本就该记录波动）；README 标注该格「跨度大，n=5 不足以分辨」 |
| 块 1 与块 4 的 WiFi 读数显著不同 | 期间有漂移（②的自检钩子响了） | **条件对照打折**：README 明写「WiFi 两块不一致，条件差与时段差不可分离」——**不要挑一块当代表** |
| 设备弹出任何需要授权的对话框 | 不应发生（本批观察 only、不 `performAction`） | 停止采集，核实是否误触发了注入路径，走 ①B 重核 |


### ④-A 作废之后什么时候重跑（2026-08-29 补——本表四处写「作废重跑」，却从没说**何时**）

**规则：作废的格必须在同一个设备窗内补回；跨窗补跑的格不得并入本批对照。**

**为什么**：§2 的锚格量的是**本批时间窗内**的漂移（块 1 的 F1/WiFi 与末尾同格重跑相比）。
一个隔天、换地点、换 AP 补跑的格，**既不在锚格的跨度里、又会被当成同一批的一格并进对照**——
**条件差与窗间差就此不可分离**，正是 **D-393** 那个形状。**它不会报错，只会让结论悄悄失去依据。**

| 情形 | 怎么办 |
|---|---|
| 某格作废，**窗内还有时间** | 立即补跑，排在**当前块内**（别挪到别的块，块内条件才是纯的） |
| 某格作废，**窗内已来不及** | **不要跨窗补**。该格记 `NOT_EXECUTED` 并写明作废原因；**本批按 11 格出结论**并在 README 点名缺哪一格 |
| **锚格**本身作废 | **锚格不可跨窗补**——它的全部意义是**框住本批的时间跨度**。锚格没了就**如实写「本批无漂移判据」**，所有跨块对照**降一档表述** |
| 作废的格多到**跑不完** | **减范围而不是拖窗**：砍掉整个功能（F5／F6 优先砍，它们是 P1/P2），保住「剩下功能的两条件对照 ＋ 锚格」——**宁可少测一个功能，不要把一批摊到两个窗** |

> **这条与「不要临时改设计」不冲突**：那句禁的是**窗内改判据／改口径**；本条规定的是**范围取舍**，
> 而范围取舍**必须当场做、且写进 README**——事后再说「我们本来打算测六个功能」是没有用的。

### ④-A′ 作废的格补跑**写回哪里**（2026-08-29 补——v3 从 HEAD 源码核出；§④-A 只说了「何时」没说「写哪」，而这一条不定，就与「`t`≠5 作废」判据打架且不收敛。这是「怎么跑一格」(§2) 与「何时补跑」(§④-A) 之间没人认领的第十一个无主项）

**规则：补跑前，先把作废的格目录改名 `<格名>_VOID<k>/`（`k`＝该格第几次作废，从 1 起），补跑写进干净的新 `<格名>/`。作废目录留着不删。**

**为什么必须改名（判据全在 HEAD 源码，可复跑）**：`e234_collect.py` 对同一 `--out` 目录**静默复用**（`e234_common.py` 里搜 `exist_ok=True`；`assert_isolation` 只查 dry-run 标记、**不查目录是否已用过**），且**四个产物开法不一致**——`mark_rtt.jsonl` 是**追加**（`:228` `"a"`），而 `adapter.log`／`screencap_index.jsonl`／`collect_notes.json` 是**覆盖**（`:279 "wb"`／`:359 "w"`／`:380 "w"`），`self.n` 每进程**从 0 重起**（`:227`）。直接补进同一目录撞三个坑：

1. **t 判据不收敛**：判据是「`t` 数 ≠ 5 ⇒ 作废」（上方 §④ 表）。漏按得 3 个 `t` → 作废 → 正确补 5 个 → 文件里合计 **8**（追加）→ 仍 ≠5 → **又该作废** → 再补 → 13。**每补一次离 5 更远**，而操作者现场执行的正是「立刻数 `t` 是不是 5」这一步。
2. **marker 与通道数据不可对齐**：marker 追加（描述「作废＋补跑」两轮），`adapter.log` 覆盖（只剩「补跑」一轮）；8 个 `t` 配 5 轮的 `adapter.log`，判读侧按 `t` 切轮次就是**切在空气上**，且**没有一处会报错**。`self.n` 重置还使同文件内 `n` 重复，任何以 `n` 为键的消费方直接碰撞。
3. **红线自证 ③「t 之和」被两个相反口径共用**：对**额度最小化**，「含作废次的 `t` 之和」是**对的**（那次确实烧了额度）；对**轮数/n**，含作废次是**错的**——同一个数两个相反口径＝D-326「同名不同义比不同名更危险」。

**改名后三个好处一次拿到**：①**作废证据保住**（改名非删；`_VOID*` 目录天然就是「作废发生过」的**显式记录**，正接 ⑤/⑦「没触发也要说没触发」的正面）；②**`t` 判据收敛**（每个 `<格名>/` 只含一轮的 `t`）；③ 红线自证「实际轮数」拆成两个口径**各取各的数**——`额度 = 各格 + 各 _VOID* 的 t 之和`、`轮数/n = 只数各干净格`，不再共用同一个数。

> **不在开窗前改代码**（同 D-578「窗前不修、窗后修」；`e234_collect.py` 本会话已被动过两处，窗前再动不划算）——本条是**纯协议步骤，零代码风险**。窗后再决定要不要把 marker 也改成覆盖。
> **若已按旧形状采过某格**：`n` 从高位跌回 1 处**就是进程边界**，可事后切分——但这是**补救判据，不是设计**。
---

## ⑤ 收窗动作（照根 `CLAUDE.md` 设备实况流程）

1. 停本次启动的一切应用/服务（含 PCAPdroid、a11y 观察）；**并删掉 ①G 量 ROI 时落在仓根的 `roi_idle.raw` / `roi_busy.raw`**——📌 **按 §⓪ 的顺序，量 ROI 排在「开豆包问一句」之后，所以 `roi_busy.raw` 就是一张「回答进行中」的整屏原始像素**，而本批红线是「只计长不读内容」；**一张原始截屏就是内容本身**。（仓库已加 `/*.raw` 忽略规则兜底——**根锚定，不动 `app/**/res/raw/`**——但**忽略不等于删除**，文件仍躺在你的磁盘上。）
2. 恢复本次改动的临时设置（网络条件切回原值、`stayon` 等）；
   > **⚠ 这两条都是「你不用做什么」型断言，它们过期时不会报错——表现只是你「照做了什么都没做」。所以开窗时各花 5 秒复读一次**：`adb shell settings get global stay_on_while_plugged_in` 应为 **`7`**、`adb shell dumpsys deviceidle whitelist` 应含 **`com.aneb.probe`**；**任一不符，「不需要改」这个结论就不成立**，按本节正文处理并记进 README 偏离。（两条 2026-08-29 深夜已复验一次，逐字未变；**这行写的是复验方法，不是又一个会过期的日期戳**。）
   >
   > **⚠ `stay_on_while_plugged_in` 同理（2026-08-29 补）**：实测**现值已是 `7`**，本批**不需要改它**，
   > 因此**收窗时也没有什么要恢复**——本条原文「恢复…`stayon` 等」是**假定它被改过**，而本批的正确做法是
   > **一个字都不动**。若你确实临时改过，才按原值改回，并在 README 记下改前/改后两个值。
   >
   > **⚠ `deviceidle` 白名单：只撤本次加的（2026-08-29 补——①C 要求「已加」，而本节原先从没说要撤）**
   > D-52 的运维沉淀逐字：「**测试期 `deviceidle` 白名单加/撤成对**」。但**本批很可能一条都不用加**——
   > 08-29 锁屏下只读实测，`com.aneb.probe` **已在**白名单（`user,com.aneb.probe,10306`）。
   > 故：**本次没加就不撤**（撤掉是在改别人留下的状态），但**要在 README 记一笔**——
   > 它是**既有残留**，即「加/撤成对」这条纪律**已经失效过一次**的现存实例，
   > 交由属主决定何时清。**别默默撤掉，也别默默当它是本批加的。**
3. 回华为桌面并**立即复验干净**（仅桌面可见但仍有后台/VPN/临时规则时，**不得称干净**）；
4. 产物落 `evidence/doubao_wave0_<日期>/` 并写 README。**落完立刻跑一次 `python scripts/check_evidence.py`（秒级，有违规 exit 1）**——📌 **它是决定这套目录合不合规的那道守卫，而它是 08-29 才落的新守卫**：`doubao_wave0_<YYYYMMDD>` 会被它判为**日期包**（`DATE_PKG_RE = _(20\d{6})(?:_|$)`，2026-08-29 按本批实际落点名验过命中），**日期包必须自带 `README.md`**；它还会读 `evidence/` 下**每个 `*.log` 并要求 UTF-8 可解**（`adapter.log` 是原始 logcat 字节；仓内既有的都过得了，**但若你这份不过，如实记录、不要重新编码去「修」它——那会毁掉原始证据**）。**当场跑掉，别留给下一个人**：他看到红也不知道那窗发生过什么，而你知道。**（窗后有人跑 `verify_all` 时它也会跑一次，`--root evidence` 全树扫；在 `scripts/verify_all.ps1` 里搜 `check_evidence` 或它的 `evidence-rules` 结果名。⚠ 逐条对过：**能咬到本包的只有上面两条**——`STATUS.json` 那三条检查（schema／PASS 有证据／`evidence_files` 存在）**本包不产 `STATUS.json`，全部不适用**；`sha256-manifest.txt` 那条**守卫根本不查**（生成器只覆盖 `phase0`，23 个日期包 0 个有，见本节前文）。**所以窗后 `verify_all` 若因本包变红，只可能是「缺 README」或「某个 `.log` 不是 UTF-8」——先看这两处。**）
   > **README 必填清单（2026-08-29 汇总——此前这些要求散在 6 个节里，照本条原文「格阵实况+每格时间戳+全部偏离」写会漏掉大半）**：
   >
   > | # | 必填项 | 出处 | 漏了会怎样 |
   > |---|---|---|---|
   > | 1 | 批次/条件/格阵/时间窗（**首行**） | §3 战役标识 | 本批**不用 `campaign_id`**，独立批次全靠目录名与这一行认定，漏了验收认不出 |
   > | 2 | 格阵实况 + **每格时间戳** | 本条原文、§2 | 自检钩子与锚格对照都要用它对齐 |
   > | 3 | **每次网络切换的时间戳** | §②-A G-4 | 同上；且块中途切换只能靠它发现 |
   > | 3b | **每格前后各一次的 `gsm.network.type` 原样值**（两个值并列），以及由此判定的整格/格内漂移 | §②-A G-5 | **NR↔LTE 漂移不记就永远看不见**，而它会让「蜂窝」变成两个条件——D-393 的原样重犯 |
   > | 4 | **每功能的提示词逐字** | §②-A G-2 | 工作负载不可复算；锚格是否真同负载无从判 |
   > | 5 | **F3/F4 素材的文件名与字节大小** | §②-A G-2 注 | 上行结论不可复算，且事后补不回来 |
   > | 6 | **UI 栈判定的 `cadence_p50` 读数** | §1.D | 它是开窗第一件事的判据，读数不留＝整批的口径前提无据 |
   > | 7 | **ROI 值与两帧自检的两个均值** | §1.G | 只写 ROI 不写自检结果＝无法证明它没框在静态区 |
   > | 8 | **锚格 vs 块 1 的对照结论**（含「一致＝诚实的否定」那一支） | §2 | 只在有差异时才写＝选择性汇报 |
   > | 9 | **`deviceidle` 白名单是既有残留**这一笔 | §5-2 | 下一个人会误以为是本批加的而去撤它 |
   > | 10 | **全部偏离**（含每格是否跑满、有无被 900s 截断） | 本条原文、§4 | 四态诚实（R-10）；截断格若不标，末轮数据会被当完整轮读 |
   > | 11 | **已装探针的 `versionName` 与 `lastUpdateTime`** | §1.B、§1.D | 让这批语料**可归属到一个具体二进制**。**这正是 D-405 那笔债的正向用法**——当年适配器规格「验于哪个版本」没人记，于是永远不可考；**这次我们记得到，就该记**（记漏了，下一个人会重蹈「给一次没记版本的测量硬安版本号」的坑） |
   > | 11b | **凡在 README 里写下的绝对值（首帧时长、上传耗时、上行速率…），一律带上本窗时间窗**；相对/对照结论不必带 | §3 的「时段边界」表 | **README 是第一批总结句被写下的地方**——那里的措辞会被后续报告原样沿用。**对照类受同窗交替控制故可直述；绝对值只对该时段成立**（§3 已给判读方，此处是给**写 README 的人**的同一条） |
   > | 12 | **复现命令**（本批用的完整 `e234_collect.py` 命令行，含 `--roi` 实测值、`--device-window` 号，**以及最终采用的 `--screencap-period-ms`**——它随机器/USB 链路变，是判读输入） | `evidence/README.md`「目录形态」节 | 该节对战役包的要求逐字是「每个包**自带 README** 说明**批次、口径与复现命令**」——**前两项我原清单已覆盖，复现命令这一项漏了** |
   >
   > **⚠ 另有一条 `evidence/` 规则本批做不到，如实写在这里而不是静默略过**：
   > 该 README 规则 3 要求 `sha256-manifest.txt` **由脚本生成、禁止手动维护**，
   > 且 2026-07-31 那节明写「清单脚本化…**对两者一视同仁**」（阶段证据与战役证据包）。
   > **实况（2026-08-29 实查）**：`phase0/1/2/3` **四个阶段目录全都有** manifest，
   > 而 **23 个战役/日期包里 0 个有**；**生成器存在但只覆盖 `phase0`**（`scripts/verify_all.ps1` 里搜 `sha256-manifest`；其文件头注释也自述 scope＝`evidence/phase0`）——
   > **⚠ 此句 2026-08-29 经 v4 订正**：我原写「全仓没有生成脚本」**不准确**，正确说法是**脚本只覆盖 phase0**；
   > 而 `phase1/2/3` 虽有 manifest，**来源未核**（若系手工则本身违规）。
   > **⚠ 同批我反向订正一处**：转述里说「战役包实为 25 个、你数 23 后期间新增 2」——**不是新增，是两种口径**：
   > `evidence/*_2026*/` 得 **23**（日期命名的战役包），「非 `phaseN` 子目录」得 **25**，
   > 多出的两个是 **`evidence/e1/` 与 `evidence/e234/`**，**首次入库 2026-08-01／08-02**（三四周前）。
   > 且这两个**既非 `phaseN` 也非 `<战役>_<YYYYMMDD>`**——是**规范里没写的第三种目录形态**，一并交治理项。
   > 故**本批同样产不出**。这**不是本批的偏离，是一条写着覆盖战役包、而战役包从未执行过的规则**——
   > **已报大脑，属全仓治理项，不在本批顺手补**（补它要先有脚本，且要回填 23 个既有包才谈得上一致）。
   > **收窗时在 README 里点名这一条**，让缺失是**显式**的，不是看起来「大家都没有所以没问题」。
5. 板面 T78 更新状态与证据路径，出 where-are-we 简报。**⚠ 进展怎么报，有一条现成的规矩，别自己发明**：[`CORPUS_LEDGER.md`](CORPUS_LEDGER.md) 是「进展单一事实源」（SPEC-3 §3.1），它**开头就点名了「豆包先行批」**并立着——**观察通道批次产出 `0` 条 wire run**（产物喂 `validate_results.py` 即 contract VIOLATIONS，**结构上进不了 wire 池**），**「把观察批写成『真实 run +N』正是本台账要拦的那种手抄」**。⇒ **正确说法是「观察通道采集目录 +13」（12 格＋锚格；台账第四节，判据＝目录里有 `RUN_KIND.json`，采集器自动写，你不用管），不是「真实 run +65」。** **65 是你跑的轮数，不是语料条数**——两者差着一整条链。
6. **窗后待办（D-578 裁「窗前不修、窗后修」）**：以**一次 `spec/adapters/doubao.json` + `app/probe/src/main/assets/spec_adapters/doubao.json` 同提交**修两条已成误导的 note——
   ①`send_button.note` 的「待真机 `ADAPTER_EVT` 诊断反推回填」（**D-52 已跑过并证伪**：豆包自定义 View 零 `TYPE_VIEW_CLICKED`；四正则留 null＝**有意保留能力**给标准控件 App）；
   ②`package_note` 的「三条节点规则本就全 `PENDING-VALIDATION`（正则全 null）」（**对本文件为假**：`input_node`=`VALIDATED`、`response_node`=`VALIDATED-PARTIAL`，各带两条正则；该句只对 kimi/tongyi 成立，deepseek 亦部分假——顺带核 deepseek 那份）。
   并定 `send_button` 的 `PENDING` 状态去留，跑 `validate_adapters.py` A3 + `AdapterSpecTest` + `verify_all` 后配 D 号收尾。执行方 v2 或 v3。
   > ⚠ **D-578 原文称本条「已列入协议窗后清单防漏」，实测当时 ⑤ 只有 5 条、并无此条**——那句是意图不是实况，现补齐（已回报大脑）。

7. **红线收窗自证（本条 2026-08-29 补——红线在本文开头声明、在 §1.H 与 §4 里零散执行，而 ⑤ 此前零处确认；「红线／自证／确认未」在收窗段命中数为 0）**：
   **逐条在 README 里给一个「是／否 ＋ 依据」**，**不复述红线正文**（正文在本文开头那行）：
   > | 红线 | 收窗要答的 | 依据从哪来 |
   > |---|---|---|
   > | 观察 only（绝不 `performAction`／绝不代启动 App） | 本窗**全程由人手动驱动豆包**？ | 操作者陈述 ＋ §4「设备弹出需授权对话框」那行**没触发**的记录 |
   > | 不解密不 MITM | 通道 D **只留方向＋字节＋时戳**、**未留载荷**？若曾产完整 pcap，**是否已提取统计并删除原始**？ | §1.H 的三条要求逐条对答 |
   > | **通道 A 的产物内容**（本行 2026-08-29 补——通道 D 有「不留载荷」明文，**通道 A 从来没有对应的一句**） | **别抽查，全量数**——`grep -rhoE "desc=[^ ]+" <该窗目录> \| grep -vx "desc=null" \| wc -l`。**历史基线：全仓 `evidence/` 共 1555 条 `ADAPTER_EVT`，`desc=null` 1555 条、非 null 0 条**（08-29 实测）⇒ **本窗只要出现任何一条非 null，就是从未见过的新现象，停下来看它是控件标签还是消息内容，别自己判「像是标签」就放过**。⚠ **这一行不是假设**：已实测装机构建 `pkgFlags=[ DEBUGGABLE … ]`，而源码注释写明该行**刻意用 `Log.i` 而非 `Log.d`**（华为 EMUI 丢弃 D 级），**采集器的 `-s AnebProbe:I` 正好抓 INFO** ⇒ **`desc=` 一定会进 `adapter.log`，进而进 `evidence/`**。（检测命令 08-29 双向验过：造一条非 null 反例报 **1**，跑真实 `evidence/` 报 **0**。） | 见下方实证段 |
   > | 额度与频次最小化 | **实际跑了多少轮**（含**前置的 UI 栈判定／量 ROI 各约 1 轮**、试水 3 轮与重跑），与计划 **≈69–70 轮**（正式批 65 ＋ 前置约 5）的差额及原因 | 各格 ＋ 各 `_VOID*` 的 `mark_rtt.jsonl` `t` 计数之和（额度**含**作废次故对；**轮数/n 只数干净格**，两口径分列见 §④-A′） |
   >
   > **为什么必须显式答，而不是「没出事就算合规」**：**红线是本树对外的承诺，而证据包是唯一对外可查的东西**——
   > 不自证，合规就只存在于当事人的记忆里。这与本节下一条要求「**没触发也要说没触发**」是同一条纪律：
   > **「没人报告违规」不等于「已核对无违规」**。
   >
   > **通道 A 到底落进什么（2026-08-29 读源码＋读设备得出，非推断）**：
   > `adapter.log` 收 `AnebProbe:I`，其中两类行——
   > **①`ADAPTER_OBS`**：纯计数与时长，**无任何文本字段**（13 字段见 §1.D）。
   > **②`ADAPTER_EVT`**：`type=<t> cls=<c> desc=<d> txt_len=<n> pkg=<p> t_boot_ns=<ns>`，`BuildConfig.DEBUG` 门控——
   > **而设备实测 `flags=[ DEBUGGABLE … ]`，即本批会打这行**。逐字段看：
   > **`txt_len` 只计长度不读内容**（源码注释逐字：「**只计长不读内容——文本红线不变**」）✅；
   > **`desc` 是 `contentDescription`，截断上限 `EVT_LOG_DESC_MAX = 40` 字符** ⚠。
   > 故**本批产物会含豆包 UI 元素的无障碍标签（每条不超过 40 字符）**。
   > **这不是违规**——是 a11y 元数据，不是解密流量、不是用户输入、不是模型回答正文；
   > **但它此前从没被写出来过**，而「产物里有什么」正是证据包对外时第一个被问的问题。
   > **所以要求的是抽查＋披露，不是禁止**：README 里写明「通道 A 含 `desc` 字段（a11y 标签，不超过 40 字符）」，
   > 抽查若发现超出控件标签范畴的内容，**照记并在交接里点名**。
   > **若某条答「否」或「不确定」**：**照答不改**，并在 README 与给判读侧的交接里点名——
   > **一条如实的「不确定」，比一句想当然的「合规」有用得多**。

8. **交接给判读侧（本条 2026-08-29 补——此前 ⑤ 只有「板面更新＋简报」，那是广播不是定向交接；`v3` 在本文出现十余次却全是上下文引用，没有一条说「采完之后谁通知谁、带哪些限定」）**：
   收窗后**直接知会分析层属主（v3）**，消息里**只给四样，不复述限定**（限定条目已在下列各处，重列就是造第二真相源）：
   > | 给什么 | 指向哪 | 为什么不能省 |
   > |---|---|---|
   > | ① 语料路径 | `evidence/doubao_wave0_<YYYYMMDD>/` | 不给路径它无从开始 |
   > | ② README 路径 | 同目录 | **本批全部实况与偏离都在里面**（必填清单逐项） |
   > | ③ 判读口径入口 | 本文 **§3** | 含「本批默认只用 `e2_analyze`、`e3`/`e4` 待一格实语料验」与**核心量「投影已修（`4441ea6`）、聚合未做」**那条（判据可复跑，见 §3 该表——指判据不指结论，§2.18） |
   > | ④ **中止判据的实际触发记录** | 本文 **§4** 里当天真触发过的那几行 | **没触发也要说「没触发」**——否则判读方无法区分「没发生」与「没记录」 |
   >
   > **⚠ 特别点名两条最容易被判读方误用的**（**只点名、不复述，详见 README 与 §1**）：
   > **(a)** `rule_matched` 的基线是**批内首格**、**不是** D-50 的历史读数；
   > **(b)** 蜂窝各格的制式是**前后两读**，判读须按「整格／格内漂移」分类，且措辞不得写成「全程 NR」。

---

*T78 先行批采集操作协议 · 2026-08-29 · 开窗前逐条核 ①，开窗后照 ② 跑，异常查 ④，收窗照 ⑤*
