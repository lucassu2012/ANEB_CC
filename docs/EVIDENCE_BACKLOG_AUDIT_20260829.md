# 历史证据区陈债考古 — 三批未入库/未提交项处置建议（T86）

> **属主**：老 v2（会话 `cd5239ba`）· 2026-08-29 · **大脑派件 T86**（协调侧 M-B-001，B-003 例外类）
> **性质**：**纯只读考古 + 建议**。本文**不动任何被审文件**（不 commit、不删、不改 `.gitignore`）——**处置权在大脑，不在本单**。
> **方法**：`git status` / `git log` / `git diff --numstat` / `git ls-files` / `ls -la mtime`，全程只读。凡未能亲验的写「未核」不圆场（D-311）。
> **状态词**：入库归档 / 清理 / 登记豁免（＝加 `.gitignore` 并在此登记，让「缺失是显式的」）。

---

## 0. 一页结论（三批 × 三分建议）

| 批 | 是什么 | 量 | 建议处置 | 一句话理由 |
|---|---|---|---|---|
| **①** `evidence/nr_timeline_20260802/` 三个**已跟踪文件的未提交追加** | NR 战役监控日志（append-only） | +66 / +65 / **+10544** 行 | **入库归档**（提交追加） | 真实监控证据、纯追加、与已提交的战役目录同源；⚠ rat_watch 实际跨 08-02→08-21，非「08-02 尾巴」 |
| **②** `evidence/phase0/` **12 份未跟踪** `verify_all_*.log` | 每次门跑的一次性日志 | 12 份，08-02..08-29 | **登记豁免**（gitignore `verify_all_*.log`） | 权威证据由**已跟踪**的 `STATUS.json`/`badges.txt` **点名特定日志**承载（07-12 与 08-29-214430 两条实测**均已跟踪**）；这 12 份**无人引用**、是本地逐跑产物 |
| **③** `.db-shm`/`.db-wal` 8 份 + `voice30_aneb-probe.db` | SQLite 临时边车 + 112MB 全库 | 边车 8＋全库 1 | **清理＋登记豁免**（gitignore `*.db-shm`/`*.db-wal`＋该全库） | 边车是**已跟踪 `.db` 的临时文件、绝不可入库**（会在下次开库时损坏 `.db`）；全库 112MB 超 GitHub 限、已**刻意不入库**（入库的是单表抽取 `voice30_voice_result_only.db`） |

> **⚠ 共性根因（本文最要紧的一条，见 §4）**：`.gitignore` **对这三类临时产物零覆盖**——所以它们**永久出现在 `git status`**、并**易被并发会话的提交顺手卷入**（本会话即亲历：我的未提交编辑被他窗 `a33fc5d` 连同卷走）。**一条 gitignore 策略同时解决三批的复发**。

---

## 1. 批① `evidence/nr_timeline_20260802/`（已跟踪文件的未提交追加）

**现状（`git status --porcelain`）**：三个 `M`（已跟踪、工作区改动）——
`nr_batch_brain_20260802.log`（+66/-0，mtime 08-03 11:44）、`nr_pounce_log.txt`（+65/-0，mtime 08-04 09:40）、`rat_watch.tsv`（**+10544/-0**，mtime 08-21 01:16）。

**归属考古**：
- 目录最近提交＝`f7e818f`（D-460 T42 wave-0 首格）/`f9984e0`（T42 DW-20260804-01）/`19ff4fb`（D-466 T46 报告）——即 **08-02 NR 战役 + T42/T46 系**的证据目录，早已入库。
- 三个文件的改动**全是 append-only**（`numstat` 全 `+N/-0`；`rat_watch.tsv` diff＝`@@ -1099,3 +1099,10544 @@` 纯尾追）。
- **内容跨度（实测首尾行）**：`nr_pounce` 末行＝`2026-08-04T09:40:44 NR pounce finished (pounces=2)`（08-04 停）；`rat_watch` 追加**首行 08-02T13:00、末行 08-21T01:16（NR_SA）**——**是跨 08-02→08-21 的常驻 RAT 监控，不是单纯「08-02 战役尾巴」**（派件此处假设需订正）。

**建议＝入库归档**（提交这三段追加）：
- 纯追加的真实监控证据，与已提交的同目录同源、同 append-only 惯例（历史证据 append-only 是本区惯例）。
- ⚠ **两点交大脑定**：(a) `rat_watch.tsv` +10544 行较大且跨 19 天，若视为「常驻监控的临时流水」而非「战役定格证据」，可改判**登记豁免**；(b) 这三个是**已跟踪文件**，「处置」实为「提交追加 or 还原」——**提交者应是该监控的属主（大脑/nr 自动化线），非本单**（我只读、不提交）。

---

## 2. 批② `evidence/phase0/` 12 份未跟踪 `verify_all_*.log`

**现状**：12 份 `??`——`verify_all_20260802-011607 / -012809 / 20260819-131221 / 20260820-141845 / -155300 / -155740 / -160055 / -202053 / -203710 / 20260822-003456 / 20260828-231718 / 20260829-213741.log`。
（派件写「11 份」；复查为 **12**——`20260829-213741.log` 系派件后新增一跑，属正常漂移。）

**归属考古**：
- `evidence/phase0/` 是**已跟踪的证据目录**（`STATUS.json`/`badges.txt`/`build_*.log`/`c11/c14_*.log` 等均在册）。
- **权威证据靠「点名特定日志」而非「收全部逐跑日志」**：`STATUS.json` 的 `evidence_files` 点名 `verify_all_20260712-103217.log`（07-12）；`badges.txt` 头 `gate_count_source＝verify_all_20260829-214430.log`。**这两条被点名的日志实测均已 TRACKED**（无悬空引用）。
- ⇒ 这 12 份未跟踪日志**不在任何跟踪证据的引用集里**，是**门每跑一次就落一份**的一次性本地产物。

**建议＝登记豁免**（`.gitignore` 加 `evidence/phase0/verify_all_*.log`）：
- 每跑一份、无人引用、可无限增生——正是该被 ignore 的本地产物；durable 信号已由**已跟踪的** `STATUS.json`/`badges.txt` ＋ 它们**显式点名**的少数日志承载。
- **配套策略**：需要留证的某一跑，走 `git add -f <该 log>` 显式入库并写进 `STATUS.json`/`badges.txt`（现行做法即如此）。
- 备选＝**清理**（删这 12 份）：更干净但丢历史；豁免保留本地、只停 `git status` 噪音，更稳。**二选一交大脑**。

---

## 3. 批③ `.db-shm`/`.db-wal`（8 份）＋ `voice30_aneb-probe.db`

**现状**：`acceptance_20260820/` 2 份、`phase3/realdevice_data/` 6 份 `.db-shm`/`.db-wal`，外加未跟踪的 `voice30_aneb-probe.db`（及其两边车）。

**归属考古**：
- 三个父 `.db`（`aneb-probe.db`、`aneb-probe-cellular.db`、`acceptance_pull_aneb-probe.db`）**均已 TRACKED**。
- `.db-shm`（32768B，**mtime 08-29 21:44**）＋ `.db-wal`（多为 0B，已 checkpoint）＝ **SQLite 的 WAL＋共享内存边车**。mtime 是**今天 21:44**——说明**刚有进程打开过这些已跟踪的库**（分析/门跑/有人开库）。
- `voice30_aneb-probe.db`：从台账＝**112MB 全库、超 GitHub 单文件限、刻意不入库**；入库的是单表抽取 `voice30_voice_result_only.db`（回核级 M7 语料）。

**建议＝清理 ＋ 登记豁免**：
- `.db-shm`/`.db-wal`：**`.gitignore` 加 `*.db-shm`、`*.db-wal`**，并**清理**（删）。⚠ **两条硬约束**：
  1. **绝不入库**——把边车随已跟踪 `.db` 提交，会在下次 SQLite 开库时因 WAL/shm 与已提交 `.db` 不一致而**损坏 `.db`**。
  2. **删除时机**：`.db-shm` 非零＋mtime 今天，**可能有进程正持库**；对**正被打开的库**删边车不安全——**先 gitignore（即刻止噪），删待无进程持库时**（或由持库方 checkpoint 后删）。
- `voice30_aneb-probe.db`：**登记豁免**（`.gitignore` 加该路径）——超限、刻意不入库，已有单表抽取代它入库；此处登记让「它本地在、仓里没有」是**显式**的。

---

## 4. 共性根因与一条策略建议（交大脑裁）

三批的复发都源于**一件事**：**`.gitignore` 对本仓的临时产物零覆盖**（实测无 `db-shm`/`db-wal`/`verify_all`/`*.log` 任何模式）。后果有二：①它们**永久出现在 `git status`**，每个会话都要重新考古「这些是什么、该不该动」（本文即是）；②它们**易被并发会话的提交顺手卷入**——本会话亲历：我对 T78 协议的未提交编辑被他窗 `a33fc5d` **连同卷走**（内容对、归属静默），SQLite 边车若被同样卷入则会**损坏库**。

**建议的 `.gitignore` 追加（供大脑裁，本单不代改——`.gitignore` 属共享文件、在本单可写域外）**：
```gitignore
# SQLite 临时边车——绝不入库（随已跟踪 .db 提交会损坏库）
*.db-shm
*.db-wal
# 门跑逐份日志——durable 信号在 STATUS.json/badges.txt 点名的少数日志；留证走 git add -f
evidence/phase0/verify_all_*.log
# 超 GitHub 限的全库（入库的是单表抽取 voice30_voice_result_only.db）
evidence/phase3/realdevice_data/voice30_aneb-probe.db
```

---

## 5. 本文的诚实边界（未核/待大脑定）

1. **批①三文件是「已跟踪＋未提交追加」**，其「处置」本质是「提交 or 还原」——**提交动作应由该监控线属主做，非本单**；本文只判「追加是真实证据、宜入库」，不代提交。
2. **rat_watch 跨 08-21** 与派件「08-02 尾巴」的假设不符——已订正；「战役定格 vs 常驻流水」的定性交大脑（影响 ①是入库还是豁免）。
3. **批② 12 vs 派件 11**＝派件后新增一跑，非矛盾。
4. **`.db-shm` mtime 今天 21:44** 提示可能有进程正持库——**删边车前须确认无进程持库**（本文只读，未探进程持库状态）。
5. 本文**未改任何被审文件、未改 `.gitignore`、未提交/删除任何项**——全部为建议，处置权在大脑（T86 边界）。

---

*T86 历史证据区陈债考古 · 2026-08-29 · 老 v2（cd5239ba）· 只读＋建议，处置权在大脑*
