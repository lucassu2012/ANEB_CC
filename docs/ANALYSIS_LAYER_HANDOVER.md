# 分析层交接：设计原则、测试策略、当前状态

> **给谁**：接手 `scripts/` 分析层的人（换人，或未来的自己）。
> **为什么需要**：这一层的设计原则散落在 `DECISION_LOG.md` 里（本层自 D-87 起的一长串），
> 逐条读要很久；而这些原则**不是风格偏好，是这层存在的理由**——违反其中任何一条，
> 报告就会开始说谎，而且是那种看起来很正常的谎。
> **配套**：`scripts/README.md`（逐工具口径）、`M2_CAMPAIGN_RUNBOOK.md`（外场操作）、
> `M2_REPORT_TEMPLATE.md`（交付物形态）。

---

## 1. 这一层是什么

把 ANEB 探针产出的 **result-run JSONL** 变成 M2 交付物《城市 AI 业务网络体验热力卡与归因
报告》。纯 Python 标准库、无第三方依赖、锁无关、设备无关——**任何人在任何机器上都能跑**。

接入 `verify_all.ps1` 的 `campaign-analysis-unit` 门。
（**工具数与 golden 条数都不写在这里**——以 `scripts/README.md` 的逐工具小节和
`run_all.py` 的实际输出为准。上一版这里写着「21 个工具」，D-356 加进
`round_effect.py` 之后无人改它：**写死的计数正是本层反复治理的那种文档漂移，
而这一处恰好漂在「不要写死计数」那句话的上一行**，2026-07-31 实测已对不上。）

---

## 2. 不可违反的原则

这些是**红线**，不是建议。**凡约束代码行为的条目都有 golden 守着**，改动前请先确认自己不是在拆守卫。
（**范围声明**——按 §2.17 自己的规矩：§2.17 是**写法类**红线，约束的是文档与注释怎么写，**没有也无法有 golden**；它靠评审与本节的自引用维持，不要据这句话推断它有守卫。）

### 2.1 不可计算 ≠ 零（R-10）
缺测量的量输出 `None`、渲染成 `—`，**绝不**以 0 或默认值顶替。一个 0 会被读成"测了，结果是 0"，
而真相是"没测到"。同理：空 buffering 块 = 未检测（不是"无批化"）、缺 `attribution` = `unknown`
（不是 `none`）、样本不足 = `low_confidence` 显式标注（不是悄悄呈现）。

### 2.2 "没法查" ≠ "查过了"
schema 不可读、无 `order_index` 证据、无 clock 标注——一律记 WARN / NOT_EXECUTED 并写明，
**绝不**记 PASS。`publish_check` 的 WARN 永不自动升格为 PASS；`coverage_matrix` 在 `--config`
未能给出**完整**目标网格（**任一维度为空**即算）时**硬错误退出**，而不是悄悄降级为描述模式
（D-119 是全空的情形，D-173 是只错一个键名的情形——后者更常见，也更难看出来）。

**"没法查"还得再分两种，而这一刀切在对象上、不切在证据上**：**有对象而缺证据**（有格但无
`server_tier_endpoint`、有块但字段全空）→ **WARN**，须由人解释；**连对象都没有**（单战役谈不上
前后效应量、无同格双介质可比谈不上介质差异）→ **N/A，未作判断**。**两者都绝不记 PASS。**
这条曾只在三个检查项上做到（D-163/D-198/D-170），其余 8 条分支把"没得核"印成绿勾，
最露骨的一条是「无同格双介质可比，**或**蜂窝不劣于 wifi」——**一条 PASS 兼表"核过了、干净"
与"从没核过"**，21 份语料里出现 15 次、其中 **12 次实为无可比对象**（D-229）。
配套要求：**N/A 行的字面与机器判定互为充要**（本层的实现是说明里一律含「未核算」，
并由守卫断言双条件），否则读者看到的和工具认定的会各走各的。

### 2.3 报告不可比性，绝不静默丢格
同格混 profile 版本 / 混直方图边界 / 混 quick-forensic → 标 `MIXED_*` 并**照常出数**；
缺层级 → 记 `TIER_MISSING` **不外推**；负增量 → 记 `inversion` **不归零**。
删掉一格比标注它更危险：读者不会追问看不见的东西。

### 2.4 绝不静默截断
显示上限必须**声明**省略了多少、去哪看全量（稳定性段就是这样）；且只折叠"无信号"的行——
超门与不可计算的行**永不省略**。为报告设的上限也不该套到独立 CLI 上（D-130）。

### 2.5 转义而非拒收
人工输入的标签含 `|`、换行、HTML——**转义后如实显示**。若某点位真叫 `SZ|CBD`，
报告应显示它，而不是丢弃或崩溃。（转义只属于渲染层；CSV 与分组键用原值。）

### 2.6 三个输出面必须同时改
报告有 **markdown / HTML / CSV** 三个输出面。同一个缺陷曾在三面**各犯一次**
（D-135→140→141：跨战役混池的中位数在 md 有标记、HTML 全缺、CSV 零标记且
`low_confidence=False` 暗示可信）。**CSV 是最要紧的一面**——分析员在它上面做计算，
而它只有列、没有横幅可看。加任何信息时：**核对三面，而不是只看自己刚编辑的那个**。
跨面共享的文字用单一事实源函数（如 `corpus_warnings()`），不要靠文本抽取搬运。

**「同时改」还要问「比的是什么」**：此层的跨面守卫长期只比**标记与段落在不在**，
**没有一条比过数字**（D-302）。三件事因此各自躲了很久：①页面与导出的数值从没被对过账
（实测热力卡 32 格、分 KPI 128 格两侧全等——诚实的否定，但此前无人核对；补上过账守卫后，
谁给一面加过滤都会被咬住）；②**合成数据警告只印在有横幅的两个面上**，13 张 CSV 里两张
一个字的证据都没有，其余各表的 `SYNTH-` 字样纯属点位/战役 id 恰好带前缀（D-303）；
③**一个模块渲染两张表、只导出一张**，而按「模块」计数的守卫照样答「有」——无线的忙闲
可比性判读与 validity 的按日趋势就这样只活在页面上（D-304）。**判据要落到「每一张表」
与「每一个数」，不是「每一个模块」。**

### 2.7 虚构数字必须不可混淆
彩排语料带**双重标记**（加性 `synthetic` 块 + `SYNTH-` 前缀），任缺其一仍可检出——
改标签洗不掉、删块也洗不掉；报告在标题正下方印红色警告，`publish_check` 直接 FAIL 阻断。
真实语料**零横幅**（狼来了会训练读者忽略警告）。

### 2.8 给出差值，就要给出"多大才算数"的尺度
只报 Δ 不报噪声量级，等于邀请读者把复测抖动当成效果（+3 分看起来像发现，而该格 CV 可能就有 5%）。
前后对比给指示性噪声尺度并标 `噪声内`；噪声不可估的格是**第三态**，既不是"噪声内"也不是
"真实变化"。同理，采集期要能反过来问"当前 n 能分辨多小的差异"（`stability --plan`）。

### 2.9 沉默的检查项等于没有检查项
新增检查时容易只想"触发时说什么"，忘了"不触发时是否还看得见"。`publish_check` 的每一项
在**任何语料形态下都必须出行**（不适用就写"不适用"），否则扫清单的人分不清"没问题"和
"工具漏了"。已有不变量测试钉住"各形态下检查项集合相同"。

### 2.10 "未检出异常" ≠ "证明相同"
筛查没报异常，只说明没有单元越过阈值，不代表各单元一致——满网格实测 MAD/典型 ≈ 29%。
判读措辞只能陈述**做过的检查**，并把离散度一并给出，让读者自己判断。

### 2.11 无歧义的替使用者清理，有歧义的连证据一起交回去
标签首尾空白无合法含义且在渲染中不可见 → 直接规范化；大小写/全角**可能**是有意区分 →
只检出并明写"**这不是自动合并的**"，给出两条出路。与 2.5「转义而非拒收」是同一原则的两面。

### 2.12 会说谎的守卫比没有守卫更危险
一次六透镜对抗审计的 8 条高危发现里，**6 条不是"缺检查"，而是检查给出了反向或虚假的结论**：
否决语义写反会把外场排查引向反方向；前后配对按字母序把改善印成回退；标签全塌成一格照样 PASS；
`started_at_epoch_ms` 写成秒仍照常排序，而结论栏还写着"按时间排的"。
**没有守卫时人会自己存疑；守卫说了话，人就不查了**——所以审查守卫要问的不是"有没有检查"，
而是**"这个检查在它失败的那一刻，会说出什么"**。

三个已反复出现的机械成因，值得逐一对照：

1. **守卫的判据与消费方的判据不是同一个**。`coverage_matrix` 的硬错误判「任一维度非空」，
   而 `analyze()` 要的是「每一维都非空」——网格是三维的积，少一维就是空的，于是**只写错一个
   键名**的配置穿过硬错误、落进描述模式，横幅还印「未声明目标网格」（D-173）。
   **判据要么共用一个函数，要么在两处都写明彼此的关系。**
2. **同一份文字在多处各维护一份**。D-155/157/158 的标记在 markdown 出现数十次、
   HTML 与 CSV **各 0 次**，因为三个渲染器各有一份标记清单（D-160）。
   修法是**单一事实源函数**（`incomparability_flags()` / `premise_notes()` / `heatcard_notes()`），
   不是逐处补——见 2.6。
3. **字段的取值范围从没被核对过**。可用不等于可信：一个数量级写错的时间戳照样排序，
   只是排出来的先后是错的（D-176）。**凡是参与排序、分档、差分的字段，都要问一句"什么样的
   取值是不可能的"。**
4. **一处横幅替所有下游作了承诺，而承诺从没逐个核对过**。语料横幅与发布自检都写着
   「不可能的取值已排除出中位数，受影响的格标 `IMPLAUSIBLE_VALUE`」——这句话此前只在
   **七个池化段落里的两个**成立（D-197）。代价不是漏了一句提醒，而是**那句提醒本身成了假话**：
   读者看到横幅就不会再怀疑下面的表。它与前三条的区别在于**看起来已经修好了**——
   守卫在、文字在、测试全绿，缺的只是"谁在兑现它"这一次点名。
   **凡是替别人作出的承诺，都要有一条逐消费方的核对**（`_POOLING_SECTIONS` 即为此而设）。
   同族的一个变体：`buffering_rollup` 算出了标记却没有任何渲染器读它——
   **输出到不了任何一个面的守卫，不是守卫**。
5. **被归档为"决定结论"的东西，本身没被核对过它是否真的决定结论**。溯源段印着
   「**生效门限**（改动其一即改变报告结论，复现须同值）」，而 `segment_outlier_k` 归档时
   **早已不可达**——它是我在 D-200 里为"让 manifest 保持一个稳定键名"专门留下的（D-204）。
   **一份掺了死条目的清单，会教读者把整张清单当摆设。** 两条配套规矩：
   **①凡归档为门限者必须活读**（写成默认参数会在 `def` 时绑定，归档值与实际用值可以分家）；
   **②每条归档门限都要有一次扰动证明它真的改变报告**——与「决定输出的门限必须全部归档」
   （D-198）互为对偶，两条合起来才是 `归档 ⇔ 决定输出`。
6. **读者做的第一个动作，恰好是报告最没准备好的那个**。他不会通读，他会**拿数字去对判据、
   拿清单去找最严重的、照着「见 X」去翻、把两列一减看对不对得上**——而这四件事各自都出过错：
   84.96 印成 `85` 却标 `good`（D-207）、「前三个」列出的是最轻的三个（D-208）、
   「见 `CV 不可计算` 一列」指向一个不存在的列（D-212）、before/after/Δ 三个数减不平（D-207）。
   共同形状是**同一件事由两套口径产生**：显示与判定、排序与严重度、指路与被指之物、
   各自舍入的三个数。**凡两个东西并排出现且读者会把它们对起来看，就必须由同一条线算出。**
   还有两条相关的：**「未知/无标签」桶一旦进入有序集合就会获得它没有的语义**
   （被当战役排进时序 D-210、被当地点排进最差榜 D-211）；
   **凡出现「X/Y」，都要能说清 Y 怎么来的、被排除的去了哪里**（D-209）。

7. **一处图例替下面每一行作了承诺，而其中一部分行根本没跑**。`publish_check` 的表头写着
   `PASS=该项无问题`，220 个 PASS 行里 29 个（13%）其实是"这项检查连对象都没有"——
   读者对这张表的第一个动作是扫图标，绿勾就是他读到的全部（D-229，详见 §2.2）。
   与第 5 条同源：**凡一句总述覆盖多行，就要逐行核对它对每一行都成立**。

**这一条的另一半**：本层曾在连续两轮里**自己引入**其中两条——快速迭代时最容易违反自己刚立的
规则。所以修法优先选**结构性消除**（让错误不可能被表达），而不是在出问题的地方各打一个补丁。

---

### 2.13 输出侧也是前门
前门加固做过五轮，**全在输入侧**（空语料、坏行、glob 不匹配、人手写的网格配置）。
输出侧一次都没做过，而外场收工最常见的手误恰恰是**输出路径打错**：四个输出旗标遇到
不存在的目录，四条**全部抛 Python 栈回溯**（D-306）。**它比输入侧的失败更难收拾**——
md 先于 html 先于 csv 写出，`--md ok.md --csv nope/c` 会**先落一份 markdown 再崩**，
操作者拿到半套交付物，而栈回溯里看不出磁盘上到底有什么。规矩：**能事先枚举的目的地问题，
在读语料之前就拒绝**（退出码与用法错误同档，且**一个文件都不产出**）；判定不了的
（目录在但写不进）如实留给写入时报错，不要拿探针文件去试。

另一半是**策略要被决定，而不是默认**（D-307）：命名**目录**的旗标（`--out-dir`）可以自建，
命名**文件或前缀**的旗标不可以——替一个打错的父目录建目录，等于把交付物撒到没人会去找的
地方。两种策略各有守卫钉住，免得日后有人「统一」成一种。

### 2.14 同一个东西写在几处，就必须有东西逼它们一致
本层有意保留了几份**独立实现**：`analyze_results.py` 与 `dashboard.py` 的 docstring 都明示
「stdlib only」，且**全仓无人 import 它们**——正因如此，它们各自带着一份 `fnum` 和
`load_records`。这个取舍**不改**（让它们依赖 `campaign_common` 会改掉它们被写进 docstring
的性质），但代价是真实的：**修复只落在被触发的那一份上**。

实测两次。`campaign_common.fnum` 在 D-148 学会拒绝 NaN/±Infinity，另两份**都没有**：一个
`nan` 让 per-run 摘要打印出 median=nan、n=20（20 个样本的中位数被其中一个毁掉，exit 0
无告警，D-314），并**印进了 dashboard 的交付页面**（D-315）。`cc.load_records` 按
`run.run_id` 去重是因为重复计数会「silently INFLATING apparent confidence」，另两份也都
没有：同一个文件在命令行**列两次**（重叠通配符、或 D-09 双写文件），20 条语料变
`records=40`，页面上每个 n 翻倍，两个面都不吭声（D-315）。

规矩：**副本可以留，分叉不可以**。每一组同名实现都要有一条守卫，拿**同一批输入**问它们
要**同一个答案**——比在旁边写句注释硬得多，下次只修一处会当场失败。两条落地要点：
①**按行为钉，不按实现钉**（三个 loader 只需在「首个 `run_id` 重复的语料」上返回同一个数，
各自怎么写不管）；②**比较方式本身别说谎**——`NaN != NaN`，用相等判断会让两个 NaN
悄悄通过，改用 `repr` 比对（承 §2.12）。

找副本的手法是**按名字分组，不要顺着 import 图找**：没有依赖边的副本正是漏网的那一种。
把 `scripts/*.py` 的顶层 `def` 按名分组，七组重名里三组是诚实的否定（签名不同的重名、
只转调的薄壳、逐字相同的 `html.escape` 包装），两组是真缺陷。

**一致不止于行为，还包括它们交回来的字段叫什么、是什么意思**。三个 loader 都回填一份计数
字典，而 `campaign_common` 计 `lines`/`kept`/`duplicates`、两只 per-run 曾计 `read`/`dropped`；
消费方用后者的名字去读前者的字典，`.get(key, 0)` 把「键名写错」伪装成「值为 0」，
在一份三行语料上印出「读 2 行」（D-325）。规矩两条：**跨模块读别人产出的字典，用会抛异常的
取值**，别让默认值替你圆场；**统一命名时要重新计数而不是贴标签**——`read` 只数解析成功的记录，
`lines` 含坏行，**同名不同义比不同名更危险**。宁可让键集是对方的**子集**（per-run 不捕
`OSError`，就不该有 `unreadable_files`），也不要补一个恒为 0、谎报能力的键（D-326）。

### 2.15 一个汇池出来的数，必须交代它汇了谁、每份多大

**这一条是真实语料教出来的**（D-360/D-361，2026-07-31 第一天实采即中两处），
它比前十四条更晚被发现，原因正是 §3 那句话的下一个版本：合成语料**每格样本数整齐**，
于是"汇了谁""每份多大"在彩排里从来无关紧要，一到外场就同时失效。两个形状：

**①「中位数」谁都不代表。** 分 KPI 热力卡按 (点位, 运营商, 时段) 汇池，把一格里
**三个 profile 的所有场景值倒进同一个桶**——而它们测的不是同一件事：实测
`u1_goodput_mbps` 在 `s1_chat` 与 `s3_agent` 之间差出数倍，汇池中位落在两者之间，
**没有任何一个 profile 长这样**。读者拿它去对某个业务，对不上。
规矩：**印一个汇池统计量，就要在同一行交代汇池维度与各份的跨度**——
现已由 `profile_span_text()` 与 `KPI_HEAT_SPAN_CAVEAT` 供 markdown / HTML / CSV 三面共读。

**②测过一次的单元与测过十一次的等权。** 跨单元异常筛查用 MAD 比较各单元中位，
本轮 6 个参与单元里 **3 个是 `n=1`**：它们既把阈值拉动，也可能自己被点名，
而报告对此**只字未提**。规矩：**凡把若干单元并排比较，就要让「每个单元有多少样本」
与结论同屏**——被点名的离群单元若自身样本不足，名字后要带 `*` 并在正文说明
（`segment_profile.low_conf_cells` + `_seg_outliers_text()`，D-361）。

**合起来是一句话**：一个数被"合并"或"比较"过之后，**合并的口径和每一份的分量
必须跟着这个数一起走到读者面前**，否则它看起来和一个可信的数一模一样。

### 2.16 一道守卫"绿"之前，先证明它**被执行了**

**这一条是一天之内踩中六次教出来的**（D-394，2026-08-01；其中一次直接写进交付物的
头条结论并**通过了大脑验收**）。它与 §2.12「会说谎的守卫」是同一族的上游：
§2.12 说的是守卫给出**反向或虚假的结论**，这一条说的是守卫**根本没有运行**、
或者**你用来看它的那个量法本身坏了**——而两者的表现完全一样：**不报错、不返回空、
不留痕迹，只给出一个看起来完整的答案。**

**六个实例，每个都曾污染结论**：

| # | 量法 | 它安静地做了什么 |
|---|---|---|
| ① | `Select-String -Context N,M` | 在第 N+M 行**截断**多点锚曲线，于是「该锚只有两个点」写进了头条算术——实为五个点 |
| ② | `Get-Content`（无 `-Encoding`） | 按 GBK 解 UTF-8，459 行中文文件读成 252 行 |
| ③ | 交替式 grep `a\|b` | 让 `b` 的命中**冒充** `a` 的命中：报「8 份命中」，实为 `a` **0 份** |
| ④ | `[System.IO.*]` + PowerShell `cd` | `cd` 不改 .NET 当前目录 → 突变写到错路径、**突变根本没发生**，脚本照报「还原核验 True」 |
| ⑤ | `gradlew :probe:testDebugUnitTest` | 只有**模块外**文件变化时判 `UP-TO-DATE` **整个跳过**，三处 spec 突变全存活 |
| ⑥ | `dumpsys window \| grep mCurrentFocus` | 屏灭锁屏时**恒报** `NotificationShade`，被读成「用户把通知栏拉下来了」 |

**⑥ 还带一条独立的教训**：同一个窗口 ID 横跨四次预检与收尾、**十小时不变**——
**一个不动的 ID 本身就说明它不是真实焦点**，而前四次都没把它当线索读。
**凡一个"状态量"在你预期它会变的场合纹丝不动，那是量法坏了的信号，不是状态稳定的证据。**

**规矩三条**：
1. **凡守卫报绿，先造一个它必须逮住的反例**——不是推理"它应该能逮住"。
   ⑤ 就是这样暴露的：三处突变全存活，才发现任务被 `UP-TO-DATE` 跳过了。
2. **凡"读出来的东西"要当证据用，先问它会不会静默截断/合并/跳过/漏配**，
   而不是先看结果合不合理——**合理正是它们的伪装**。
3. **换一种够得到的量法交叉核实**（§2.2 的下一步）。⑥ 就是靠 `mFocusedApp` +
   `Recent #0` + `screenState` 三个独立读数当场推翻的；而此前为该现象编的
   "华为 ROM 收起面板是动画、抓早了"，是**套在坏量法上的一个合理故事**。

**合起来是一句话**：**"我查过了"这句话的有效期，止于"我证明过那次查真的发生了"。**

**追加两例（2026-08-02，D-415 对抗核验）——不并入上表改写成"八个实例"，单独记，理由见下**：

| # | 量法 | 它安静地做了什么 |
|---|---|---|
| ⑦ | "首末时间戳相减"类核对方式 | 差值算对了（61.390s），不代表任一个绝对时刻本身被核过——"核对了 A 和 B"与"核对了 A−B"是两句不同的话，写复核记录时容易把后者说成前者（`evidence/e1_window_narrative_review_20260802.md` 文末订正即此例，犯者=本会话） |
| ⑧ | adb-on-Windows 产物 + universal-newline 分行（Python `splitlines()`/PowerShell `Get-Content`） | 真实行尾是 `\r\r\n`，universal-newline 规则把每条真实行切成**两条**，数出来的行号比 `head -N`/`grep -n` 的口径**近乎多一倍**；必须按 `\n` 计数才是同一口径。核验 D-411「第 4 行是进程段表头」时，核验员自己先按前一种量法数错，用 `od -c` 排除后确认——**在正确口径下 D-411 的结论仍成立**，这一条不是推翻它，是记下差点绕进去的那个坑 |

**为什么不改"六个实例"那句话**：这两条是本条以外的另一次对抗核验才发现的，晚于 D-394
一天之内踩中的那六个；两批各自的发现时间与触发场景不同，合并计数会抹掉"这条红线本身还在
持续长出新实例"这件事——按此文件一贯的纪律，新增记新增，不回填旧计数。

**再追加两例（2026-08-02，T23/T24，`docs/T23_RADIO_SAMPLER_LIFECYCLE_REVIEW.md` 订正段
同源）——这两条不是命令行量法陷阱，是根因推理/假设验证层面的同族变体，判据结构相同：
「查过了」与「查完了」是两句不同的话**：

| # | 量法 | 它安静地做了什么 |
|---|---|---|
| ⑨ | 「必要性冒充充分性」的假设验证 | 验证了「候选原因 A 能推出观测到的现象 B」（能），却没有反过来验证「B 是不是只能由 A 推出」——一个协程活着但每秒吐全空样本，与一个协程被杀死后队列不再写入，能产出**完全相同**的下游观测形状（`sampled_n=0, stale=true`）；只验证单向蕴含，就当作候选被确认，实为两个不同成因共用同一个可观测表象 |
| ⑩ | 「预测清单在确认处截断」的候选排查 | 给候选写可观测预测，写到**能对上手头已有证据**为止就停手下结论，而不是写全这个假设**逻辑上蕴含**的全部后果——本例中漏掉的那条预测（"若真有异常在无异常处理器的情况下穿出协程，进程该崩"）恰与另一项已有证据（"run 正常跑完并发出报告"）直接矛盾，本该在停手前被列出来 |

**这两条与①–⑧不同的地方**：①–⑧抓的是**工具/命令**层面的量法失真（截断、编码错读、
UP-TO-DATE 误判等），⑨⑩抓的是**人在做根因推理时**的同一种失真——把「验证过一部分」
读成「验证过全部」。归入同一族，是因为 §2.16 的收尾句本就没有把范围限定在命令行工具上：
**"我查过了"这句话的有效期，止于"我证明过那次查真的发生了"**，对一次假设验证同样成立。

**再追加一例（2026-08-03，D-435，本会话自查发现，大脑核验点名值得记）——这条不是
命令行量法陷阱，也不是根因推理陷阱，是台账记录本身的同族变体**：

| # | 量法 | 它安静地做了什么 |
|---|---|---|
| ⑪ | 决策日志条目自证"已完成" | D-435 条目文字宣称"新增计数器逻辑…全量 `:probe` 681/681"，测试确实真跑过、数字属实；但重连后核对 `git status`/`git diff` 发现对应的 `RadioCollector.kt` 代码改动**本身当时并未提交**——条目登记的动作与它在仓库里的落地状态是两件事，条目完整不代表代码已入库。此前的变体是"脚本自称还原、工作树说没有"（D-321），这次是"台账自称已做、仓库说没有"，同一红线换了个记录载体 |

**修法**：**一条台账条目宣称的动作，它的入库状态是这个宣称的一部分**——写完条目、
引用了某个文件的改动后，回头对该文件跑一次 `git status`/`git diff`，确认引用的改动
确实在暂存区或已提交，不是只停留在工作区口述或已跑但未落盘的状态。同 ⑨⑩ 一样，
不并入前十条改写计数，新增记新增。

**再追加一例（2026-08-03，D-449，双探针装载中抓获，②GBK 错读的近亲变体——
"解码器不一致"，两层教训各自的犯者不同，出处各标各的）**：

| # | 量法 | 它安静地做了什么 |
|---|---|---|
| ⑫ | 语法检查通过/换一种解码器做证伪，冒充"验证过运行时行为" | 无 BOM 的 UTF-8 `.ps1` 文件被 PowerShell 5.1 的 `-File` 执行模式按系统 ANSI（CP936）解码，.NET DBCS 解码器把"行尾 GBK 首字节范围字节+紧邻换行符"当一对吞掉，下一行整行并入上一行注释（实锤：`Start-Sleep -Seconds 60` 被吞，`nr_pounce.ps1` 的 60 秒延迟探针从未武装；连带发现 `svc power stayon true` 同机制在 gen-2/gen-3 从未执行过，T26/D-427② 的 stayon 缓解从未被真实测试）。**①**（犯者=本会话）：AST `Parser::ParseFile` 语法检查通过≠运行时行为——`ParseFile` 默认按 UTF-8 解码，`-File` 执行按系统 ANSI 解码，是两种解码器读同一串字节，语法树"合法"不代表运行时读到的是同一份文本。**②**（犯者=大脑）：第一次尝试"证伪"吞换行理论时用 Python 的 `gbk` 解码器——Python 遇到孤立首字节（无法配对的 GBK 前导字节）会保留紧随其后的 `0x0A`，.NET 的 DBCS 解码器却把它一并吃掉；用与目标运行时不同的解码器做证伪等于没证伪，几分钟后被字节级复现打脸 |

**修法**：**凡 PowerShell 脚本含非 ASCII 字符且会被 `powershell -File` 执行，BOM 是
强制项**——"脚本改完 AST 过了"不算完，要么加 BOM，要么补一次针对目标运行时的真执行
冒烟（不能用另一种语言/另一个解码器实现代替，即便号称"同一种编码"）。这条与 ②（GBK
误读）同源但不同形状：② 是"读文件的人自己选错了解码器"，⑫ 是"两个不同工具/两种
证伪手段各自认定的'同一种编码'在边界字节上的行为并不相同"——静态检查与目标运行时
使用不同解码器时，语法/结构层面的"通过"什么都不能证明。

### 2.17 一句"检查过了"必须写清它**比对了哪些面**——否则它会替没查过的东西背书

自证类的句子（"口径一致"、"未发现漂移"、"全部覆盖"）有一个共同的失效方式：
**它当初真的查过，只是查的范围比读者以为的小**。范围不写出来，读者按最大范围读，
于是它替一个自己从没查过的东西背了书；更糟的是，随着外部变化，这句话会从
"真但范围小"变成**"一致地过期"**——所有被它点名的面确实还彼此一致，只是一起错了。

**实例（2026-08-29，D-569 级联件）**：M3 增补文档 §5 走查写着"n≥15：§1 与 §5 第 3/4 条
口径一致，未发现漂移"。那句当年**是真的**——它比对的是**文内**两处。可 `n≥15` 后来
被 D-471/D-474 整体废止，判据换成分 KPI×分 RAT 的 n 表；文内两处依旧彼此一致，
于是这句自证在**一份外场现场逐条执行的文档**里，一路把废弃口径护送到了今天。

**同族的另一半**：承诺写在 docstring 里而实现没兑现（同日实例：`room_dbs` 的
docstring 写"读不了如实记"，代码却把损坏的库渲染成三个 `—`，与"表不存在"无从区分）。
一个是范围没写清，一个是承诺没人验——都是**这句话本身没有守卫**。

**修法三条**：
1. 自证句一律**写清比对面**："§1 与 §5（**仅文内**，未比对台账裁定）口径一致"；
2. 凡涉及可被外部裁定推翻的量（阈值、样本量、口径名），自证要**点名它的上游**，
   或干脆改成指针（"以 X 为准"）而不是复述结论——复述会过期，指针不会；
3. 落笔前问一句：**这句话如果错了，谁会红？** 没有答案就说明它是无守卫的散文，
   要么给它配一条守卫（§2.14 的做法），要么把它降格成带日期的观察而非承诺。

**推论：「先怀疑量法」里的「量法」包括读数那一步。** 本仓一天之内撞了四次
「两个数字打架」，四次都是量法错、被查的对象没错：三次错在自造的扫描
（basename 误配、正则把当前文件的 §N 配给随后提到的文件名、把「计划新增的节」
当成「引用现有节」），**第四次错在读输出**——终端把「豁免 14」渲染乱了，
被读成「悬空 14」，于是两个本来一致的结论看起来在打架。所以核对差异时，
**先复现对方的读数动作，再怀疑对方的代码**；输出里凡是要被人读来做判断的计数，
都该带上它自己的名字（「豁免(目标不编号) 14」好过孤零零一个 14）。

**同族的一条价值判断**：「一个只能抓已修缺陷的守卫，不值当」——这个**判据**是对的，
但它的输入是「建这条守卫要花多少」。若判据本身能做严谨（如上面三类坑各有解法），
成本塌下来，账就翻过来。**会过期的是结论，不是判据**——撤回一次结论时，
别把当初那条判据一起撤了。


## 3. 测试策略：换一个维度，就抓到一个真 bug

**教训（本层最贵的一条）**：干净 fixture 与"已知输入→已知输出"的 golden 有**系统性盲区**——
它们只能验证你想到的情形。每换一个测试维度，都抓到过真 bug。

| 维度 | 手段 | 曾抓到的真 bug |
|---|---|---|
| 已知输入→已知输出 | 各工具 golden | 方法学不变量回归 |
| 整体形态 | 整报告快照冻结 | 段落被删 / 重排 / 措辞变 |
| 规模 | 12960 run 压测 | （无 O(n²)，确认线性） |
| 混乱语料 | `--chaos` 10 种外场病理 | 摘要标签歧义（丢 profile_id 维） |
| 随机语料 | 20 种子属性测试 | 竖线劈裂 markdown 表格 |
| 敌意输入 | 人工标签含 `\|` / 换行 / HTML | 每张表同时崩 |
| 编码 | 中文标签 + Excel | CSV 无 BOM → 乱码 |
| 流程 | 照字面走 runbook | 一天跨点位被打成同一点位 |
| 跨工具 | 报告级设定用到独立 CLI | 折叠上限误伤专门看稳定性的人 |
| 文档覆盖面 | 每个 `scripts/*.py` 必须在 README | 两个工具从未被记载 |
| 三面一致 | md / HTML / CSV 同一断言 | 同一缺陷每面各犯一次 |
| **输入顺序** | 打乱记录顺序重出报告 | **等级在 good / poor 间翻转**（并列按插入序裁决） |
| **非法数值** | 注入 NaN / Infinity | 一个 NaN 毁掉同格其余四个值的中位数 |
| **标签同名异写** | 尾随空格 / 大小写 / 全角 | 同一点位裂成 5 个 `low_conf` 格 |
| **单位 / 量级** | 时间戳写成秒 / 微秒 | 更好的一轮排到 1970 成了"before"，改善印成回退 |
| **对抗审计** | 多透镜并行找，再交怀疑者证伪 | 8 条高危里 6 条是"守卫在说谎"（见 2.12） |
| **验证装置自检** | 问"这套彩排/fixture 能不能演示阳性结论" | 彩排永远只会说"没超过噪声"，阴阳不分 |
| **承诺的兑现面** | 拿横幅说的话去**逐个消费方**点名 | 七个池化段落里五个从没接过取值守卫（D-197） |
| **列的位置** | CSV 表头与数据行分两处写，比对**名与值** | 同列数、错次序：值全归到相邻列名下，DictReader 不报错 |
| **归档承诺的兑现** | 逐个扰动溯源段归档的门限，看报告变不变 | 归档了一个**不可达**的门限（D-204） |
| **消费方复算** | 只拿 CSV 重算摘要里的每个数字 | 排除 62/72 格的判据从未导出（D-205） |
| **交付面的结构** | HTML 标签配平 + 敌意标签不得成活标记 | （无缺陷；此前该面零结构性覆盖，D-206） |
| **显示值 vs 判词** | 把印出来的数读回去，重新过一遍判据 | 84.96 印成 `85` 却标 `good`（D-207） |
| **清单的顺序** | 「前三个」是不是最严重的三个 | 172 个里列出最轻的三个（D-208） |
| **分桶守恒** | 各类计数之和是否等于总数 | 「2/3 单元」而语料有 7 个（D-209） |
| **桶的位置** | 「未知/无标签」有没有混进有序集合 | `unlabeled` 被当战役排进时序（D-210）、被当地点排进最差榜（D-211） |
| **报告内部指路** | 「见 X 段的 `Y`」里的 X/Y 是否真的存在 | 指向一个不存在的列（D-212） |
| **渲染后的样子** | 源码里整齐的表，渲染出来还是不是表 | 裸 `\|` 劈掉单元格、空行截断表格，决策日志 212 行里约 190 行渲染成纯文本（D-214） |
| **孪生实现的分支集合** | 同一判据在两处实现，分支数是否相同 | `publish_check` 介质项有四支、摘要只有一支，噪声不可估被当成「已核查为否」（D-216） |
| **率的分子是哪几列** | 表里印出的比率，读者能否从旁边的列认出它的分子 | 分子是「有效+低置信」，而旁边恰有一列就叫「有效」——`有效 0` 与 `有效率 100%` 并排（D-218） |
| **页面上的算术还成立吗** | 读者能用印出的数自行核对的每一条关系（和／跨度／差／比），各自舍入后是否仍成立 | 归因分解 36% 加不起来（D-219）、子分极差 23% 对不上（D-220）、介质 Δ 8/30 与前后 Δ 10/41 相减不符（D-221）。**已改为可枚举清单 `_ARITH_CASES` 一次扫完，每项带非空转下限**；新增会印数字的关系时，往清单里加一条 |
| **一处总述替多行作的承诺** | 图例/横幅说的话，是否对它覆盖的**每一行**都成立 | `PASS=该项无问题`，而 220 个 PASS 行里 29 个其实是"这项检查连对象都没有"；最露骨的一条 PASS 兼表"核过了、干净"与"从没核过"，15 次里 12 次是后者（D-229） |
| **文档写明的"不会分歧"** | 把两处实现的**数字**各自从渲染面数出来对比，而不是比对它们的输入 | README 承诺闸门与介质段共用判据，此前无任何东西核对；补守卫后 0 矛盾，**但随机语料只走到"无发现"那半边，真发现侧要靠专门夹具**（D-230） |
| **守卫有没有碰过被测的那一面** | 断言里出现的到底是渲染结果，还是某个纯函数的性质 | `test_null_medians_never_render_as_zero` 在 `if 值 is None` 分支里断言 `fmt_num(None)=="—"`——**连渲染器都没调用**；往渲染器塞一个 `or 0`，470 个测试全绿（D-232） |
| **承诺落在哪个界面上** | 函数返回值 / 渲染文本 / 磁盘文件 / 退出码是四个面，测了一个不代表另一个 | 「不覆盖输入」只在内存 dict 上验过（D-236）；「ERROR=exit 1」的 CLI 从没被跑过（D-237）；**决定报告能不能发的那个退出码无人守**（D-238） |
| **一次发现变成一次清点** | 抓到一处后，把同一个问题问遍整个目录 | 9 个工具有非零退出路径、3 个从未被调用过；其中两个有 `verify_all` 兜底，`publish_check` 两处都没有（D-238） |
| **共享模块：不数调用，去掐断** | 对没有专属测试文件的模块，逐个函数 `return None` 跑全套——**有没有反应才是覆盖的定义** | 31 个未被直接调用的函数里 29 个有反应；两个存活项，一个是真缺陷（`mde_power` 只写不读，D-240）、一个真承重却无人守（GBK 控制台，D-241） |
| **守卫「跳过」了什么** | 跳过要能说出名字和理由，**而那个理由本身也要被验过** | 两条渲染守卫整段绕过 `trend` 且不作声；我记的理由「不是一格一行」是错的，真因是战役数不足 3 时该段不出表；补语料后仍 MISSED——**整排列住在一个 list 里**，而击落法只挑标量键（D-242） |
| **规则点名了几个，守卫比了几个** | 把红线或守卫名字里说的对象数一遍，再数它实际比对了几个 | §2.6 写的是三个输出面，段落级守卫只比了 markdown 与 HTML 两个；第三个面里 `order_effect` 整段从没导出过（D-246） |
| **判据是不是承诺本身** | 守卫问出口的那个问题，和承诺要保证的那件事，是不是同一件 | 「这个模块有没有 CSV、有没有数据行」放行了一张只覆盖三分之一 KPI 的导出——文件在、有行、`kpi` 列也在（D-247） |
| **「every」背后的过滤条件** | 先读它 `continue` 掉了什么。**类型过滤最隐蔽**，不像手写清单那样一眼可数 | 「覆盖每个输出决定门」的扫描只枚举 `int/float`，5 个非数字门改了就换报告数字却没归档（D-248）；「每一份文档」只扫手写五份，22 份里 16 份含表从没查过（D-249） |
| **推导出来的清单也有盲区** | 「是推导的」不等于免检，照样要问它把什么排除在视野之外 | 正则漏掉小写的 `low_conf` 与 `inversion`；改用 AST 取 `append` 字面量后，表驱动的三个 `MIXED_*` 又推不出来——append 处是变量不是字面量（D-250） |
| **「咬住了」点名的是不是它** | 审计报 CAUGHT 时核对失败信息指的对象；遍历型守卫在第一个就失败，后面根本没跑到 | 想证明 attribution 被覆盖，锚点却同时影响热力卡，报的是 `heat_cells`（D-251）；**等长突变落在同一时间戳刻度内，`__pycache__` 让子进程跑上一条的字节码**（D-247，已加固为每次运行独立 `PYTHONPYCACHEPREFIX`） |
| **加检查前先量语料撑不撑得住** | 新关系在现有语料上有几行真能核；撑不起来时，该补的是语料不是断言 | 归因分解在 21 份语料上 204 行里只有 1 行全数值，照直加就是一条下限只能定到 1 的空转检查；换三层齐全的语料后 72 行全可核（D-252） |
| **扩覆盖时计数要分开** | 新旧两侧各设各的下限；合成一个总数，缩窄的一侧会被另一侧的体量盖住 | 表宽守卫只看报告，而 `coverage_matrix` 与 `publish_check` 的表报告根本不渲染；扩到 `_SWEEP` 后新增独立下限 150，报告侧 300 不动（D-253） |
| **对账的两侧各自从哪来** | 「A 与 B 对得上」时，先问 A、B 各自是推导的还是手写的；有一侧手写，对上的就只是两份声明 | `BLOCK_LEVEL` 是手写的五个字段名，于是守卫核的是「名字对得上」而不是「真有代码在评估」——往字面集合里加一个无人评估的名字照样通过（D-262）。同形地，「因为 X 所以豁免」的清单要挑 X 能机械核验的一组去核：头三条里两条措辞不实（D-263） |
| **常量的读者是谁** | 先 grep 它的读者。读者为零，说明真正承载那个语义的是别的东西；读者不为零，就改它一次，看数字动不动 | `cc.UNLABELED` 之外还有十三处硬编码同一字面量，改名即裂成两个桶、三处计数各 +1（D-264）；`GRADE_ORDER` 声明 best→worst 却零消费，次序实际由分带阈值承载、「什么算差」另有手写（D-266） |
| **跨层的词汇** | 文档点名的那个「权威」，有没有东西在核对它 | README 写着 `KpiGrading.kt` 是这些等级串的权威，而此前无人核对。实测词汇一致（诚实的否定）——但那些词一旦改，每个格会把新词以「无数据」的灰渲染出来，且无人作声（D-266） |
| **报错通道自己的输入** | 拿会让被测代码失败的内容，去喂那条负责**报告失败**的语句 | `run_all.py` 的失败行是裸 `print`，而断言文本引用报告、报告里带 ⚠；它在遍历失败的循环里抛，摘要行已先打——操作者知道有 N 条失败，却一条都看不见（D-265） |
| **双向承诺的另一半** | 「A ⇔ B」有两个方向，先问哪一边有测试 | 「归档的必须决定点什么」有测试；「决定输出的必须归档」全押在一张 25 条豁免表上，而该表只被核过「常量还在、理由非空」。补上对称的扰动守卫后，它第一次运行就咬住同日新造成的 `GRADE_ORDER`（D-267） |
| **「这东西是什么」问谁** | 别问那个名字最像答案的常量，去问产生它的函数实际吐出了什么 | `validity_rollup` 的真实格键是 `CELL_DIMS + profile_id`，写在建键处、组 `cell` 处与渲染器三地——**那个叫 `CELL_DIMS` 的常量低报了它自己的格**（D-268） |
| **守卫说「查不了」之后** | 记下来是诚实，**查出来才是结论**——换一种够得到的量法（签名捕获够不到，就改源码让子进程重导入） | 七条「无法扰动」的豁免里，两个决定全部格怎么划的常量各移动 418/494 个印出的数字，而 `thresholds` 与 `params` 都没有它们（D-269） |
| **验收清单 vs 演示能力** | 清单全过不等于装置演示得出你要教的那件事；把产物按标记分布数一遍 | 彩排 192 行归因**全部**带 `MIXED_CAMPAIGN`，一行干净的都没有；加 `--campaign` 后 58 行才是外场真正要读的那种行（D-270） |
| **点名式免责的被点名方** | 「除 A 段外」这类横幅，先看 A 段在当前语料下出不出得来 | 两个战役时「纵向趋势」整段无声消失，而横幅仍在点名它，报告不给任何理由（D-271） |
| **成熟处置落在哪几条路径** | 仓库对某类危险已有处置时，数它覆盖了哪几条输入——**人手写的那条最易漏** | BOM 在语料侧早已点名文件/行号/解法并拒绝出报告，而操作者手写的网格配置与 `--map` 两处都以栈回溯崩掉（D-272） |
| **提案的前提对过账没有** | 一份提案/计划里被当作既定事实的每一条，去决策日志里查它还成不成立——**写提案的人与记决策的人是同一个时最危险** | 外场网格提案 v1.0 成文于 D-48 之后八天，仍按三层级排 480 run 并把「三层级必须齐」列为不可交易；照批则行程按三倍规模排向并不存在的镜像端（D-283） |
| **这条守卫能不能触发** | 不问「它通过了吗」，问「什么输入会让它失败」——**再算一遍那个输入在真实语料里存不存在** | `RADIO_THIN` 拿整格 sampled_n **总和**比下限，而任何真实格都有十几个场景、总和必然过线：一条永不触发的守卫，读起来却像一次通过的检查（D-284） |
| **规格要的字段谁在读** | 交出去的字段表里逐行填「消费方（函数级）」这一列——**填不出来的那一行，就是不该要的字段** | 无线接线规格因此排除了设备侧有、本层无人读的 `rsrq` 并写明理由；守卫从字段表首列取名，逐个 AST 核对读者（D-284 承 D-276） |
| **横跨两层的替代方案** | 一处失效后写下的补偿措施，点名的那个替代物**在下游拿不拿得到** | 三级归因取消后记下的替代是「多维协变量（无线上下文…）」，而无线量从未进过导出契约——补偿措施本身从没被核对过（D-284） |
| **最小语料** | 按外场第一天的样子生成（只跑通一个点位），从**第一行**开始读 | 摘要头条对着 1 个格说「无 fair/poor 格」，读起来是「普查过了没问题」；另一支路给分子不给分母（D-300） |
| **判据换名后的残留面** | 一个门限/判据被替换时，数它的**旧名字还留在几个面上**——最可能漏的是摘要 | 固定 3σ 早已换成标定 K，只改了正文：摘要报「3σ 筛查」而同一结论在正文印 `K=8`，**严了三倍且方向危险**；更糟的是有守卫在钉着那个旧名字（D-301） |
| **标记的到达面** | 把报告里**每个落在表格行上**的标记，拿去全部 CSV 里找一遍 | 两条真缺失（忙闲可比性判读）；其余多为 CSV 用小写类型化列承载同一事实——**字面匹配是错的量法，先修工具再下结论**（D-304） |
| **输出路径** | 四个输出旗标各指向不存在的目录，看退出码与有没有栈回溯 | 四条全抛 `FileNotFoundError`；且 md 先于 csv 写出，`--md ok.md --csv nope/c` 先落一份 markdown 再崩（D-306） |
| **同类横比** | 修完一个对象，把**同类对象的行为并排列出来**——不一致处往往是从没被决定过的默认 | 目录型旗标自建、前缀型拒绝：差异是对的，却无人写下、无守卫钉住，删掉一行 `makedirs` 文档工作流就崩（D-307） |
| **装置 vs 生产者** | 彩排语料的字段集合，与 App 今天真写出来的**取差集**；差集每一块都问「前门会不会误拒」与「有没有消费方偷偷读它」 | 两块并列评分块（`run.aqs_v02`/`run.aqs_token`）彩排里一块都没有；实测门放行、报告除溯源哈希外逐字节相同=诚实的否定，但此前无人核对（D-309）；KPI 层三方矩阵四格闭合（D-310） |
| **被执行的那份文件** | 同一前提写在哪几份文档里——**最危险的是现场逐条执行的那份**，不是被审阅的那份 | 提案已改 v2.1，而 runbook §2 仍指挥跑三层级、对着已放弃的镜像端排三倍行程（D-311）；n=11 已拍板而两处仍写 5，提案自己把代价算高三倍（D-312） |
| **样本不足的那一轮** | 按外场被迫收工的样子生成（每格只跑到 3 轮），**从第一行读起** | 试点形状下 32 格全部 low_confidence，而头条与跑满 11 轮**逐字相同**——补了「总体有多大」不等于补了「总体可不可信」（D-313） |
| **同名的几份实现** | 顶层 `def` **按名字分组**（不顺 import 图），每组问「它们在同一批输入上答得一样吗」 | `fnum` 有三份、`load_records` 有三份，两只 per-run CLI 既不拒 NaN 也不按 `run.run_id` 去重：一个 `nan` 印上交付页面，同一文件列两次即 20 条变 40 条而两个面都不吭声（D-314/D-315）；另有三组重名是诚实的否定 |
| **真实语料（最贵也最狠的一维）** | 跑一次真外场，**把出来的报告从第一行读到最后一行** | 第一天即五处：发布门三个分支恒定点名「三层级」而语料只有一层（D-350）；`n=1` 的格被标成与自己不可比（D-351）；每条 run 都带 `low_confidence` 而摘要头条只字未提（D-352）；汇池中位谁都不代表（D-360）；`n=1` 与 `n=11` 在异常筛查里等权（D-361）。**合成语料每格样本整齐、维度齐全，这五条一条也演不出来** |
| **换一种运行模式** | 「快测跑通」不等于「取证跑通」——真实语料也分模式，**没跑过的那个模式等于没测** | 取证模式从未被真实数据检验过；一跑就同时得到两件事：序位效应**首次可判定**（每位次 n=4，9 处全可判），以及**快测永远只测冷的那一轮**——同一份语料按轮次切才看见首轮系统性更差（D-354/D-355，随后升成 `round_effect.py`，D-356） |
| **量法自身的编码** | 两种量法对**同一份产物**给出不同的数时，先怀疑量法，别急着下结论 | PowerShell `Get-Content` 不带 `-Encoding UTF8` 时按系统 ANSI 解码 UTF-8 中文，**吞掉 207 个换行**：459 行的本文件被数成 252 行。仓库自己的 `verify_all.ps1` 读文件带 `-Encoding UTF8`、两处写也指定 utf8——**是诚实的否定，坏的是我的核查命令**（承 D-323：报出压倒性差异时先修量法） |

**还有一类，且最难自查**：测试自身空转——**它通过了，但不是因为代码对**。已反复发生，
形状有三种，都值得在写断言时对照一遍：

- **fixture 撑不起被测分支**：语料没有 scenarios，用例走了"无数据"分支，
  "每行都带标记"在**零行**上真空通过。→ 加一条 `assert rows` 自守卫。
- **断言的字符串另有来源**：`"T4 严重卡顿率" in html` 看着在测卡片图例，
  实际摘要早就把这句话带进 HTML 了——**没有这次改动它也通过**。
  → 先数一下该串在产物里出现几次，再决定断言什么（计数，或换一个该处独有的串）。
- **断言用了太宽的词**：拿一个在**免责文字里也出现**的词当"标记未触发"的证据，
  或按朴素 `split("|")` 数列而把正确转义的 `\|` 当分隔符。→ 匹配行内形态，别匹配词。
- **装置本身只能产出一种结论**：彩排语料的设计效应（+3 分）小于它自己产生的噪声（±6），
  于是每次彩排都说"没有超过噪声的变化"——**"改善检测坏了"与"工具很诚实"输出一模一样**。
  → 验证装置必须能演示**阴阳两种**结论，且预期答案要**写下来**（`DESIGNED_EFFECTS`、
  `CHAOS_PATHOLOGIES`），否则彩排只是演示，不是核对。

**写守卫时务必负向验证**：把缺陷注入回去，确认它真的会失败。本层每一条重要守卫都这样验过，
D-173 那条正是**因为原测试从未负向验证，才把缺陷写成了期望行为**。

**审计"某条红线有没有守卫"时，同样只能靠负向验证。** 用 grep 找词汇会给出**错误的结论**，
而且两个方向都会错：搜得太宽，会把免责文字里**提到**标记名的地方当成"标记出现了"（D-181）；
搜得太窄，会把**用另一套说法**写的守卫判成"这条红线没人守"——§2.5 就被我这样误判过一次，
是把 `md_cell` 的转义拆掉之后，才看见 `test_report_properties` 里那条一直都在的守卫（D-185）。
**要知道谁在守某条红线，就把它弄坏，看谁喊。**

**§2 的十二条已按此法整体验过一遍**（D-186）：对每条红线施加一处最小突变、跑全套、
在 `finally` 中还原、最后以 `git hash-object --no-filters` 比对确认工作区与 HEAD 逐字节一致
（**`--no-filters` 不可省，理由见下文**）。
**17 条突变，17 条被咬住，零漏网**——所以本文开头那句"每一条都有 golden 守着"
是**实测结论，不是断言**。两点值得接手人知道：多数突变会同时打断 3~24 条测试
（红线之间的守卫是**交叉**的），而 **§2.10 只有报告快照加一条专门测试咬住，是最薄的一处**。
改动那一带时请格外小心。

**D-197 的十条守卫按同法验过**（五个取值守卫、CV 符号、批化标记渲染、CSV 的列/宽/序）：
**10 条突变，10 条被咬住**，收尾同样以 `git hash-object --no-filters` 确认逐字节复原。这一轮的形状与
D-186 不同：**多数突变只打断 1~3 条测试**，因为这些守卫是新的、彼此不交叉——
其中 `order_effect` 的取值守卫与 `buffering` 的标记渲染**各只有 `_POOLING_SECTIONS`
那一条在守**（该测试的文档字符串已按下述规矩自报家门）。

**为什么 `--no-filters` 不可省（D-213，实测）**：不带该参数时，`git hash-object` 会先跑一遍
**clean 过滤器**再算哈希；本仓库 `core.autocrlf=true`，过滤器做的正是 CRLF→LF 归一化。
后果是**同一份内容的 LF 版与 CRLF 版会算出同一个哈希**——2026-07-28 在工作区实地量到：
`publish_check.py` 是 LF、`coverage_matrix.py` 是 CRLF，原始字节不同，过滤后哈希却都等于 HEAD。
也就是说，一个把文件按另一种换行写回的还原脚本，**能干干净净地通过"逐字节一致"这项检查**，
而 `git diff` 同样看不见它（diff 也走归一化）。这一条的形状与 §2.12 是同一个：
**检查嘴上给的保证，比它实际够得着的范围大**。所以还原验证必须落在原始字节上——
`git hash-object --no-filters`，或 `cmp <(git cat-file blob HEAD:<path>) <path>`。
BOM 不在此列：它不经 EOL 过滤器，所以 D-199 那次剥掉 BOM 是能被普通 hash-object 咬住的。

**「测试失败了」不等于「守卫咬住了」（D-220）**：审计 D-220 时，第一次突变删掉了 `reconciled`
的赋值却留下了引用它的那两行，测试确实失败——**但失败信息是 `NameError`，是模块崩了，
不是守卫发现数字不对**。只看退出码，这一条会被记成「CAUGHT」，而实际上什么也没验到。
所以**每一次突变都要读失败信息，确认它死在你想验的那一步上**；本层的审计脚本一律把首行
错误打出来，就是为了这一步不能省。这与 D-204 的无效探针、D-210 的空转逃生口是同一个家族：
**验证动作本身也需要被验证**。
**推论：突变的锚点必须唯一。** 给两条测试加计数下限时，我用的那行锚点
在同一文件里**一字不差地出现两次**，按首次匹配替换改的是排在前面的另一条测试——
于是审计报了个 **MISSED**，而那既不是下限失效、也不是守卫不咬，**只是我改错了地方**。
锚点要么取到被测函数独有的一段，要么先数一遍它在文件里出现几次。

**这套审计不做成常设工具**：一个会就地改写源文件的脚本是把双刃刀。17 处突变点逐条记在
`DECISION_LOG.md` D-186，D-197 的十处记在同文件 D-197，需要复验时照图施工即可。

**薄厚地图**（同一审计的副产品，统计每条红线被**几条针对性测试**咬住；报告快照不计，
因为它对任何改动都响、也就意味着 `--update` 会把真回归一并批准）：

| 守卫厚度 | 红线 |
|---|---|
| 交叉厚守（7–23 条） | §2.3 绝不丢格、§2.12a severe 须强调 |
| 常规（2–4 条） | §2.1、§2.4b、§2.5、§2.6、§2.7、§2.8、§2.9、§2.11 |
| **恰好 1 条** | **§2.2 没法查≠查过了、§2.4a 省略须声明、§2.10 免责措辞、§2.12b 判据一致** |

最后四条是**单点守卫**：删掉那一条，红线就裸了。**没有为它们各加一条近似重复的测试**——
那是噪声，本层反对（见「宽泛/重复守卫比没有守卫更糟」）。改为**把警告放在读者会看到的地方**：
这四个测试函数体首行各有一条 `⚠ SOLE targeted guard on …` 注释，编辑它们的人当场就会看见。
**若要替换其中任何一条，先把替代品放进去，再动原件。**

D-197 又添两处单点：`order_effect` 的取值守卫、`buffering` 的标记渲染，二者都只由
`test_every_pooling_section_keeps_the_banner_s_promise` 咬住，该函数的文档字符串已按同一格式标注。
**这份名单本身要跟着改**：每做一轮突变审计，把新出现的单点守卫补进来——
一份漏项的单点清单，正是 §2.12 第 4 条说的那种"看起来已经修好了"。

---

## 4. 当前状态

**已完成**：工具链、M2 规模彩排、混乱语料彩排、发布前自检门、交付骨架、外场 runbook、
标签接线规格、网格设计提案。**六透镜对抗审计的 28 条确认发现已全部处置**（含 13 条高危；
另有 3 条被怀疑者驳回、42 项查清无碍）——其形状与教训见 §2.12，逐条见 `DECISION_LOG.md`
D-159..176。**其后又做了三轮收口**（D-178..193；上一版写 D-177..193，而 D-177 从未写入日志——号被跳过，D-364）：**守卫质量**——突变审计验证 §2 全部红线确有守卫，
并量出薄厚地图（见 §3）；**流程**——照字面重走 runbook，抓到"三层级被统一打标成 metro、
报告反过来说没测过"（D-189）与"发布自检把同一个格按 KPI 数两遍"（D-191）；**文档**——
把"活文档抄了会过期的东西"扫完，计数与清单一起治（D-192/193）。
**再往后三轮，主题一路收窄**：**读者视角**（D-194..212）——七透镜审计与其后自选九维度，
共同形状是"读者做的第一个动作，恰好是报告最没准备好的那个"（§2.12 第 6 条）；
**守卫质量**（D-213..228）——把突变审计本身治严（只认 `AssertionError`、锚点先数唯一性、
还原按原始字节核对），并把"页面上的算术"「闸门与摘要」等散点检查改成**可枚举清单**；
**守卫的出处**（D-229..238）——对每一句写下来的承诺问一句**"谁在测它"**。
最后这一轮的收获集中在一个形状：**被测的那个面，往往不是承诺所说的那个面**——
断言里出现的是纯函数的性质而非渲染结果（D-232）、是内存 dict 而非磁盘文件（D-236）、
是进程内调用而非退出码（D-237/238）、甚至只是"那句话被印出来了"（D-233）。
**这一轮已走完一遍**：`scripts/README.md` 里断言代码行为的句子（用「绝不/一律/永不/必须/
不会/均不/不得/同一判据」等模态词一扫即得，**清单靠重扫复现，不靠手抄**）当时 17 条，
其中 7 条补了新守卫（D-230/232/233/234/235/236/237/238），其余逐条读过、确认已有**同名守卫**
（如 `test_marker_survives_losing_either_half`、`test_cell_with_no_subscores_is_absent_not_all_good`）。
**这句话本身也纠过一次**：上一版写的是「约 6 处仍无守卫」，那是没核实的估计，
下一小时的逐条核对就推翻了它——**连"还剩几处"这种数字，也要数过再写**。
**再往后是同一条线索的两个转弯**（D-243..245）：先把「三个输出面同时改」这条红线从**标记级**
提到**段落级**（此前一整段只进 markdown 不进 HTML 也照样通过，D-243）；再回头问
**"我自己的改动让哪些既有文字变成了假话"**——D-229 加了第四种严重度而外场手册仍写三态（D-244）、
D-240 加了第二列 MDE 而 README 与手册各只讲了一半（D-245）。
后者的收尾不是"以后记得改"，而是**把这个回头动作交给机制**：本仓的 OUTPUT-QUOTES 契约块
（文档登记引自工具的原话 + 必须仍能产出它的渲染器）此前没登记这些列，登记后
「工具改名而手册不动」当场被点名。

**再往后的八条，是同一个问题被问了八遍**（D-246..253）：**一句自称覆盖全部的检查，到底把什么排除在视野之外**。
先是第三个输出面从没被问过完不完整——报告调用的十个模块里九个有 CSV，而 `order_effect`
在页面上印着每个执行位次的中位数、极差和「疑似序位偏倚」的判词，导出为零（D-246）。
补完当天就发现**我补的那张表自己丢了三分之二**，且我同时写的守卫放行了它：
它的判据是「这个模块有没有 CSV、有没有数据行」，而承诺是「段落有没有真的抵达 CSV」——
**判据比承诺粗，就是这样过去的**（D-247）。
同一句话再问向别处，一路都有：溯源清单的完备性扫描在自己的注释里写着只枚举 `int/float`，
五个非数字的门改了就换报告数字却没归档（D-248）；「每一份文档」的表格守卫只扫手写的五份，
而 `docs/` 里 22 份有 16 份含表、从没人查过它们渲染出来还是不是表（D-249）；
「每个归因标记都抵达三个面」钉住的是语料碰巧产出的三个，而标记共有十五种（D-250）；
横幅承诺的守卫漏掉的，正是报告里最大的那个分析段（D-251）；
算术关系守卫的 docstring 说自己是被归因矩阵那次对不上账逼出来的，而归因矩阵不在它的清单里（D-252）；
表宽守卫只读报告，而 `coverage_matrix` 与 `publish_check` 的表报告根本不渲染（D-253）。
**八条里有三次，主角是我自己的过滤条件**——所以要记住：**推导出来的清单同样有盲区，
「是推导的」不等于免检**。**还有两次，主角是我刚写下的东西**：D-247 那张导出，
以及 D-251 第一轮那个「咬住了、但点名了错文件」的审计。后者留下一条铁律：
**审计报 CAUGHT 时，先核对它点名的对象对不对**——遍历型守卫在第一个就失败，后面根本没跑到；
另外，**字节数相同的两次突变若落在同一个文件系统时间戳刻度内，`__pycache__` 会让子进程跑上一条的字节码**
（已加固为每次运行指定独立的缓存目录）。
**再往后到 D-320 为止，本节不逐条复述**——67 条的账本只会造出第二份会过期的清单（D-262）；
每一条的维度与收获在 §3 的表里，逐条在 `DECISION_LOG.md`。这里只记**反复出现的四个形状**，
因为它们决定接手后该往哪儿看：**①同一件事写在几处，修复只落在被触发的那一份**
（`fnum` 三份、`load_records` 三份，D-314/D-315，规矩见 §2.14）；
**②一条规则写下来之后，它自己覆盖了几个**（§2.14 说「每一组」，一小时后数下来只钉了三组里的两组，D-317）；
**③被现场逐条执行的那份文件，最后才被更新**（runbook 指挥跑已放弃的三层级 D-311、
仍写 n=5 D-312、每日收工那条命令指向一个不存在的网格文件 D-320）；
**④带渲染门槛的段落从没进过任何 golden**，于是它的标题可以说谎而无人失败
（趋势表标题声称 UTC 而分桶是本地，D-318/D-319）。
**并且这四个形状，有三个是我自己刚写下的东西造成的**——所以 §5 最后一条那一问要当真。
**哪些守卫是单点的，是量出来的不是猜的**（D-321：重放 12 处突变、记录每次的失败测试集合，
9 处只有一条测试接住；那 5 条已在首行标 `⚠ SOLE targeted guard`）。

`verify_all` **全门 PASS**、反射套件全绿。（门数与用例数不写在本文里——**活文档里的计数必然过期**，以 `scripts/verify_all.ps1` 与 `scripts/tests/run_all.py` 的实际输出为准；带日期的历史计数见 `DECISION_LOG.md`。）

**2026-07-31：这一层第一次吃到真实数据，C2 不再是阻塞项。**
PO 批复 D1=「只测当前手机所在位置」后，「实地走点」这项要求本身消失了；当天在 P40 上采到
三份语料（**快测蜂窝 11 轮 + 取证蜂窝 4 轮 + 取证 WiFi 4 轮**），覆盖率 100%、发布门 FAIL 0，
交付物见 [`M2_PILOT_REPORT_2026-07-31.md`](M2_PILOT_REPORT_2026-07-31.md)，产物与采集日志在
`evidence/m2_pilot_20260731/`（D-349/D-353/D-355/D-358）。

**接手人最该知道的是：真实数据在这一天的主要身份不是「结果」，而是审计器械**——
它一次咬出**五处**分析层缺陷（D-350/351/352/360/361，逐条见 §3 表的「真实语料」行），
且五条**全都是合成语料在结构上演不出来的**：彩排语料每格样本整齐、三层级齐全，
而外场第一天就是单层级、单格 `n=1`、每条 run 都 `low_confidence`。
**新增的红线 §2.15 就来自这一天**。另有两条是测量结论、会改变报告怎么读：
实测 CV 为 5.5 / 10.3 / 5.9%，**推翻了 n=11 所依据的 CV≈5% 假设**（该效应量需 n≥13–47）；
以及**快测的绝对值全部是冷启动读数**，成因由 WiFi 对照实验判定为**主因无线唤醒、
残余 App/TLS 冷启动**（D-358）。

**仍在 PO 处、无法自主推进的两项**：

| 项 | 阻塞原因 | 谁能解 |
|---|---|---|
| 点位真名 | `campaign_grid_shenzhen.json` 现为占位 `SZ-PILOT-01`，PO 已说明真名后补 | PO |
| 冷启动协议定值 | 报告 §2.4 给了三个选项（推荐①：丢弃一轮预热），选哪个会改变往后每次外场的行程 | PO |

**已由文档解阻、待他人实施**：

| 项 | 交付物 | 待谁 |
|---|---|---|
| C1 标签生产接线 | [`CAMPAIGN_LABELS_WIRING_SPEC.md`](CAMPAIGN_LABELS_WIRING_SPEC.md) | spec 属主 / P1 lane |
| C3 目标网格定值 —— **已定值并随仓库交付，且 PO 已批复** | [`campaign_grid_shenzhen.json`](campaign_grid_shenzhen.json)；D1–D4 的论证见 [`M2_GRID_DESIGN_PROPOSAL.md`](M2_GRID_DESIGN_PROPOSAL.md) v2.2（2026-07-31 PO 批复：1 点位=当前位置 × ctcc × 忙时，n=11 维持，D-345） | **不再阻塞任何工作**：runbook §0 与 §3 已直接指向该文件。无待确认项——四项均为 PO 2026-07-31 逐项批复（D-311 / D-312 / D-320 / D-345） |
| 设备标识字段（`MIXED_DEVICE` 的前提） | 同规格 §9（D-156） | spec 属主 / P1 lane |
| `validity` 枚举与实际输出大小写不一致 | ~~同规格 §10（D-190）~~ **已解（D-371，2026-08-01）**：schema 枚举按实况对齐为小写，大写成为漂移侧记 advisory——契约恢复可判 | ~~spec 属主~~ 已闭环 |

**墙钟战线（2026-08-19..22 追加；本段刷新至 D-552）**：设备墙钟被裁定不可信、E-01 服务端钟
为基准（D-506；`clock.wall_skew_ms` = 设备 − 服务端，D-547 对真实 E-01 实测 skew=26ms）。
分析层对策不是改数——**KPI 计时走单调钟、值不受墙钟影响**——而是把「哪天测的／忙还是闲／
谁在前」这些**判读前提**逐个消费方处置：按日分桶警示（D-541）、推断时段翻转计数（D-543）、
对比次序翻转点名（D-545）、发布门三分支含「未核算≠对得上」（D-549）、trend N 战役时序
翻转点名（D-552）；信任卷叠自带第四信号（`wall_annotated`/`wall_suspect`，自有分母）。
「服务端时刻」只有一份实现：`cc.run_server_started_ms`（D-552）。**方法教训两条**：
①消费方清单是手写的就会漏——「四个消费方」漏了 trend，靠 grep 从产物枚举才找到（D-273
再证；教训已写进 `campaign_common.py` 该注释自身）；②docstring 里替渲染侧许的诺（「说
无从判断而不是沉默」）悬空了一轮才被补上（D-267 再证）。~~待裁：分桶键~~ **已裁已落地
（同日）**：B2 终裁（全证据整表换服务端键／混合整表回退＋横幅／零证据逐字节不变）见
[`DECISION_REQUEST_WALL_DAY_BUCKET_20260822.md`](DECISION_REQUEST_WALL_DAY_BUCKET_20260822.md)
与 D-555（施工），劝告句随后全部指针化（D-559）——分桶钟源看趋势表头与 CSV `day_clock` 列。

---

## 5. 接手后从哪开始

1. `python scripts/tests/run_all.py` —— 应**全部通过**；跑不过先修环境，别改码。
2. 跑一次彩排（runbook §0.5 **两趟**：A 补注演练 + B 读报告演练），**对着 §0.5 的
   「彩排的正确答案」逐条核**——合成语料是按已知答案设计的（`synth_campaign.DESIGNED_EFFECTS`），
   对不上就是工具链坏了，不是数据没效果。别只"看看"报告长什么样（D-182）。
3. **再读一份真的**：`evidence/m2_pilot_20260731/pilot_report.md` 是这一层第一份真实数据
   报告。彩排告诉你工具链是不是好的，**它告诉你外场真实的形状**——单层级、单格 `n=1`、
   每条 run 都 `low_confidence`，五处缺陷正是这样被咬出来的（§2.15、§3「真实语料」行）。
   **往后每次改动，问一句「这在真语料上长什么样」，别只问「彩排过不过」。**
4. 读本文 §2 原则清单 —— **改动前确认自己不是在拆守卫**。
5. 要加分析段：仿 `buffering_rollup.py`（最小完整样板：`analyze` / `render_markdown` / CLI
   + golden + 接进报告 + CSV + README 一节），并遵守 §2。
6. 边界：本 lane 只动 `scripts/`、`docs/` 与 `evidence/`——最后一项不是手写的，是
   `verify_all` 自己归档的门禁日志与 `sha256-manifest.txt`，随对应决策一起提交；
   **不碰** `app/`、`spec/`、`server/`
   （跨 lane，走 spec-first 交接）。
7. 收工前问一句：**这次改动新增了哪句承诺，谁去测它？** 自己写下的话同样会变成无人看管的
   承诺——D-229 给发布闸加了第四种严重度，「N/A 不参与阻断」这句就是那时造出来的，
   而直到 D-238 才有东西守着它。

---

*v1.2 · 2026-07-31（**本次刷新的分界是「第一次吃到真实数据」**：新增 §2.15、§3 三个维度行、
§4 试点状态与 §5 第 3 步；上一版 v1.1 · 2026-07-27 刷到 D-345）·
对应 `DECISION_LOG.md` 中本层的决策序列（自 D-87 起，以该文件为准）。*
