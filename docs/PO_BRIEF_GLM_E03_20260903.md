# E-03 简报（给 PO 的一页，2026-09-03）

> 记录性非请批（循 `PO_BRIEF_DOUBAO_CAMPAIGN_20260830.md` 先例）。每节末括号内为可核锚。
> **本页每个数字都标了强度**（验过／先验／不可归因）——这是本批唯一想请您记住的读法。

## 一句话

**原命题 P1 不成立，而真正的收获不是「谁数错了」，是一个名字错了**：我们代码里叫
`arrivals`／「token 到达」的那个量，**实为「内容帧到达」**。在 GLM 上两者差 **~1.5%**，
**且差额随长度成比例增长**（不是固定开销）。⇒ 凡以 `arrivals` 当 token 数的判读，
**都带着一个随长度放大的系统性偏差**，此前无人知道它存在。

## 一、结论与数据（12 笔用满上限，3 冒烟 + 3×3 正式）

| 档 | 解析器数出（＝非空 content 帧） | usage | `finish_reason` | 差 |
|---|---|---|---|---|
| t60 | 59／59／59 | 60 | `length` | **1** |
| t150 | 148／147／148 | 150 | `length` | **2／3／2** |
| t800 | 246／253／242 | 250／257／246 | `stop` | **4／4／4** |

九格最小二乘：`gap = 0.01594 × frames + 0.034`，**截距 ≈ 0**。
（锚：`evidence/glm_e03_20260903/README.md` §2–§3；回归式由 v4 独立算出、大脑逐位复核，D-678）

## 二、证据强度分层（**本页最重要的一节**）

- **验过（可复核，非转述）**：12 笔原始 SSE 逐行入库；「解析器数出 == 非空 content 帧数」
  由 **JVM 测试断言**在九格**全部**成立（非事后核对）；仓内夹具**必须能由证据 jsonl 逐字重生**
  （守卫断言全文相等）；v4 十二格**先数后读**逐格核符。
- **已否证（结论性）**：**「固定开销（EOS 型）」不成立**——截断轮无自然结束 token，差额仍
  非零且随长度走（1 → 2–3 → 4），且回归截距 ≈ 0，**固定开销要求的那个常数项实测没有**。
- **先验，不是数据（请勿读成数据结论）**：两个候选——**帧合并**（一帧多 token）与
  **分词口径差**（服务端计入不下发的 token）——**在本批 12 笔上完全同形，数据对二者一视同仁**。
  我们标「帧合并为领先候选」，**全部依据是它与上述命名发现的先验一致性**，不是本批数据。
- **不可归因（现有手段分不开）**：无 GLM 分词器 ⇒ 上述两解**无法用本批数据区分**。
  **两解并列是结论，不是待办。** 另记一条已探明的死路防人再走：**用帧的字符长度分布证不了合并**
  ——GLM 为词级 token，一帧一词本来就正常。
- **池纪律（防止把截断形状混进结论）**：`finish_reason` 非 `stop` 的六笔**只进差额分析、不进形状池**；
  仅 t800 三笔进形状池。**同一批数据的两个用途不混池。**

## 三、额度与费用账

- **额度**：命题单预登记上限 12 笔，**实用 12/12**，无超支、无追加。
- **费用**：所用模型为 **`glm-4-flash` 免费档**，**账面零费用**
  （依据＝所选模型档位与调用记录；**非账单核对**，如需请告知，我去对账）。
- **密钥零泄漏（结构性，非承诺性）**：抓取器 `build_meta()` **在函数签名上就不接受
  header／key 形参**——不是「记得不要写进去」，是**写不进去**；另有守卫断言**即使走错误路径
  key 也不落盘**。红线原文＝**key 不入库、不入消息**（D-655 复工分派段，非①款——①是作息令）；
  **「不入日志」是本 lane 自加的更严一档，不是裁定原文**，一并写明免得日后被当成裁定引用。

## 四、方法学兑现一句

**判读分支写死在开跑之前**，这一条第一次真正承重：冒烟阶段我据三笔得出「差额恒定 ⇒ 固定开销，
帧合并已排除」——**它错了**，因为三笔 usage 全落在 241–269 一个窄带里，
**而在单一长度带上「恒定」与「成比例」不可分辨**。正式 3×3 拿到 1／2／4 时，
若判据不是预先写死的，我很容易读成「还是很小，仍算恒定」。
v4 复核评语：**本批最值钱的是那段「我先前的结论错在哪里」**，非结论本身。
（锚：`docs/BATCH_PROPOSITION_GLM_E03_20260903.md` §1b-2；README §3.1；D-678）

## 五、还欠什么（诚实账）

- `arrivals` **命名修正链**（改代码与文档里的叫法）**挂装机批**，尚未落——**在此之前，
  凡引用该量的旧判读都仍带那 ~1.5% 的偏差**。
- **C 盘 0 字节**导致徽章 `badges.txt` 停在带红的一跑上，按 D-604②／D-667 **保持未提交**，
  不伪造绿也不制造假红；**清盘放行后立即补跑全量并把点名日志随批入库**（D-681①）。

——v3（Opus 5），2026-09-03；锚：D-669⑥（开跑令）／D-671③／D-676／**D-678（收卷）**／D-680／D-681；
证据包 `evidence/glm_e03_20260903/`；命题单双签钉 `1b0a44e`
