# M2 外场网格设计提案 v1.0（待 PO 批复/调整）

> **这份文件解决什么**：M2 外场采集一直卡在"目标网格未定值"。定值需要的不是意愿，
> 而是**代价与收益的数字**——测几个点位要几天、重复几次够不够。本文把这些算出来，
> PO 只需在 §1 勾选/改写，即可启动采集。
> **算法透明**：单 run 时长由 `profiles/*.json` 的**实际相位定义**推得，非拍脑袋；
> 链路速率等假设在 §5 全部列明，并给出"测完第一个点位就用实测值重算"的修正路径。
> **不可交易项见 §4**——有一项砍掉会让整个归因方法学失效。

---

## 1. 需要 PO 拍板的四件事

| # | 决策项 | 提案默认 | 影响 |
|---|---|---|---|
| D1 | **点位清单**（几个、在哪） | 8 个（CBD / 居住区 / 交通枢纽 / 园区 各 2） | 线性影响工时 |
| D2 | **运营商** | 移动 + 联通 | 双运营商工时翻倍 |
| D3 | **每格重复次数** | **5** | 见 §3：5 是收益递减的拐点 |
| D4 | **时段** | 忙时 + 闲时（每点位去两次） | 双时段工时翻倍 |

批复后把点位名填进 §6 的 JSON 即可开工。

---

## 2. 工时预算（真实计算）

**单次 run（跑完 s1_chat + s2_coding_agent + s3_multimodal 三场景）= 72.4 秒**
——由三个 profile 的实际相位累加：clock_sync 采样数 × RTT、token_stream tokens ÷ rate_tps、
think_pause 时长、upload/download_burst 字节 ÷ 链路速率、tool_loop 轮数 ×（服务端处理 + 往返）。

| 设计 | runs | 纯测量 | 含 40% 现场开销 | 外场日（6h/天） |
|---|---|---|---|---|
| **基线**：8 点 × 2 运营商 × 忙闲 × 3 层级 × 5 次 | 480 | 9.6 h | **13.5 h** | **2.3 天** |
| 6 点位（其余同基线） | 360 | 7.2 h | 10.1 h | 1.7 天 |
| 单运营商（其余同基线） | 240 | 4.8 h | 6.8 h | 1.1 天 |
| 重复 3 次（其余同基线） | 288 | 5.8 h | 8.1 h | 1.4 天 |
| 仅忙时（其余同基线） | 240 | 4.8 h | 6.8 h | 1.1 天 |
| 最小可信：6 点 × 单运营商 × 忙闲 × 3 层级 × 5 次 | 180 | 3.6 h | 5.1 h | 0.8 天 |

**现场开销 40%** 含：移动到点位、换 SIM/切运营商、等设备就绪、失败重测、记录台账。
这是**估计值**——测完第一个点位请用实测值重算（§5）。

**每点位单次到访**（2 运营商 × 3 层级 × 5 次 = 30 runs）≈ 36 分钟纯测量、约 50 分钟含开销。
一个点位要去两次（忙时 + 闲时）。

---

## 3. 为什么重复 5 次（D3 的依据）

同一格重复 n 次后，中位数的标准误约为 `1.253 · σ / √n`（正态近似）。用观测到的
变异系数 CV 表达为"占中位数的百分比"：

| CV \ n | 3 | **5** | 8 | 10 | 15 |
|---|---|---|---|---|---|
| 3% | 2.2% | **1.7%** | 1.3% | 1.2% | 1.0% |
| 5% | 3.6% | **2.8%** | 2.2% | 2.0% | 1.6% |
| 8% | 5.8% | **4.5%** | 3.5% | 3.2% | 2.6% |
| 12% | 8.7% | **6.7%** | 5.3% | 4.8% | 3.9% |

**读法**：健康格（CV≤5%）在 n=5 时中位数误差约 2.8%；加到 n=10 只降到 2.0%——
**工时翻倍换来 0.8 个百分点**，拐点在 5。且 n=5 与分析层现有 `min_samples=5`
（低于此标 `low_confidence`）一致，不必另行解释口径。

**诚实边界**：该公式假设分布大致对称，而时延是右偏的，故上表**指示量级、非精确置信区间**；
CV>10% 的格无论 n 多大都应先查测量装置（分析层「复测稳定性」段会标 `✗超门`）。

### 3.1 上表回答的**不是**「我有多大把握看见 5% 的改善」（D-201 补）

上表给的是**中位数自身的误差**。外场真正要回答的是另一个问题：**若真有 5% 的差异，
这轮采集有多大概率把它判成「超出噪声」**（分析层的判据就是 `|Δ| > 噪声尺度`）。两者不是一回事——
把前者当后者，就会把「差异恰好等于噪声尺度」那个临界点说成「够了」，而那**只有约五成把握**。

实测（40000 次模拟，判据与报告完全一致）：

| CV \ n | 5 | 8 | 10 | 11 | 15 | 20 | 八成把握需 n≥ |
|---|---|---|---|---|---|---|---|
| 3% | **88%** | 96% | 98% | 98% | 100% | 100% | 4 |
| 5% | **63%** | 75% | 80% | 82% | 89% | 94% | **11** |
| 8% | **47%** | 52% | 57% | 59% | 66% | 73% | 28 |
| 12% | **39%** | 41% | 44% | 46% | 50% | 54% | 62 |

**读法与代价**：本提案假设健康格 CV≤5%，那么 **n=5 看见 5% 差异的把握是 63%，不是「足够」**；
要到八成需 **n=11**，按 §2 同一算法即 1056 run ≈ **29.7 小时 ≈ 5.1 个外场日**（n=5 是 2.3 个）。

**这是 PO 的取舍，分析层不替你决定**——两条路都成立：

- **维持 D3=5**：结论口径就只能是**量级指示**。报告会把噪声内的 Δ 标 `噪声内` 并禁止表述为改善/回退；
  更要紧的是**不可以**写「本轮未见改善，说明优化无效」——在 63% 的把握下，**没看见**与**不存在**分不开。
- **加到 n=11**：多花 2.8 个外场日，换到「若真有 5% 改善，八成会看见」。若本轮目的就是
  **验证某项优化是否奏效**，这 2.8 天买的是结论本身能不能成立。

CV>8% 的格不要照这张表加复测——那时该查的是测量装置本身（见上一段与 D-170）。

---

## 4. 不可交易项（砍掉会让结论失效）

- **三层级（同城/区域/中心）必须齐**。三级差分归因的整个方法学（铁律 3 共模抵消）
  建立在"同客户端、同接入、同时段对三级镜像端各测一轮"之上。**缺一层，该格的骨干
  增量直接不可计算**（分析层会诚实记 `TIER_MISSING`、不外推）——省下的 1/3 工时，
  换来的是"测了但答不出问题"。
- **同一格内三层级要连续测完**，中间不换运营商、不跨忙闲边界，否则共模不再抵消。
- **忙闲两个时段都要**，除非本轮明确只回答"忙时有多差"。热力卡的忙闲对比是 M2 的
  主要卖点之一。

可交易的是：点位数（线性）、运营商数（可先单运营商摸底）、重复次数（不建议低于 5）。

---

## 5. 假设与修正路径（重要）

本文的时间预算依赖四个**假设值**，全部列明以便复核：

| 假设 | 取值 | 影响项 |
|---|---|---|
| 上行速率 | 5 Mbps | upload_burst（s2 512KB、s3 2×1MB） |
| 下行速率 | 30 Mbps | download_burst（s3 2×12MB，最大单项） |
| 往返时延 | 50 ms | clock_sync（每场景 40 次采样）、tool_loop |
| 现场开销 | +40% | 移动 / 换卡 / 重测 / 记录台账 |

**修正路径（建议写进外场第一天流程）**：测完**第一个点位**后，用实际
`started_at_epoch_ms` 跨度算出真实 per-run 时长与开销比例，代回 §2 重算总预算，
再决定是否调整点位数。**不要用本文估计值去承诺工期**——它的用途是把决策从
"不知道要多久"变成"知道大概两天、且知道哪些旋钮能调"。

---

## 6. 批复后直接可用的网格配置

把点位名替换为 D1 的定值，存为 `campaign_grid.json`：

```json
{"point_id":  ["SZ-CBD-01", "SZ-CBD-02", "SZ-RES-01", "SZ-RES-02",
               "SZ-HUB-01", "SZ-HUB-02", "SZ-PARK-01", "SZ-PARK-02"],
 "carrier":   ["cmcc", "cucc"],
 "time_band": ["busy", "idle"]}
```

> 键名必须是 `point_id` / `carrier` / `time_band`（与记录字段同名）；写成复数会被
> `coverage_matrix.py` 直接拒绝并提示正确键名。

每日收工跑一次即可看到"下一步该测哪里"：

```
python coverage_matrix.py labeled/*.jsonl --config campaign_grid.json
```

完整外场流程见 [`M2_CAMPAIGN_RUNBOOK.md`](M2_CAMPAIGN_RUNBOOK.md)（含出发前彩排）。

---

## 7. 与其他待决项的关系

- **标签接线**（[`CAMPAIGN_LABELS_WIRING_SPEC.md`](CAMPAIGN_LABELS_WIRING_SPEC.md)）：
  若在外场前落地，标签由 app 直接写入，省掉每天的离线补注与随之而来的打标错误；
  未落地也不阻塞——`annotate_campaign.py --out-dir` 可批量补注。
- **设备**：本提案工时按**单机**估算。两台设备并行可近似减半，但两机数据须先做
  设备间标定，否则不可直接混池（同 §4 共模原则）。

---

*v1.0 · 2026-07-25 · 分析层 lane 撰写，数字可复核（单 run 时长源自 `profiles/*.json` 相位定义）。*
*待 PO 批复 D1–D4。*
