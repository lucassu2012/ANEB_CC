# INBOX · 20260711_ANEB测试方案_v1（新在上）

## M-V1-007 · 2026-09-03 20:45Z · 【L2】≈20h 无动作，已请大脑接管 T88 链（致 61bd2401）
你自 D-655（09-03 08:20 +0800）起无任何可见动作；盘满已解（C 盘 29.5GB，D-693）、E-3 已绕开、E-1 设备在线。协调侧已请大脑本人接管 T88 装机→重验→开窗链（M-B-007）。你若在跑：立即在板面 T80 写一行实况（哪怕是「阻塞＝X」），然后从 M-V1-005 第 2 点开始；大脑已接手则从格 1 接。

## M-V1-006 · 2026-09-03 04:5xZ · 复工催办（致 61bd2401）
D-655 已发四小时，上游未见你任何动作（无装机／无入册／无开窗）。若你在跑：第一动作＝按 M-V1-005 第 2 点装 T88（`assembleDebug` 前先看 C 盘余量——D-667 记 0 字节，11:37 曾有 352.9MB，Gradle 可能因盘满假败，先量盘再归因）→ D-581 重验 → 按 D-655 格阵开窗并在板面 T80 登记 DW 号。若盘满装不了：先做 P0-2 ①③④（不依赖构建）并在板面写一行「阻塞＝C 盘」，别静默。收工提交末尾写「下次预计开工／停机至」。

## M-V1-005 · 2026-09-02 · 【PO 裁定】持续推进；T88 自装授权；整形线前置动作（致 61bd2401）
1. PO：无作息窗，持续推进；恢复即按 P0-2→P0-3，不等任何人——窗令若未到，先做 P0-2 ①③④⑤ 与本条第 3 点（不依赖窗令）。
2. **T88 装机不需 PO 在场**（先例 D-610，0 层授权）：`assembleDebug` → `adb -s <SN> install -r` → `dumpsys package com.aneb.probe | grep lastUpdateTime` 入册（判据禁 versionName）→ `settings get secure enabled_accessibility_services` 须含 ANEB 服务（被清空按 D-634 授权用 adb 恢复，勿 force-stop）→ D-581 重验 → 再开窗。
3. **整形线前置（不需 E-2 的部分先做）**：gnirehtet 反向 USB tether 装机与 P40 侧 VPN 授权（设备已解锁、0 层授权覆盖）；PC↔P40 经 USB 连通性干跑（`ip route get` 出口应为 tether 接口）并记 README；clumsy/WinDivert 装载与「转发流量可被整形」的 0 成本干跑等 E-2 提权到位后做（PO 已被请以管理员身份起终端）。
4. GLM key／吞吐随时可消费：吞吐线设备侧准备按大脑排期。

## M-V1-004 · 2026-09-02 · 评审要点与复工清单（致 61bd2401＝端点 `local_85fe7be8`，SPEC-1+SPEC-2 属主）
评审全文 `REVIEW_20260902.md`（§4 计划、§6 各会话一句）。**你是全项目关键路径：下窗只差你一句 go/no-go（D-654 尾句）。** 复工顺序：
1. 等／催大脑一句窗令（P0-1：T88 装不装、停窗规则取一条、t90 台账口径、格阵）——没有它 `e234_collect` 排窗门会假拒。
2. **P0-2 五件（<1h，全设备侧）**：①通道 A 功能验证（开被测 App 看 `ADAPTER_EVT`）并记 `dumpsys power` 的 `mWakefulness=Awake`；②DeepSeek 三轮试水（发送键／F6 是否短答／有无限流；两开关按 D-641 置位并入 README）；③改掉三处会误导自己的移交件——速查卡 3a-2「17.4s 截断六轮」已被 wave0 README §11b 撤回、卡 §1「余量约一半」按实测 ≈1.2s dump 节拍重算（90Hz 界 1.41s 余量仅 ~15%）、wave1 README §5.4③「ds 驱动器尚未创建」已由 `34ca13f` 解；④`drive_cell.py`／`drive_cell_ds.py` 里 `e1_io.pin_console_utf8()` 现在写在 `main()` 的 **docstring 内、从未执行**（AST 零 Call 节点，D-648③ 在这两只上空转）——移到函数体首行；`focus_ok` 改解析 `mCurrentFocus` 含 PKG 且 `mWakefulness=Awake` 否则停；附 `git diff` 实录证时序未动、更新窗令 P3 哈希、D-650④ 出订正条；⑤收窗时按 D-597 由你重算台账（`corpus_ledger.py` + `--check`）。
3. **P0-3 跑窗**：窗内文档冻结（D-643(a)）；逐格记开关态与格间单调钟间隔（D-640④）；逐轮核内容事件 >0（`wifi_f6_b_VOID1` 轮 1/3 是零回答轮，与图层失效无关）；每格约 2 分钟处复查 C 侧满帧；收窗自证范围＝仓根＋会话 scratchpad（D-644④）；滑落次数以窗令点名的那条规则为准。
4. SPEC-1 回填（P1-5：基线四处 PO 占位填 D-587/D-592/D-595、总计划 #3/#4 划线注 D-587、7-1 的 E1 状态以 D 号对账、交付包状态改「已验收（D-587 #1）」、按基线 L3 规则升 v3.0）排窗后，或等大脑裁给 cd5239ba。
5. 收工提交末尾请加一行「下次预计开工：…／停机至：…」——协调侧据此判静默，不再按 7×24 猜。

## M-V1-003 · 2026-08-28 · 【PO 裁定转达】E-1 解锁授权，T80 即刻激活
PO 经协调通道明示（2026-08-28，原话）："P40 没有锁屏密码，你可以自行解锁"。即：
1. **E-1 清零**——解锁不再是 PO 到场动作，执行会话可自行 adb 解锁（既有 WAKEUP+swipe 序列即可）；一页清 #4 的 E-1 半项可标 ✅（E-2 提权/E-3 热点硬件仍待 PO，只影响 L3 整形批）。
2. **行动**：按 T78 豆包先行批采集协议 v1.1 开跑 2.1——**测前实况预检照 CLAUDE.md P40 五步不减**（确认无冲突会话/VPN/抓包、警戒线 3 同刻一会话用机；测后清场复验）；语料落库后板面 @v3 启动 T+1 报告时钟。
3. 请按纪律把本裁定入册（D 号，来源注明"PO 2026-08-28 经远程协调通道指令"），并在 T80 行更新状态。

## M-V1-002 · 2026-08-28
1.2 决策请求一页清最急（它是 D0 清欠会的议程，也是解开 SPEC-2 全线前置的钥匙），请先于 1.1 产出。SPEC-4（v4）会在 D2 前交来 portraits 三态论证供裁项 B 引用——未到货前该裁项留占位即可，不要等它压住整页发出。

## M-V1-001 · 2026-08-28 · 随 T79 认领落一行板面指针（PO 已批）
认领 T79 时，请在 `docs/BRAIN_TASKBOARD.md` 头部注释区加入下面这一行（逐字）：

> 远程协调通道（PO 2026-08-28 批准）：各执行会话每次开工/收工执行 `git fetch origin claude/aneb-project-progress-analysis-x2wn7x` 并读该分支 `docs/coordination/INBOX_V<n>.md` 与 `BROADCAST.md`；协议见同目录 PROTOCOL.md。

这是本协议对 v2/v3/v4 的安装动作——板面是全员每次开工必读的唯一文件。落行后在回执里引用本编号。
