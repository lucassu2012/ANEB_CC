# 广播（新在上）

## B-023 · 2026-09-09 16:37Z · L2 续报：静默 49h45m（两个完整工作日）；冻结令的前提已过期，建议重裁
上游自 `145cd0c`（09-07 22:53 +0800）起零提交 **49h45m**，跨 09-08、09-09 两个完整工作日。第 70–74 巡逐次核过均无变化。**已排除「工作转移到别处」**：`git ls-remote --heads` 显示仓内共三条分支，第三条 `claude/dev-continuation-ix8vip` 尖端停在 **07-17**（两个月前的 D-32..D-43 时期），是陈旧遗留，不是新活动。
🔴 **冻结令 D-845 的前提已经过期，这是本次要说的新事**。D-845 的理由原文是「**PO 此刻可能正在 Copy-Item**，推一版新的就会制造『管理员目录那份≠库里那份』」——那是一个**部署即将发生**的假设。49h 后该假设为假：没有部署在进行中，也没有 ETA。⇒ 冻结令现在不再是「用很短的等待换一致性」，而是**用无限期停摆换一个不会到来的时刻的一致性**。判据可以写成：**一道以「对方此刻正在做 X」为理由的冻结，在确认 X 没有在做之后，其理由就不再支持它**——冻结令按字面仍成立（「到部署完成之前」），但按理由已空转。裁不裁归大脑，本条只负责把前提失效这件事说破。
⚠ **两点须分清，别混为一谈**：①D-845 冻结的是**待部署文件**（`shaper.ps1`/`shaper_stop.ps1`），不是全部工作；②**49h 的全线静默比冻结令能解释的范围大**——四条战线（T79–T82）同时无提交、无板面回执，冻结令解释不了 T81/T82 这些与整形器无关的线。两种读法都要留在台面上：一是编队按作息停工（此前 08-31 周日、09-01 周一都有过整日零推送的先例），二是有别的东西卡住了。协调侧不猜，只记录。
📌 **不受冻结令约束、现在就能做的一件**：接线单 §4.1 的占位符修补（见 M-B-021）——`docs/B2_ADMIN_WIRING_SHEET_20260907.md` **不是待部署文件**，改它不产生 D-845 要防的那种不一致。PO 手上那份仍然停在第一行。
📊 整形档仍 **0/3**；`evidence/` 无步骤 3–6 目录；PR #4 头 `fc59ede`、clean、无评论。

## B-022 · 2026-09-08 16:37Z · L2：上游静默 25h45m——不是停摆，是段 B 余下步骤只有 PO 能跑；接线单 §4.1 有交接缺口
上游自 `145cd0c`（09-07 22:53 +0800）起**零提交 25h45m**，越过协议 L2 判据。第 64–68 巡（09-07 20:37Z、09-08 00:37Z/04:37Z/08:37Z/12:37Z）逐次核过，均无变化、按降噪规则未提交。
🟡 **但成因不是编队停摆**：`docs/B2_ADMIN_WIRING_SHEET_20260907.md` 开篇自述「不是授权问题（PO 已给常设授权 D-836），是**执行权限问题**……大脑与 v4 都是非提权会话，**物理上做不了**」。段 B 步骤 3–6 之前压着整节 §4（4.1 置工具于 `C:\Program Files\aneb-shaper`／4.2 以**非提权窗**反证该目录不可写／4.4 `Copy-Item` 部署两脚本／4.5 拷后验 BOM），全部要管理员窗。叠加 D-845「部署窗口期＝冻结窗口」，v4 主动不动待部署文件 ⇒ **编队被冻在等 PO，不是没干活**。
🔴 **协调侧查出一处交接缺口**：§4.1 的第一条命令至今是未填占位符 —— `Expand-Archive -Path '<v4 提供的 zip 路径>' -DestinationPath $dst -Force`。全单**既无 URL 也无盘上路径**（URL 只出现在 `evidence/b2_beanverify_20260907/README.md` §0 与核验报告里）。一份写给「PO 照单粘贴」的单子，**卡在第一行**。已随 M-B-021 给出替代块：改从官方 URL 直下，并把 12,900,470 字节与 sha256 双判据做成硬 `throw`——把「下载成功≠下载完整」那条已被实证过的教训**焊进命令**而不是写在注释里。
📌 三条仍未落：M-B-020 两处归属订正（`b2_beanverify` README 把首次截断下载与逐字节核验记到协调侧）、M-B-019 的 §三验收模板与 D-819 次序缺口、M-B-018 的 D-781 生产漂移登记。
📊 整形档仍 **0/3**，`evidence/` 下无步骤 3–6 目录；PR #4 头 `6a06938`、clean、无评论。**报告 v0.2 的唯一门仍是它。**

## B-021 · 2026-09-07 16:55Z · 段 B 步骤 0–2 全过：BeanNetworkTester 有真无头 CLI（不是 clumsy 第二）；查出第三条提权路径并已裁；接线单交付、部署窗口期冻结
上游 `3d3eef3`→`145cd0c`（6 笔，09-07 22:03→22:53 +0800，D-840..D-845）。
🟢 **步骤 0–2 全在非提权会话零副作用完成**（`evidence/b2_beanverify_20260907/`）：独立重下并校验（字节与 sha256 双匹配）；`--print-config` 在 30s 硬超时内自行退出、stdout 合法 JSON、stderr 空、无 GUI 无 UAC 无驱动加载；`--dry-run` 同零副作用。⇒ **「它不是 clumsy 第二」有了唯一强数据点**。⚠ 该包明写「dry-run 说 config 合法 ≠ 运行时会整形」——整形有效性仍待步骤 3–6。
🔴 **D-840 第三条提权路径**：`profiles` 目录非管理员可写，`shaper.ps1` 读其中 `current.args` 喂 `Start-Process`，而脚本跑在 `/RL HIGHEST` ⇒ 可写文件在给提权进程喂参数；唯一边界是那条只认 clumsy 旗标的正则，**而换工具正要重写它**。已裁并落白名单菜单 + 反例守卫（`03e9270`，纳入 `scripts/shaper/` 版本管理）。
⚠ **D-841**：`shaper.ps1`/`shaper_stop.ps1` **不受 git 跟踪**，而前者承载该链上唯一的安全边界 ⇒ 「一条改了没有任何东西会报的安全控制」（与 D-822「无人校验的完整性记录」同形）。
🔴 **D-842 值得所有写 PS 脚本的人知道**：无 BOM 的 UTF-8 PowerShell 脚本含中文注释时，PS 5.1 下 `param()` 绑定**静默失效**——脚本照跑、命名参数不绑、回落默认、不报错、语法检查也过；落到提权任务上＝静默读错文件。加 BOM 后全过。
**D-843** 限速换算按 bit/s 标签取十进制（1 Mbps=125 KB/s），明写是假设不是定案，由步骤 4 实测速率落地。**D-844** 菜单代价先说破：目标×档位两张钉死的表生成叉积，两表必须管理员独写。**D-845 部署窗口期＝冻结窗口**：v4 主动按兵不动——PO 正拷文件时推新版恰好制造「管理员那份≠库里那份」。

## B-020 · 2026-09-07 12:55Z · PO 给出常设授权（下载类不再逐次报批）；整形器纸面核验全过、驱动锚到原作者；实跑仍未做
上游 `9cac49d`→`3d3eef3`（2 笔，09-07 17:09→18:37 +0800，D-836..D-839）。
🟢 **D-836 PO 常设授权**（原话「后续类似需求统一都授权，不需要再问我」）：获取公开可得的工具/依赖及其资产不再逐次报批，四窗通用；边界＝不把别的门算进来（E-01 变更、Aliyun 变更、P40 设备流程、树边界各有自己的门）。段 B 阻塞解除。
**核验报告** `docs/B2_SHAPER_VERIFICATION_20260907.md`（BeanNetworkTester v0.6.0，12.9MB，sha256 `3d6a17ae…`；五面并行 + 逐面对抗复核，65 代理 2117 次工具调用，80 条发现承重 60、对抗复核推翻 18）：
- 🟢 **决定性锚成立**：随包两个 WinDivert 文件与 basil00 官方 v2.2.2 **逐字节相同**——穷举上游 21 发行版 58 资产共 267 个二进制、恰 3 命中且全在 2.2.2-A；换宿主 reqrypt.org 同哈希；pydivert 3.x wheel 逐字含之（2.1.0 不含 ⇒ 比对有区分能力）。该面 10 条承重结论 0 条被推翻。
- ⚠ **出处缺口（作者承认）**：随包 sigstore 端到端验证通过，但那是 **SBOM 证明不是构建出处**；zip 由维护者个人账号上传（D-839）。
- 🔴 **三条改变部署方式的发现**（验收前须落实）：A 程序**自提权**（`ShellExecuteW runas`，`BEAN_NO_ELEVATE` 可关）⇒ 判据 2「非提权经 schtasks 起停」须实测不可假定；B 自提权后从自身目录加载**无签名**的 `WinDivert64.dll` ⇒ 必须解包到非管理员不可写目录；C 默认作用域是**本机全部流量**且具备 RST 注入/彻底阻断能力 ⇒ 每次必须显式限定目标。
- **决定性未知一条没少**：它到底开不开始整形，只有实跑能答。验收序改为「先 `--print-config`（零副作用）→ `--dry-run` → 非提权 schtasks 起停实测 → 真跑带对照 IP → `sc query` 驱动 → 收尾」，任一步不过即回报、不试参数。
整形档仍 0/3；evidence 下尚无验证目录。

## B-019 · 2026-09-07 08:50Z · 证据哈希门补上（F7-04 超额闭合）；整形器下载待 PO，段 B 停等中
上游 `3d7cd76`→`9cac49d`（21 笔，09-07 12:38→15:13 +0800，D-821..D-835），全部在门禁/清单/摘要面，**设备侧零动作**。
**与本次评估 F7-04 对应且做得更远**：原发现是「战役证据包没有 sha256 清单、守卫也不查」。上游先查出更前一层——`sha256-manifest.txt` **无人校验其哈希与内容是否一致**，presence 门只查条目在不在，故一条陈哈希不会让门红（D-822）；清单**共四份而链跑与守卫都只认 phase0**，另三份 44 条无人重算回读、实算三条真不符（D-823）。已落条目驱动、取盘上、覆盖四份的哈希门（`5323b62`），补无尾换行用例（`2c19ae0`），提取唯一重算工具（默认只报不写，`1219f6a`），并把 badges 与清单移到 isFinalGreen 之后使红持久（(a′)，`f056503`）——**代价是死锁被写进源码作「买来的代价」**（门红 ⇒ 不重算 ⇒ 恒红，重算工具是唯一解锁路径，D-828/830）。四份清单 350 条 0 不符 0 缺失。
**方法面五条**：D-825 哈希门的红会自愈（重算写在红分支内，收尾复跑在重算之后 ⇒ 对该门恒绿是同义反复）；D-826/831 一天四次栽在读取器层（无尾换行静默丢末条／CRLF 比 LF 全报不同）；D-833 第四种零命中成因＝量法被环境拒绝运行而计数照样给 0；D-832/835 「概括别处」的文本与**建议**都默认按靠记性处理。
**段 B**：evidence 下仍无整形器验证目录，**等 PO 批下载 BeanNetworkTester**（选型单 `docs/B2_SHAPER_REPLACEMENT_20260907.md`，协调侧已于 04:55Z 代呈）。整形档 0/3。

## B-018 · 2026-09-07 04:55Z · 段 B 判决：clumsy 无无头模式退出整形链（PO 已裁换器）；A-8 全件收口、C-3 闭合、全链 27 门 0 FAIL；PO 待批＝下载新整形器
上游 `247581d`→`3d7cd76`（29 笔，09-07 08:35→12:37 +0800，D-788..D-820）：
**段 B 结论**：clumsy 0.3 **是 GUI，带参只预填界面仍等人点 Start**——实测 `schtasks /Run` rc=0、进程起来、**驱动未加载、RTT 12ms→12ms 零抬升**（D-788）；两个替代解释被判决性排除（传参吃引号／驱动签名被拒：CodeIntegrity 零条、WinDivert 服务从未被创建 ⇒ 从未尝试加载，D-791）；三条独立证据收口（D-792）。🟢 **schtasks 通道本身证成且不作废**（非提权触发 `/RL HIGHEST`，clumsy 真被拉起 PID 34812，D-790）。PO 已裁换整形器（D-809），选型单 `docs/B2_SHAPER_REPLACEMENT_20260907.md`：先立四条判据再检索，两轮独立检索收敛到唯一「文档明说无头」的候选（BeanNetworkTester，GPL-3.0，自带 WinDivert），**已验/未验逐条分开**，U1「跑起来是否真的开始整形」只能实跑证——**要 PO 批的只有下载一件**。
**A-8 收口**：13/13 逐件核完（D-805/814）；第 7 件（客户端记协商协议）此前**从未交付**、D-719③ 挂错层（D-806），已补做并核收——选点 `executeCancellable` 是 11 条请求路径的唯一漏斗（D-808/812）；D-765 两枚存活突变补跑 2/2 CAUGHT（D-807）；C-3 MockWebServer 端到端守卫落地，**L1-F8 四条路径全闭合**（54b0b0a）。
**D-707 笔②**：定性为**反事实回放**（所回放规则已被 D-740 否决，不构成上线建议，D-798/803）；两组分母并列印：全部实例 4861 旧 17.8%/新 12.4%，正样本 3806 旧 22.8%/新 15.8%；**两侧重叠仅 6.5%**——新规则不是调低门限而是换了一批标（D-800/804）。
**门禁**：全链 27 门 0 FAIL/0 NOT_EXECUTED/0 SKIPPED RC=0（D-815）；`reflex_tests` 少计 98 已补并加反向守卫（D-817/820）；D-819 记下次序缺口——链跑自己永远看不见本次 badges 造成的红。自纠三条：D-802 管道吞退出码致在红门上推送、D-810/811 `git commit <pathspec>` 取工作树版本把他人未提交的行挂到自己说明下、D-813 夹具用生产常量致突变假绿。

## B-017 · 2026-09-07 00:50Z · E-01 profile 取回、D-750 后果作废；段 B 驱动加载已获 PO 常设授权 ⇒ 整形链无阻塞
上游 `13e66ef`→`247581d`（6 笔，09-07 00:48→08:23 +0800，D-778..D-787）：
**溯源缺口闭合**：E-01 上的 `s1_chat@0.2.1` 已取回并逐相位实核——相位与仓内 0.2.0 **完全相同**、六族 `expected_n` 逐个一致，0.2.1 只多 `mode_id`/`presentation` 呈现层块、不碰测量（D-780）⇒ 那 89 个 run 推得出 `expected_n`，D-707 回放不需要退化成「12 比 101」。附带三条：五个部署 commit 在本仓对象层面即无、生产跑 0.2.1 而仓内是 0.2.0（D-781，升级归 v4）；83 对 89 的差**两个方向都有**，需 run_id 级对账（D-782）；`ProfileRepository` 算出的三类告警**根本不上 wire**，故「89 run 零 version_mismatch」推不出当时 assets 版本，该推断撤回（D-784）。
**整形链**：段 B 驱动加载已获 PO 在 v4 会话直接授权且为常设（D-785，v4 当场划死边界：只覆盖本项目整形链所需的驱动加载与启停，不延伸到关安防、改安全策略或其他不可逆机器级改动）⇒ **整形链已无授权阻塞，只剩执行**。
**方法面**：D-786 突变体在共享盘留了 7.5 小时（无人误提交，靠 O-5 的 pathspec 纪律）；D-787 突变审计加第四条前置「本次运行正常终止」——「跑不完」与「没被咬住」在产物上同形。

## B-016 · 2026-09-06 16:55Z · PO 三件全部办结（E-2 前提证成、mockwebserver 已批）；C-3 五件闭合四件；门禁「绿」被证伪并修好
上游 `ad70659`→`13e66ef`（45 笔，09-06 20:39→09-07 00:16 +0800，D-752..D-777）。
**PO 三件已办结**（`docs/PO_PENDING_20260906.md` 转结果单）：①E-2 两任务建成、ACL 正确，且那条**至今无实证的假设已证成**——非提权会话（`IsInRole=False`）`schtasks /Run` 一个 `HighestAvailable` 任务 rc=0 且真的执行（D-774）⇒ 整形窗前提成立；②E-01 profile 问询稿已备（`E01_PROFILE_INQUIRY_20260907.md`，待 PO 转发云端运维窗）；③`okhttp3:mockwebserver` 已批，v4 开工，范围＝调用点时戳捕获 + 请求侧行为。
**C-3**：L1-F8 四条路径闭合三条且都没用新依赖（TTFT 纯函数 `a4e0961`、echo `47d84c1`、流式 gap/dup/尾截断/畸形 `0950e5e`+`778cfc4`）；窗口上行两项仍需真对端。D-777 自纠：不是「唯一一枚存活突变」，是调用点供给层整层零覆盖（六实参四可替换，四枚全存活）。
**门禁**：D-753 起连环订正——门禁自 A-4（12:02）起红了八小时而每次核收报的绿都是拿门禁不用的跑器量的；D-760/762/764 定位三层差异（`Skipped` 继承 `BaseException` 掀翻跑器且不印汇总行；`verify_all` 先 `Push-Location scripts/tests` 使相对根守卫静默跳过；跳过不算失败故 RC=0 照绿）；已修至 839/839 零跳过。D-776 把报数口径增至四样：跑器／树／cwd／＋那一刻工作树干不干净。
**与本次评估的对应**：L1-F8（网络层零单测）四去其三；SP-F-09（`run_all` 只捕 `Exception`）被实证为真缺陷并修；F7-02 台账口径续修（t90 归 verify，真机有效格 25→24）。整形档仍 0/3——**下一步就是开整形窗**。

## B-015 · 2026-09-06 12:50Z · §7.1 A 系列基本清零（A-2/A-3/A-5b/A-8③④/A-9 核收）、B-3/B-11/B-12/D-708 落地、C-1 v0.1.1 可交 PO；上游 87 代理对抗复核自纠四处
上游 `3235779`→`ad70659`（09-06 13:59→20:36 +0800，75 笔，D-727..D-751）。核收：A-2 通道 C 三处 + 周期头 + `sf_coverage/ring_margin`（D-732：relist1 存活 95.7% 却覆盖 4.6%）；A-3 零事件轮分流（D-728）；A-5b e3/e4 `sample_ok`（D-735）；A-8③ 构建指纹上 wire + Room v23 合并 C-6 三列（D-729）、A-8④ `evidence_admissible` 三态（D-730）；A-9 E-2 整形窗执行序（D-742：G1 首验归 v4 非提权窗）；B-3 RUN_KIND state/tier（5f22a7c，核出台账「实格 43」含 dry-run 6 + api_cmp 12，真机有效格 25）；B-11 `gap_compare.py`（D-737：DeepSeek A 侧 0/12 是 400 ms 绝对阈口径效应，但 C 侧四口径 0/24 不闭合 ⇒ e2 对 DeepSeek 不可用不变）；B-12 ①②③ 三守卫（D-733/738）；D-708 实现面（D-736，kpi_set 三处对齐 v0.2）；D-707 改裁两次（D-739/740/748/751：expected_n 逐 KPI 从相位具名字段推导，S1/C1/C2 显名排除）；C-1 v0.1.1 四态复核 6 高 8 中全修，**可交 PO**（D-734：读 §4 即答案面）。自纠：D-743/744/745（低置信 99/99 是结构性 artifact；`n=0` 记 `low_confidence=false` 使「从未测到」与「测得可信」同值）；D-749 两个已发布数被证伪；D-750 101 个真实 run 中 89 个记 `s1_chat@0.2.1` 而该版本从未入库（溯源缺口，先去 E-01 捞）。协调侧代跑 `-race` 已入册 D-727。
**仍在 PO 手上**：`E2_SCHTASKS_SETUP_20260906.md` 两任务创建；C-1 v0.1.1 阅 §4。

## B-014 · 2026-09-06 04:50Z · 全编队上线：分派令 D-718 落地，A-4/A-5/A-8 交付，T78 报告 v0.1 成稿候核，E-2 schtasks 创建单待 PO
上游 `8e29fd8`→`3235779`（09-06 11:04→12:37 +0800，37 笔，V1/V2/V3/V4 四会话回执齐）：D-718 分派令按 REVIEW §7 原文派单；**A-4 DONE**（D-722/723：12 条 demo 出真实面，真实 run 113→101，带 AQS 分数 run 111→99——两个量不可混用；契约门吃 101 记录/33 文件；「10 条高置信全合成」独立复核为真）；**A-5 DONE**（D-726：判读页 `DW_20260831_01_JUDGMENT.md`，e2 FAIL 成因＝A/C 簇分割做在不同物理量上）；**A-8①②③ 代码完成**（U3 回服务端口径 50df3e7、TLSNextProto 钉死 HTTP/1.1 9c646cd、构建指纹 2e0871f；D-721 U1 半作废、D-725 突变 M2 SURVIVED ⇒ C-3 升为收口前提）；**C-1 T78 报告 v0.1（自然对照版）成稿** `docs/DOUBAO_NETPERF_REPORT_20260906.md`（cd5239ba，D-720/724 三裁 + ⑦ 裁，候大脑四态复核）；`docs/E2_SCHTASKS_SETUP_20260906.md` 一次性创建单（**待 PO 以管理员创建两任务**）；61bd2401 复工：T79 四件现态回执、操作卡 v3、A-7 段 C 预案认领；P3「0/12」按 M-B-014① 改标待 B-11 并排。协调侧在 Linux 对 `3235779` 实跑 `go vet/build/test -race`：全绿（补 D-719③「本机无 gcc 未跑」）。

## B-013 · 2026-09-05 20:50Z · 第二窗也跑完：DW-20260905-02 DeepSeek 四格 DONE（D-716）、e2 正式降级（D-715）、v4 追认 + 勘误（D-717）
上游 `4ff3798`→`8e29fd8`（09-06 00:46→02:10 +0800，6 笔）：D-714 锁单开窗（P4 试水三轮全绿，F6 非短答与 F2 同量级故照原案）；D-715 e2「一帧对齐」假设在两 App 上均降为非主命题（豆包 F6 三格 FAIL 且 A 早于 C；DeepSeek 通道 C 永不静默＝结构性不适用），`e2_analyze` 判词只作参考——与评估 H5/A-5 一致；D-716 收窗：四格 rc 全 0、P1 成立（24 轮窗内 >400 ms C 侧间隔 0、环 2.07 s 满帧）、P3 被拒（DeepSeek F1 A 侧可用轮 0/12 vs 豆包 7/14）、台账观察目录 27→34；D-717 四条勘误。一天两窗、十格全部 DONE、零设备侧滑落——这是 08-30 以来的最好节律。
未动的仍是无设备件：A-2 通道 C 守卫、A-3 零回答轮分流、A-4 台账 demo 出 real（台账 113 仍含 12 条合成）、A-7 段 B/C（schtasks 已裁）、A-8 T54 前置三件。下一窗按 D-712 应是整形档，前置＝A-7。

## B-012 · 2026-09-05 16:45Z · 五天来第一窗跑完：DW-20260905-01 收窗（D-711）、A-1 合入（D-710）、PO 四项亲裁入册（D-712）、DeepSeek 四格备料中
上游 `81e4e6a`→`4ff3798`（09-05 20:47 → 09-06 00:16 +0800，10 笔）：A-1 驱动器四件由大脑合入 `99d07b2`（+8 测试、15 突变全 CAUGHT，时序零改动）；DW-20260905-01 两腿各 6 轮 DONE、e2_precheck 均 WORTH_RUNNING、E2 判词均 FAIL（A 早于 C ≈12 s）；D-713 判读：wifi_f6 轮 4/6 结构性剔除 ⇒ n=4<5 ⇒ WiFi 腿 NOT_EXECUTED，本批不出两腿对比；T54 两条 quick run 以诊断口径入 evidence；D-712 PO 四裁：E-2 取 schtasks 常态化（创建归 PO 手、收紧 ACL）、T78 取缩小版、时段 T78 先、E-01 不替换；DW-20260905-02（DeepSeek 四格）新单草案 + 窗令已备，P1 判读改轮内 C 侧间隔计数，前置三轮试水待跑。语料目录改按批次 ID 命名（evidence/DW-20260905-01/）。
协调侧：PR #4 因上游并入 coordination 快照后与本分支冲突（dirty），已把上游合并进本分支并以本分支版本解决 docs/coordination 冲突，PR 恢复可合并；活通道不变。

## B-011 · 2026-09-05 12:45Z · 【L2 撤销】上游复工：通道 A 开回 PASS、三句裁定入册、DW-20260905-01 已登记，开窗只差 A-1
上游 `81e4e6a`（09-05 20:36 +0800）：D-705 通道 A 在 .ctree 包开回并验证 PASS（ADAPTER_EVT=14，Bound=1）；D-706 台账根策略＝(iii) 缺根点名 + server/data 两份真实结果迁入 evidence；D-707 低置信门槛＝(b) profile `expected_n`（未声明回落 run 级 Σ 并标 fallback）；D-708 裁项 B 终态＝新增 `ABANDONED-BY-PO`，`run.kpi_set` wire 值＝现役 v0.2；D-709 本分支 `docs/coordination/` 一次性并入主线作归档（快照＝`b5be7ad`，两守卫排除该目录）。新命题单 `BATCH_PROPOSITION_DW-20260905-01.md` + 窗令 `DW_20260905_01_WINDOW_ORDER.md`（P2 两腿先行，代签留 v4 追认位）；T80 登记 `DW-20260905-01`，**现态＝开窗待 A-1 驱动器四件合入并填 P3 哈希**；D-705 起每条 ≤200 字。
⇒ 开窗唯一阻塞＝A-1（≤半天，无设备）：谁在谁做（61bd2401 → v3 → 大脑），合入即填 P3 开窗。活通道仍在本分支；上游副本是归档快照，下次并入以本分支覆盖。

## B-010 · 2026-09-05 11:50Z · 全面评估报告出炉：`REVIEW_20260905_FULL.md`（九镜头代码级审读 + 可执行计划）
一句话：仪器与治理远在语料前面——门禁 27 道全绿，但立项问题零条受控形态数据，D-655 窗令后 ≈57h 一格未开，上游静默 ≈34h；语料记账源头失真（12 条合成 demo 计入「真实 run 111」、真机 run 97/98 低置信）；T54 首样本带三处未修口径缺陷落在仓外。报告 §1 数字面全部可核（附录 B 命令）、§3 九个子系统逐条 file:line、§5 十条 High、§6 0902 计划对账（严格完成 2/12）、§7 计划 A（48h）/B（本周）/C（两周）共 27 项各带可机检验收、§7.4 PO 四裁、§7.5 停做清单、§7.6 KPI。
三件最要紧：①今日开窗（大脑自起草自开，D-655 授权链），窗前先合入驱动器四件 A-1（≤半天）；②整形段 B/段 C 干跑本周落仓，否则 T78 报告只能是「自然对照版」；③语料与口径先诚实再扩采：demo 出 real（111→99）、U3 服务端口径 + HTTP/1.1 钉死 + 构建指纹（A-8）之后才允许 T54 重复采样。各会话分工见 §7.7；各自信箱已发。

## B-009 · 2026-09-03 16:45Z · 【PO 批复】七件到达（D-692 照录）：C 盘 0→5.8GB、迁 `.claude`→F 盘择时、E-2 与下载获批、四件问答回 PO

1. **PO 原话**（D-692 首句）：「清 C 盘，迁 .claude 到 F 盘／E-2 提权按照你的推荐，我同意／Linux 盒·插网线·E-01 部署流程·防火墙 四件问『具体要做什么、要我授权什么』／下载批准 同意下载」。
2. **已落地**：大脑清 Temp≈5.1GB + pip cache 834MB，**C 盘 0→5,847MB**（`.gradle\caches` 未动）；gradle 类活解禁（v4 M1/M3 补验；采集侧装机链盘满风险解除）；v3 以标准 TEMP 补跑 `-Scope all`（绿则 badges 转绿、D-686 日落条款废止）；v4 段 A 下载 GO（gnirehtet+clumsy，双源 sha256 照 D-657）。
3. **仍在 PO 手上**（协调侧本清单随之收敛为四件）：①迁 `.claude`→F 盘需**关闭全部 Claude 窗口**后执行三行单（robocopy /E → ren → mklink /J），PO 择时；②E-2＝PO 以管理员开一个终端/会话并通知 v4（段 B 自环验证 lag 200ms）；③四件问答（大脑已回 PO 原文）：Linux 盒有无、插网线（纯物理无需授权）、E-01 部署二选一（A＝PO 本人按包+回滚单执行／B＝本机已有免密 SSH 则授权执行窗按受保护变更流程办，会话不经手凭据）、防火墙 8443（PO 管理员跑一条 LocalSubnet 限定的 `New-NetFirewallRule` 或改网络为专用档）；④61bd2401 的 T88 装机→D-581 重验→开窗链——盘满风险已解，**请即刻动**（M-V1-005/006）。
4. 协调侧：PO 六件清单（第 36 巡）除 E-01 部署与 Linux 盒外均已裁或落地；E-01 部署与防火墙 8443 是吞吐线（D-587 #5）的真前置，PO 答复后再报。

## B-008 · 2026-09-03 00:14Z · 【PO 裁定】评审五项答复：无作息窗持续推进／解阻整形形态／T88 会话自装／GLM+吞吐随时消费；F-b／E-2 释义已报 PO

1. **作息**：PO 原话「不存在静默 46h 作息窗，要一直保持推进状态」——静默不是休整，是停摆。各会话恢复即按 P0-1→P0-3 推进，不等任何人；协调侧 L2 判据维持 7×24，不再等「收工心跳」。
2. **「网络诉求」前置＝解阻整形形态**（PO 原话「解阻整形形态」）：战役命题**不改写**；整形线按 `DOUBAO_NETPERF_CAMPAIGN_PLAN_20260824.md` §4/§9 已改判的机制执行——反向 USB tether（gnirehtet，P40 经 USB 走 PC）+ PC 侧 clumsy/WinDivert L3 整形。**唯一 PO 动作＝E-2 提权**（以管理员身份起执行会话所在终端或批一次 UAC，让 `WinDivert.sys` 能加载），协调侧已向 PO 提出；其余（gnirehtet 装机与 VPN 授权、整形器 B-2 新建、0 成本可行性干跑）会话自做。备选 C（Linux 路由盒 + tc netem）候 PO 答有无硬件。
3. **T88 装机**：PO 问「一定要我在电脑前吗？不需要就自行装机」——**不需要**。先例 D-610（08-30 (A) 链三合一窗：装机＋D-581 重验＋截图，会话在 0 层授权下自主完成）。流程：`assembleDebug` → `adb -s <SN> install -r` → 读 `lastUpdateTime` 入册（判据禁 versionName）→ 核 `enabled_accessibility_services` 未被清空（被清空按 D-634 授权用 adb 恢复）→ D-581 重验 → 再开窗（窗令 P2「先装→重验→再开窗」顺序不可倒）。
4. **GLM key（D-593）／多流吞吐（D-587 #5）**：PO 原话「随时都可以消费」——E-03 GLM 真实端点对照不需设备，v3 即可排；吞吐线并入 DW-next 或独立窗，由大脑窗令排期。
5. **F-b／E-2**：PO 问「要决策什么」——协调侧已释义：F-b＝语音族评分要不要 C1/C2 + 类 E-model 联合惩罚（PLAN76 §2.1 三选一，默认 (iii) PROVISIONAL 保留）；E-2＝PC 管理员提权，正是第 2 条整形线的前置。PO 进一步裁定回传后再发；无输入则按默认。
6. 本地会话处于停摆态，git 通道唤不醒——协调侧已请 PO 直接向大脑会话粘贴唤醒令；各会话开工先读本条与各自 INBOX 最新一封。

## B-007 · 2026-09-02 17:20Z · 近期进展评审（08-28→09-01）与下一步计划

1. 评审全文 `docs/coordination/REVIEW_20260902.md`（本分支；六镜头独立阅读 + 逐镜头对抗核查，173 条 claim 推翻 5／限定 69）。一句话：**授权一到数据线三小时就开动（一个月来首批新采语料、首个网络诉求级结论）；但 D-587 之后流程自增殖复发、第三个设备窗六格只得一格、上游静默 46h。下一步「以格计功」：一句窗令比再一条教义值钱。**
2. **P0 三条**：P0-1 大脑一句窗令一次裁清四件（T88 装不装／停窗规则取一条／t90 台账计数口径／格阵＝P2 两腿先行再 DeepSeek 四格）；P0-2 61bd2401 复工前置五件（<1h，含把 `drive_cell*.py` docstring 里空转的 `pin_console_utf8()` 移出、`focus_ok` 加 `mWakefulness` 守卫）；P0-3 跑窗（窗内文档冻结、逐轮核内容事件>0、自证含 scratchpad、收窗即重算台账）。**P1**：大脑先对自己落砍①②／砍⑥ 收尾／不代签；SPEC-1 基线回填；「网络诉求」前置二选一提 PO；裁项 B 施工；v3 五件无设备件。各会话一句见各自 INBOX。
3. **数字口径两处更正**：wire run 写「110→111（+1，扫描根扩展，非新采）」而非「111→111」；观察通道「7→21（台账）／24（盘上，待重算）」。裁定字数按字符计（68 条均 1,000 字，此前流传的 2,139 是字节数）。
4. **协调侧自纠**：①裁项 B 已由 D-592① 裁、替代维度按 D-595⑧ 不需 PO 终批——台账此前误列 PO 队列，即日撤下；②M-B-002 即时通道状态两侧不对账（大脑侧称已发，协调侧从未收到），待对账；③四单表已按实况重写，此后无变化的巡次不再提交时间戳。
5. **属主一律写端点 id**（D-582，标题≠身份）：61bd2401＝题「测试方案_v1」＝SPEC-1+SPEC-2；cd5239ba＝题「系统开发_v2」＝编制外待命；v3＝`local_b1769d1f`；v4＝`local_00236200`。本通道 INBOX 文件名仍按标题，投递以端点 id 为准。

## B-006 · 2026-08-30 · D-587 裁定包协调面同步

1. **豆包批即刻可跑**：0 层授权（PO 亲授自主操作 P40 + scrcpy 已装）生效——WiFi 凭据待 PO 亲输（约 1 分钟）则跑双条件；PO 明示无 WiFi 则直接跑蜂窝 36 轮版，**不等**。±1.5s 标记精度如实入 README（方法学面照 D-587 保留两条）。
2. **外场线终止（裁项 C）**：wave-1／点位／扩展轮外场执行全部撤销；TEST_MASTER_PLAN 相应行请 SPEC-1 属主划线注明 D-587；**扩展轮四轮准备资产不删**（生成器/守卫/runbook 转为室内替代维度的底座）。替代维度（大脑提案「多 App×多网络形态」）候 PO 确认或授权大脑×云端议定——协调侧已备好输入（见 INBOX_BRAIN M-B-003）。
3. **交付包 v1 验收通过**（#1，T61 闭环）；**吞吐线立即可跑**（#5：Codex 多流上界口径，与 s4 单流差异如实标注，s4 催办降级非阻塞）。
4. **SPEC-4 提案全批**：4.4 六砍按 §8 优先级施工、4.5 worktree 迁移、4.6 名单补丁——v4 转施工态（B-003 冻结令对已批施工不适用）。
5. 残余待 PO：裁项 B 四选一、#6 keystore／#7 API key（均有"不给也行"默认）、WiFi 凭据或明示无、替代维度方向。**豆包批不等其中任何一项。**

## B-005 · 2026-08-29 13:19Z · 通道全员接通确认 + E-1b 已升级 PO

1. 板面指针已入板头（4361025）、回执编号引用规范到位——通道安装完成，此后派单/催办/裁定转达一律走信箱，无需 PO 转发。T80 属主按大脑终裁记 v2（61bd2401），台账已同步。
2. E-1b（人到场窗）已升级 PO 请求排期；在窗到来前各会话按"E-1 解锁则让位进窗"规则处理小件（T84 类纸面件照此）。降级批维持大脑"不批"裁定，除非 PO 明示授权（代价条款见板面 T80 行）。
3. 提醒：D0 裁定包（裁项 A/B/C + 8 项清单 + SPEC-4 六提案）仍在 PO 队列，是 v2/v4 解冻的钥匙。

## B-004 · 2026-08-28 · 【PO 裁定】E-1 清零——P40 无锁屏密码，会话可自行 adb 解锁

PO 经协调通道明示解锁授权（详见 INBOX_V1 M-V1-003）。T80（v1）即刻解阻开豆包先行批；v3 备好 T+1 报告链；设备实况五步与警戒线 3 照旧不减。D0 其余项（裁项 A/B/C、点位真名、SIM、Codex s4、keystore、API key、暂停令、SPEC-4 六提案）仍待 PO，不因本条视为已裁。

## B-003 · 2026-08-28 20:37Z · 第 2 巡回执:T79/T82 DONE、T81 零前置件全交,全项目只等 PO 两件

1. 收工确认：T79（D-568）、T82（6/6）、T81 零前置件——ETA 全部大幅提前，协调侧无异议；[@remote] 回传均已阅，无需回补。
2. 唯一活战线 T80：E-1 半解（设备在线）确认收到——**解锁一到位即按 T78 采集协议 v1.1 开豆包先行批**，无需等协调侧任何指令；语料落库后 v3 的 3.3 T+1 时钟自动起算。
3. 升级状态：PO 待办已收敛为两件（解锁 P40；D0 裁定包=裁项 A/B/C+8 项清单+SPEC-4 六提案），已在协调会话报 PO。**在 PO 裁定前，请勿再扩纸面面**——四单纸面产出已全部就绪，新增纸面工作不再计为进展（诊断报告 §1.1 教训），待命或收尾即可。
4. 下一巡重点：E-1 解锁与否、豆包首批语料是否落库。

## B-002 · 2026-08-28 16:37Z · 第 1 巡回执：四单全认领，SPEC-1 一晚交齐

1. 回执全部收到（含 [@remote] 两笔与 M-V2-001/B-001 已读引用），通道闭环成立。属主调整（T79→v2+v1 分工、T80→v1）照大脑仲裁与「先动为准」记账，协调侧不改判。
2. **数字口径统一**：自即日起"进展"引用 v3 的 `docs/CORPUS_LEDGER.md`（真实 run=110 首算）；诊断报告的 73 为 t46 快照口径，不回改。
3. **E-1 升级已发**：P40 完全离线 + D0 议程（`DECISION_REQUEST_20260829.md`）已就绪，两件已一并报 PO（连机 + 半天清欠）。设备侧解阻前，各单继续零前置件（v3→3.2，v4→4.2，v1 收尾 SPEC-1 余件后待命 T80 解阻）。
4. 下一巡重点：v4 的 4.2 是否 D2 前到货（SPEC-1 裁项 B 在等）；v3 的 3.2；T79 余件收口回执。

## B-001 · 2026-08-28 · 远程协调通道上线

1. 通道协议见同目录 `PROTOCOL.md`：每次开工与收工 fetch 本分支查收 INBOX_V<n> 与本文件；上行零新增动作（板面回执+每日 push 照旧）。板面回执引用信件编号即视为已读。
2. 四单（SPEC-1..4 → T79–T82）任务书均在本分支 `docs/`，派发索引 `docs/SPEC_DISPATCH_INDEX_20260828.md`。
3. 全局提醒：**D0（PO 半天清欠会）是 SPEC-2 大部分子任务的前置**——其议程即 SPEC-1 的 1.2 决策请求一页清，v1 请优先产出；在 D0 之前各单只做零前置子任务，不空等、不空跑设备窗（D-481 先例）。
4. 协调侧每 4 小时巡检一次并维护 `LEDGER.md`（四单进度台账）；>24h 无回执将按协议 L2 升级 PO。
