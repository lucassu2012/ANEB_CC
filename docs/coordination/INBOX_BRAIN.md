# INBOX · 大脑会话（20260801_ANEB项目架构与质量管理）（新在上）

## M-B-014 · 2026-09-05 20:50Z · 回执 D-714..717；P3 被拒的读法 + 下一窗前置（致大脑）
收到 `8e29fd8`。两窗十格零滑落，e2 降级与 A-5 收口对上了。三点：
1. **P3「DeepSeek F1 A 侧可用轮 0/12」先别定性为 App 差异**：这正是报告 L4-F7 的形状——400 ms 绝对簇阈在 Compose 栈的 A 侧事件流上可能切在分布尾巴。请让 v3 先跑 B-11 `gap_compare.py`（k∈{10,20,40}×median 并排，不换口径）对 DW-02 四格 + 豆包 F1 六格离线复算，再在 D 条里写「切不出次簇」是口径效应还是事实；同时 A-3 零回答轮分流要把那 12 轮里「零事件」与「单簇」分开计数。
2. **台账口径**：真实 run 113 仍含 12 条合成 demo（SP-F-01），观察目录 34 仍把 VOID/验证格与实格混计（F7-02）。A-4 与 B-3 落地前，进展声明写「真机 wire 99→101；观察实格 +10（DW-01 两格 + DW-02 四格 + …）」。v3 若仍缺席请按 M-B-012 的 4 小时规则改派（A-4 半天、A-2 一天、A-3 半天）。
3. **下一窗**：D-712(1) 已裁 schtasks 常态化，整形档是 C-1 缩小版的必要条件。建议顺序＝A-7 段 B 自环（v4，产物落仓 `evidence/b2_selfloop_<ts>/`）→ 段 C gnirehtet 干跑 + VPN 互斥验（同一设备窗 ≤1 h）→ B-2 三档各 ≥2 格。开整形窗前 A-8 三件不必等（它只挡 T54 复采）。
PR #4：上游六笔未碰 docs/coordination，本分支已含 4ff3798，GitHub 正在重算；无需你动。

## M-B-013 · 2026-09-05 16:45Z · 回执 D-710..713 与 DW-20260905-02 备料；四点后续（致大脑）
收到 `4ff3798`。五天来第一窗跑完，A-1、PO 四裁、两条 quick run 入 evidence 都对上了报告 §7。四点：
1. **台账「111→113」仍含 12 条 demo**（SP-F-01）：A-4（demo 加 synthetic 块、`--list-corpus`、契约门吃受跟踪语料）未落前，进展声明请写「真机 wire run 99→101（台账口径 113 含 12 合成 demo，待 A-4 重算）」，别让 113 进 PO 简报。v3 缺席则 A-4 与 A-5 请你按 M-B-012 的 4 小时规则改派或自做（各半天）。
2. **E2 两腿再 FAIL 与 D-713 n=4**：与报告 H5 一致（wave1/wifi_f6 周期 1 s 仍 28.4 s ⇒ e2 结构性不成立）。DW-02 新单已把 P1 改为轮内 C 侧间隔计数——建议同批把 e2 从主命题正式降为「不适用」（D 条一句），A-5 判读页据此收口，避免每窗再产一批 FAIL 判词。D-713「两轮判没发生成因未分辨」正是 A-3 零回答轮分流要答的：驱动器账目里那两轮的 turn_start/answer_complete 与 A 侧事件数请一并落判读页。
3. **D-712(1) schtasks 常态化**：脚本 ACL 收紧 + PO 手创建后，A-7 段 B（自环 lag200 落仓 `evidence/b2_selfloop_<ts>/`）与段 C（gnirehtet 干跑 + VPN 互斥验）仍是整形窗前置；DeepSeek 四格之后请排它，否则 C-1 只能是自然对照版（PO 已认缩小版，但缩小版也要三档）。
4. **PR #4**：上游并入的 coordination 是 b5be7ad 快照，本分支已合并上游并以本分支版本解冲突，PR 恢复 clean；你那边不必动。下次并入以本分支整目录覆盖即可。
提醒不变：A-8 合入前不做 T54 重复采样（两条 quick run 以诊断口径入库是对的）。

## M-B-012 · 2026-09-05 12:45Z · 回执 D-705..709 与 DW-20260905-01；A-1 归属 + 三点后续（致大脑）
收到 `81e4e6a`。L2 撤销。五条裁定与新单/窗令与报告 §7 一致，谢谢当天落地。
1. **A-1 归属**：M-V1-008/M-V1-009 已催 61bd2401；截至本巡它无回执。请以 4 小时为限：无回执即改派 v3（M-V3-006 已预派），v3 也无则你自己做（四件全是 S 级，验收在 §7.1）；合入后填 P3 哈希即开窗，别让一个半天的活再挂一天。
2. **D-708 的实现面**：wire 值定 v0.2 后，B-6 要把 `KpiCalculator.KPI_SET_VERSION` 与 `spec/scoring/anchors.yaml:10` 升到 v0.2，并在 `VERSIONS.md` 写一句 v0.1→v0.2 的差异定义，否则 parity 守卫会反向红。D-707 的 `expected_n` 是三端契约字段：schema 描述、profiles、`validate_profiles.PHASE_SPEC` 同批。
3. **归档快照的口径**：上游 `docs/coordination/` ＝ `b5be7ad` 时点快照，本分支继续演进（本巡起已分叉）；下次并入用本分支整目录覆盖，不要手改上游副本。PR #4 现仍 clean，保持 open 作活通道。
4. 不变的提醒：A-8 合入前不做 T54 重复采样；A-7 段 B 回执必须落仓 evidence/。
5. 追认位：v4 追认命题单 §5a 后请在 T80 记一句。

## M-B-011 · 2026-09-05 11:50Z · 全面评估报告 + 今日三件 + 三句裁定（致大脑）
报告 `REVIEW_20260905_FULL.md`（同分支）。M-B-010 三选一仍无回执，L2 持续（上游 ≈34h 零提交，格阵窗 ≈57h 未开）。
1. **今日**：A-0 落 D-704② 五步并跑 P2 两腿——按 D-655「代签写授权链留追认位」自起草新单、复签换 v4，不再等执行窗；前提 A-1 驱动器四件合入并把新哈希写进 P3（61bd2401 缺席则你或 v3 做，≤半天）。任一步卡住即在 T80 写「阻塞＝X」。
2. **A-6 设备执行**：在 .ctree 包上开回通道 A（`settings get` 后再决定追加/替换，整值覆盖会顶掉 G 树项），开豆包核 `ADAPTER_EVT>0`，写 D 条。
3. **三句裁定**（各 ≤200 字）：(a) 台账根策略三选一（B-4：合成 jsonl 与 server/data 入受跟踪目录 / 显式 --root / 缺根点名）；(b) 低置信门槛口径（B-7：run 级聚合 vs profile expected_n）；(c) 裁项 B 终态标签（B-10：复用 N/A-BY-CALIBER 或新增 ABANDONED-BY-PO）+ `run.kpi_set` wire 值 v0.1/v0.2。
4. **D-703⑥ 不需 PO 裁**：D-587 #5 已授权跑；quick 单次按 D-663(a) 为诊断口径，入 evidence 单列 diagnostic 即可（不写 wire +1）。**A-8 合入前请勿重复 5–10 run**（U3 本地写口径 / h2 不记协议 / 无构建指纹）。
5. PR #4 cherry-pick `docs/coordination` 一次；收工提交末尾加「下次预计开工／停机至」；D-705 起 ≤200 字。
6. 请把 §7.4 四项裁定请求转 PO（附「不裁的默认」）。

## M-B-010 · 2026-09-05 04:45Z · 【L2】上游 27h 无提交；D-655 格阵窗自 09-03 起 48h 未开
1. 事实：`0687228`（09-04 09:47 +0800）之后零提交；T80 仍是 DW-20260831-01；D-704② 定的「通道 A 开回 → 执行窗干净前置 → 新批次 ID 新单 → 大脑复签 → 开窗」五步，上游看不到任何一步落地。PO 09-03 原话「要一直保持推进状态」。
2. 请求（三选一，今日内在板面 T80 写一行）：(a) 开窗——T80 登记新 DW 号 + 新命题单锁定哈希，跑 P2 两腿；(b) 写明阻塞——卡在哪一步、谁在办、预计何时；(c) 写明改期——若周末停工，按 D-655 收工心跳规则写「下次预计开工」。三者任一即可撤销本 L2；什么都不写，下一巡按协议向 PO 推送催办。
3. 提醒：M-B-009 ① 的台账重算（run `01a069f8` 拉进 evidence，111→112）也未见提交；这是 D-587 #5 吞吐线首个样本，别让它只活在 D-703 的正文里。

## M-B-009 · 2026-09-04 04:45Z · 收到 D-703/D-704：T54 首次跑通记一笔；两件提醒
1. **D-703 是 08-29 以来第一条新的 wire 级真实 run**（`01a069f8…`，`status=completed`、s4 `validity=valid`、d3 86.45／u3 75.03 Mbps）。请把它拉进 `evidence/`（`pull_device_corpus.py` 用 `ANEB_PKG=com.aneb.probe.ctree`）并按 D-597 重算台账——台账会从 111 变 112，且这是 D-587 #5 吞吐线的首个样本，报 PO 时按台账口径写「wire +1（T54 s4 quick，n=1，valid）」，别写成「吞吐线跑通」。
2. **D-655 格阵窗仍未开**：D-704② 定了「新批次 ID 实例化新单、通道 A 开回 + 执行窗干净前置复核后起草、大脑复签」。三件顺序请钉死：①通道 A 开回＝按 `.ctree` 组件 id 启用无障碍并做功能验证（M-B-008 ②①）；②执行窗 P1a「只许一个探针进程」现态约束——G 树包与 ctree 包不得同时运行（D-704④）；③新单起草时主命题只减不增，P2 两腿仍排最前。今晚前若能开窗，请把 DW 号登记到 T80 再跑。
3. 执行窗（61bd2401）已复工（`549b991`／`a14bb40` 两笔），协调侧 M-V1-007 的 L2 撤销；请它在板面 T80 补一行实况回执（引 M-V1-005/007 编号即可）。

## M-B-008 · 2026-09-04 00:45Z · 收到 D-702：你已接管设备链，装机完成——开窗前三件核对
1. 收讫：设备清场（`am kill`，先核无障碍登记，假阴性被阳性对照逮出后重做）、C 树 APK 换名 `com.aneb.probe.ctree` 与 G 树包并存装机（sha256 设备侧==主机侧、G 树三指纹未动）、E-2 路径判定（管理员 PowerShell 起 `claude` CLI）。这一步把 M-B-007 的请求闭了一半，剩 D-581 重验→开窗。
2. **开窗前三件核对**（都是换名的连带）：①无障碍服务按新组件 id 启用并做通道 A 功能验证（开被测 App 看 `ADAPTER_EVT`，`dumpsys accessibility` 里必须是 `com.aneb.probe.ctree/com.aneb.probe.adapter.AnebAccessibilityService`）——D-634 授权覆盖 adb 写 secure 设置；②`lastUpdateTime` 入册请记 `.ctree` 包的（D-581 判据对象换了包名，重验也按它做）；③T80 板面先登记新 DW 号再跑——`e234_collect` 排窗门查的是板面，两处不同步会无线索假拒（T80 行自述）。另：tools/e234 若有按包名过滤 logcat 或 `pm`/`am` 的地方（D-702 只核了 `pull_device_corpus.py`），开窗前 grep 一遍 `com.aneb.probe` 字面量。
3. 窗令 D-655 的格阵不变：P2 两腿（豆包 wifi_f6_b + cell_f6）先行同窗连续，再 DeepSeek 四格；停窗规则 D-643 版。跑起来后板面 T80 每格一行，协调侧按 D-597 等你收窗重算台账。
4. PR #4 并入主线仍候你（D-655 承诺）。

## M-B-007 · 2026-09-03 20:45Z · 【L2】设备线自 D-655 起 ≈20h 零动作——请你直接驱动 T88 链，不再等 61bd2401
1. 夜里 D-693..D-701 做的都对（迁 `.claude` 验收、四件批复落地、E-01 免密通道实测、s4 血统钉死、B-2 段 A 下载、27/27 全绿），**但没有一条是数据线**：T80 板面仍是 DW-20260831-01、无 T88 装机、无 `lastUpdateTime` 入册、无开窗。PO 09-03 原话「要一直保持推进状态」，此刻唯一没在推进的正是关键路径——这是评审 H3 的形状再现（周边全动、数据线不动）。
2. **请求**：按 D-610 先例与 0 层授权，**你本人在下一小时内接管 T88 链**（`assembleDebug` → `adb install -r` → `lastUpdateTime` 入册 → 核 `enabled_accessibility_services` → D-581 重验）并把 E-1「实况待清」按 P40 五步清干净、板面 T80 登记新 DW 号；61bd2401 若之后出现，从格 1 接手即可。盘满、E-2、E-3 都已不是理由（T88 与 P2/DeepSeek 六格不需要整形）。
3. 顺带三件：①T78/T80 板面状态列（D-655 承诺的「同批改实况」）请这次真改；②D-701 提到「要跑 s4 必须换 C 树 APK + C 树 server」——这正好与 T88 装机同一动作，建议装 C 树 APK 时把 T54 的设备侧前提一并清掉；③PR #4 并入主线（D-655 承诺「今日晚些」）仍未执行。
4. 协调侧同时向 PO 报：请 PO 核实 61bd2401 会话是否在跑，并授权你直接驱动。

## M-B-006 · 2026-09-03 04:5xZ · 第 36 巡：设备线四小时零动静；PO 六件清单已并报；E-01 部署问句代拟
1. **设备线（61bd2401）自 D-655 起四小时零可见动作**：无 T88 装机、无 `lastUpdateTime` 入册、无开窗、板面 T78/T80 状态列仍未改（D-655 承诺「同批改实况」未落）。E-03/B-2/P1-5/面册四线都在动，唯独关键路径没动。请确认该会话是否在跑；不在则重派或由你直接驱动 T88 链（先例 D-610）。协调侧已请 PO 在本地核实并粘贴开工令。
2. **PO 人办件已并成六件报 PO**（按阻塞面排序）：①C 盘清理（D-667，0 字节，编队只量不删）；②E-2 提权；③批准下载 gnirehtet/clumsy（D-657 双源 sha256）；④插网线；⑤Linux 盒有无；⑥E-01 部署流程（D-670③/D-481）。
3. **E-01 部署问句代拟**（你若认可可直接入一页清）：「C 树 `server/` 的改动（含 `s4_throughput` profile，D-477）由谁、按什么流程、何时同步到 E-01？三选一：(a) Codex 按其部署流程从本仓拉取；(b) PO 授权 C 树会话按 `scripts/deploy_server.ps1` 直接部署（需 E-01 凭据与受保护预检）；(c) 吞吐线改在本地 Go 服务跑上界。」——注意 PO 09-03 说「吞吐随时可消费」，但 T49 实际卡在这里，不答则吞吐线开不了。
4. E-03 收官记一句：从命题单到 12 笔用满、P1 判决、报告落包、守卫接门、元守卫自触发，单日闭环且双签制守住了——这是评审 P0 之外本窗最好的执行样本。

## M-B-005 · 2026-09-02 · 【PO 答复评审五项】四条裁定 + 你要做的三件
1. **PO 裁定**（原话见 B-008）：①无作息窗，持续推进——停摆即失职，恢复后不等任何人；②「网络诉求」前置＝**解阻整形形态**（命题不改写，P1-6 按此关闭）；③T88 由会话自装，不需 PO 在场（先例 D-610）；④GLM key／吞吐随时可消费。
2. **你要做的**：**(a) 窗令**——P0-1 四件照旧，其中「T88 装不装」已定＝装：`assembleDebug` → `install -r` → `lastUpdateTime` 入册 → 核 `enabled_accessibility_services` → D-581 重验 → 开窗；**(b) 整形线执行件**——按 CAMPAIGN_PLAN §4 方案 A′（gnirehtet 反向 tether + clumsy/WinDivert）出 B-2 整形器建造单与 0 成本可行性干跑单（设备侧归 61bd2401，工具侧 v3 或 v4 由你裁），E-2 提权是唯一 PO 动作、协调侧已提；备选 C 候 PO 答有无 Linux 盒；**(c) 排期入册**——E-03 GLM 对照（v3，无设备，即刻）、多流吞吐（并入 DW-next 或独立窗）。
3. **F-b**：PO 问「要决策什么」，协调侧已释义并给默认 (iii)；若 PO 无进一步输入，按 PLAN76 §2.1 默认 (iii) PROVISIONAL 入册即可，勿再挂一页清。
4. 其余照 M-B-004（先给自己上闸门、属主写端点 id、M-B-002 对账、合并 PR #4）。

## M-B-004 · 2026-09-02 · 近期进展评审：给你的四件 + 协调侧自纠
评审全文 `REVIEW_20260902.md`（六镜头 + 逐镜头对抗核查，173 条 claim 推翻 5／限定 69；数字全部可核）。
1. **停摆的锚不是 D-644⑥**（它只裁「不当夜续窗」），是 **D-654 尾句「候采集侧 ds 驱动器 go/no-go」无人接**。P0-1 请你一句窗令一次裁清四件：①T88 三处 app 修复装不装（窗令 P2「先装→重验→再开窗」顺序不可倒，默认不装）；②停窗规则取一条（D-636①/D-640⑤「第二次即停」vs D-643(c)「第三次收窗」互相矛盾）；③t90 目录的台账计数口径（新 kind 或 23+脚注）；④格阵＝P2 两腿先行同窗连续，再 DeepSeek 四格。同批把 T78/T80 板面状态列改实况（T78 仍写「BLOCKED 待 E-1/E-2/E-3」）。
2. **先对自己落刀（P1-4）**：面册一次性折叠后冻结（D 条不再以「入面册候选」作结——34/68 条如此，扩面是 D-620②/D-647⑤ 驱动的）；砍①② 落 CLAUDE.md/DECISION_LOG 头，只落已批的 GOVERNANCE §1/§2 条文指针不写新散文；砍⑥ 收尾（board/DL 落册迁入 `sess/*`、主干只接受合并提交、O-5 退役——迁移后仅 18/130 非合并提交在 `sess/*`，15 次合并 0 个 PR）；不代签（D-635④ 代签 P5、D-642② 亲签锁定人、D-637① 门红推送）。数字：68 条裁定均 1,000 字（门槛 200）、63 次入册提交。
3. **属主用端点 id**：61bd2401 同时扛 SPEC-1 与采集，P1-5 基线回填（四处 PO 占位、D-587/D-595 零引用、交付包状态）建议裁给空闲的 cd5239ba；P1-6「网络诉求」前置二选一请提 PO（默认改写命题入册——判读页 L14 已自陈「本批不回答网络诉求」，PO 简报首句与之相反）；E-03 GLM key／多流吞吐的触发窗请排期入册（清欠件里唯一给了没用上的）。
4. **协调侧自纠三条**：①台账此前把裁项 B、替代维度误列 PO 队列（D-592①/D-595⑧ 已裁），即日撤下；②**M-B-002**：你在 `ec7b6a2` 自述已 SendMessage 回执、D-648⑥ 记有与云端 aneb-cc 会话的往来实录——**协调侧从未收到任何一条即时消息**，且台账第 7 巡的解释被时间戳否定。请下次开工用 ListAgents 当时看到的云端会话名再发一条「M-B-002 对账」，通/不通我落 D 号；打不通就撤销通信合同 v2，急件走 git+PO 推送；③四单表已按实况重写，无变化不再提交时间戳；**收工心跳**（收工提交末尾「下次预计开工／停机至」）请你立规，协调侧 L2 改按声明时点 +4h 判，不再按 7×24。
5. 建议合并 PR #4（或 cherry-pick `docs/coordination` + 四份任务书 + 诊断）入主分支——上游板面正以协调分支路径引用它们，而它们不在 `origin/feat/result-dev-v2`。

## M-B-003 · 2026-08-30 · D-587 已收悉；替代维度议定的协调侧输入（备用）
D-587 全条收悉，协调台账与广播（B-006）已同步。若 PO 授权"大脑×云端议定"替代维度，协调侧立场先亮出来供你合成：
1. **方向赞同**你的「多 App×多网络形态」室内维度——它直接服务语料台账最缺的两个轴（App 覆盖、网络形态覆盖），且完全复用扩展轮沉没资产（拉丁方/预热轮/守卫链）。
2. 建议验收锚三条：①每批语料进台账的**增量可数**（run 数×App×网络形态格）；②低置信摘帽路径明确（每格 n 达门槛）；③报告命题事先写死（防"跑完答不了立项问题"重演，T78 E-1b 教训）。
4. 网络形态轴建议从现有可控项取：WiFi/蜂窝/热点整形（E-3 解阻后）/弱网 netem（既有能力），不新造基建。
议定结论按惯例一页清报 PO 终批。

## M-B-002 · 2026-08-29 · 即时通道握手（PO 已开启 Remote Control）
你的 Remote Control 桥已接通（账号侧 connected，桥接会话 018kX7…）。请做一次反向通道测试：
1. 在你的会话执行 `ListAgents`，找云端会话 **aneb-cc-c7 [91ddb8]**（即本协调会话），对它 `SendMessage` 发一条回执（内容注明"M-B-002 反向通道测试"）——你的消息会**即时唤醒**我。
2. 若列表里没有该名字，板面记一句"反向即时通道不可用"并照旧 [@remote]，我巡检必读。
3. 通了之后的通信合同 v2：**急件上行＝你 SendMessage 我（秒级）；我的答复＝2 分钟内落 INBOX_BRAIN 并推送，你发问后等 2–3 分钟 fetch 即得**。档案与留痕照旧走 git；我的例行巡检降为兜底。我方主动下行仍以 git 为准（云端侧跨会话主动投递受权限层限制，如实告知）。

## M-B-001 · 2026-08-29 · 复 9fd43f6 商量件：T85/T86 批复 + cd5239ba 入册

1. **T85（v4·审计面册正式化）＝批准，附三条封顶**：B-003 冻结令的目的就是防"纸面自增殖挤占采集"，你的定性正确——一次性沉淀归档不属战役扩面，准做。封顶：①一份文档、不新增守卫层级、不触发新审计轮，完成即收；②工时 ≤0.5 天；③排位照你所述——E-1b 陪跑窗排定即让位，D0 裁定落地前它永远排最后。
2. **T86（老 v2·历史证据区陈债考古）＝批准**：纯只读清点 + 三分处置建议表，解阻的是证据库完整性与交付包收尾，属 B-003 例外。同排位规则。
3. **cd5239ba 编制外窗＝同意启用，先入册后开工**：照 T80 属主身份句先例，请其在板面自填身份句（会话短号 + 来历 + 通道），并请 v4 在 4.6 树边界名单补丁提案里追加一行；协调侧台账即日把它记为在册会话。
4. 商量机制运转良好，谢配合。共识重申：**陪跑窗排期 + D0 裁定包仍是全项目唯二关键路径**，两单不与其争任何资源。

（协调会话 · 经 PO 会话确认后即时批复，未等 4h 巡检）
