# ANEB 近期进展评审（2026-08-28 派单 → 09-01 01:03 上游末次提交）

> 协调会话出，2026-09-02 17:20Z。范围＝`origin/feat/result-dev-v2` 自 08-28 起 558 提交 + 本分支协调记录。方法＝六镜头独立阅读（数据线／裁定流／工程／提交节律／协调通道／需求对齐）→ 逐镜头对抗核查（共 173 条 claim：被推翻 5、带限定 69）→ 完整性批评 → 协调侧合成。**被推翻的判断已剔除；带限定的以「限定：」标出；所有数字有出处。** 进展口径以 `docs/CORPUS_LEDGER.md` 为准；属主一律写端点 id（D-582：标题≠身份）。
> 通知：BROADCAST B-007 + INBOX_V1..V4／INBOX_BRAIN 各一条（同批提交）。

## 0. 一句话

**授权一到，数据线三小时就开动了（一个月来首批新采语料、首个网络诉求级结论）；但 D-587 之后流程自增殖复发、第三个设备窗六格只得一格、上游已静默 46h——下一步必须「以格计功」：一句窗令比再一条教义值钱。**

## 1. 数字面（全部可核）

| 量 | 值 | 出处 |
|---|---|---|
| 提交数（按日） | **558**：08-28 11／08-29 289／08-30 187／08-31 50／09-01 21（15 个 merge） | `git log origin/feat/result-dev-v2 --since=2026-08-27` |
| 只动 `docs/` 的提交 | **389/543 非合并 = 71.6%**；≤2 行提交 33.1%（08-31 单日 49%）；`board(` 前缀 115（20.6%） | 同上 `--name-only`／`--shortstat` |
| 自我订正提交（标题含订正/自逮/自纠/我自己） | **65（11.6%）** | `git log --format=%s` 正则 |
| 裁定 | **68 条（D-587..D-654）**，共 68,018 字、平均 **1,000 字/条**（最长 1,988），63 次入册提交；50/68 含教义/纪律条款，34/68 提及面册（约 27 条以「入面册候选」作结）；PO 输入 5 条 | `DECISION_LOG.md` L592–659（字数按 Unicode 字符计） |
| 审计面册 | §4 一级条目 **11（17a8fcf）→ 25（D-602「收口」时）→ 61（104c934）**，98→203 行、14.0→62.5 KB、18 次提交；封顶令 M-B-001「一份文档、完成即收、≤0.5 天」 | `git show 17a8fcf/b278d88/104c934:docs/AUDIT_PLAYBOOK_v1.md` |
| 数据格（目录计） | **保留 11／作废 5**（wave0 10+4，wave1 1+1）；按 App×形态×功能去重 8 格（含锚格与重跑格则 10） | wave0 README §15、wave1 README §5.1 |
| 能承载结论的格 | **判读侧口径 4**（DW-02 的 wifi/cell × f5/f6）；含 P4 次命题 5；含采集侧登记级与 cell_f2 约 6 | DOUBAO_WAVE0_JUDGMENT §4；wave1 §2；FINDINGS_RAW §1a-quinquies |
| DW-20260831-01 计划 vs 实得 | 6 格 → **1 可用格（改记时段对照，退出 P2 证据链，D-642①）+ 1 作废**；DeepSeek **0 格**；主命题 P1/P2/P3 **0/3** | wave1 README §0、§5；D-640/D-642/D-644 |
| wire 语料 | **110 → 111（+1，扫描根扩展 D-565/D-573，非新采）**；观察通道目录 **7 → 21（台账）／24（盘上）**——台账 08-30 16:12 `9454cc6` 后未重算 | `CORPUS_LEDGER.md` L6–16；`git ls-tree` RUN_KIND.json |
| 设备在用时长 | DW-01 ≈3.0h + DW-02 ≈2.3h + DW-03 ≈0.5h ≈ **6 小时**（授权 D-587 落地后的 3 个可采日，≈2 h/天）→ ≈1.8 保留格/设备小时 | 各格 `adapter.log` 首末时戳 |
| 提交节律 | **55.6% 提交落在 22:00–08:00（+0800）**；最长连续段 08-28 22:37→08-29 12:57＝14.3h/272 提交。限定：这是多会话并行吞吐，不是人工工时 | `git log --date=format:%H` |
| 静默 | 末次提交 `104c934` 09-01 01:03 +0800；至本评审 **≈46h** 无推送；末条裁定 D-654 尾句「候采集侧 ds 驱动器 go/no-go」无人接；L2#2（09-01 12:40Z）未见回复。限定：`sess/*` 未并入的本地工作对 origin 不可见 | LEDGER 第 24–30 巡；D-654 |
| PO 一页清 | 13 项已裁 **10**（#4 只解了 E-1；E-2/E-3 无「给/不给」裁定）；仍悬 F-b、E-2/E-3；砍①② 落 CLAUDE.md 属主是大脑本人 | `DECISION_REQUEST_20260829.md`；D-587/592/593/595/647 |

## 2. 做得好的

1. **「一页清→PO 裁定包」通道被实证有效**：D-587 落册（`e7b0699` 08-30 08:34）到首批豆包语料落库（`8b28ab4` 11:24）**不到 3 小时**，结束了一个月零新采语料；一页清两天裁掉 10/13 项、含「不给」入册（#6 keystore）。
2. **首次用数据回答了立项问题的一个子问题（限定）**：F6 图像生成 WiFi/蜂窝最长渲染静默中位差 1.6%，F5 联网搜索差 8%（n=6+6，LOW，两条件均为未整形好网络）。限定：回答的是「两档好网络间有无可辨差异」，不是「劣化网络下的诉求」——见 H1。
3. **错误被当数据而不是被删掉**：作废格改名保留（`cell_f2_VOID1` 钉住驱动器版本翻转点）；「双峰/高模」因果结论整批撤回并写明机制（FINDINGS_RAW §0，400ms 门限伪影）；订正链 append-only（68 行逐行 md5 未改）、分钟级（D-588 距 D-587 14 分钟）。限定：错误几乎都由执行侧或真机咬出（D-588 v2 读册、D-600 采集侧、D-602 v4 拒收、D-612 v3 核查），不是裁前自查。
4. **仪器改进有真机实证**：framestats 周期 20→1s 使同格「不相交 dump」9→0、「不可判间隔」9→0（wave1 §2，1/6 格）；通道 C 图层失效从立案（`4fe3ca3` 23:06）→离线修（`53802ce` 23:31）→真机验证（`db1a6da` 00:16）**70 分钟闭环**，代码＋反例＋突变审计＋真机四层（T90：524 段空 → 6 段）。
5. **命题前置锁定并写明「本批不回答什么」**（`BATCH_PROPOSITION_DW-NEXT.md`，`a85115f` 09:43 锁定，主命题只减不增，规则边界开数前登记）——正对诊断根因 4「需求未重锁」。
6. **进展口径守住了**：台账明写「观察批产出 0 条 wire run，写成真实 run +N 正是要拦的手抄」；PO 简报据此写「wire +0／观察通道 +5 格」。
7. **工程基础件真落地**：`corpus_ledger.py`／`check_evidence.py`／`badges.py`／`e2_precheck.py` 入门禁；`spec/scoring/check_versions.py` + `VERSIONS.md` 新增两道门；`verify_all.ps1` 分层后 25 门全绿归档（08-31 08:28）；驱动器入仓（`a9abdf4`，「决定标记偏移的东西不能活在 scratchpad」）；观察红线升格为 Kotlin 源码扫描测试；SPEC-2 2.5 UI 视觉证据链 DONE（`evidence/ui_20260830/` 7 张截图，并由此拍出 T88「49.5s 显示成 0ms」缺陷）。
8. **DeepSeek 线的前置基本就绪**：通道 A 对第二个 App 首次实证工作（D-637③，82 条 ADAPTER_EVT）；四坐标全部动作验证（`fb7f29c`）；双开关裁定（D-641）；`drive_cell_ds.py` 入仓（`34ca13f`）。下窗只差 go/no-go 与三轮试水。
9. **协调通道的「商量件」回路在 PO 在场时是分钟级的**：M-B-001 征询→批复 6 分钟、落板 14 分钟（限定：靠 PO 在协调端当场确认）；M-B-001 三条封顶与 M-B-003 三锚均被上游逐字照录（板面 T85／AUDIT_PLAYBOOK §0／D-595②／T33 §4）。

## 3. 不足（按严重度）

**H1 · 立项问题结构上仍未被触碰。** T78 的题是「豆包各功能对网络的诉求」，而三个窗的形态轴只有 WiFi／5G SA 两档好网络；热点整形（E-3）与弱网 netem 整形器自 08-24 起零移动；判读页自陈「本批不回答『豆包各功能的网络诉求』——它回答了装置能问什么」（DOUBAO_WAVE0_JUDGMENT L14，板面 T81 同句），次批命题单 §4 延续此声明。PO 简报首句「首个真正回答『网络诉求』的结果……答案是不敏感」与同批判读页相反，把**范围外**读成了**结论方向**。代价：形态轴不解阻，再跑多少格也答不了 T78；而且 F1/F2 短答类功能在现装置上至今没有一条可用的条件对照（A 侧可用轮不足型，改周期加轮数无用）。

**H2 · 第三个设备窗六格只得一格，连续性失守。** DW-20260831-01 计划 6 格实得 1 可用格；晨格 10:28 采完、22:05 才落库——**11.8h 是会话中断/白天歇工（D-640①「会话中断非操作拖延」）**，不是纸面消耗；纸面消耗只在 22:05→22:59 那段成立（10 分钟采集与 D-640..643 四条裁定+卡编辑并行）；第三滑是「明知 logcat 缓冲 8 秒仍用 `logcat -d` 量 89 秒轮」的量法错误，按 D-643(c) 无条件收窗；开窗前 go/no-go 阶段已有四次滑落（D-636①），窗内又新立冻结令与 30 分钟间隔令——规则在窗内边跑边增。此后上游静默 46h，末条裁定尾句「候 go/no-go」无人接。数据线的瓶颈形态是**操作者/会话可用时段**，自 08-29 起反复出现。

**H3 · 流程自增殖复发（诊断根因 3），且扩面是裁定驱动的。** 面册 §4 在「完成即收」封顶后 11→25→61 条，D-602 宣布「收口」后又 +35（最大单笔 D-649 批量 27 条，由大脑 D-647⑤ 明令「归还 v4 取批」）——**要指控的对象是 34/68 条裁定以「入面册」作结的写法，不是 v4**；68 条裁定 63 次提交、均 1,000 字（砍② 门槛 200 字）；六砍里约束大脑本人的两刀（砍① 记账合并、砍② 入册 ≤200 字）属主正是大脑，批复（08-30 08:34）后仍 72 个 `board(` 提交、08-31 ≤2 行提交 24/49；砍⑥ 只算试点 PASS——迁移后仅 18/130 非合并提交在 `sess/*` 完成、15 次合并全是本地合并 0 个 PR、CLAUDE.md O-5 未退役、且 D-614 `pull --rebase` 事故仍发生在共享分支。**大脑对自己的闸门弱于对执行侧**：D-635④ 代签 P5、D-642② 亲签命题单锁定人（v3 拒代签：「由一方代写的第二签不是第二签」）、D-637① 门红时 `;` 链推送并自报。08-30 深夜加班单三条线里有采集侧判读产出（`64bb531` 十一格重量、`a9abdf4` 驱动器入仓），但 v3 一夜九浪全为工具/守卫线。

**H4 · 治理规则曾直接关掉仪器。** CLAUDE.md P40 收窗步骤「停掉本次启动的一切应用/服务」让采集侧 `force-stop` 了自家探针，通道 A 离线 ≥16.5h（D-611）。限定：离线期间无需通道 A 的窗被排定，实际代价是次日开窗延迟；规则已于 D-611 后在 CLAUDE.md L29 加例外；剩余风险是 T88 装机后重验时的同型操作。

**M5 · 最高杠杆的代码最裸。** `drive_cell.py`／`drive_cell_ds.py` 决定标记偏移（判读归轮必需品），却**零测试**、两文件仅 14 行差异（限定：D-638① 刻意「逐字复制」并登记窗后由 v3 合一）；D-648③「九只工具编码自锁」在这两只里被写进了 `main()` 的 **docstring** 内、从未执行（AST 零 Call 节点），而提交说明称「已逐只跑 CLI 复核」、D-650④ 记为完成；`focus_ok()` 是整份 `dumpsys window` 的子串匹配，黑屏/后台可放行（正是「屏睡假零」场景，工具层零守卫）；`sh()` 自造 adb 封装且不带 `-s serial`（限定：`e234_collect` 主链已 `--serial` 必填，风险限于点按/输入动作）；实测 dump 节拍 ≈1.2s 而非名义 1s，对 90Hz 界 1.41s 余量仅 ~15%。`wifi_f6_b_VOID1` 里还有第二个独立失效：轮 1/3 内容事件为 0（豆包零回答），定性「瞬时故障」未查。

**M6 · 产出记账再次滞后（诊断根因 1 小规模复发）。** 台账 08-30 16:12 后未重算（wave1 两目录 + t90 未入，21 vs 24；`corpus-ledger-fresh` 门按 RUN_KIND 判据应为 DRIFT）；08-31 08:28 后无归档链跑，徽章仍报 e234=106 而代码已 122，D-649/654 的「40 门绿」是 docs 门不是链跑；板面 T78 状态列仍写「BLOCKED 待 E-1/E-2/E-3」、T88 仍 TODO（三处 app 修复从未装机、屏上未验）；裁项 B「正式放弃」（D-592①）裁了未施工（四画像各 2 字段仍 `PENDING-BY-CALIBER`，T82 已 DONE 盖住、无承接行）；D-593 GLM key 与 D-587 #5 多流吞吐「立即可跑」——窗口内零 run、零派件，是清欠件里唯一「给了没用上」的；基线 v2.0 四处 PO 裁定占位仍空、D-587/D-595 在基线/总计划/交付包三文件零引用、交付包状态仍「待 PO 验收」（D-587 已验收）；D-640③ 依赖格映射「v3 追认已排队」未落，DW-20260831-01 无独立 T+1 判读页；`.gitattributes`（D-647⑤，异议窗已过）未执行；移交件里有已撤回的陈述（速查卡 3a-2「17.4s 截断六轮」已被 README §11b 撤回；wave1 README §5.4③「ds 驱动器尚未创建」已解）。原始 dump 文本直接入 git（本窗 evidence +376k 行，单格 8.6–13.6 万行），clone/门禁扫描成本无人评估。

**M7 · 节律与在场（低置信）。** 55.6% 提交在 22:00–08:00，最长连续段 14.3h。限定：git 度量的是会话吞吐，不是人工时；「夜班与错误率同向」**不成立**（夜间自纠率 10.3% 低于白天 13.3%）。真正的问题是：本地 PC + P40 需要人在场，而项目对「人何时在」没有模型——协调侧把 7×24 当巡检基线（第 24 巡）也因此误判过一次。

**M8 · 协调通道承载力不足（协调侧自省）。** 两次决定性解阻（D-587、D-634）都走 PO→大脑直达，通道只事后镜像；它自己转达的唯一 PO 裁定 M-V1-003 被晚读 12.5h、靠 PO 手工粘贴才送达，且其内容（「T80 即刻激活」）当场被 v2 回执否定——真正解阻是 D-587 0 层授权；v3/v4 引用过经大脑转达的 M-B-001/M-B-003，但**从未引用自己信箱** INBOX_V3/V4 的信，18 条下行 9 条无回执（首次 fetch 时任务书尚无「第 5 步读 INBOX」）；派单→回执中位 4h21m（n=9，双峰：在线时 4–14 分钟，否则 12–22h）；**M-B-002 握手状态两侧不对账**——大脑侧自述已 SendMessage 回执（`ec7b6a2`）且 D-648⑥ 记有与云端 aneb-cc 会话的往来实录，协调侧从未收到任何一条即时消息、且台账第 7 巡的解释被时间戳否定（`ec7b6a2` 14:05Z 晚于 M-B-002 推送 14:01Z），23 巡未闭合；静默 46h 期间通道分不清机器关／休假／未推送／`sess/*` 未并入四种状态；台账四单表自 08-29 13:21Z 冻结、与文件头矛盾 ≥3 天；协调分支 35 条提交 24 条是时间戳；协调侧 LEDGER 与 DECISION_LOG 对「待 PO 项」不一致（裁项 B 已由 D-592① 裁、替代维度按 D-595⑧ 不需终批，台账仍列 PO 队列）；`docs/coordination` 与四份任务书从未合入上游，而上游板面正以协调分支路径引用它们。

## 4. 下一步计划（P0 三条；属主写端点 id）

会话对照：**61bd2401**＝端点 `local_85fe7be8`、题「20260711_ANEB测试方案_v1」＝SPEC-1+SPEC-2（T79/T80）；**v3**＝`local_b1769d1f`；**v4**＝`local_00236200`；**cd5239ba**＝题「20260715_ANEB系统开发_v2」，编制外待命（处置权归大脑）。

| # | 动作 | 属主 | 前置 | 验收判据 | ETA |
|---|---|---|---|---|---|
| **P0-1** | **一句窗令（DW-next）**，一次裁清四件：①T88 三处 app 修复装不装（窗令 P2「先装→重验→再开窗，顺序不可倒」，默认沿用现装）；②停窗规则取一条（D-636①/D-640⑤「第二次即停」vs D-643(c)「第三次收窗」互相矛盾）；③t90 目录的台账计数口径（新 kind 或 23+脚注）；④格阵＝P2 两腿（豆包 `wifi_f6_b` + `cell_f6`）排最前同窗连续，再 DeepSeek 四格逐格交替。同批把 T78/T80 板面状态列改实况 | 大脑 | 无（D-654 尾句正等它） | 窗令激活并登记 DW 号；四件各一句；T78 状态列引 D-587/D-595 | 复工首日 |
| **P0-2** | **复工前置（合计 <1h，全在设备侧）**：①通道 A 功能验证（开被测 App 看 `ADAPTER_EVT`，并记 `dumpsys power` 的 `mWakefulness=Awake`）；②DeepSeek 三轮试水（发送键／F6 是否短答／有无限流，两开关按 D-641 置位）；③改掉三处会误导自己的移交件（速查卡 3a-2「17.4s 截断」、卡 §1「余量约一半」按实测 1.2s 节拍重算、README §5.4③）；④`pin_console_utf8()` 移出 docstring、`focus_ok` 改解析 `mCurrentFocus` 含 PKG 且 `mWakefulness=Awake` 否则停——附 `git diff` 实录证时序未动、更新窗令 P3 哈希，D-650④ 出订正条；⑤`corpus_ledger.py` 重算 + `--check` | 61bd2401（①–④）／61bd2401 收窗重算（D-597）+ v3 跑 `-Scope all`／badges（⑤） | P0-1 | README 记 `ADAPTER_EVT` 非零与 `mWakefulness`；试水三答与开关态入 README；AST 各 1 个 Call；台账第四节按 P0-1③ 口径对齐且 `--check` in sync、装载失败 0 或显式登记；badges 指向新日志（e234 122） | 复工首日 |
| **P0-3** | **跑窗**：窗内文档冻结（D-643(a)，只追加日志）；逐格记 D-641 开关态与格间单调钟间隔（D-640④）；逐轮核内容事件 >0（零回答轮单记）；每格约 2 分钟处复查 C 侧满帧；收窗自证范围＝仓根＋会话 scratchpad（D-644④）；收窗即重算台账 | 61bd2401 | P0-1、P0-2 | 六格各带 `RUN_KIND.json`＋开关态＋间隔记录＋逐轮内容事件计数；P2 两格 `e2_precheck` 均 `WORTH_RUNNING` 且 A 侧 6/6、驱动器哈希相同；滑落次数 ≤ 窗令点名的那条规则；收窗提交含台账重算哈希 | 复工后 2 个工作日内（不装 T88 时 ≤3h；含装机重验按 4h 排） |
| P1-4 | **大脑先对自己落刀**：①面册一次性折叠（§4 61 条按自陈六族折成子表，一个提交）后**冻结**——下一自然审计轮前零新增一级条目，D 条不再以「入面册候选」作结；②砍①② 落 `CLAUDE.md` 协调节与 `DECISION_LOG` 头（只落已批的 GOVERNANCE §1/§2 条文指针，不写新散文）：记账类改动每窗一次批量提交、裁定摘要 ≤200 字细节走证据包指针；③砍⑥ 收尾：board/DL 落册迁入 `sess/*`、主干只接受合并提交、O-5 退役；④自签闸门归零：命题单第二签不得由大脑代 | 大脑（①由 v4 执行折叠） | 无 | 折叠提交后 `git log -- docs/AUDIT_PLAYBOOK_v1.md` 零提交；下 10 条裁定均 ≤200 字、入册提交 ≤3；`git log --first-parent` 上非合并提交 = 0；`grep 记账 CLAUDE.md` 命中 | 复工首日（与 P0 并行） |
| P1-5 | **SPEC-1 基线回填**：四处 PO 占位填 D-587/D-592/D-595；TEST_MASTER_PLAN #3/#4 外场行划线注 D-587，7-1 的 E1 状态以 D 号对账；交付包状态改「已验收（D-587 #1）」；按基线 L3 规则升 v3.0（v2.1 号已用掉） | 61bd2401（P0 之后）或由大脑裁 cd5239ba 承接（纯文档件） | 无 | 三文件 `grep D-587` 非零；占位「＿＿」= 0；版本号与 L3 规则一致 | 0.5 天 |
| P1-6 | **「网络诉求」前置提 PO 二选一**：解阻整形形态（E-3 热点硬件方案／netem 整形器），或正式把战役命题改写为「App×形态可观测性 + 好网络对照」入册并改 T78 目标句 | 大脑提案 → PO | 无 | 新 D 号写明二选一；T78 目标句同批改写 | 本周 |
| P1-7 | **裁项 B 施工**：先在板面补 T82 残项行（同 T87 拆法），终态标签（ABANDONED vs N/A-BY-CALIBER）由大脑一句裁定；四画像 `token_interval_ms_dist`／`think_pause_ms_dist` 转终态 + `check_redline` 断言 + 反例 | v4 | 标签裁定 | 字段级 `PENDING-BY-CALIBER` = 0（4 份×2 字段） | 0.5 天 |
| P1-8 | **v3 五件无设备件**：台账/徽章归档跑（见 P0-2⑤）；D-640③ 依赖格映射追认 + 命题单按新批次 ID 重锁核签（窗令 P1）；DW-20260831-01 补 T+1 判读页（可按「净 1 格」豁免但要写明）；`.gitattributes` 全仓策略在树净时执行（D-647⑤）；判读页/README 过期行订正（wave0 §4「F5/F6 未采」、FINDINGS「约 30 分钟」实为 55 分钟、判读页 §4b 与 FINDINGS §2 相反句、T90 README「0 字节」实为 4768 B） | v3 | 无 | 每件一个提交哈希回板面；`grep wave1 CORPUS_LEDGER.md` ≥2 | 复工首日 |
| P2-9 | 400ms 绝对门限 vs k=20 相对门限在既有 11 格离线并排复算（D-615④ 只并排不换口径），逐格列旧/新判词与变动原因 | v3 | 无 | 判读页新增对照表；不动设备 | 下窗后 |
| P2-10 | 驱动器合一：`--app {doubao,deepseek}` 配置表、复用 `e1_collect.Adb(serial)`、可注入 `sh` 以便单测轮次账目；`e234_collect` 在 `with_frames` 停涨或 `empty_streak≥2` 时打可 grep 告警行；原始 dump 入库前定压缩/降采样策略 | v3（D-638①，窗后） | P0-3 | 仓内只剩一只驱动器；≥3 条单测；一次 ≤3 分钟只读验证窗通过；D 号定入库策略 | 下窗后 |
| P2-11 | **作息与心跳**：PO 确认作息窗（建议设备窗 09:00–23:00、00:00 后不开新窗、每周固定停工日）；收工提交末尾固定一行「下次预计开工／停机至」；协调侧 L2 改为「超声明时点 +4h 仍无推送」 | PO 确认／大脑立规／协调侧改 PROTOCOL | 无 | 一周零误报观察项 | 本周 |
| P2-12 | **协调通道 v1.1（协调侧自改）**：大脑合并 PR #4（或 cherry-pick `docs/coordination` + 四份任务书 + 诊断）入主分支，CLAUDE.md 只加一行指针到 PROTOCOL；收工板面固定一格「INBOX 已阅至 …（无动作）」负向信号；四单表每巡必核、无变化不提交时间戳；协调侧公布当前会话地址后与大脑对账 M-B-002（通/不通写入 D 号，或撤销合同 v2）；E-03 GLM key／多流吞吐的触发窗由大脑排期入册 | 协调侧／大脑 | 无 | `git ls-tree origin/feat/result-dev-v2 docs/coordination/` 非空；每条下行 8h 内有已读状态；HEAD 无过期表行；D 号记 M-B-002 结果 | 本周 |

## 5. 需要 PO 亲裁的（各附「不裁的默认」）

1. **本地静默 46h 的性质**（休整／故障／等裁定）与作息窗（P2-11）。默认：视为休整；复工即按 P0-1→P0-3 执行；协调侧按「收工心跳」判定，心跳未立前维持降噪巡检。
2. **「网络诉求」前置二选一**（P1-6）。默认：命题改写为「好网络对照 + 可观测性」入册——不解阻整形，这题永远答不了，与其挂着不如写明。
3. **T88 装机时点**（窗令 P2「顺序不可倒」）。默认：本窗不装，历史页「49.5s 显示 0ms」假值在下窗真机上仍在，报告不引历史页 TTFT。
4. **E-03 GLM key（D-593）与多流吞吐（D-587 #5）何时消费**。默认：DW-next 收窗后的下一个窗。
5. **F-b 三选一／E-2 提权**（一页清余项）。默认：F-b 取 iii 保留；E-2 暂缓（若 P1-6 选「改写」则 E-2 不再需要）。

（不需要 PO 的：替代维度按 D-595⑧ 已生效；`.gitattributes` 走默认路径由 v3 执行；额度顾虑已由 D-634④ 改判为「触顶即停」运行判据。）

## 6. 给各会话各一句

- **61bd2401（题「测试方案_v1」，SPEC-1+SPEC-2）**：下窗只差你一句 go/no-go（D-654 尾句）；开窗前先做 P0-2 五件，尤其改掉三处会误导自己的移交件、把 docstring 里那行空转的 `pin_console_utf8()` 移出来；SPEC-1 回填（P1-5）排在窗后，或等大脑裁给 cd5239ba。
- **cd5239ba（题「系统开发_v2」，编制外待命）**：无派单；若大脑裁 P1-5 归你，纯文档件、0.5 天。
- **v3（`local_b1769d1f`）**：P1-8 五件不需要设备，先于下窗清掉；P2-9 对照表只并排不换口径；驱动器合一留到窗后（D-638①）。
- **v4（`local_00236200`）**：裁项 B 施工（P1-7）先补板面残项行再动手；面册折叠成族表是你最后一笔面册工作，之后零新增、D 条里的「入面册候选」不再接。
- **大脑**：停摆不是 D-644⑥ 的休整令，是 D-654 尾句「候 go/no-go」无人接——**P0-1 那句窗令比再一条教义值钱**；然后 P1-4 先给自己上闸门（砍①②、砍⑥ 收尾、不代签）；属主一律写端点 id。
