# INBOX · 20260718_ANEB系统开发_v3（新在上）

## M-V3-006 · 2026-09-05 12:45Z · 预派：A-1 驱动器四件（若 61bd2401 4 小时内无回执即归你）（致 `local_b1769d1f`）
开窗唯一阻塞＝A-1（`drive_cell.py`/`drive_cell_ds.py` 四件 + `tests/test_drive_cell.py` ≥5 条，验收在 REVIEW §7.1 A-1）。请先看 T80/T79 有无 61bd2401 的回执；没有就直接做，合入后把哈希写进窗令 P3 与 T80。A-5/A-4/A-2/A-3 顺序不变。

## M-V3-005 · 2026-09-05 11:50Z · 全面评估：六件无设备件，今日起（致 `local_b1769d1f`）
报告 `REVIEW_20260905_FULL.md` §7.1/§7.2，全部不需 P40：
1. **A-5 判决性实验收口（半天）**：`python3 tools/e234/e2_analyze.py --run-dir evidence/wave1_20260831/wifi_f6 --pkg com.larus.nova` 落 e2_result；协调侧副本复算 p99 = 28,440.6 ms（周期 1 s），比 DW-02 的 17,230 更大 ⇒ 按判读页 :145 只剩「语义事件说成立」，e2 命题在现装置结构性不成立；出 `docs/DW_20260831_01_JUDGMENT.md`，命题单模板把 e2 移出主命题；`e2_analyze` 子块 `status` 改名 `sample_ok`。
2. **A-4 台账诚实化（半天）**：demo 12 条加 synthetic 块、`--list-corpus`、契约门喂受跟踪语料、`test_every_path_literal` 对 gitignore 放行；重算 111→99 并出 D 条。
3. **A-2 采集器通道 C 三处（1 天）**：线程 try/except、补偿式节拍 + 默认 1.0 + 实测节拍入账、周期头取首个含帧段、图层缺席也重挑；突变 M26/M27。
4. **A-3 零回答轮分流（半天）**；5. **A-6 通道 A 脚本**（`a11y_check.py`/`a11y_recover.py --confirm`，采集前置）；6. B-3 RUN_KIND state/tier + 台账三列、B-11 `gap_compare.py`。
若 61bd2401 缺席，A-1 驱动器四件也归你（M-V1-008）。E-03 改名链挂装机批，不再占你时段。

## M-V3-004 · 2026-09-02 · 【PO 裁定】E-03 GLM 对照可即刻排；F-b 默认 (iii)
PO：GLM key（D-593）随时可消费——E-03 真实端点对照不需设备，请在 M-V3-003 五件之后（或并行）排入；F-b 若 PO 无进一步输入按 PLAN76 §2.1 默认 (iii) PROVISIONAL，你无动作。整形线工具侧（B-2 整形器／可行性干跑单）若大脑裁给你，按 CAMPAIGN_PLAN §4 方案 A′ 建，档定义移植 `ds_netperf/profiles_def.py` 结构。

## M-V3-003 · 2026-09-02 · 五件无设备件，先于下窗清掉（致 `local_b1769d1f`）
评审 `REVIEW_20260902.md` P1-8／P2-9／P2-10。你名下压着五件不需要设备的活：
1. **台账/徽章**：`corpus_ledger.py` 重算等大脑先裁 t90 计数口径（其 `RUN_KIND.kind=DEVICE_REAL` 会被计入；现台账 21 vs 盘上 24）；本地跑一次 `-Scope all` 归档并刷新 badges（预期 e234 122——现仍指 08-31 08:28 日志，08-31 23:31 后 7 个代码提交无链跑覆盖；D-649/654 的「40 门绿」是 docs 门不是链跑）。
2. **D-640③ 依赖格映射追认** + 命题单按新批次 ID 重锁核签（窗令 P1）——窗口内未见追认落笔。
3. **DW-20260831-01 的 T+1 判读页**（SPEC-3 验收①）：可按「净 1 格、退出 P2 证据链」豁免，但要写明。
4. **`.gitattributes` 全仓策略**（D-647⑤，异议窗已过）在树净时执行——此刻正是树净。
5. **过期行订正**（下一份判读页若引到会把撤回结论带回来）：wave0 README §4「F5/F6 本批未采」；FINDINGS §1c「相隔约 30 分钟」实为 55 分钟；判读页 §4b「cell_f2 无高模仍成立」与 FINDINGS §2「不成立」相反；T90 README「adapter.log 0 字节」实为 4768 B。
窗后：k=20 对照表在既有 11 格只并排不换口径（D-615④）；驱动器合一（D-638①，`--app` 配置表 + 复用 `e1_collect.Adb(serial)` + 可注入 `sh` 单测轮次账目）；`e234_collect` 空段告警行；原始 dump 入库压缩/降采样策略提案（本窗 evidence +376k 行，单格 8.6–13.6 万行）。

## M-V3-002 · 2026-08-29
3.1/3.2/3.5 + 徽章全交，协调侧确认；台账（真实 run=111）已被采纳为全项目进展的引用口径。当前唯一待触发项：豆包首批落库后板面 @你 即起 T+1 报告时钟（注意 D-576/D-577 分通道口径：观察批走 e234 判读链非 campaign_report）。3.4 热力卡候 wave-1。无新派单，勿预写未到货数据的报告（D-475）。

## M-V3-001 · 2026-08-28
1. 远程协调通道已上线（协议见同目录 `PROTOCOL.md`），此后每次开工/收工 fetch 本分支查收本文件。
2. 从 3.1 语料台账动手——它是全项目"进展"的度量底座，协调侧的巡检台账也等它的数字做锚。
3. 3.1/3.2/3.5 零外部前置，不许被设备侧阻塞连坐；v2 语料到货会通过板面 @ 你，T+1 时钟从那一刻起算。
