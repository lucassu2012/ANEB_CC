# 批次报告命题单（模板）

> **给谁**：开窗前填、窗内照跑、判读时照答。采集侧与判读侧**共读同一张单子**。
> **为什么存在（D-595②⑥）**：首窗实证——**采完了，答不上立项时想问的问题**。
> 采集参数、轮数、格数都调得很对，而**没人在开跑前写下「这批要回答什么」**，
> 于是判读页的一句话结论成了「本批答的是装置边界，不是网络诉求」。
> **命题不先写死，采集调得再对也救不回来。**
> **不是什么**：不是采集协议（那管「怎么采」），不是判读页（那是事后）。
> 本单只管一件事：**这批打算回答什么，以及什么情况下答不了。**
> **用法**：复制为 `docs/BATCH_PROPOSITION_<批次ID>.md`，**开窗前**填完 §1–§4 并锁定，
> 删掉本引言块。窗内不得新增主命题（见 §5 锁定规则）。

---

## 0. 一页速查（填单人照此顺序，**每格都不许空**）

| # | 做什么 | 不做会怎样 |
|---|---|---|
| 1 | 写 §1 主命题：**每条都要能用一句陈述句证伪** | 写成「探索 X 的表现」这种不可证伪的题目，事后怎么写都算答了 |
| 2 | 每条填 §2 的**放弃条件**（「出现 A 就写不能给」） | 事后拿能拿到的数去凑一个没被问的问题 |
| 3 | 每条填**判读命令与它真正读的产物** | 验收的产物与下游读的产物不是同一个（协议让数 A，判读器只读 B） |
| 4 | 每条填**最小可判样本量**，并核它在本批格数下够不够 | 跑完才发现 n 不够，门直接 NOT_EXECUTED |
| 5 | 填 §3 仪器前置，**开窗前逐条跑一遍** | 烧掉整格才发现这条通道结构上产不出该量 |
| 6 | 填 §4 **事先声明不答什么** | 读者默认「没写＝没测出来」，把范围外读成阴性结果 |
| 7 | §5 锁定（时间＋签署），此后主命题只减不增 | 窗内随手加题＝事后挑一个答得上的说成本批目标 |

---

## 1. 主命题（本批存在的理由；建议 ≤3 条）

| # | 命题（一句可证伪的陈述） | 判它需要的对照 | 依赖格 | 优先级 |
|---|---|---|---|---|
| P1 | — | — | — | 主 |
| P2 | — | — | — | 主 |
| P3 | — | — | — | 次 |

> **写法要求**：
> - **可证伪**：「F2 在蜂窝下的 TTFT 显著高于 WiFi」可证伪；「考察 F2 的网络敏感性」不可。
> - **对照栏写死「变哪一个量」**。⚠ **「时间」也是一个量**：两组排在不同时段
>   ＝多变了一个量，阴性结果无法归因。**同一格内交替两种参数把时段摊平**，
>   不要为省额度而分时段——省下的每分钟都换成一个无法排除的混淆。
> - **优先级**：主＝本批为它而开；次＝顺带；**机会命题不写进本表**（见 §4 末）。

## 2. 逐条展开（每条主命题一节）

### P1：（复述命题）

- **判读命令**：`（照抄可执行的那一条，含参数）`
- **它真正读的产物**：`（文件名 + 字段）`
  > ⚠ **给每个产物逐个找读者**：协议让操作者数的那个文件，是不是下游判读器真正读的那个？
  > 再问**它由几次独立写入产生**——两次以上必分叉（一次吞行就静默少数据，且分叉方向常是最坏那个）。
- **最小可判样本量**：`n >= __`，依据：`（写清是哪条门的算术下界，别自造一个数）`
- **本批能给的 n**：`__`（格数 x 每格轮数 − 预计丢弃）⇒ **够 / 不够**
- **放弃条件（开窗前写死）**：出现 `______` 时，判读页写「本批不能给 P1」，**不替换成别的问题**。
- **仪器前置**：见 §3 第 `__` 条。

### P2 / P3：同上结构

## 3. 仪器前置清单（**开窗前**逐条跑，全绿才开窗）

| # | 前置 | 怎么验（可执行） | 不绿怎么办 |
|---|---|---|---|
| 1 | e2 类命题：该 App/场景**装置看得见静默** | `python tools/e234/e2_precheck.py --run-dir <已有的同类格>` | `FAIL`＝结构性不适用，**删掉该命题**；`NOT_EXECUTED`＝先修采集排期（多为 dump 周期），**别当成不适用** |
| 2 | 通道 C 图层找得到 | 跑后核 `collect_notes.json` 的 `sf_status` | 为 `NOT_EXECUTED` ⇒ 该格 C 侧全空，e2 每轮都会被丢 |
| 3 | 采集周期与环缓冲匹配 | 读 `e2_precheck` 印出的「建议 --framestats-period-s」 | 现值大于建议值 ⇒ 期间的帧**永远丢了**，而丢帧与静默在序列里不可区分 |
| 4 | 改任一采集参数前，**列出还有谁在用同一条通道** | 人工核：通道 B 的 `screencap` 与通道 C 的 dump 走**同一条 adb** | 未核就改 ⇒ 争用把另一条通道压垮，且不报错 |
| 5 | （按本批补） | — | — |

> **⚠ 前置本身要计入额度**：为守某条红线而新增的检查动作，自己也会吃窗内时间。
> 若前置预计吃掉 >1 格的额度，**在 §5 锁定时写明**，别让它悄悄挤掉主命题的轮数。

## 4. 本批**事先声明不回答**的（同样重要）

- `（例：本批不回答「哪个功能对上行最敏感」——缺 pcapdroid/，且本批不打算补。）`

> **为什么要写**：不写的话，读者默认「报告里没有＝测了但没结果」，
> 把**范围外**读成**阴性结果**。**「没测」≠「没诉求」。**
>
> **机会命题**（窗内临时冒出来的）也记在这一节下方，注明「非本批目标」。

## 5. 锁定

- **锁定时间**：`____-__-__ __:__`（**必须早于开窗**）
- **锁定人 / 复核人**：`____ / ____`（复核人须非填单人）
- **锁定后规则**：主命题**只减不增**。窗内确需新增 ⇒ 记为**机会命题**，
  在判读页单独成节并注明「非本批目标」——**不得事后提为主命题**。
- **减题也要留痕**：删掉哪条、为什么（多为 §3 前置不绿），一行写清。

---

_本单每批由判读侧（v3）起草、采集侧复核；判读页首段须引本单编号，并逐条对应「答了/没答/为什么」。_
