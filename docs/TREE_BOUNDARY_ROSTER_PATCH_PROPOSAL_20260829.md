# 树边界会话名单补丁提案（SPEC-4 / 4.6）

> 属主：v4（spec lane，T82）。**性质：CLAUDE.md 修订提案，PO 批准后由属主落正文，本提案不擅改 CLAUDE.md。**
> 单源：诊断报告 §2.2「⚠ v4 至今不在 CLAUDE.md 树边界会话名单（只列了 v2/v3），先补一条入册再派单」+ SPEC-4 任务书 §4.6。
> **一页提案，供 PO 一批。**

---

## 1. 现状（实测）

根 `CLAUDE.md` §「树边界」第 1 条现文——锚文字：
> 「**本树（`E:\C Project\ANEB`）= Fable 5 大脑会话 + Opus 5 执行会话（20260715_ANEB系统开发_v2 / _v3）的工作区。**」

**两处与现实不符**：
1. **v4 不在名单**——名单只列 v2/v3，而 v4 是本会话（正在执行 SPEC-4/T82），全程受这套树边界纪律约束、也一直在遵守它。
2. **「两个执行会话」已过期**——同 §「多会话协调」第 2 条「两个执行会话不得同时改同一模块」也写"两个"，实际当前是**四个**执行会话（v1/v2/v3/v4）分执 SPEC-1..4。

---

## 2. 补丁提案（PO 批后由属主落 CLAUDE.md 正文）

**§「树边界」第 1 条改为**：
> 「本树（`E:\C Project\ANEB`）= Fable 5 大脑会话 + Opus 5 执行会话的工作区。当前执行会话（2026-08-28 SPEC 分派）：
> **v1（20260711_ANEB测试方案_v1）=SPEC-1 需求基线；v2（20260715_ANEB系统开发_v2）=SPEC-2 设备采集；
> v3（20260715_ANEB系统开发_v3）=SPEC-3 分析报告；v4（20260715_ANEB系统开发_v4）=SPEC-4 spec/治理**。
> **编制外临时窗（经协调侧 M-B-001 同意启用，B-003 例外类；处置权与排位归大脑）**：`cd5239ba`（老 v2 会话）＝T86 历史证据区陈债考古（只读＋登记，见 `docs/EVIDENCE_BACKLOG_AUDIT_20260829.md`）。」

**§「多会话协调」第 2 条**「两个执行会话」→「各执行会话」（去掉"两个"这个会随会话数变化的字面量——同砍④"数字不写死"精神）。

---

## 3. 20260711_v1 现状确认（任务书 §4.6 要求）

- **v1 会话本身活跃**：SPEC-1（需求基线 v2.0）由 v1 属主在跑，本会话与它有过多次交互（4.2 供稿、基线 v2.1 升级、4.3 的 target 补登记请求）——**v1 可用，SPEC-1 归它无需备案**。
- **但 `E:\C Project\ANEB-UI` 是另一回事（诊断 §4.2 的无痕第三平行线）**：该本地目录**无法访问、全仓 561 条决策/任务板/全部交接件对它零引用**。这**不是 v1 会话**（v1 在本树跑 SPEC-1），而是一条独立于本树的 UI 产出线。
- **建议（供 PO）**：`ANEB-UI` 按诊断 §4.2 处置——若其中有 UI 产出，按跨树纪律收编入册（复制入本树+注来源）；若已废弃，在 DECISION_LOG 明文一条作废。**这条不阻塞本名单补丁**（名单补的是本树四会话，ANEB-UI 是另一议题）。

---

## 4. 验收 + 生效

- **一页提案**（本文）✓。
- **PO 批后由属主落正文**：树边界节的修订属根 `CLAUDE.md`，本会话（v4）**不擅改**——PO 批准后由 CLAUDE.md 的维护属主（大脑/PO）落正文，v4 提供本补丁文本即可。
- **入册**：本补丁提案发出即挂"待 PO"，不占 D 号；PO 批准落正文时记一条 D 号。
- **⚠ 自我边界**：v4 补自己进名单——这**必须走提案+PO 批**，不能 v4 自己改 CLAUDE.md 把自己加进去（那是自我授权，违树边界纪律的精神）。本提案正是这条边界的体现。
