# DW-NEXT 战役窗令（预置稿 · 未激活）

> **状态＝预置稿**：本令在「激活条件」全部满足并由大脑发激活消息后方才生效；激活消息将赋正式批次 ID（格式 `DW-YYYYMMDD-NN`，以激活当日为准）。
> 预置于 2026-08-31 凌晨（夜班收官后、通道 A 恢复前），目的＝恢复信号一到即可开窗，不再临场拼装。
> 本令只做「已裁定事项的集成」，**逐条挂锚**；与命题单/速查卡冲突时，以两者为准并当场报大脑。

## 一、激活条件（缺一不开窗）

| # | 条件 | 判据 | 锚 |
|---|---|---|---|
| P0 | 通道 A 功能验证通过 | 采集侧只读验证：开一次豆包看 `ADAPTER_EVT`，**不信 settings 读回** | D-611 ③④ |
| P1 | 命题单 §5 锁定 | 批次 ID 占位替换＋**复核人＝采集侧（非填单人）**逐节核签 | D-603 ②；D-615 ③（k=20 与作废判据已登记）；D-622 ①（取点披露） |
| P2 | 构建对应性成立 | 判据只认 `lastUpdateTime`（禁 versionName）；默认用现装 08-30 13:07 构建（已重验 `b8ec1bb`）。**若 T88 修的 UI 改动要上机，先装→重验→再开窗，顺序不可倒** | D-581；D-610 ②；D-608 排期 |
| P3 | 驱动器身份可记 | 本窗驱动器＝`tools/e234/drive_cell.py`@`a9abdf4`（四版签名 −2.0～−2.1s），每格 README 记哈希 | D-619 ②；D-621 ③；D-624 |
| P4 | DeepSeek 额度试水 | 其首格前单独 3 轮试水，无限流提示再进正式批；**不许拿豆包 >93 外推** | D-622 ② |
| P5 | 速查卡终版对版签核 | 三方对版毕（现版已「写好且被走过」，D-628） | D-633 ⑤(v) |

## 二、窗内规程（全部为已裁事项的指针，不复述）

- **格阵与格序**：照 T33 排期件 §2/§3 与速查卡 X6（{豆包, DeepSeek}×{WiFi, 蜂窝}，6 格×6 轮）；**逐格交替、不按条件分组**（X1，D-622 ②）；格序细节以对版终审为准。
- **dump 周期**：`--framestats-period-s 1`；界＝`127 × sf_latency 首行纳秒数`，开窗读上一批的＋首格产物落地回验一次（D-599/D-600/N10 修订；首行非纯数字即停）。
- **每格必做**：开跑前/跑完后各读一次 `gsm.network.type`；切网后 `ip route get` 核实生效（禁 dumpsys connectivity）；步 4b 当场看 `clock_pin.status`/`wall_to_boot.status`/`turn_method`（D-627）；静置期时长定死＋逐轮实际值落盘（README §15.1 两条件）；每格 README 记驱动器哈希（P3）。
- **标记**：开格前声明模式（陪跑/自主，B3）；标记↔事件匹配一律最近邻（D-617 ⑤）；偏移引用写明取斜坡哪一端（D-622 ①）。
- **额度**：触顶即停、记已完成格数与触顶轮次、不换账号（D-49；每格首轮经 scrcpy 人眼确认一次限流提示，B1 修订）。
- **收窗**：照速查卡 §4 C1–C6 与 README §15 标准；**自家探针禁 `force-stop`**（D-611，用 `am kill`/退桌面）；`corpus_ledger` 重算与 badges/manifest/点名日志三件同批（D-604 ②＋D-630 ④ 门已在守）。
- **预期失败格**：第 3 格（DeepSeek 首格）预期会失败——**失败是产出不是故障，别当采集出错重跑**（D-622 ②/X6b）。

## 三、角色

操作者＝采集侧 61bd2401；T+1 判读＝v3（含 e2_precheck 前置与新旧口径并排对照，D-615 ④）；值守与裁定＝大脑。

## 四、本窗不回答什么

照命题单 §4 五条，原样生效；任何窗内新观察按「先登记后归因」处理（D-625 ③ 异常预登记）。
