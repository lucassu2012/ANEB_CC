# 业务进展视图（周报模板）

> **给谁**：PO，每周一读，一页读完。
> **不是什么**：不是决策台账（`DECISION_LOG.md` 那 560+ 条是工程记账，PO 不必读）；
> 不是任务板。本页只回答三个业务问题，其余一概不进。
> **数字从哪来**：语料类数字引 `docs/CORPUS_LEDGER.md`（`python scripts/corpus_ledger.py` 重算）；
> **门数与测试数引 `evidence/phase0/badges.txt` 的键**（`gate_count` / `reflex_tests`），
> 由收官全绿链跑自动产出、每次覆盖，**不要手敲「N 道门」「M 条测试」**——手敲的会过期
> （治理减负砍④）。该文件里 `unknown` 表示**本次没测到**，既不是 0 也不是沿用上次；
> 每个值旁的 `*_source` 说明它读的是哪一份日志——**引用前先确认那就是你要引的那次链跑**。
> **不手抄**——SPEC-3 §3.1 使用规则：进展声明必须引台账总数与增量。
> **用法**：复制本模板到 `docs/BIZ_PROGRESS_VIEW_<YYYYMMDD>.md`，填三节，删掉本引言块。

---

## 一、本周新增数据（引台账增量）

| 项 | 上周 | 本周 | 增量 |
|---|---|---|---|
| 真实 run | — | — | — |
| 场景 | — | — | — |
| 覆盖点位 | — | — | — |
| 覆盖运营商 | — | — | — |
| 观察通道采集（真机目录，**不与上面相加**） | — | — | — |

> **填表前先查一件事：两次之间台账的口径变过没有**（`git log docs/CORPUS_LEDGER.csv`
> 看有没有扩扫描根／改判据的提交）。变过就**把差额拆成两半**分别写：
> 「口径订正 +X（同一批语料被数得更全，非新采）／真实新增 +Y」。
> **不拆就会把口径差异写成时间变化，凭空造出一次「本周有新数据」**——已发生过一次：
> 110→111 全部来自扫描根扩至 `server/data/results/`（`83c4c7c`），一条新数据都没有。

一句话结论：（例：「本周 +0 run，设备侧阻塞未解，数据资产原地」——**零增量要照实写**，
这正是 PO 需要看见的信号。）

> ⚠ **「+0 run」不等于「什么都没发生」**：观察通道批次（豆包先行批等）产出 **0 条
> wire run**（其产物结构上进不了 wire 池），却可能是一整个设备窗的采集。照抄上面
> 那句例子会把它写成「数据资产原地」——**实质是错的**。两条链各报各的数，
> 台账第四节给观察通道的目录数；**两个数不可相加**，理由见台账该节。

## 二、回答了什么业务问题

只列**这周新能回答的**，每条附证据锚（报告文件 / D 号）。答不了的写进第三节，不写在这里。

- （例：「深圳三点位晚高峰谁更差？」→ 尚不能，见第三节第 2 步。）

## 三、离《城市热力卡与归因报告》还差几步

| # | 缺什么 | 卡在谁 | 解除条件 |
|---|---|---|---|
| 1 | — | — | — |

**当前判断**：（一句话，例：「工具链已就绪并空转验证过，缺的是外场多点位真实语料。」）

---

_本页每期由 v3（分析 lane）出，数字与结论口径均可回溯至台账与发布门。_
