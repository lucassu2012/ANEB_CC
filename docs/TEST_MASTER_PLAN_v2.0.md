# ANEB 测试执行总计划 v2.0

> **版本**：v2.0 · 2026-08-28 · 属主：v2 会话（SPEC-1 §1.3）· 配套需求基线：[`REQUIREMENTS_BASELINE_v2.0.md`](REQUIREMENTS_BASELINE_v2.0.md)
> **本文要解决的问题**：五条执行计划此前散在四五份 runbook / spec / 战役方案里，执行会话拿到设备当天要先做一轮考古才知道"今天能跑什么"。
> **用法（一句话）**：看 §1 总表 → 找**前置全绿**的那一行 → 翻它在 §2–§6 的展开段 → 照命令跑 → 产物落到「证据落点」列写的路径。
> **纪律**：所有工时/样本量数字**逐个标注真实出处**（本轮实查发现上游引用有误，见基线 §4.3 N-2）；未实测的估算标「估算」；不写死门数与测试数（以链跑实际输出为准）。
> **裁项依赖**：带 🔴 的前置项指向 [`DECISION_REQUEST_20260829.md`](DECISION_REQUEST_20260829.md) 的编号，PO 未裁前该行不可执行。

---

## 1. 总表：今天设备在手能跑什么

| # | 执行项 | 前置条件（🔴=待 PO / 🟡=待外部 / ✅=已备） | 预计设备日 | 证据落点 |
|---|---|---|---|---|
| **1** | **豆包先行批**（基线 + WiFi/蜂窝自然对照） | 🔴 **仅 E-1**（P40 连上并解锁，一页清 #4）；E-2/E-3 **不需要**（那两条是 L3 整形批的前置） | **≈2 小时 ≈ 0.5 个设备窗**（**65 轮** = 6 功能 × 2 条件 × N≥5 ＋ 同工作负载锚格 5 轮；推导见方案 §10，**操作协议见 `T78_DOUBAO_PILOT_COLLECTION_PROTOCOL_20260829.md`**） | `evidence/doubao_wave0_<日期>/`（SPEC-2 §2.1 验收判据指定名；含语料 + 流水 + README 含可复跑命令） |
| **2** | **豆包 L3 整形批**（批 D/G/A 受控扫描） | 🔴 E-1 ∧ 🔴 E-2（管理员提权）∧ 🔴 E-3（机制定：反向 tether 或独立网卡） | 按窗排（每格 N≥5，登记级；回核级 N≥20 需多窗） | 同上 + 整形档位记录 |
| **3** | **wave-1 外场**（2–3 点位） | 🔴 裁项 C（暂停令）∧ 🔴 一页清 #2（点位真名 2–3 个近点即可）∧ 🔴 #3（第二张 SIM：不给则单运营商如实标注） | **2 点位 ≈ 0.853 外场日 / 3 点位 ≈ 1.280 外场日**（双运营商版；单运营商须重算）⚠ **以 16 run/格（n≥15+1 预热）为基数——n 判据已由 D-474 重推，改 n 即须重算，见 §3.2** | `evidence/<战役>_<日期>/`（语料 + trust/radio rollup + README） |
| **4** | **扩展轮**（网格采样） | 同 #3，且需 §3.3 的四项硬前置（radio 八字段 + 出口 IP 随采等） | **两档：160 run ≈ 0.75 外场日（n=5 摸底）／352 run ≈ 1.65 外场日（n=11）**；**完整 32 格 = 3.414 外场日**（同以 16 run/格 为基数，⚠ 同上须按 n 需求表重算） | 同上 + 取证子集**单独出报告过门** |
| **5** | **s4 真机闭环** | 🟡 一页清 #5（Codex 部署 s4_throughput 到 E-01）；客户端侧 ✅ 全就绪 | ~0.5 设备日（估算：quick 数 run + 边界样本） | `evidence/t47_s4throughput_*/`（同族目录） |
| **6** | **E1 真机窗**（打点误差门） | ✅ 装置就绪（dry-run 过）；需**任一 Android 设备**（明文规定**不必是 P40**、不装豆包、不耗额度） | ~0.5 设备日 | `evidence/e1/<YYYYMMDD-HHMMSS>/` |
| **7** | **E2–E4 实验** | 🔴 **均需 P40 设备窗**（即一页清 #4 的 E-1 到场动作）：E2 需 P40 + 已装 App + 少量额度；E3 需通道 C 已验证；E4 可与 E3 并行共用同批会话 | 各 ~0.5 设备日（需排窗） | `evidence/e234/<窗ID>/` |
| **9** | **UI 视觉证据链**（SPEC-2 §2.5；**本表初版漏项，2026-08-29 补**） | 🔴 **仅 E-1**，且**可搭车任意设备窗**（不单独占窗） | **≈0.5 小时**（搭车，不计独立设备日） | `evidence/ui_<日期>/`（判据原文写 `ui_20260829`，**日期应取实际开窗日**——写死的日期会在开窗延后时过期；或按判据允许的「并入当日战役目录」）：**≥10 张带文件名语义的截图 + 一页索引** |
| **9-附** | **同窗顺带闭环一条债务**：`LiveMetric.render` 渲染接线 | 同 #9 | 0（并入 #9 的 0.5 小时） | **实证（2026-08-29）**：`LiveMetricStrip.kt` 已渲染 **render 四型全部**（RUNNING_NUMBER/WAVEFORM/GAUGE/BAR）并接入三屏（T77 批 5a/5b），**「渲染接线」这半已完成**；交付包债务表第 82 行仍写「渲染接线 / D-69 已知剩余项 / 待设备窗验手感」——**前半已过时、后半仍成立**。#9 拍截图时**正好把四型在真机上的手感一并验掉**，该债务即可整条闭环（**交付包属主非本会话，只报不代改**） |
| **8** | **语音 M7 锚回核** | ✅ 回核级语料已达成（n=30 跨 3 种网络条件）；**分布报告已产出并经独立核验** | 0 设备日 | `docs/T65_M7_RECALIBRATION_DISTRIBUTION_20260819.md`（分布报告）+ `docs/M7_RECALIBRATION_INDEPENDENT_VERIFICATION_20260819.md`（独立核验）+ `docs/M7_FIRST_BATCH_REGISTRATION_20260805.md`（登记级首批） |

> **读法提醒**：#1 是**当前前置最少**的一行（只卡 E-1）；**#9 是成本最低的一行**（0.5 小时搭车，不占窗）；#8 **已完成**（分布报告在册，下一步是改锚级复核而非重跑）。
>
> **三列完整性自查（2026-08-29，对照 SPEC-1 任务书 §1.3 验收判据「每一项带前置条件/预计设备日/证据落点三列」）**：8 行 × 3 列 = **24 格全部有实质内容**，判据达标。（自查时我的脚本把 #8 的「0 设备日」按字数标成"过短可疑"——**那是假阳性**：M7 回核是分析任务、**确实不需要设备**，「0」是准确完整的信息不是占位符。**这是基线 §5 纪律 9 的第五个实例，也是第一次当场识别而没有去"修"一个不需要修的格**。）#3/#4 的三个工时数字口径不同、不可互相替代（wave-1 子集 / 网格两档 / 完整 32 格）。

### 1.1 裁定 → 解锁什么（D0 会上的决策者视角；§1 总表是执行者视角）

> **为什么要这张表**：§1 总表回答「今天设备在手能跑什么」，那是**执行者**的问法。
> PO 在清欠会上的问法是反过来的：「**我批了这一条，明天能多跑什么、值几个设备日**」。
> 本表只把已有信息按这个方向重组，**不替 PO 排序、不预设推荐**（裁项本身见基线 §3 与一页清）。

| 若 PO 批了 | 立刻解锁 | 代价（设备日） | 仍被别的条卡着 |
|---|---|---|---|
| **E-1 解锁**（一页清 #4，到场动作——⚠ **实为两件：解锁 ＋ 开窗地点要有能连上的 WiFi**，见下方注） | **#1 豆包先行批**（全批 **65 轮**）+ **#6 E1 真机窗** + **#7 E2–E4** + **#5 的验证半**（若 s4 已部署） | ≈0.5 窗 + 0.5 + 各 0.5 | 无——**这一条单独就能开跑**，是当前杠杆最高的一批 |
| **裁项 C 解除外场暂停令** | 无（**单独解除不够**） | — | 还需 #2 点位真名（≥2 个近点） |
| **裁项 C + 点位真名（#2）** | **#3 wave-1 外场** | 0.85（2 点位）／1.28（3 点位）；按 n 需求表重算见 §3.2.1 | #3 若要双运营商还需 #3 第二张 SIM；不给则单运营商如实标注、**不阻塞开跑** |
| **上述再加 SIM（#3）** | #3 的**双运营商维度** | 同上（0.853/1.280 本就是双运营商版的数） | 无 |
| **#5 催成 Codex 部署 s4** | **#5 s4 真机闭环**（客户端侧已全就绪） | ≈0.5 窗 | 需与 E-1 同窗（要真机） |
| **E-2 提权 + E-3 机制定** | **#2 豆包 L3 整形批** | 按窗排（每格 N≥5） | 需 E-1 同时满足 |
| **裁项 B（画像口径）** | **不解锁任何采集** | 0 | ——它结的是治理债；**画像门本身被另四个裸 `PENDING` 挡着**，与本裁项无关（基线 §3 已订正该假因果） |
| **裁项 A-1（主交付物排序）** | 不解锁新采集，但**决定上面几条的执行顺序** | 0 | 三者环境阻塞高度重叠，同一批到场动作可同时解锁多条 |
| **#1 交付包验收 / #6 keystore / #7 E-03 key** | 各自对应线收尾，**均不解锁采集** | 0 | — |

> **⚠ 2026-08-29 订正（本表此前高估了 E-1 的效力）**：**「E-1 解锁」实为两件到场动作**——**①解锁 ②开窗地点要有一个能连得上的 WiFi**。
> 实测（v2 只读亲验）：设备 **WiFi 已被 AP 断开两天、流量现走蜂窝**（`wifi_on=1` 只是射频开 ≠ 已关联）。
> 豆包批 12 格里**块 1／块 4 与漂移锚格全在 WiFi 上**——**连不上不是「少一个条件」，是整批设计塌掉一半**。
> 开窗第 0 件事＝确认 `ip route get 1.1.1.1` 走 `wlan*` 而非 `rmnet*`（协议 §1.B）。

**这张表最该被看见的一行**：**E-1 是唯一「单独批了就能立刻开跑」的**（**前提是上一条的 WiFi 可达**）——它不依赖任何其他裁定，且一次解锁四项（豆包先行批 / E1 / E2–E4 / s4 验证半）。其余各条要么需要组合（C + #2 + 可选 SIM），要么本就不解锁采集（A-1 / B / 验收类）。

---

---

## 2. 豆包战役（先行批 + L3 批）

**出处**：`DOUBAO_NETPERF_CAMPAIGN_PLAN_20260824.md` §6/§9（板号 T78）

### 2.1 先行批（只卡 E-1）
- **内容原文**：「一条不需整形、不需提权的先行批（E-1 解锁即可跑）：基线 + 自然网络对照——用现役观察通道（A 无障碍 / B 帧差 / D PCAPdroid 免解密）测豆包各功能在当前 WiFi 与切蜂窝（切手机 radio，零 PC 介入）两条件下的 UI-TTFT/cadence/RCT 与多模态上行字节。」
- **它不给什么**（原文）：「不给受控档扫描，但给出每功能的基线网络行为 + 一个自然的双条件对照，是缩小版战役的合理第一步。」
- **执行闸门原文**：「受控扫描（批 D/G/A）= E-1 ∧ E-2 ∧（机制定）；基线批 = 仅 E-1。三条环境阻塞未解前不开设备窗（避免开窗即空跑，D-481 先例）。」
- **N 与工时（已补，2026-08-29）**：**65 轮 = 6 功能（F1–F6；F7 拉伸不进）× 2 条件 × 每格 N≥5（60）＋ 同工作负载锚格 5 轮**（2026-08-29 追加：协议 §2 原有的「块 1 WiFi vs 块 4 WiFi」漂移自检**是坏的**——两块条件同但功能不同，漂移与功能差完全共线；改为末尾重跑一格 F1/WiFi 与块 1 同格可比。+5 轮 ≈+6min ≈全批 5%，结论不变）；**≈2 小时纯执行 ≈ 0.5 个设备窗**。数字来源分层（⚓实测锚 `ttft_ui_ms=1984` / `cadence_ui_ms=100`，均出自 `spec/portraits/doubao.yaml` D-52/D-54；📐假设；🔁沿用 +40% 现场开销惯例）见方案 **§10**。⚠ 开窗前须先核**豆包免费档轮次上限**（**65 轮**可能触顶）。

### 2.2 L3 整形批（批 D/G/A）
- 规模：批 D（F3 识图/F4 文件上传，good+up2m/512k/128k，每格 N≥5）；批 G（F2 深度思考 vs F1，good+loss5/loss10）；批 A（F1 文本对话，good+lat200/lat800+loss5）。
- 口径红线：「N≥5 是登记级下限……样本量不足以给置信区间，产出恒标 LOW/登记级」；全矩阵「35 档 × 6 功能 × N≥20 ≈ 4200 轮人工对话」不可行。
- 机制现状：原「PC 热点 + WinDivert」在本机不可行（单网卡、`Hosted network supported=No`）；改推荐「反向 USB tether（gnirehtet）+ clumsy/WinDivert 在 PC 转发路径整形」，仍需 E-2 提权；备选 C＝加 USB 网卡走独立 Linux/树莓派 `tc netem`（最干净）。

---

## 3. 外场：wave-1 与扩展轮

**出处**：`T43_FIELD_EXECUTION_PACKAGE_20260804.md` §2（wave-1 工时）、`M2_GRID_DESIGN_PROPOSAL.md` §0（两档）、`M3_EXPANSION_ROUND_RUNBOOK_ADDENDUM.md`（前置与收工清单）、`T33_GRID_PREP_20260803.md` §3（完整 32 格重算）

### 3.1 wave-1（2–3 点位）
- **2 点位**：quick 主体 128 run × 72.4s ≈ 2.574h + 取证子集 10 run × 6.5min ≈ 1.083h = 纯测量 3.657h → +40% 现场开销 → 5.120h → ÷6h/外场日 ≈ **0.853 外场日**。
- **3 点位**：同法 ≈ **1.280 外场日**。
- ⚠ 两个数字均为**双运营商**版本；cmcc 双卡缺口未解时须按**单运营商重算**（run 总数各减半）——该重算「届时另算」尚未落盘（一页清 #3）。

### 3.2 扩展轮两档与完整轮
- **160 run ≈ 0.75 外场日**（n=5，「若只求第一张热力卡摸底，n=5 的 0.75 天足够」）。
- **352 run ≈ 1.65 外场日**（n=11；「已定（2026-07-30，依『我授权你决策』取值，PO 可随时推翻）：取 n=11 那一行」）。
- **完整 32 格 = 3.414 外场日**（quick 主体 512 run × 72.4s ≈ 10.297h + 取证子集 40 run × 6.5min ≈ 4.333h = 14.630h → +40% → 20.482h ÷ 6 = 3.414）。
- **采样量纪律（口径优先序，本轮订正）**：
  1. **现行判据 = 分 KPI 类别 × 分 RAT 的 n 需求表**（`docs/N_SAMPLE_SIZE_BY_KPI_RAT_20260804.md`，**D-474** 重推落地：弃用 CV²-快捷近似式、改用 `campaign_common.py::required_n_at_power` 原始公式，两位独立核验员全量重算）——**§3.2 的 n=5 / n=11 两档与「每格 n≥15」都是它之前的旧口径**，排窗时以 n 需求表为准；
  2. 旧档位保留在本文只为**换算工时**（0.75 / 1.65 外场日两个数按 160/352 run 算出，改 n 即须重算工时）；
  3. **记账法不变**：每格 = n_计入 + 1 预热丢弃（取 n≥15 则每格 16 次采集动作、15 条计入）；取证格同样先跑一轮丢弃的预热。
  （**自纠**：本文初稿把三个口径并列印成「现行」而未提 D-471/D-474 已重推——**这正是本文自称要终结的那种考古**，经对抗审查订正。）

#### 3.2.1 n → 外场日换算表（把「须重算」变成可用的表；2026-08-29 补，销缺口 7-7）

> **用法**：先按 `N_SAMPLE_SIZE_BY_KPI_RAT_20260804.md` §3 定**本次要保障哪个 KPI×RAT**，得到 n，再查本表。
> **公式**：`runs = 格数 × (n+1)`（每格 n 计入 + 1 预热丢弃）；`外场日 = (runs×72.4s + 取证格数×5×6.5min) × 1.4 ÷ 6h`。
> 取证子集固定 5 轮/格（D-379）**不随 n 变**。**自校验**：n=15 代入得 0.853 / 1.280 / 3.414，与既有三数逐位吻合——公式无误。

| n（每格计入） | wave-1 · 2 点位（8 格） | wave-1 · 3 点位（12 格） | 完整 32 格 |
|---|---|---|---|
| **n=5** | 48 run / **0.48 外场日** | 72 run / **0.72 外场日** | 192 run / **1.91 外场日** |
| **n=10** | 88 run / **0.67 外场日** | 132 run / **1.00 外场日** | 352 run / **2.66 外场日** |
| **n=15**（旧单一判据） | 128 run / **0.85 外场日** | 192 run / **1.28 外场日** | 512 run / **3.41 外场日** |
| **n=20** | 168 run / **1.04 外场日** | 252 run / **1.56 外场日** | 672 run / **4.16 外场日** |
| **n=38** | 312 run / **1.72 外场日** | 468 run / **2.58 外场日** | 1248 run / **6.87 外场日** |
| **n=52** | 424 run / **2.24 外场日** | 636 run / **3.36 外场日** | 1696 run / **8.97 外场日** |
| **n=95** | 768 run / **3.86 外场日** | 1152 run / **5.79 外场日** | 3072 run / **15.43 外场日** |

**⚠ 两条警示必须随表一起转述**（否则会拿一个可信度存疑的 n 去算预算）：
1. **RTT×NR 的上界 81 不作为典型值**——n 表 §3.1 实测 wave0 那一格存在**真实时间漂移**
   （Spearman ρ=−0.508 超阈，2.1 小时窗内 RTT 中位 80.7→64.4ms 降 20%），不满足 n∝CV² 模型
   「同分布重复抽样」前提；**外场预算优先按 9–38 规划**，81 标「待排查是否系统性偏高」。
2. **TTFT 组的 n（上界 95–109）能否定义「网络」采样量，证据本身是混的**——n 表 §3.3：
   只有原锚点战役 pilot 的 TTFT 抖动被证实是纯场景侧噪声（TTFT~RTT 相关系数≈0），
   其余三战役相关系数 +0.19～+0.68，**TTFT 变异有部分随网络走**。拿 TTFT 组的大 n 当
   「网络路径精度所需采样量」可信度存疑。

> **本表是换算工具，不是判据**——判据永远是 n 需求表（分 KPI × 分 RAT）。本表只回答
> 「若取某个 n，代价是多少外场日」，不回答「该取哪个 n」。

### 3.3 硬前置（不满足则「格子差 vs 信号差 vs 制式差」不可分离）
1. **radio 八字段**（rat / rsrp_dbm / sinr_db / pci / tac / arfcn / sampled_n / stale）落 `scenarios[].network_snapshot.radio`；
2. **CGNAT 出口 IP** 落 `scenarios[].network_snapshot.server_observed_addr`；首次外场窗顺手做一次外部交叉核验（查询页截图 vs `server_observed_addr`）；
3. **分面工具** `scripts/split_by_run_mode.py` 出发前必做（D-415 升级为前置项）；
4. **`--plan` 采样量核算**是首点位测完**当天**的时敏动作，须跑两条（`t1_ttft_ms` + `n1_rtt_p50_ms`）且只喂 quick 分面。

### 3.4 收工清单节奏
1–5 项每个采集日收工时各跑一次；第 6 项（出正式报告）留到全部点位测完后统一做。
⛔ **取证子集必须单独出一次报告、单独过一次门**——池化会把「格构成差」误判成时序位效应（实测：池化判 `CELL_CONFOUNDED`、印出 21% 首轮劣势而各轮样本 1443/88/91；单独跑取证子集三条全 PASS）。

### 3.5 取证模式触发命令
```
adb shell am start -n com.aneb.probe/.ui.MainActivity --es server <url> --ez autorun true --es mode forensic [--es transport auto|wifi|cellular] [--es inject truncate:50]
```
一次 `am start` 产出一条完整 9 场景取证 run（3×3 拉丁方 × 每格 5 轮），`scenario_order` 由 App 自动生成。

---

## 4. E1–E4 实验

**出处**：`spec/adapters/INSTRUMENTATION_SPEC.md` §3.3；`tools/e1/README.md`；`tools/e234/README.md`

| 实验 | 判据（原文要点） | 资源/前置 | 当前状态 |
|---|---|---|---|
| **E1** 已知真值刺激 | 三通道各自 `t_obs − t0` 分布（p50/p90/p99 + n）；⚠ D-417/D-418 追补：「这条判法对通道 C 不成立」（可行 t0 只有 `t_commit`，总量混有 `E_pipeline`） | **任一 Android 设备**（不必 P40、不装豆包、不耗额度）；`tools/e1` 装置就绪 | spec 状态行仍写 NOT_EXECUTED；**工具侧已有真机 run1–run3 记录**（run3 通道 C 总量 FAIL：n=53, p99=29.427ms > 16.667ms）——⚠ 两处状态未对账（见 §7 缺口） |
| **E2** 三通道同轨对拍 | `\|t_A − t_C\|` 的 p99 > 1 帧 → 「通道 A 单独不足以支撑 M3 门（须以 C 为主判据，A 降为交叉验证）」 | P40 + 已装 App + 少量额度；需排窗 | NOT_EXECUTED（真实语料侧） |
| **E3** A0 缺口专项 | 通道 C 的 A0 与通道 A 的 A0′ 求差，通道 E 外部相机校核；「它不是『误差』，是被测 App 的输入处理耗时」 | P40 + 通道 C；需排窗 | NOT_EXECUTED |
| **E4** T_quiet 标定 | 增量间隔分布中「流式最大停顿」与「结束后静默」的分离点；**若分布重叠 → C-1 单独不可用、A4 必须走 C-3 合取，这是合法且有价值的否定结论，不得为了拿到数值而硬凑** | P40 + 额度（多轮长回答）；需排窗 | NOT_EXECUTED |

- **依赖序**：E1（无需 P40，可先做，且能独立证伪「通道 A 够不够」）→ E2 → { E3（需通道 C 已验证）, E4（可与 E3 并行，共用同一批会话录轨） }。
- **设备红线（e1 装置内建）**：**绝不装 P40**——`DENY_MODELS` 精确匹配 `ELS-AN00 / ELS-NX9 / ELS-N04 / ELS-TN00`，带 `--allow-real-device` 也拒；型号读不到一律拒。真机需显式 `--allow-real-device`，`--serial` 必填。
- **dry-run 隔离（e234 装置内建）**：产出目录名必须带 `dryrun` 否则拒绝写盘；真实采集反向也不许穿 dryrun 外衣；dry-run 数字一个都不许进真实语料池。
- ⚠ **待裁**：e234 的 W-1（`--device-window <ID>` 排窗解锁形状）README 仍标「属大脑裁定」。

---

## 5. s4 真机闭环

- **前置**：Codex 把 `s4_throughput` 相位部署到 E-01（一页清 #5）。**决定性证据**：D-547 只读探 `/api/v1/profiles` 仍是 4 个（`basic_network@0.1.0` + s1/s2/s3），无 s4。
- **客户端侧**：✅ 全就绪（D-478 全链接线、D-499 三常量转正、D-514 假置信修复、D-544 `window_underrun` 进契约）。
- **一到位即插单**：优先级**高于外场**（部署到位是随时可跑的短任务，而外场需成窗）。
- **证据落点**：`evidence/t47_s4throughput_devverify_*/` 同族目录；语料须与 USB 隧道那条（D-479）**分开标注**——后者「路径过快，不构成边界样本分布依据」。

---

## 6. 语音 M7 锚回核

- **触发门槛三级**：登记级（随时，无门槛）／**回核级 = ≥30 个独立真实语音会话且覆盖至少两种网络条件**／改锚级 = 回核级 + 分布报告结论清晰 + 大脑/PO 复核批准。
- **当前状态**：回核级已达成（n=30 跨 3 种网络条件，D-507 口径），**分布报告已于 2026-08-19 产出**（`T65_M7_RECALIBRATION_DISTRIBUTION_20260819.md`）**并经独立核验**（`M7_RECALIBRATION_INDEPENDENT_VERIFICATION_20260819.md`）。→ **下一步是改锚级判断**（需大脑/PO 复核批准），不是重做分布报告。（**自纠**：本文初稿把已完成 9 天的报告列为「当前可做」，经对抗审查订正。）
- **回核范围**：只回核 `1000` 这一点（五锚 [0,100] [60,85] [150,70] [400,40] [1000,0] 中唯一 PROVISIONAL；60=`CODEC_JB_BUDGET_MS`，150/400 来自 `PROFILE_FRAMEWORK` §4.1 第一/第三档）。
- **分布报告要什么**：n 数（独立会话，不数间隔行）、跨会话 p50/p90/p95/p99/max、五档锚区间（<60 / 60–150 / 150–400 / 400–1000 / ≥1000ms）会话占比、≥1000ms 逐条案例场景描述。**明文不做**：拟合优度检验、极值理论（GEV/GPD）尾部拟合。
- **改锚两情形**：A（尚无按 v0.2 打分的真实分数）→ 直接原地改 `spec/scoring/anchors.yaml` 的 `M7_MAX_GAP` 与 `AqsScorer.kt` 的 `M7_MAX_GAP_ANCHORS` 是安全的，「这是当前最可能出现的情形」；B（已有 v0.2 真实分数）→ 不改原常量，新增 `M7_MAX_GAP_ANCHORS_V03` + 按版本查表 + `AQS_VERSION_VOICE_V03` 跳级。
- **落点（实况，非缺口）**：预案正文确实未写落点，但**实际落点已成事实**——报告与核验件均落 `docs/`（见上两份）。此后同类回核报告沿用该落点。

---

## 7. 本文起草期发现的对账缺口（只列不改，供属主处置）

| # | 缺口 | 影响 | 建议归宿 |
|---|---|---|---|
| 7-1 | **E1 状态两处不一致**：`INSTRUMENTATION_SPEC` §3.3 仍写 NOT_EXECUTED，而 `tools/e1/README.md` §3.5 已记 2026-08-02 真机 run1–run3（run3 通道 C 总量 FAIL、W-2 转可判定，D-413/D-417/D-418） | 读 spec 的人会以为 E1 一次没跑过 | SPEC-4（规格侧）或工具属主，一条 D 号对账。**〔2026-08-29 实查补充，第三处不一致，比原条更具体〕** `tools/e1/e1_analyze.py` 的 docstring 仍写「`AnebAccessibilityService` 今天只打两种日志：`ADAPTER_EVT`（**仅 click 事件**，且 DEBUG 门控）」——**半句已过期**：实查 `AnebAccessibilityService.kt`，`ADAPTER_EVT` **早已扩到内容变化事件**（`TYPE_WINDOW_CONTENT_CHANGED` + `TYPE_VIEW_TEXT_CHANGED` → `logAdapterEvent(…, "content", now)`）**并带 `t_boot_ns=`**（即 T7/T11 记的「probe 补时戳一行」**已落**，`:194` 注释点名该欠账）；「DEBUG 门控」那半句仍准确（`if (!BuildConfig.DEBUG) return`，日志级别已改 `Log.i` 以适配 EMUI 丢弃 D 级）。**精确定性（离线实证，2026-08-29）**：不是「脚本不知道」，是**同一文件里两处 docstring 打架**——模块头（读者打开文件第一眼）写「仅 click 事件」，而函数 `parse_adapter_events` 的 docstring 写「click 与 content 两类**2026-08-02 起均已携带该字段**（`3d31512`，T27 补账）」。**新的那处是对的、旧的那处是错的，而错的恰是先被读到的那处**（本项目红线族「读者做的第一个动作恰好是最没准备好的那个」的又一例）。**离线实证**：造 4 行合成日志喂 `parse_adapter_events`——2 条 content + 1 条 click **全被正确收取**，无 `t_boot_ns` 的那行**如实忽略**（不编时戳）。**结论：通道 A 的装置能力已就绪，无需补代码**；风险只在读模块头的人误判、误走 fallback，**而 E1 执行者恰是最需要这条实况的人**。**归宿**：`tools/` 不在 SPEC-2 可写域（§3 只列 app/evidence/docs），已报大脑与工具属主，**本文不代改**；E1 开窗时以本行实况为准 |
| ~~7-2~~ | ~~豆包先行批无 N 与工时估算~~ **已销账 2026-08-29**：T78 属主（本会话）补方案 §10——**65 轮**（60 + 锚格 5）/ ≈2 小时 / ≈0.5 设备窗，数字按⚓实测锚与📐假设分层 | — | **已闭环**（总表 #1 与 §2.1 同步） |
| ~~7-3~~ | ~~M7 回核报告落点未约定~~ **已解决 2026-08-29**：实证发现报告**早已产出并归档**（`T65_…_20260819.md` + 独立核验件），**落点已成事实＝`docs/`**；§6 与总表 #8 均已改写，原「建议落 evidence/m7_recal_」的提法**撤回**（照做会造出第二个落点） | — | **已闭环** |
| 7-4 | **wave-1 单运营商重算未落盘** | #3 若按单运营商跑，0.853/1.280 两个数字不成立 | 随一页清 #3 裁定一并落 |
| ~~7-5~~ | ~~e234 W-1 排窗解锁形状待裁~~ **已裁 2026-08-29**：大脑裁**案 A（接受硬约束）**——开窗前先在板面登记 DW 号。附带我实证的门真实语义（纯子串匹配：**撤销窗/别人窗/前缀号三种都放行**），大脑三分处置：**前缀包含=修**（词边界一行，并入 v3 在飞笔）／**撤销窗、别人窗=不修**（甄别靠措辞是新假拒来源；授权强度本来自「人去板上登记」的动作与事后可查） | — | **已闭环**（T78 协议 ①E 已同步实况与裁定） |
| 7-6 | **上游 ADDENDUM 未接 D-471 订正级联**：`M3_EXPANSION_ROUND_RUNBOOK_ADDENDUM.md` §1 标题至今仍是「采样量：每格 **n ≥ 15**」，全文对 D-471/D-473/D-474 零引用（D-471 派单点名的影响面是提案 §1.3 / T33 / T46 §11，**ADDENDUM 不在名单内**——级联本身漏了这一份） | 照 ADDENDUM 现场执行的人拿到的仍是旧判据；本文 §3.2 已就地订正，但上游未修 | **ADDENDUM 属主**（只列不改，本文不改他人 runbook 正文——SPEC-1 §2 只读边界） |
| 7-8 | **本表初版漏了 SPEC-2 §2.5（UI 视觉证据链）整整一项**——它是我名下子任务、0.5 小时可搭车、且仓内 UI 截图**实测停在 07-14**（`find evidence -iname "*.png" -newermt 2026-07-20` 零命中） | 「让 PO 看见进展」成本最低的动作**漏了就不会被排进任何窗** | **已补为 #9**。**成因**：我按「战役/实验」的心智列表，UI 证据不属这两类因而落在视野外——**清单要从任务书逐条对账，不按自己的分类凭印象列**（同族：手写清单必漏，D-275） |
| ~~7-7~~ | ~~三个外场日数字需随 n 判据重算~~ **已销账 2026-08-29**：不再只标注——**§3.2.1 给出 n→外场日换算表**（7 档 n × 3 种规模，公式经 n=15 三数自校验），并把 n 表 §3.1/§3.2 的两条可信度警示随表转述 | — | **已闭环**（本表是换算工具不是判据；「该取哪个 n」仍归 n 需求表） |

---

*测试执行总计划 v2.0 · SPEC-1 §1.3 产出 · 每项三列硬信息（前置条件/预计设备日/证据落点）· 数字出处更正见基线 §4.3 N-2*
